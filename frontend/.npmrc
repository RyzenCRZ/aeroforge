# 规格 §3.5：npm 拉取慢时走 npmmirror。项目级配置（不污染全局 npm 配置）。
# 构建与运行本身永不联网；此镜像仅用于依赖安装期。
registry=https://registry.npmmirror.com

# 沙箱禁止写入系统默认缓存目录（npm 默认落在 D:\nodejs\npm-cache，触发 EPERM）。
# 与 uv 的处理方式一致：把缓存重定向到工程内自包含目录 .tools/（见规格 §15）。
cache=../.tools/npm-cache
