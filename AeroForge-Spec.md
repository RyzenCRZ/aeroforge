# AeroForge（航天飞行器参数化建模与性能评估平台）技术规格

> **AeroForge-Spec.md** — 平台开发总纲
> 面向**参数化 3D 建模**与**运力性能评估**的航天飞行器设计平台：从几何参数出发，经推进热力学与质量估算，给出可溯源、带置信区间的运载能力结论。

---

## 文档信息

| 项 | 内容 |
|---|---|
| 文档版本 | v0.6.1（**M2 收口续：OI-19「3D 分级显隐 + 剖切」落地**，§1.7.4 OI-33）——GLB 场景图由「1 节点 / 1 网格 / 无节点名」改为 `vehicle`（根，不持 mesh）+ 逐段具名 `seg-<i>`，**分级显隐这才有可操作对象**（此前无节点名 ⇒ 点了没反应 ⇒ 静默失效）；示意通道同步逐段渲染（`ValidationReport.segment_outline`），两通道同粒度、组件树 ↔ 视口双向联动；剖切走 `clippingPlanes`（纯渲染裁剪，不重建几何、不可导出）。**`SPEC_VERSION` 随之为 `0.6.1`**——形状未变但**同一输入的 GLB 字节已变**，按 §9.2 精确化后的判据必须递增，否则会静默命中旧键、供应单节点的陈旧产物<br>（上一版）v0.6.0（**第二轮「设计目标 Callback」：G1–G13 共 13 条裁决全部落地**，§1.7.2）——2D 剖面图升级为**工程剖面图**（9 段轴向分区 / 共底开关 / 标准输送管道 / 全尺寸标注 / **箱体比例由 O/F 与两剂密度派生**）、发射场与轨道字段补齐进 Schema、气动参数（Cd / 参考面积）入 Schema、补 **TLI · TMI** 轨道、补 **C3** 输出与 **ΔV 瀑布图**与**运力—纬度曲线**、纬度变更**两阶段**重算（先点值后区间）、补 **PNG** 导出、3D **剖切 + 分级显隐**落 M1 与**爆炸视图**落 M5、§13.2 基准增设**土星五号**并建立**内置示例火箭模板**；计算库维持自研（R-05）、图表维持 ECharts。**`SPEC_VERSION` 随之为 `0.6.0`**——箱体比例派生改变了同一输入的几何产出，按 §9.2 既有 `artifacts/` 缓存整体失效属期望行为<br>（上一版）v0.5.2（M2 首项：双通道朝向目视核对结案——查出并修复权威通道被多转 -90° 的真实缺陷，§13.6 / §16.3；另结清作业进度通道的握手悬置缺陷，§10.2 / §16.3；并结清冻结命令的产物目录漂移缺陷，§16.2 / 附录 D；**并完成一次「设计目标 Callback」全篇盘查，落定 OI-07～OI-16 共 10 条新裁决**：显示单位固定一套 / 诊断阈值可配置 / 只做中文 / AI 走外部 API 且离线降级 / M2 范围裁剪 / 建立 CI / 「产品可用」门槛 / backlog 收纳机制 / 断网复跑提级 / 图片需求处置，§1.7.1） |
| 平台名称 | AeroForge / 参数化航天器设计与评估平台 |
| 目标平台 | Windows 10/11 x64（首发）→ Linux（后续） |
| 授权策略 | 平台源码私有；第三方依赖**原则上**为宽松开源许可（Apache-2.0 / MIT / BSD 等）。**已裁决的两处例外**：PyInstaller 为 GPL-2.0 **with bootloader exception**（仅构建期工具，不传染产物）、Microsoft Edge WebView2 Runtime 为微软专有但免费可再分发的系统组件（详见 §3.4） |
| 交付形态 | **Windows 桌面应用（exe 双击启动）**，非网页、非在线服务。详见 ADR-015 与 §16 M0.5 |
| 修订规则 | 任何架构级变更必须先修订本文档再写代码（Doc-First） |

## 修订记录

| 日期 | 版本 | 摘要 |
|---|---|---|
| 2026-09-10 | v0.1 | 立项。基于原方案技术风险审阅（R1–R16）重构架构：确立 build123d 单几何内核、NASA 官方 `cea`（Apache-2.0）替代 GPL-3 的 rocketcea、uv + Python 3.12 环境、五层性能模型、内容寻址缓存与全链路溯源 |
| 2026-09-10 | v0.2 | 回补原方案中缺失的产品能力：**2D→3D 轮廓编辑**、**任务事件时序**、**回收与复用建模**、火箭/发动机/材料数据体系与**三层标签**、GEO 与 Hohmann 转移、最优混合比搜索、完整 **UI 设计系统与交互流程**、**应用内 AI 助手**、**Agent Skills 与 AGENTS.md 契约** |
| 2026-09-18 | v0.3 | 依据补充需求手册（火箭设计数据可视化软件 · AI Agent 开发指导手册 v1.8+）建立**需求基线**：新增 §1.5–§1.7（需求来源 / 追踪矩阵 / 待澄清项）；新增 **2D 推进剂结构剖面视图**、3D 比例尺叠加层、光照亮度调节、右键平移、材质渲染差异、参数与报告导出、状态栏反馈契约；补入加注比例 / 储箱排列 / 干质比 / 发动机高度 / 偏度系数等参数；新增 **ADR-011/012/013** 裁决三处架构冲突（计算权威位置、3D 双通道预览、设计 token 分层与主题）；补非功能验收与风险 R-25～R-28 |
| 2026-09-18 | v0.3.1 | **待澄清项（OI-01～OI-06）全部裁决并落地**：偏度系数按统计偏度定义并**重新归类为 §8.7/8.8 的派生统计量**（含 `n<3`/`m₂=0` 返回 `null` 的规则）；干质比 = `m_dry/m_prop`，σ 为存储权威、干质比与推进剂质量分数为派生表示；加注比例 = `实际/满装` 默认上限 100%，超装须显式启用「最大允许加注量」；"自动更新"关闭语义定为冻结 + 常驻标记 + 重开即重算；默认主题定为 `default-dark`；发动机高度定为**含喷管**（安装基准面 → 喷管出口端面）。同步新增 §13.6 六项口径测试与 §8.8 `moments` 输出字段 |
| 2026-09-18 | v0.3.2 | **M0 地基：依赖运行时探验（实测）**。新增 §3.2「M0 依赖运行时探验」事实表与**黄金测试锚点**（LOX/RP-1、LOX/CH4、LOX/LH2 三组 ε=40 工况实测值）；新增 **ADR-014（CEA 调用契约）**——揭示两处**静默失效**：① `Isp`/`Isp_vacuum` 字段量纲实为**有效排气速度 m/s**（若当秒用则 Δv 高估 9.80665×）；② 产物列表中凝相物种**位置**敏感，可在 `last_error = 0` 下返回 Isp 腰斩的错解；新增风险 **R-29 / R-30**；§15 目录补 `.tools/` 工程内自包含工具链与 `tools/preflight.py` 预检入口 |
| 2026-09-18 | v0.3.3 | **M0 验收通过**。§16 新增「M0 验收记录」实测门禁表（后端 `pytest`/`ruff`/`mypy`/`preflight` 四绿 + 前端 `typecheck`/`test`/`build` 三绿 + 经 Vite 代理打通 `/api/health`）；§3.2 补 **6 条前端工具链实测事实**（含 **`typescript` 必须锁 5.x**——7.x 缺 Compiler API 会打断 `openapi-typescript`，以及 npm 缓存须落工程内 `.tools/npm-cache` 以规避沙箱 `EPERM`）；附录 A 将立项预估版本（`vite ^6`/`vitest ^2`）替换为**实测锁定版本**（`vite ^8.3`/`vitest ^5.0`/`jsdom ^30` 等）；§15 补 `npm-cache/`、`package-lock.json`、`specs/openapi.json` 与契约新鲜度约定；新增风险 **R-31** |
| 2026-09-18 | v0.4.0 | **交付形态校正：明确"exe 双击即用的桌面应用，不是网页"**（用户裁决）。原 §11.1 假设"M0–M6 浏览器访问 + M7 才套桌面壳"，会让 M1–M6 全部 UI 工作建立在未验证的打包假设上——新增 **R-32** 并将该风险消除。变更：① 新增 **ADR-015**：桌面交付走 **PyInstaller + pywebview（WebView2）原生窗口，不用 Tauri**（平台质量全在 Python 侧，只维护一套打包链；壳可替换故不锁死未来）；② **新增 §16.2「M0.5 桌面壳探针」**里程碑（顺序铁律第 6 条：必须早于 M1），含冻结方式、必收资源、空闲端口、SPA fallback、3D(WebGL2) 判据、退出归零及四个已知坑；③ §4.1 补**生产形态同源契约**（打包后无 Vite，后端托管 `dist/` 于动态端口，故前端必须恒用同源相对路径）；④ §3.1/§3.4 登记 PyInstaller 与 WebView2 并**显式裁决两处许可例外**（GPL-with-bootloader-exception 不传染产物；WebView2 为唯一非开源依赖及对策）；⑤ M7 重定义为安装包/分发/AI 助手（打包移出）；⑥ §15 补 `desktop/` 目录；新增风险 **R-33**（WebView2 / WebGL2 可用性） |
| 2026-09-19 | v0.4.1 | **M0.5 冻结链修正**（实测发现问题并回改规格）。① **预检实现下沉**：唯一实现从 `tools/preflight.py` 迁至 `backend/aeroforge/selfcheck.py`，脚本改为转调；冻结产物以 `AeroForge.exe --preflight` 暴露同一入口——落实 R-30「预检必须保留在打包交付中」；② §16.2 新增「预检保留」契约行，并**实测更正必收资源口径**：OCCT 主体虽静态链接进 89.4 MB 的 `OCP.pyd`，但该 `.pyd` 导入表直接引用 5 个改名 `TK*.dll`，另在 `site-packages/cadquery_ocp_novtk.libs/` 有 **70 个 delvewheel 改名 DLL（61.9 MB）**由 `os.add_dll_directory()` 运行期挂载——§16.2 头号坑位「动态库收集不全」**确实成立**，判据只能是冻结产物真跑 `import build123d`；③ 记入**失败模式**：PyInstaller 只为**导入图内出现过**的包执行 hook，只写 hook 而无导入路径回指该包时 hook 静默不执行（实测产物缺 `OCP`/`cea`，仅 88.4 MB）；④ §15 目录同步 `selfcheck.py` 与 `--preflight`；⑤ 补 `mypy` 覆盖项——`cea` 随包的 `libcea.pyi` 有语法级缺陷（`libcea.pyi:215`）会中断 mypy，改为不跟随其 stub，CEA 边界的静态类型让位于 ADR-014 的运行时门禁；⑥ 新增 **`hook-lib3mf.py`**——`build123d/mesher.py` 拉入的 `lib3mf` 以**自拼路径**加载 `lib3mf.dll`，且该 DLL 不在任何 PE 导入表内，**实测冻结产物 `import build123d` 正崩在此处**（由 `--preflight` 拦下，未流入 UI）；⑦ 冻结产物由 88.4 MB 增至 449.1 MB，`--preflight` 在冻结态三项全通过 |
| 2026-09-19 | v0.4.2 | **M0.5 验收通过：交付链路的四个坑位逐一结案**。§16.2 新增「M0.5 验收记录」（产物体积 451.55 MB、必收资源 8 项逐条核对、冻结态 `--preflight` 与 `--probe` 实测输出、启动耗时三基线、零残留）与**退出码约定表**。四项坑位结论：① **OCCT 动态库收集不全 → 已解决**（46 个 `TK*.dll` + 70 个 libs DLL 进包，冻结态几何自检跑通）；② **杀软误报 → 未复现**（本机 4 款安全产品共存下 `NotSigned` 产物全程未被拦截，印证 `onedir` 相对 `onefile` 的误报优势；M7 待办签名与申诉）；③ **WebView2 缺失 → 已解决**（以 monkeypatch 注入验证报错路径而非卸载系统组件，返回码 2 + 安装指引）；④ **中文路径 → 坑位成立且为致命，已采纳 fail-fast 规避**：NASA `cea` 的 C 扩展用窄字符 API 打开数据表，**安装路径含非 ASCII 字符时必定失败**，报错却是*"Could not locate thermo database file"*（文件其实存在，误导性极强）——受控对照实验（同一份数据表仅换目录名：`C:\ProgramData\…` 通过 / `D:\测试 目录\…` 失败）定位根因，`GetShortPathName` 因本机禁用 8.3 名称而不可用作规避；启动器新增**资源根非 ASCII 判定**（[resource_path.py](file:///d:/Vibe%20Coding/aeroforge/desktop/resource_path.py) `ascii_root()`）并**前置到 `--preflight` 之前**，返回码 **5** 并给出换目录指引；同时记录**已实测可行但未采纳**的升级路径（`CEA_SKIP_INIT=1` + 显式 `libcea.init(path=<ASCII>)`）留给 M7 决策，并提示 M7 安装器**不应默认装到 `%LOCALAPPDATA%`**（用户名可能含中文）。**空格与中文经分离验证**：`d:\Vibe Coding\aeroforge`（含空格、纯 ASCII）全程正常。⑤ 新增风险 **R-34**（安装路径非 ASCII 致 CEA 不可用），把该交付链约束纳入风险登记册并写明 M7 待办 |
| 2026-09-19 | v0.5.0 | **M1 几何闭环启动**。新增 **§16.3「M1 实现契约」**——把 §16 里程碑行的 M1 范围裁剪为可验收契约：① **曲线族裁剪**（`line`/`arc`/`ellipse` 三段型覆盖柱/锥/球冠/椭球/共底五构型，其余 §5.3 曲线族随 M5）；② **作业模型 M1 化**（§9.1 进程池降为单 worker 后台线程、四条硬规则保留、M4 扩池）；③ **缓存键 M1 子集**（`sha256(canonical_json + kernel_version + spec_version)`，数据快照/CEA 表字段预留）；④ **API 形状**（contour 存取 / validate 同步校验+采样点 / build 缓存命中即同步返回 / jobs+WS / artifacts）；⑤ **前端范围**（三栏骨架、母线编辑器以表单+SVG 落地、示意通道消费后端采样点以保双通道包围盒一致）；⑥ **验收门禁映射**（四项验收 → 具体测试落点） |
| 2026-09-19 | v0.5.1 | **M1 验收通过：几何闭环全链落地并留下实测数字**。① **§16.3 新增「M1 验收记录」**——八项门禁实测（后端 `pytest` **45 passed** / `ruff check` / `ruff format --check` 45 files / `mypy` 27 source files；前端 `tsc -b` / vitest **29 passed** / `vite build` 1173 kB + 9.36 kB CSS；契约 `gen:api` 后 `git diff` 为空），四项验收逐条给出落点（解析-内核体积 < 0.1%、母线往返逐字节相等、双通道包络 ≤ 1%、改参数→预览 < 500 ms）。② **「改参数 → 预览」给出端到端原始数字**：真实浏览器内对柱段 `length` 连改 5 次，测得 239.3 / 232.5 / 233.5 / 232.6 / **248.6 ms**（均值 ≈ 237 ms，全部 < 500 ms、余量 > 2×）；逐段拆解显示后端 `validate` 中位数仅 **3.76 ms**，其余几乎全是 **200 ms 防抖**这一固定项——故压延迟的有效手段是调防抖窗口，而非优化几何计算。③ **§16.2 新增「M1 交付链复验」**：重冻产物 451.72 MB，冻结态 `--preflight` 三项全通过 `exit = 0`、`--probe` **通过**（`WebGL2` 可用、后端就绪 0.62 s、首帧 4.74 s、退出零残留），并核对包内前端确为 M1 产物（`index-CSatOwYl.js` 1,173,223 B，与 `npm run build` 同名同尺寸；探针由**真实 `Viewport` 首帧**发布，`ProbeScene.tsx` 已删除）。④ **诚实留白**：明示「双通道朝向对齐」仅有静态推演、**未做目视核对**，列为 M2 首项待补（R-25 的形态分叉风险恰在此处）。⑤ AGENTS.md 同步 M1 约定：新增 `artifacts/` 内容寻址产物目录说明与"禁止入库/手工改写"禁令、`data/contours/` 母线存档位置，并纠正 `npm run test:nfr` 这一**并不存在**的脚本引用（§13.6 非功能测试当前并入 `npm run test`）。⑥ 顺带修正 `.gitignore`：新增 `debug.log`——Chromium/WebView2 子进程写到 cwd 的噪声日志（实测为搜狗输入法相关警告），非本工程产物。⑦ **新增 §13.8「工程自检门禁」**：把 M0–M1 复盘出的三类**过程性**错误固化为回归测试——**文档命令可执行性**（`test_agents_contract.py`：`AGENTS.md` 里的 `npm run X` / `uv run …` 必须真实存在，`python -m` 目标还必须有 `__main__` 入口，否则**静默无事**；附录 D 与 `AGENTS.md` 逐行比对，`difflib` 直接给出 diff；另加解析正则自检，防"守卫永远跳过"）、**测试隔离**（`test_test_isolation.py`：`data_root` / `artifacts_root` / `contours_root` 必须落在会话临时区内，拦截"产物写到 `%TEMP%` 跨会话留存致 `cache_hit` 意外为真"）、**夹具合法性**（`test_fixture_profiles.py`：所有零参剖面工厂必须显式归类为合法 / 合法但须 `warn` / 非法，且逐个过产品校验器——D 类"测试骗自己"的根治办法是**修夹具而非放宽断言**）。⑧ **新增 §13.5.1「性能预算门禁」与两条 §13.8 门禁**：把"预算只写在文档里、无人执行"（A 类）与"用全绿掩盖留白"（H 类）一并封住——**性能预算**（`test_perf_budget.py`：前端 JS 总体积 ≤ 1400 kB、冻结产物 ≤ 550 MB、`validate` p95 ≤ 100 ms 三项 CI 实测并含量级下限，防"目录空了也算在预算内"；端到端预览 500 ms 与首帧 7.0 s 因需真实浏览器/冻结产物而**显式登记为人工/产物实测**，并由测试断言"不存在无人测量的预算"）、**验收记录留白**（`test_acceptance_records.py`：§16 内每条「（已达成）」记录必须含 `**本记录明确未覆盖的项**` 一段，无留白写「无」，措辞变体即失败）。同步为 M0 / M0.5 / M1 交付链复验 / M1 四条记录补齐该段（M0、M0.5 原先的"未实测""不在本探针范围"为行文散句，格式各不相同、机器无从检查）。**顺带修一处口径错误**：包体积的 kB 由 1024 改回十进制 1000——此前按 1024 算得 1145.73，与 `vite build` 日志的 1173.22 kB 差 2.4%，极易被误读成"包变小了" |
| 2026-09-19 | v0.5.2 | **M2 首项：双通道朝向目视核对结案（M1 唯一留白），并查出真实缺陷**。① **核对查出缺陷**：视口内两通道明显不重合（示意剪影 168 × 373 px、高宽比 2.22；权威 222 × 184 px、高宽比 0.83，质心偏移约 +20%），而**控制台零报错**、尺寸类门禁（§16.3 四项之二）全绿。② **根因（读 GLB 的 JSON 块实测，非推测）**：OCCT 导出器按 glTF 规范给**根节点**写 `Rx(-90°)`（`[-0.7071, 0, 0, 0.7071]`）把 Z-up 转 Y-up，而**访问器的 min/max 仍是 Z-up 局部坐标**；`glb_bounding_box()` 只读访问器、忽略节点变换，得出"轴向在文件 z"，前端据此**又**转 -90° → 累计 **-180°**，模型躺倒。③ **修复（两处缺一不可）**：后端 `glb_bounding_box()` 改走**场景图节点变换**（新增列主序矩阵辅助 `_node_matrix` / `_matrix_multiply` / `_transform_point`，局部包围盒取 8 角点变换后合并）；前端 `AUTHORITATIVE_ROTATION` 由 `[-π/2,0,0]` 改为**恒等 `[0,0,0]`**。④ **门禁固化**：新增 `backend/tests/unit/test_dual_channel_orientation.py`（**世界坐标**口径：轴向落在世界 Y 且自 `y=0` 起；另钉住"访问器局部 Z-up vs 世界 Y-up"这一根因与矩阵列主序语义）与 `frontend/src/r3f/orientation.test.ts`（恒等常量 + Lathe 同向 + **两条反向自检**：用回 -90° 或用访问器局部盒时必然分叉）；`test_geometry_closed_loop.py` 的逐轴 `zip` 改为**显式轴向映射**（内核 Z-up `(2R, 2R, L)` vs glTF 世界 Y-up `(2R, L, 2R)`，**不可按轴序硬对齐**——这正是原先"两侧都对拍过"的漏洞）。⑤ **规格相应修订**：§13.6 新增「双通道一致性（朝向）」一行并把原行坐标系由"访问器 min/max"更正为"**世界**包围盒"；§16.3 新增结案记录（修复前/后剪影实测、根因、5 条留白），并把 M1 留白第 1 项标注结案；§16.3 两处门禁映射同步改口径。⑥ **核对方法本身也留痕**（供复用避让）：WebGL canvas `preserveDrawingBuffer=false`，帧外 `drawImage` 读回全 0，必须在 `requestAnimationFrame` 帧内读；参考网格同为中性灰会污染分类，须关闭后再量；两通道在视口内互斥，比对的是"同相机下的两次剪影"。⑦ **测试计数**：后端 `82 passed / 1 skipped`（含新增 7 条朝向门禁），前端 `34 passed`；`ruff check` / `ruff format --check`（51 files）/ `mypy`（27 files）全通过；前端包体积 **1173.22 kB**（与 M1 收尾一致）。⑧ **`SPEC_VERSION` 递增至 `0.5.2`**（与文档版本同步，由 `test_spec_version.py` 校对）：`glb_bounding_box()` 对**同一输入**返回的包络已改变（Z-up 局部 → Y-up 世界），按 §9.2 该值参与缓存键，故既有 `artifacts/` 缓存必须整体失效——这是**期望行为**，不是副作用。⑨ **交付链重冻复验**（本节修订的收尾）：修复原先只落在源码与前端产物上，`dist-desktop/` 仍是修复**前**那份（包内 JS 为 M1 的 `index-CSatOwYl.js`）⇒ **交付的 exe 仍带缺陷**，故按 §16.2 约定重冻并复跑探针，新增 **§16.2「M2 交付链复验」**记录（产物体积 451.72 MB / 7758 文件不变；冻结态 `--preflight` 三项全通过 `exit = 0`；`--probe` 通过：就绪 0.58 s、DOM 就绪 3.91 s、**首帧 4.55 s**、`WebGL2` 可用、端口 53346；包内前端已换为本次 `index-DdhGJiHF.js` 1,173,228 B——**这是"修复确实进了交付物"的唯一判据**）；顺带**更正 M0.5 审计表一处位置口径错误**（46 个 `TK*.dll` 位于 `cadquery_ocp_novtk.libs/`，是那 70 个 DLL 之内的子集，并非 `_internal/` 顶层的并列项），并把退出残留判据从"只查 `AeroForge.exe`"加强为"同时查 `msedgewebview2` 子进程并比对启动时间"（只比数量会把别家应用的 WebView2 误判为孤儿进程）。⑩ **两条人工/产物实测门禁已重跑刷新**（§13.5.1）：首帧 **4.74 → 4.55 s**、改参数→预览 **237 → 253.6 ms**（5 次最大 270.2 ms）；并在 §16.2 / §13.5.1 写明**数字时点**与预览重测的三个坑位（信号必须经后端往返；后台/被遮挡窗口的定时器节流会把 200 ms 防抖放大到 ~900 ms，测出 888.9 ms 的**无效数字**，且 `document.visibilityState` 仍报 `visible`，须实测 `setTimeout(10)` 才能判断环境是否健康）⑪ **结清「首次构建卡死」这一登记项：查出并修复作业进度通道的握手悬置缺陷**（此前只登记备查，未复现亦未定位）。根因：`vite.config.ts` 的 `server.proxy` **只转发了 `/api`、漏了 `/ws`**，WebSocket 升级请求不被转发 ⇒ 握手永久停在 `CONNECTING`；而 `openJobStream` 只监听 `onmessage`/`onerror`/`onclose`，**无 `onopen`、无握手超时** ⇒ 悬置下三个回调全不触发 ⇒ 降级轮询永不启动 ⇒ 界面冻结在「排队中（0%）」逾 2.5 分钟，**而后端早在 0.3 s 内建完并落盘**（`provenance.timings_ms.done = 299.0 ms`），全程**控制台零报错**。同时解释了"刷新后就快"：首次作业其实已把产物写盘，刷新后转为 `cache_hit = true` 的**同步**路径，压根不碰 WS。修复三处：代理补 `/ws`（`ws: true`）、`openJobStream` 增 3 s **握手超时**兜底（超时即摘回调、关 socket、回调 `onError` ⇒ 降级轮询）、`model.ts` 契约不变。**真实双轮验证**（真浏览器 + 真后端 + 真 Vite）：正常通道下 WS 逐帧推进 `0% → 40% → 70% → 90% → 已就绪（权威）`（约 232 ms，`/api/jobs` 零条目 = 未降级）；把 `window.WebSocket` 换成永不回调的替身构造悬置后，`排队中（0%）` 冻结 3599 ms → 3575 ms 出现首个轮询 → 3666 ms 收敛为 `已就绪（权威）`。**门禁固化**：前端 +3 条（`geometry.test.ts` 2 条、`model.test.ts` 1 条），测试 34 → **37 passed**；全量门禁复跑后端 **82 passed / 1 skipped**、ruff / mypy 全通过；前端包体积 1173.22 → **1173.72 kB**（§13.5.1 已同步刷新该行与「数字时点」）。**规格修订**：§10.2 新增「作业进度通道的降级契约」与「开发态代理契约」；§13.6 新增「作业进度通道降级」判据；§16.3 新增「作业进度通道缺陷修复」记录（含 5 条留白）并把双通道记录的留白第 2 项标注结案；新增风险 **R-35**（传输层静默挂起；并推广为"任何既不成功也不失败的异步等待都必须设超时"）。**过程性教训**：定位时发现人工复验所用的 uvicorn 是**陈旧进程**（早于 `SPEC_VERSION` 递增 35 分钟），会把 `cache_hit` 恒置为 `true`、`spec_version` 恒报 `0.5.1`——**任何"看界面正常"的人工复验都会静默命中旧缓存**；已按其原始命令行重启，并把"人工复验前须确认后端进程启动时间晚于最后一次契约变更"写入记录。⑫ **交付链第二次重冻复验，并结清冻结命令的产物目录漂移缺陷**：⑪ 的修复改的是**生产态也执行**的代码（握手超时兜底），而 `dist-desktop/` 仍是修复前那份（包内 JS `index-DdhGJiHF.js`）⇒ 交付的 exe 仍会在握手悬置时永久冻结在「排队中（0%）」，故再次重冻复跑——新增 **§16.2「M2 交付链复验 · 作业进度通道修复后」**（产物体积 451.73 MB / 473,668,072 B / 7758 文件不变；`--preflight` 三项全通过 `exit = 0`；`--probe` 通过：就绪 0.58 s、DOM 就绪 3.91 s、**首帧 4.51 s**、`WebGL2` 可用、端口 51332；包内前端**已换新**为本次 `index-Dyj8e-pe.js` **1,173,727 B**；退出零残留——探针前后 17 个 `msedgewebview2` 逐条比对启动时间未变）。**复验途中查出并修掉一处文档缺陷（"没报错 ≠ 正确"的又一例）**：按附录 D / `AGENTS.md` 原文执行冻结命令，产物落在 `dist/`——**PyInstaller 的默认 `--distpath` 就是 `dist`，而文档那行漏了 `--distpath`**，于是 451 MB 写进 `dist/`（未被 `.gitignore` 覆盖，以 `?? dist/` 现身，**这是它唯一的暴露途径**），而真正被 `test_perf_budget.py` 的 `FROZEN_PRODUCT` 与各处记录引用的 `dist-desktop/` **停在旧版本上**——命令"跑得通"、日志 `Build complete`、既有门禁全绿，三者都不足以证明交付物被换新。漂移只发生在这份副本上：§16.2 开头（M0.5 段）的冻结命令**本就是带 `--distpath` 的**。修复：两处文档同步补全为 `… aeroforge.spec --distpath dist-desktop --workpath .tools/pyinstaller-build --noconfirm`；门禁：`test_agents_contract.py` 新增 `test_pyinstaller_command_writes_where_the_doc_claims`（声明「产物在 `X/`」就必须带 `--distpath X`，缺省按默认值 `dist` 判），并在解析自检里**用本次真实的漂移形态断言守卫会响**（`("dist-desktop", "dist")`）——只钉正则不钉"它会响"仍是空壳；`test_acceptance_records.py` 的 `KNOWN_RECORDS` 7 → 8。**SPEC_VERSION 保持 `0.5.2` 不变**：本次无几何语义变化，文档变更**不得**使 `artifacts/` 缓存失效（该值只随几何语义递增，见 §9.2） |
| 2026-09-19 | v0.5.2（续 ⑬） | **设计目标 Callback：全篇盘查 8 处「未定 / 留白 / 自相矛盾」，落定 OI-07～OI-16 共 10 条新裁决**。盘查结果与裁决全表见 **§1.7.1**，逐条落地如下。<br>① **显示单位固定一套**（§6.4 新增单位对照表）：kN / t / m / s / km·s⁻¹ / bar / K / kg·m⁻³，**不做用户可切换**；明确该表是**显示口径**，canonical JSON 仍为 SI，故调整显示单位**不得**使缓存失效。<br>② **诊断阈值升级为可配置**（§6.5）：默认值即规格现值，**每条显式标注为「工程惯例值、非权威来源」**，M4 反标定时只改配置不改代码。<br>③ **M2 范围裁剪**（§6.5 / §16）：**`impact` 字段移出 M2**——它必须由 §8.7 敏感度实算，而敏感度属 M4。此前 M2 的验收标准**注定无法满足**（要么编造百分比违反 §1.4-4，要么无法验收）。M2 改为输出六字段，M4 起升为七字段。<br>④ **数据来源门禁前置**（§7.1 / §17 R-10 / 顺序铁律第 7 条）：GCAT 许可核实从"验收项"提为 **M3 开工门禁**——**先核许可、再抓取**，结论落 `LICENSE-NOTICE.md`；核不过即转自建数据集，不得"先用着、以后再补"。<br>⑤ **AI 助手通路定为外部 API + 离线降级**（§10.2 新增通路约定表 / §17 R-37）：OpenAI 兼容、密钥由用户填写、平台不自带；**三条降级触发（未配置 / 断网超时 / 上游报错）任一成立即隐藏入口并回退手工表单**。明确它**不削弱** CON-01：红线管的是"其余功能断网可用"，AI 断网不可用是其既定语义。<br>⑥ **建立 CI**（新增 **§13.9** / `.github/workflows/ci.yml` / §17 R-36）：规格 **22 处**提 CI 而仓库内**无任何工作流文件**（"CI 实测""长期在 CI 内运行"一度是**无实体的措辞**）。工作流**单一 job、步骤严格按序**，并规定「后端测试」必须排在前端构建之后——否则 §13.5.1 的包体积预算每轮 skip，"CI 实测"重新变成空话。契约新鲜度检查**走两步**（后端重导 `specs/openapi.json` + 前端 `gen:api`），只做第二步发现不了"接口改了、契约没重导"。**三项人工/产物实测（端到端预览、`--probe` 首帧、断网复跑）明确不进 CI**，避免假绿。<br>⑦ **「产品可用」门槛定在 M4 之后**（§1.1 / §16）：M0–M7 原全是**工程验收**，没有一条回答"到哪一步它能真用来设计火箭"。判据为「可信运力数字（误差 < 15%）+ 最优 O/F + 多级迭代」三项同时成立；**M0–M3 不得对外宣称可用**。<br>⑧ **只建 `docs/backlog.md`**（新建文件 / §15 / §17 R-16 · R-20）：R-16 与 R-20 两条**高风险**的对策都写着"进 `docs/backlog.md`"，而该文件**从未建立**——对策悬空。同时把 §15 曾声明的 `docs/architecture.md`（同样从未建立）**明确剔除**，并写入新规则：**目录清单不得声明不存在的文件**。本轮 17 条被否选项连同理由已记入该文件。<br>⑨ **断网复跑提级**（§13.7 / §16.2）：CON-01 是全篇**第一条红线**，却只挂在 M7 验收上——等于**最末期才第一次验证**，且"断网可用"恰恰最容易被一个 CDN 字体或一条遥测无声破坏（联网时全部门禁都会绿）。§16.2 新增**交付链复验的固定步骤**（7 步），第 6 步即断网复跑，且须在 **AI 未配置**状态下执行。<br>⑩ **只做简体中文**（§1.3 新增非目标）：不引入 i18n、不做语言切换；约束的是**文案**而非编码，不得据此要求代码注释改英文。<br>⑪ **图片需求的处置**（§1.5 / §1.7.1 OI-16）：定为**人工口述转述**后补入，不引入 OCR / 多模态链路。⚠ **口述内容截至本版尚未提供**，故 5 张参考图的效力**仍为空**——该项是本轮唯一未闭环的裁决输入。<br>⑫ **顺序铁律新增两条**（§16）：第 7 条「GCAT 许可核实先于 M3 抓取」、第 8 条「**CI 必须早于 M3**」（M3 起引入外部数据与黄金基准，回归成本陡升）。<br>⑬ **AGENTS.md 同步**（附录 D 逐行一致）：`项目结构` 补 `/docs/` 与 `/.github/`；`关键命令` 补持续集成一行；`禁止操作` 补四条——不引入 i18n、不就地格式化单位、不用经验系数编造 `impact`、不把未采纳想法直接排期。由 `test_agents_contract.py` 强制两份副本一致。<br>**SPEC_VERSION 仍保持 `0.5.2` 不变**：本轮全部为**文档 / 流程 / 范围类**裁决，**无几何语义变化**，文档变更**不得**使 `artifacts/` 缓存失效（§9.2 的既有约定；这也是与上一版 v0.5.1→v0.5.2 递增形成对照的一例）。 |
| 2026-09-19 | v0.6.0 | **第二轮「设计目标 Callback」：13 处缺口 / 冲突（G1–G13）全部裁决并落地**（全表见 §1.7.2）。起因：用户重新陈述**核心目标**——「用户自定义火箭几何与性能参数，自动生成 2D 工程剖面图与 3D 参数化模型，后端估算常见轨道运力；**调整发射场纬度时运力须即时重算**；UI 直观易上手，**同一界面内同时具备 3D 视图、2D 平面模型与参数调整模块**」。逐条比对规格后落定：<br>① **界面布局维持 §11.5 现状**（中栏纯 3D、2D 在右栏、计算结果走底部弹出面板）——「同屏」由三栏同时可见满足，不为字面「中栏放两张图」而拆掉右栏的信息密度。<br>② **2D 推进剂剖面图升级为「工程剖面图」**（§5.9 新增建模口径 / §11.10 升级契约）：先总结公开火箭平面剖面图的**七条共性**再建模——**9 段轴向固定分区**（整流罩→载荷适配器→仪器舱→前裙→氧化剂箱→[级间舱 | 共底]→燃料箱→推力结构→后裙→发动机/喷管→尾段整流罩）、**级间段（interstage）与级间舱（intertank）是两个部件**、**共底**为共享隔板、**氧化剂箱容积通常大于燃料箱**、箱内附件固定清单、**输送方式两类**（泵压式/挤压式；上箱输送管穿下箱内部或沿箭体外侧）、**尺寸标注是固定成分**。⚠ 同时点明一条**非铁律**：氧化剂箱在上/在下**无统一规则**（S-IC / S-II / Falcon 9 一级二级皆 LOX 在上，而 S-IVB 是 LH₂ 在上），故必须是**用户可配置项**（§6.1 既有「储箱排列」字段方向正确）。**箱体比例由混合比派生**：`V_ox / V_fuel = (O/F) · (ρ_fuel / ρ_ox)`，两次独立反算核对（S-IC 公式 1.60 vs 实测 334 500/209 000 = 1.60；S-II 公式 0.311 vs 实测 83 000/260 000 = 0.319）。**四项建模口径**：按 9 段固定分区建模；共底默认关闭、可开启（开启时级长缩减量由公式反算并标注，含 LH₂ 侧强制隔热层）；输送管默认**外置沿箭体**并提供「内置穿越」开关、**不引入任何 CFD / 流阻计算**；出**全部**尺寸线且**数值一律后端下发**（ADR-011）。<br>③ **3D 交互**（§11.4 / §16）：**分级显隐 + 剖切**落 M1（作为 M1 增补项，并入 M2 验收），**爆炸视图**落 M5。<br>④ **显示单位维持 OI-07 固定一套**，不改为用户可切换（与 §6.4 现状一致，本轮为**确认性裁决**）。<br>⑤ **发射场与轨道字段全部补齐进 Schema**（§6.1）：新增 `LaunchSite` 层（名称 / 纬度 / 海拔 / 发射方位）与 `Mission` 层轨道要素（轨道高度 / 倾角 / 偏心率 / 近地点高度 / 远地点高度）。<br>⑥ **补 TLI / TMI 轨道类型**（§8.6 / §8.10）。<br>⑦ **补三项输出**（§8.8 / §11.5）：**C3** 为输出项、**ΔV 瀑布图**（理想 ΔV → 各损失项 → 总 ΔV）、**运力—纬度曲线**（扫纬度出运力，验证"低纬向东运力更高"这一验收趋势）。<br>⑧ **气动参数入 Schema**（§6.1 新增 `Aero` 层：阻力系数 `Cd`、参考面积 `A_ref` 默认取最大截面积），**显式标注为「工程惯例」**，MVP 只做简化模型。<br>⑨ **「即时重算」定为两阶段**（§8.7 / §11.5）：改纬度先返回**解析点值**（毫秒级，立即刷新运力表与 Δv 预算），**MC 区间异步到达后覆盖**（10 000 样本不得进入交互路径）。<br>⑩ **补 PNG 导出**（§11.12）：2D 工程剖面图与 3D 视口快照。<br>⑪ **计算库维持自研**，**不引 poliastro / astropy / RocketPy**（与 R-05 一致，本轮为**确认性裁决**）。<br>⑫ **图表维持 ECharts**（§11.1 现状），**不改为 Plotly / D3**。<br>⑬ **§13.2 基准增设土星五号**（3 级，LOX/RP-1 + LOX/LH₂，LEO ≈ 140 t / TLI ≈ 43.5 t）**并建立内置示例火箭模板**——用户补充的目的原话：「**主要是快速对现有服役与历史上的火箭型号进行模型，节省外部查资料时间**」，故模板须覆盖 Falcon 9 / 长征五号 / 土星五号等公开型号的公开参数。<br>**SPEC_VERSION 递增至 `0.6.0`**（与文档版本同步，由 `test_spec_version.py` 校对）：第 ② 条的**箱体比例由 O/F 与两剂密度派生**改变了**同一输入的几何产出**，按 §9.2 该值参与缓存键，故既有 `artifacts/` 缓存必须整体失效——**期望行为**。⚠ 对照口径：第 ⑦⑩ 条（C3 / 瀑布图 / 纬度曲线 / PNG）属**显示与输出类**变更，**不构成**递增理由；本轮唯一的递增依据是第 ② 条。<br>**同步**：`AGENTS.md` ↔ 附录 D 逐行一致（新增 **5 条禁止项**——视图状态不得写回几何 / 箱体比例不得当输入 / MC 不得进交互路径 / 剖面图不得做成通用制图 / 模板不得另建数字，与 **3 条领域规则**——9 段分区枚举共用 / C3 与 ΔV 成对且互算 / 纬度是自转加成的唯一输入）；§1.6 追踪矩阵补 FR-17～FR-23 七条需求及其验收方式；§17 新增 R-38（工程剖面图范围失控）· R-39（MC 进入交互路径）· R-40（模板与基准各建数字）。 |
| 2026-09-19 | v0.6.0（续 · M2 参数系统） | **M2 参数系统交付并验收**（v0.6.0 的落地实现，非新裁决）。① **新增 §16.4「M2 参数系统（实现契约）」与「M2 验收记录」**：十项门禁实测（后端 `pytest` **219 passed / 1 skipped**（共 220 项）、`ruff format --check` **67 files**、`ruff check`、`mypy` **35 source files**（CI 裸口径）/ **57 files**（`mypy backend` 本地口径）；前端 `tsc -b`、vitest **37 passed**（6 files）、`vite build` **1173.72 kB**；OpenAPI 契约两步刷新后**各自逐字节可复现**）。② **落地物**：`backend/aeroforge/params/` 下 schema / dag / constraints / diagnostics / units / report / propellants 七个模块 + `api/params.py` 的 **`POST /api/params/diagnose`**；契约刷新 `specs/openapi.json` **+1226/−41**、`frontend/src/api/schema.d.ts` **+684**；新增测试 7 个文件 **136 条**。③ **逐条兑现 §16 的 M2 验收标准**：非法参数被拒并给字段级建议、循环依赖可检出（报出环路路径）、单位往返恒等（8 量纲双向）、**诊断输出恰六字段**（`impact` 归 M4，OI-11）、纬度/海拔/方位与轨道要素可存取且过校验、**`V_ox/V_fuel` 与 §5.9 的两次公开数据反算一致**（S-IC 与 S-II 各按「公式结果」与公开实测容积比**两侧核对**）。④ **诊断不把"判不了"静默跳过**：逐规则给出三条账目 `diagnostics` / `deferred_reason` / `uncovered`，故"未验证"与"已验证"不同貌——M2 可判 4 条（结构质量比 / 长径比 / 壁厚 / 推重比的前半段），显式 deferred 4 条（级间 ΔV 分配 / 上面级余量 / 运力缺口 / 喷管出口直径，均属 M4），另有 2 处分支留痕（固体助推阈值缺 Schema 标识、贮箱工艺下限须显式配置）。⑤ **查出并修复一处真实契约缺陷（"没报错 ≠ 正确"的又一例）**：**FastAPI 的 `RequestValidationError.loc` 比 pydantic 多一层请求位置标记 `"body"`**，致 API 通路下发的字段路径为 `body.stages[0].engine.mixture_ratio`，与 §6.3 约束层的 `stages[0]…` **差一层前缀** ⇒ 前端「路径 → 控件」映射不可能对两类清单同时成立；而错误文本本身"看起来很正常"（消息 / 建议 / 级别都对），只有把两条通路放在一起比对才暴露。修复：`errors_to_diagnostics` 入口统一剥除该前缀，并补两条防回归机检（同一错误列表加 / 不加 `"body"` 前缀，产物路径必须一致）。⑥ **规格 ↔ 代码逐行机检**：新增 `test_params_spec_contract.py` 直接解析 §6.4 单位表与 §6.5 规则表的**原文行**比对单位符号、阈值默认值与规则名/级别，并内置**解析器自检**——否则排版一变即解析为空集，全部断言会假通过。⑦ **顺带清掉两处既有类型债**：`uv run mypy backend` 的 2 处 `no-any-return` 未清（CI 的裸 `mypy` 看不见），本轮一并修绿，使两条口径同时成立。⑧ **诚实留白（8 项）**：其中首条即 **§16 里程碑表把 M1 增补项（OI-19：3D 分级显隐 + 剖切）标注为「并入 M2 验收」，而该增补项尚未实现**——故 M2 行**不得据本记录判定为整行交付完成**；其余为 `impact` 与四条 M4 规则未判定、固体助推标识缺失、贮箱工艺下限默认未配置、诊断**尚未接入任何界面**、**未在冻结产物内跑诊断**（`dist-desktop/` 未重冻）、阈值本身未标定。⑨ `test_acceptance_records.py` 的 `KNOWN_RECORDS` 8 → 9。**`SPEC_VERSION` 保持 `0.6.0` 不变**：本轮为**实现落地**，**无新增几何语义变化**（§5.9 的箱体比例派生已在 v0.6.0 计入并已使缓存失效一次），文档变更**不得**使 `artifacts/` 缓存再失效 |
| 2026-09-19 | v0.6.0（续 · M2 收口：诊断接入界面 + 两处配置项） | **第三轮「设计目标 Callback」：M2 留白中 4 项"能力已写进规格、代码却判不了或看不到"的条目逐条裁决并落地**（全表见 **§1.7.3**，本行为裁决部分；实现与验收记入 §16.4）。起因是 M2 验收记录的 8 项留白里有 4 项同源。**先联网核查业界通行做法再裁决**：① **相态标识**——OpenRocket 的发动机类型字符串（`Type ∈ {single-use, hybrid, reloadable}`）与 RocketPy 的电机变体类（`SolidMotor` / `HybridMotor` / `LiquidMotor` / `GenericMotor`）一致指向"相态是**挂在发动机上的枚举属性**"，座落在 **`Engine`** 层；② **贮箱壁厚量级**——NASA Centaur 不锈钢"气球箱" **0.51 mm（0.020 in）**，公开液体火箭的实际下限量级为**亚毫米**。据此落定三条：<br>**OI-30 推进剂相态标识**：`Engine` 新增 `propellant_phase`（`liquid` / `solid` / `hybrid`，默认 `liquid`，Schema 扩展不破坏既有参数）；它是 §6.5 起飞推重比**第二档（1.5）的唯一判据**，档位取**第一级**相态且**必须印在规则的 `threshold` 与 `uncovered` 账目里**（档位选择不得隐式）；**不新增**"是否固体助推"布尔字段（那会与相态形成两份真相，R-28 同族错误）；附 `COMPAT_` 相容警告——相态为 `solid` 时同级的共底 / 储箱排列 / 混合比 / 加注比例不生效（不判非法）。<br>**OI-31 阈值配置入口与工艺下限口径**：`config.example.toml` 模板 + **`GET` / `PUT /api/params/thresholds`**（生效值 / 来源 / 写回 `config.toml`）；**「最小工艺厚度」仍不设数值默认值**——未配置即不判定并留 `deferred_reason`，但界面**须显式标注「未配置」**（"未配置"是必须可见的状态，写死默认值等于抹掉这一留痕）；Centaur 的 0.51 mm **只作量级参照写进模板注释**。<br>**OI-32 单位表与「路径 → 控件」契约**：新增 **`GET /api/params/units`** 下发 §6.4 全表（`factor` 方向固定 **SI = 显示值 × factor**），前端**零换算系数**（P1 / ADR-011）；§6.3 与 §6.5 两条通路的 `field_path` **共用一张映射**、控件以 **`data-field-path`** 承载同值；**定位失败须降级显示路径文本，不得丢弃该条**（丢弃会让清单看起来"全绿"）。<br>**同步**：§6.1（`Engine` 行）、§6.3（相容约束示例 + `field_path` 唯一口径）、§6.4（表由端点下发）、§6.5（配置入口 + 三条账目 + 界面消费契约 + T/W 档位注明）、§10.1（三个新端点）、§11.5（`DiagnosticsPanel` 与定位五条）、§18.4（配置模板与优先级）。**`SPEC_VERSION` 保持 `0.6.0` 不变**：三项裁决均**无几何语义变更**，按 §9.2 不得使 `artifacts/` 缓存失效 |
| 2026-09-19 | v0.6.0（续 · M2 收口实现与验收） | **§1.7.3 三条裁决落地并验收**（v0.6.0 的落地实现，非新裁决）。① **§16.4 新增「M2 收口验收记录 · 诊断接入界面 + 两处配置项」**：九项门禁实测（后端 `pytest` **243 passed / 1 skipped**（共 244 项）、`ruff check`、`ruff format --check` **70 files**、`mypy` **37 source files**；前端 `tsc -b`、vitest **78 passed**（11 files）、`vite build` **1194.32 kB** + 14.24 kB CSS，638 ms；OpenAPI 契约两步刷新后**各自逐字节可复现**）。② **落地物**：后端 `Engine.propellant_phase` + 新增 `params/thresholds.py`、`params/template.py` 与三个端点（`GET /api/params/units`、`GET`/`PUT /api/params/thresholds`、`GET /api/params/template`，`api/params.py` **+129**）；前端新增 `VehiclePanel` / `DiagnosticsPanel` / `ThresholdPanel` 三个面板与 `store/vehicle.ts`、`store/fieldPath.ts`、`api/units.ts`、`api/params.ts`、`api/client.ts`；仓库根新增 `config.example.toml`；契约 `specs/openapi.json` **+313**、`schema.d.ts` **+305**。③ **测试**：后端 **+24 条**（`test_params_thresholds.py` 17 条为新文件，另扩充 diagnostics 30→34 / constraints 20→22 / schema 23→24），前端 **+41 条**（5 个新测试文件）。④ **真跑一遍端到端**（真起 `uvicorn` + `vite`，非 jsdom）：两条路径均实测——降级路径（`ENGINEER_AERO_DEFAULTED` 的 `aero` 在面板无对应控件，降级提示出现且**该条仍留在清单里**）与成功路径（改 `fill_fraction = 1.5` → `HARD_FILL_OVERFILL` → 点「定位到控件」→ `.field-located` 命中该 input、1.6 s 后自动摘掉（2.5 s 后计数为 0））。⑤ **端到端实测查出三处真实缺陷**——静态检查与单元测试**全绿**时均不可见：**422 时三条账目不存在却被渲染成「（0）/ 无」**（读起来是"判过了、没有未判定的"，正是 §13.8 要拦的形态 → 引入 `ledgerReady` 并改写文案）、**定位失败后按钮仍在**（违背"降级时路径不可点击" → 失败即撤按钮）、**`deferred_reason` 未显示**（"未判"与"未通过"在界面上无从区分 → 补「缺什么」一行并以告警色呈现）。⑥ **诚实留白（8 项）**：首条仍为 **OI-19（3D 分级显隐 + 剖切）未实现**（本轮交付范围为参数系统，用户裁定），故 §16 的 M2 行**仍不得判定为整行交付完成**；另明示三项已结条目各自的残余——**固体助推 T/W 与贮箱工艺下限已真判定/真有入口**，但「最小工艺厚度」交付的是**入口而非数值**（实测 `origin` 仍为未配置，且未在真实界面上点一次"改阈值 → 诊断随之变化"的完整回路，该回路只在 TestClient 与 jsdom 两层分别验证）、**定位降级只目视核对了一例**（`aero`，未穷举无控件路径）；仍未结的为 `impact` 与 §6.5 四条规则归 M4、阈值本身未经标定、`dist-desktop/` **未重冻**；另新增"起始箭是**未经来源核对的示例骨架**"一条。⑦ `test_acceptance_records.py` 的 `KNOWN_RECORDS` 9 → 10。⑧ **两条方法记要**：端口占用的症状是 **404 而不是连接失败**（旧 uvicorn 进程缺新端点），故"先探端点再信端口"；`vite` 5173 被占用时实际服务在 5174。**`SPEC_VERSION` 保持 `0.6.0` 不变**：本轮为**实现落地**，无几何语义变化，文档变更**不得**使 `artifacts/` 缓存失效 |
| 2026-09-19 | v0.6.1 | **第四轮「设计目标 Callback」：M2 留白首项 OI-19（3D 分级显隐 + 剖切）裁决并落地**（裁决全表见 **§1.7.4**）。开工前实测发现该条的**硬前提不成立**：权威产物 `model_lod2.glb` = **1 个节点 / 1 个网格 / 3 个 primitive 且无节点名**、`build_solid()` 把**整条母线段链**一次回转成**单体**、组件树显示的也正是段链而非规格举例的整流罩/贮箱/喷管——**几何里没有可分级的对象**，不裁决粒度就只能退化为"点了没反应"或用剖切冒充隐藏。故先做探针验证（`Compound(children=[具名子体])` → GLB 得 `root + seg-0 + seg-1` 各持一个 mesh）再落定 **OI-33** 五条：<br>① **粒度 = 母线段链**——**具名节点是唯一的可分级机制**，故这不是降级实现；M5 出现真部件时只把节点名从 `seg-<i>` 换成分区名，机制不动。② **场景图契约**：`vehicle`（根，**不持 mesh**）→ `seg-0` / `seg-1` / …（`<i>` 为 `profile.segments` 的 0 基下标，与 `stages[i]` **无关**），三条硬约束各对应一种静默失效——**节点名必须存在**（缺名 = 前端找不到节点 = 不隐藏，全程零报错）、**根节点不得持 mesh**（否则整体网格与分段网格**叠加渲染**，同一处画两遍）、**退化段不产出节点**。③ **两通道同粒度**：`ValidationReport` 新增 **`segment_outline`**（逐段闭合轮廓，退化段**空列表以保下标对齐**——跳过会让前端按 `seg-<i>` 隐错**相邻的另一段**），示意通道逐段建网格，否则"隐藏"只在一侧生效。④ **量测与导出不受影响**：体积 / 表面积 / 质心 / `model.step` 一律仍取 `build_solid()` 的整体体，分段体只用于 GLB 与显隐；段间新增的分界面在未隐藏任何段时被实体完全包住、从外部不可见，且隐藏某段时露出的是该处真实分界——"看到的"与"算的"仍是同一个外形（P1 / ADR-012）。⑤ **`SPEC_VERSION` 0.6.0 → `0.6.1`**。<br>**本轮递增的性质（判据的精确化）**：这是一次**形状未变但产物字节改变**的递增——按 §9.2 原措辞"只随**几何语义**递增"会被判为"不递增"，而缓存键里只有 `canonical_json` / `kernel_version` / `spec_version` 三项可变，不递增即会**静默供应单节点的陈旧产物**（表现为"显隐点了没反应"且全程零报错）。故 §9.2 新增判据表，把判据精确化为「**同一输入的产物字节是否变了**」，并列明四类情形的对照（几何语义变化 / 形状不变而字节变化 / 只加字段与端点 / 只改显示单位），并写明递增后必须按 §16.2 **重冻 `dist-desktop/` 复跑探针**（冻结产物内也带旧 `SPEC_VERSION`）。<br>**同步**：§9.2（判据精确化 + 对照表）、§11.4（分级显隐粒度表 + 三条硬约束）、§13.6（新增三条判据：GLB 场景图契约 / 显隐与剖切是纯视图状态 / 组件树 ↔ 视口双向联动）、§1.7.4（裁决）、§16.4（实现与验收记录）。⚠ 实现口径与门禁数字见 §16.4 的本轮记录 |
| 2026-09-19 | v0.6.1（续 · M3 前参数盘点 QA + OI-34） | **第五轮「设计目标 Callback」：M3 开工前的参数系统 QA，5 条裁决全部落地**（全表见 **§1.7.5**）。用户发起盘点——列出 §6.1 十层全部用户可自定义参数与派生量清单，确认哪些不必要；并提出两条设计要求：**已有型号参数必须是"提示形式"**（按用户自定火箭名判断命中，提示后可二次更改）与**参数模块按火箭功能与系统工程划分、不得混成一团**（盘点确认 §6.1 十层已是该划分，前端面板亦按层分区）。裁决：**QA-1 修**——级层比冲唯一权威（`default` ⇒ 级层省略 `isp_*` 由后端从发动机复制下发；不一致 hard / 一致冗余 warn / `custom` 缺值 hard；DAG 解析 custom 缺值拒绝），消除与 `Engine.isp_*` 的两份真相；**QA-2 不修**（双层加注比例保留，用户裁决）；**QA-3** 材料引用化随 M3 材料库建库；**QA-4** 占位字段全部保留（用户裁决）；**OI-34 型号提示交互**（名称匹配 → 提示条 → 载入后全可编辑 + `sourced_fields` 随行、改动后出处转"用户修改"）落 **M3** 模板库交付。实现：schema.py（`Stage.isp_*` 改可选）、constraints.py（`HARD_ISP_DEFAULT_MISMATCH` / `ENGINEER_ISP_DEFAULT_REDUNDANT` / `HARD_ISP_CUSTOM_REQUIRES_VALUES`）、dag.py（default 解析 + custom 缺值拒绝）、template.py 与测试夹具改为省略形态、前端 `VehiclePanel`（default 下隐藏级比冲控件、切回 default 清空覆写值、未填字段显示空串而非 0）。**`SPEC_VERSION` 保持 `0.6.1` 不变**：参数层语义变更不改变同一输入的 GLB 字节（§9.2 判据），不得使 `artifacts/` 缓存失效 |
| 2026-09-19 | v0.6.1（续 · OI-35 环境条件口径） | **发动机推力 / 比冲的环境条件口径裁决并落地**（全表见 **§1.7.5**）。用户指令原文要求：海平面参数对应标准大气条件（海拔 0 m、15 °C、101.325 kPa），真空参数为理想真空下的理论性能值；所有设计文档、仿真模型与性能评估报告**必须清晰标注每个参数所适用的环境条件**，防止口径混淆导致工程计算错误。裁决与落地：① **口径定义**（§6.1 新增专门段）——海平面 = **ISA 标准海平面**（0 m / 15 °C / 101.325 kPa），真空 = **理想真空（背压 0）下的理论值**；② **标注强制**——Schema 字段名已带口径（`thrust_sea_level_n` / `thrust_vacuum_n` / `isp_sea_level_s` / `isp_vacuum_s`，描述注明 ISA 参考条件），UI 标签、`metrics` 输出、`provenance` 与 `sourced_fields` 随值带口径；**无环境限定的「推力 / 比冲」视为缺陷**；③ **配对规则**——`ṁ = F_vac / (Isp_vac · g₀)` **真空推力只配真空比冲**，T/W 用**一级海平面总推力**配 GLOW，禁止混用；④ **GCAT 数据层纪律**（§7.3）——stages `Thrust` / `ThrustSL` 官方已分列（真空 / 海平面）按原样映射；engines `Thrust` **官方未声明环境口径** → 口径中立命名 `typical_thrust_n` 且**不参与配对计算**；lv `TO_Thrust` 官方只写「Launch thrust kN」未显式声明环境，按行业惯例判为海平面起飞推力（N-1 45.3 MN 对照），M4 需真空侧须另觅来源；⑤ **§7.3 更正**——engines `mass_kg` 官方原文为 "loaded (solids only)"，映射名更正为 `loaded_mass_kg`（原「干重」表述有误）。**`SPEC_VERSION` 保持 `0.6.1` 不变**：口径定义与标注属参数 / 数据层语义，不改变同一输入的 GLB 字节（§9.2 判据），不得使 `artifacts/` 缓存失效 |

---

## 1. 愿景与范围

### 1.1 一句话定位

让「改一个参数 → 立刻看到形状与运力变化」成为默认体验，且**每个数字都可溯源、每个结论都带误差区间**的航天器快速设计平台。

> **「产品可用」门槛（OI-13）**：本平台到 **M4 之后**才算"能用来设计火箭"——判据是它**能给出第一个可信的运力数字**（基准火箭误差 < 15%，§13.2），并附带最优 O/F 与多级迭代结果。
> **M0–M3 定位为地基与数据准备**（打包链路、几何闭环、参数体系、数据层），此间**不得对外宣称"可用"**，也不得据此召集试用。

### 1.2 核心能力（十块）

| 能力块 | 内容 | 交付物 |
|---|---|---|
| **2D→3D 建模** | 2D 母线轮廓编辑（含参数化约束）→ 旋转/放样 → 曲面光顺 → 3D 实体 | 轮廓草图 + 实体 |
| **参数化几何** | 旋转体构型（头锥/柱段/级间段/共底贮箱/喷管）+ 尾翼 + 装配树 | BREP/STEP（精确）+ glTF（预览） |
| **结构可视化** | 3D 视口（比例尺、光照可调、材质区分）＋ **2D 推进剂剖面图**（按加注比例绘制液面、标注发动机舱） | 可交互视口 + 剖面图 |
| **任务时序** | 点火／级分离／整流罩抛离／入轨等事件序列与时间线 | 时序表 + 时间线视图 |
| **回收与复用** | 可回收级、回收方式、回收质量与推进剂代价核算 | 复用构型 + 代价明细 |
| **数据体系** | 火箭型号库、发动机族谱、材料库、推进剂库，三层标签索引 | 可检索的结构化数据库 |
| **推进性能** | 推进剂组合热力学 → 理想 Isp → 最优混合比 → 效率修正 → 实际 Isp | Isp 表 + 工作点 |
| **运力评估** | 质量估算 → 多级迭代 → 损失模型 → 运载能力（LEO/GTO/GEO/SSO） | 运力 + 置信区间 + 敏感度 |
| **设计与寻优** | 多目标优化、权衡研究、批量扫描、方案诊断与改进建议 | 方案对比报告 |
| **人机协作** | 应用内 AI 助手（自然语言 → 参数与模型）＋ 开发期 Agent 工作流 | 会话记录 + 可审计变更 |

**能力之间的关系**：2D→3D 是入口，参数化几何是主体，**结构可视化是几何与计算结果的呈现面**（3D 看外形、2D 看内部构型），任务时序与回收复用是**改构型**的两条支线，数据体系提供工程基线，推进与运力是**算结论**的两层，寻优与 AI 助手是**加速决策**的两条通路。

### 1.3 非目标（明确不做）

- ❌ 通用 CAD 的替代品。**边界澄清**：本平台提供母线轮廓编辑器（§5.2），但它是**面向回转体的专用轮廓工具**，不是通用草图的几何约束求解器（不做尺寸链求解、不做装配配合约束求解）；不做通用装配运动学
- ❌ 三维 CFD / 有限元结构强度分析（只做几何与质量的工程估算）
- ❌ 完整 6-DOF 轨道动力学与任务规划（不做入轨后的长期轨道演化）
- ❌ 任何形式的**运行时联网服务**（云求解、在线数据拉取、遥测）。**唯一例外**：应用内 AI 助手可选用外部 LLM，但它是纯可选、可关闭的加速通路——断网时其余全部功能必须照常可用（§10.2 离线降级契约）
- ❌ 多人协作 / 云端项目同步 / 账号体系
- ❌ **国际化 / 多语言**。界面文案**只做简体中文**，不引入 i18n 框架、不做语言切换、不维护多份文案表。**代码标识符仍为英文**（§3.5 文档语言策略：全中文优先、标识符英文）。理由：单人开发的本地工具，多语言是纯负债；一旦引入 i18n，每条文案都要走 key、每次改文案都要动两份文件，而收益（境外用户）不在本阶段范围内。⚠ 该裁决约束**文案**而非**编码**：源码、注释、docstring 全中文（§18.1），不得据此反向要求代码英文注释

### 1.4 硬性约束（红线，违反即 Bug）

1. **离线红线**：`uv sync` 与依赖安装完成后，**断网环境必须全功能可用**。所有数据源快照化入库，所有性能表预计算落盘。
2. **许可红线**：禁入 GPL/AGPL/SSPL 传染性许可。引入任何依赖须先过 §3.4 许可审计。
3. **单位红线**：**内部计算一律 SI**（m / kg / s / N / K / Pa）。任何非 SI 输入必须在边界处转换并显式标注来源单位。
4. **溯源红线**：任何数值结论必须能反查到「参数集 + 数据快照哈希 + 内核版本」三元组，否则视为 Bug。
5. **纯 ASCII 路径**：工程与数据路径不得含中文/空格（历史教训：中文路径导致工具链崩溃）。

### 1.5 需求来源与基线

| 来源 | 标识 | 说明 | 效力 |
|---|---|---|---|
| 原始方案（551 行） | 原 | AeroForge 立项文本 | 需求来源；技术主张须先经 §3.2 核实 |
| 本规格 v0.1–v0.2 | 本 | 已裁决的架构与能力定义 | **权威**，冲突时优先 |
| 补充需求手册 v1.8+ | 补 | 《火箭设计数据可视化软件 · AI Agent 开发指导手册》 | 需求来源；**涉及计算位置、几何生成位置、配色与设计 token 的主张一律不直接落地**，须先经 ADR 裁决 |
| 图片资料（5 张） | 图 | 界面参考图 | ⚠ **截至本版仍未采信任何来自图片的需求**。**已裁决处置方式（2026-09-19）**：由人工（项目所有者）**口述转述为文字**后补入 §1.7 或对应章节——不引入 OCR / 多模态解析链路，不把图片当作需求载体。**当前状态：口述内容尚未提供**，故本条效力仍为空；转述到达后须逐条判定是否与红线（§1.4）或既有 ADR 冲突，冲突者先入 §1.7 裁决 |

### 1.6 需求追踪矩阵

每条需求必须可追踪到「设计章节 → 验收方式」。**无验收方式的需求不予排期**（系统工程门禁）。

**功能需求（FR）**

| ID | 需求 | 设计章节 | 验收方式 |
|---|---|---|---|
| FR-01 | 多级参数输入（全局 + 分级），改动即触发全局重算 | §6.1 §11.5 | 集成测试：改参 → 结果变化 |
| FR-02 | 输入控件支持数字步进器与双向绑定 | §11.5 | 前端测试：步进值 == 手输值 |
| FR-03 | 参数集导出（JSON / CSV） | §11.12 | 导出 → 回读一致 |
| FR-04 | 参数驱动的 3D 重建（箭体 / 整流罩 / 级间段 / 共底） | §5 §11.4 | 几何验证 + 双通道一致性测试 |
| FR-05 | 材质渲染差异（铝合金 / 碳纤维 → 金属度、粗糙度） | §11.3 | 视觉回归快照 |
| FR-06 | 3D 交互：滚轮缩放 / 左键旋转 / 右键平移 | §11.4 | 交互冒烟 + 手工验收 |
| FR-07 | 3D 视口比例尺叠加层（纵向 0～总长，横向 ±最大半径） | §11.3 | 视觉回归快照 |
| FR-08 | 光照亮度调节滑块 | §11.3 | 交互冒烟 |
| FR-09 | 2D 工程剖面图（按比例、按加注比例绘液面、配色区分推进剂、标准输送管道、全尺寸标注、缩放/复位） | §5.9 §11.10 | 像素快照 + 液面高度校验 + 分区与标注数值与后端一致 |
| FR-10 | 整箭数据面板（总质量、推进剂总质量、干重、总高、最大直径、整流罩直径） | §11.5 | 集成测试：数值与后端一致 |
| FR-11 | 轨道运力面板 + 自动更新开关（关闭即冻结并标记）+ 报告导出 | §11.5 §11.12 | 集成测试 + 冻结语义测试（§13.6） |
| FR-12 | 质量估算（推进剂质量 = 体积 × 密度 × 加注比例；干重 = 推进剂质量 × 干质比，σ 为权威） | §8.4 | 基准回归（§13.2） |
| FR-13 | 齐奥尔科夫斯基多级 ΔV | §8.5 | 解析解对照 |
| FR-14 | 运力估算（含损失，反推 LEO / GTO 载荷） | §8.6 §8.10 | 基准回归（§13.2） |
| FR-15 | 状态栏反馈（"计算完成"、"3D 已重建（N 段网格）"） | §11.11 | 前端测试：事件 → 文案映射 |
| FR-16 | 分布形态指标：偏度系数（随值声明 `n` 与 `estimator`） | §8.7 §8.8 | 与 pandas / Excel 口径对齐（§13.6） |
| FR-17 | 3D 剖切与分级显隐（按级显示/隐藏、剖切开窗） | §11.4 | 交互冒烟 + 视觉回归快照 |
| FR-18 | 3D 爆炸视图 | §11.4 | 交互冒烟 + 视觉回归快照 |
| FR-19 | 发射场纬度变更 → 运力两阶段重算（先解析点值、后 MC 区间） | §8.7 §11.5 | 集成测试：点值在防抖窗口内更新；区间到达后覆盖；**低纬向东运力 ≥ 高纬**（趋势） |
| FR-20 | C3 输出 | §8.8 §8.10 | 与双曲线能量解析式对照 |
| FR-21 | ΔV 瀑布图（理想 ΔV → 各损失项 → 总 ΔV）与运力—纬度曲线 | §8.8 §11.5 | 图表数据与后端 `delta_v_budget` / `payload_latitude_curve` 逐点一致 |
| FR-22 | 内置示例火箭模板（Falcon 9 / 长征五号 / 土星五号等公开型号） | §13.2 §11.5 | 载入模板 → 关键参数与 §13.2 基准表一致 |
| FR-23 | 图片导出（2D 工程剖面图 / 3D 视口快照 → PNG） | §11.12 | 导出文件可解码、尺寸非零、与视口当前状态一致 |

**非功能需求（NFR）**

| ID | 需求 | 设计章节 | 验收方式 |
|---|---|---|---|
| NFR-01 | 参数变更防抖，避免重建风暴 | §11.4 | 性能测试：连续改参 100 次只触发 1 次重建 |
| NFR-02 | 类型安全，禁止 `any` | §18.2 | `tsc --noEmit` + eslint 阻断 |
| NFR-03 | 3D 资源释放，无内存泄漏 | §11.3 §13.6 | 循环重建 500 次后对象数与显存稳定 |
| NFR-04 | 实时响应：面板数值 < 100 ms；3D 首帧示意 < 300 ms | §13.5 | benchmark |
| NFR-05 | 视觉一致性（仅用语义 token，禁硬编码色值） | §11.2 | stylelint 规则阻断 |
| NFR-06 | 计算与 UI 解耦（前端零物理公式） | ADR-012 §12.1 | 静态检查：前端无物理常量与公式 |
| NFR-07 | 单一数据源（SSOT） | §11.6 | 审查：前端无第二份派生物理状态 |
| NFR-08 | 离线可用 | §1.4-1 | 断网全流程复测 |

**约束（CON）**

| ID | 约束 | 章节 |
|---|---|---|
| CON-01 | 离线红线 | §1.4-1 |
| CON-02 | 许可红线（禁 GPL / AGPL / SSPL） | §1.4-2 §3.4 |
| CON-03 | 内部一律 SI | §1.4-3 §6.4 |
| CON-04 | 溯源红线（参数 + 快照 + 内核三元组） | §1.4-4 |
| CON-05 | 纯 ASCII 路径 | §1.4-5 |

### 1.7 需求裁决记录（原待澄清项，已闭环）

6 项待澄清项于 2026-09-18 全部裁决。**裁决即规格**——实现须严格按下列口径，不得另设立场、不得保留第二种解释。

| ID | 条目 | 裁决 | 落地位置 |
|---|---|---|---|
| OI-01 | **偏度系数** | 采用**统计偏度**：总体为三阶中心矩除以标准差三次方（γ₁ = μ₃ / σ³）；样本数据使用 **Excel / pandas 风格修正样本偏度**。<br>⚠ **归类修正**：按此定义它是**统计派生量，不是几何输入**——补充手册将它列在几何参数中属分类错误。故落在 §8.7 / §8.8 的统计输出，**不进入 `Geometry` 层** | §8.7 §8.8 |
| OI-02 | **干质比** | 定义为 `m_dry / m_prop`（干质量 / 推进剂质量）。推进剂质量分数若需使用，单独定义为 `m_prop / (m_dry + m_prop)`，**且必须作为派生量**（= 1 − σ）而非独立输入 | §6.1 §8.4 |
| OI-03 | **加注比例** | 定义为 `实际加注量 / 满装量`，**默认上限 100%**。确需超装时必须拆分为「标称满装量」与「最大允许加注量」两个字段并显式开启，**不得默认放开** | §6.1 |
| OI-04 | **"自动更新"关闭语义** | 停止自动重算 / 自动刷新，界面保留最后一次结果并标注「已冻结 / 可能过期」；重新开启时**立即按当前输入重算** | §11.5 §11.11 |
| OI-05 | **默认主题** | `default-dark` 为默认，`vscode-dark` 为可选主题 | §11.2 |
| OI-06 | **发动机高度** | **含喷管**：从发动机**安装基准面**到**喷管出口端面**的轴向高度。若需不含喷管的数值，**单独提供字段**，不得改变本字段语义 | §6.1 |

**门禁沿用**：后续任何新增系数或参数，凡手册未给定义者，仍须先进入本表裁决后才可进入 Schema（R-28）。

#### 1.7.1 本轮新增裁决（2026-09-19 设计目标 Callback）

对规格全篇做了一次「未定 / 留白 / 自相矛盾」盘查（共查出 8 处），逐条裁决如下。**裁决即规格**，与上表同等效力。

| ID | 条目 | 裁决 | 落地位置 |
|---|---|---|---|
| OI-07 | **界面显示单位** | **固定一套，不做用户可切换**：力 `kN` / 质量 `t` / 长度 `m` / 比冲 `s` / ΔV `km·s⁻¹` / 压力 `bar` / 温度 `K` / 密度 `kg·m⁻³`。存储与计算仍为 SI（§1.4-3 不可动摇），该组单位**只在 API 边界与显示层**出现 | §6.4 |
| OI-08 | **方案诊断阈值** | 诊断阈值**升级为可配置项**，默认值**即本规格现列数值**；每条阈值须**显式标注为「工程惯例值、非权威来源」**。后续用基准火箭反标定（§13.2 / M4）时**只改配置、不改代码** | §6.5 |
| OI-09 | **界面语言** | **只做简体中文**，不引入 i18n（§1.3 非目标新增项） | §1.3 |
| OI-10 | **AI 助手通路** | **外部 API（OpenAI 兼容）**：`base_url` + `api_key` 由用户在设置中填写，平台不自带密钥、不代管模型服务。**无 key 或断网时降级为纯本地表单操作**（AI 面板入口自动隐藏，其余功能全量可用）。⚠ 该通路是本平台**唯一**需要联网的能力，不得成为任何建模或计算流程的前置依赖 | §1.3 §10.2 §16 §17 |
| OI-11 | **M2 范围裁剪** | M2 **只交付四块**：Schema 分层 / 依赖 DAG / 约束与错误模型 / 单位系统。**「方案诊断规则」中的 `impact` 字段（§6.5）移出 M2**——它必须由 §8.7 敏感度实算得出，而敏感度属 M4；M2 只落**不依赖 `impact` 的规则**（推重比 / 壁厚 / 长径比 / 结构质量比等） | §6.5 §16 |
| OI-12 | **持续集成** | **建 GitHub Actions 工作流**（`.github/workflows/`），把后端 `pytest`/`ruff`/`mypy`、前端 `typecheck`/`test`/`build`、契约新鲜度、§13.8 五类工程自检门禁全部纳入。**三项人工 / 产物实测（端到端预览延迟、`--probe` 首帧、断网复跑）不进 CI**，在 §13.5.1 显式登记为不覆盖项 | §13.9 §16 §17 |
| OI-13 | **「产品可用」门槛** | M0–M7 全是工程验收，无法回答"到哪一步它能真用来设计火箭"。裁决：**M4 之后**为产品可用门槛（可给出**第一个可信运力数字 + 最优 O/F + 多级迭代**）；M0–M3 一律定位为地基与数据准备，**不得对外宣称可用** | §1.1 §16 |
| OI-14 | **新想法收纳机制** | **只建 `docs/backlog.md`**，不建 `docs/architecture.md`（§15 曾声明该文件却从未建立——属于"文档承诺了不存在的东西"）。R-16 / R-20 的「进 `backlog.md`」从此指向真实文件；本轮未采纳的选项一并记入 | §15 §17 `docs/backlog.md` |
| OI-15 | **离线红线复验** | 交付链复验流程固定加一步：**断网后**跑 `--preflight` / `--probe` **并**完整走一遍「改参 → 预览 → 构建」。理由：CON-01 是全篇第一条红线，却只登记在 M7 验收里，等于"最后才第一次验证" | §13.7 §16.2 |
| OI-16 | **图片需求的处置** | 由**人工口述转述**为文字后补入本表或对应章节；不引入 OCR / 多模态解析链路。⚠ **口述内容截至本版尚未提供**，故 5 张参考图的效力**仍为空**（§1.5） | §1.5 §1.7 |

> 说明：来源标识为"图"的 5 张界面参考图因工具链限制无法解析（§1.5），按 OI-16 由人工转述后补入本表或对应章节。

#### 1.7.2 第二轮新增裁决（2026-09-19 设计目标 Callback 之二）

用户**重新陈述核心目标**后，对规格全篇做第二次比对，查出 **13 处「未覆盖 / 直接冲突」**（编号 G1–G13），逐条裁决如下。**裁决即规格**，与前表同等效力。

| ID | 原编号 | 条目 | 裁决 | 落地位置 |
|---|---|---|---|---|
| OI-17 | G1 | **界面布局**（"同一界面内同时具备 3D 视图、2D 平面模型与参数调整模块"） | **维持 §11.5 现状**：中栏 = 3D 视口，右栏 = 2D 剖面图 + 整箭数据 + 轨道运力，左栏 = 组件树 + 参数面板，计算结果走**底部弹出面板**（不遮挡 3D 视口）。"同一界面"由**三栏同时可见**满足；**不**为字面理解把 3D 与 2D 叠进中栏——那会牺牲右栏的信息密度，且与 §11.9 的窄屏策略冲突 | §11.5 |
| OI-18 | G2 | **2D 图性质**（要求"2D 工程剖面图"，规格只有"推进剂剖面图"） | **升级为工程剖面图**，并按公开火箭剖面图的**七条共性**建模（§5.9）。四项建模口径：① 按 **9 段轴向固定分区**建模；② **共底默认关闭、可开启**，开启时级长缩减量由公式反算并标注，LH₂ 侧强制隔热层；③ 输送管**默认外置沿箭体**，另提供「内置穿越」开关，**不引入任何 CFD / 流阻计算**；④ 出**全部**尺寸线，**数值一律后端下发**。**箱体比例由混合比派生**：`V_ox / V_fuel = (O/F) · (ρ_fuel / ρ_ox)`（§6.1 派生量）。⚠ 附一条**非铁律**：氧化剂箱在上/在下**无统一规则**，必须是用户可配置项（`储箱排列`） | §5.9 §6.1 §11.10 |
| OI-19 | G3 | **3D 剖切 / 爆炸视图 / 分级显隐** | **分级显隐 + 剖切**落 **M1**（记作 M1 **增补项**，并入 M2 验收）；**爆炸视图**落 **M5** | §11.4 §16 |
| OI-20 | G4 | **单位切换**（用户核心目标未提切换，仅规格 OI-07 提及） | **维持 OI-07**：显示单位**固定一套**，不做用户可切换（**确认性裁决**，无改动） | §6.4 |
| OI-21 | G5 | **发射场与轨道字段缺失** | **全部补齐进 Schema**：新增 `LaunchSite` 层（名称 / 纬度 / 海拔 / 发射方位）；`Mission` 层补轨道要素（轨道高度 / 倾角 / 偏心率 / 近地点高度 / 远地点高度） | §6.1 |
| OI-22 | G6 | **轨道类型缺 TLI / TMI** | **补 TLI / TMI**（地月转移 / 地火转移），并入 §8.6 需求表与 §8.10 轨道层 | §8.6 §8.10 |
| OI-23 | G7 | **输出项缺 C3 / ΔV 瀑布图 / 运力—纬度曲线** | **三项全补**：`C3`（特征能量 km²·s⁻²）为输出项；`delta_v_budget`（理想 ΔV → 重力/气动/转向/背压各损失项 → 总 ΔV）供**瀑布图**；`payload_latitude_curve`（扫纬度出运力）供**运力—纬度曲线**（同时是 FR-19 "低纬向东运力更高"这一趋势验收的载体） | §8.8 §11.5 |
| OI-24 | G8 | **气动参数未入 Schema**（§8.6 有气动损失，却无 Cd / 参考面积输入） | 新增 `Aero` 层：`Cd`（阻力系数）、`A_ref`（参考面积，默认取最大截面积）。**显式标注为「工程惯例」**，MVP 只做简化模型（非目标 §1.3 明确不做 CFD） | §6.1 |
| OI-25 | G9 | **纬度"即时重算"与 MC 10 000 样本冲突** | **两阶段**：改纬度先返回**解析点值**（毫秒级，立即刷新运力表与 Δv 预算），**MC 区间异步到达后覆盖**。⚠ **10 000 样本不得进入交互路径**——它与"即时"在物理上不可兼得，硬凑只会得到一个既不即时也不准的数字（R-39） | §8.7 §11.5 |
| OI-26 | G10 | **导出缺 PNG** | **补 PNG**：2D 工程剖面图与 3D 视口快照。与既有 JSON / CSV / Markdown / STEP / IGES / STL / GLB 并列 | §11.12 |
| OI-27 | G11 | **是否引入 poliastro / astropy** | **计算库维持自研**，不引入第三方航天计算库（与 R-05 同口径：轨道与运力计算自研，避免范畴错误与许可不确定性）。**确认性裁决** | §3.1 §8 |
| OI-28 | G12 | **图表库是否换 Plotly / D3** | **维持 ECharts**（§11.1 现状），承载新增的 ΔV 瀑布图与运力—纬度曲线。**确认性裁决** | §11.1 |
| OI-29 | G13 | **基准火箭与模板** | ① §13.2 基准**增设土星五号**（3 级，LOX/RP-1 + LOX/LH₂，LEO ≈ 140 t / TLI ≈ 43.5 t）；② **建立内置示例火箭模板**。用户补充的目的原话：「**主要是快速对现有服役与历史上的火箭型号进行模型，节省外部查资料时间**」——故模板是**产品能力**（载入即得一套公开参数），不只是测试夹具 | §13.2 §11.5 |

> **本轮的递增口径（唯一）**：仅 OI-18 的「箱体比例由 O/F 与两剂密度派生」改变了**同一输入的几何产出**，故 `SPEC_VERSION` 0.5.2 → **0.6.0**。OI-21～OI-26 属 Schema 扩展与输出/显示类变更，**不构成**递增理由（§9.2：该值只随**同一输入的产物字节变化**而递增）。

#### 1.7.3 第三轮新增裁决（2026-09-19 M2 收口 Callback）

M2 验收记录的 8 项留白中，有 4 项属同一类病：**能力已写进规格、代码也判得出，但至今判不了或看不到**。本轮先**联网核查业界通行做法**再裁决（来源见下表），**裁决即规格**，与前两表同等效力。

| 参照工具 | 通行做法 | 结论 |
|---|---|---|
| OpenRocket | 发动机定义（`.rse` / `.eng`）携带一个**类型字符串** `Type ∈ {single-use, hybrid, reloadable}` | **相态是挂在发动机上的枚举属性**，不由别处推导 |
| RocketPy | 电机分 `SolidMotor` / `HybridMotor` / `LiquidMotor` / `GenericMotor` **四个变体类**，类型即类本身 | 同上（类型是电机的一等属性） |
| NASA（Centaur） | 不锈钢"气球箱"壁厚 **0.51 mm（0.020 in）** | 公开液体火箭贮箱壁厚的**实际下限量级为亚毫米** |

| ID | 条目 | 裁决 | 落地位置 |
|---|---|---|---|
| OI-30 | **推进剂相态标识缺失**——§6.5 的固体 T/W 档（1.5）判不了 | `Engine` 层新增 **`propellant_phase`**，枚举 `liquid \| solid \| hybrid`，**默认 `liquid`**（对齐 OpenRocket 的 `Type` 与 RocketPy 的电机变体类：相态是**发动机**的属性）。四点口径：<br>① 它是 §6.5「起飞推重比」第二档的**唯一判据**——档位取**第一级**（提供起飞推力的那一级）发动机的相态；`solid` 用 1.5，`liquid` / `hybrid` 用 1.2；<br>② **档位选择不得隐式**：本次实际采用的档位与依据必须印在该条规则的 `threshold` 与 `uncovered` 账目里并随报告下发，故默认值不构成"静默误判"；<br>③ 默认值取 `liquid` 的理由：在役运载火箭绝大多数为液体构型，且 **Schema 扩展不得破坏既有参数**（与 OI-21～OI-26 同口径）；<br>④ **不新增**"该级是否固体助推"布尔字段——那会与相态形成**两份真相**（唯一权威原则 §6.1 的同族错误，正是 R-28 要拦的东西）。<br>附**相容约束**（`COMPAT_` 前缀，警告不阻断）：相态为 `solid` 时，同级的共底 / 储箱排列 / 混合比 O/F / 加注比例**语义不成立**（固体药柱无贮箱、无 O/F）；Schema 仍要求这些字段是为多级共享结构，故只提示"这些值对该级不生效"，不判该级非法 | §6.1 §6.3 §6.5 |
| OI-31 | **贮箱最小工艺厚度默认"未配置"**——§6.5 的硬阈值没有配置入口 | **必须给出显式配置入口，且不设数值默认值**：<br>① 后端提供 `config.example.toml` 模板与 **`GET` / `PUT /api/params/thresholds`**（生效值 / 来源 / 写入 `config.toml`）；前端设置项**必须同时显示「生效值」与「来源」**；<br>② 未配置时该条判据**仍不判定**（保持 `deferred_reason`，不填凭空下界），但界面**必须显式告知"未配置"**——"未配置"是一种必须可见的状态，不是空白；<br>③ Centaur 的 0.51 mm（亚毫米）**只作为量级参照写进配置模板注释**，**不得**写成默认值（写死即抹掉"未配置"这一状态，正是本条要保留的留痕）；<br>④ **全部阈值**（不只本条）在 UI 中都须携带 §6.5 的「工程惯例值，非权威来源」标注，来源字样随 `source` 字段下发，前端不得自拟措辞 | §6.5 §10.1 §18.4 |
| OI-32 | **单位表与「诊断路径 → 控件」两条链**——只在契约层验证过，界面从未消费 | ① **单位表对外**：新增 **`GET /api/params/units`**，按 §6.4 全表下发 `{quantity, label, symbol, si_symbol, factor}`；前端**零换算系数**（P1 / ADR-011），只按后端下发的 `factor` 格式化——该端点是**只读呈现口径**，不改变存储 / 计算 / 缓存键，故调整显示单位**不得**使 `artifacts/` 缓存失效（§9.2）；<br>② **「路径 → 控件」契约**：§6.3（请求校验）与 §6.5（方案诊断）两条通路的 `field_path` **本就同构**（见 §6.3 末注），前端只维护**一张**映射；控件以 **`data-field-path`** 标记，其取值与六字段诊断的 `field_path` **逐字一致**；<br>③ **降级不是丢弃**：`field_path` 为空、或指向当前不存在（未挂载）的控件时，该诊断项**仍须渲染**（退化为显示路径文本、不可点击），**不得**静默丢弃——丢弃会让清单看起来"全绿"，与"未验证 ≠ 已验证"同源 | §6.4 §10.1 §11.5 |

> **本轮不递增 `SPEC_VERSION`**：三项裁决均**无几何语义变更**（OI-30 只加一个带默认值的枚举字段、OI-31 只加配置入口与端点、OI-32 只加只读端点与 DOM 标记），按 §9.2 不构成递增理由，也**不得**使 `artifacts/` 缓存失效。

#### 1.7.4 第四轮新增裁决（2026-09-19 OI-19 落地口径）

OI-19（3D 分级显隐 + 剖切）此前只有"落 M1 增补项、并入 M2 验收"这一句排期，**粒度与实现结构从未裁定**。开工前的实测发现它有一处硬前提不成立：

| 实测事实 | 出处 |
|---|---|
| 权威产物 `model_lod2.glb` = **1 个节点 / 1 个网格 / 3 个 primitive**，且**无节点名** | 读 GLB 的 JSON 块 |
| `build_solid()` 把**整条母线段链**一次回转成**一个体**——几何层不存在"级 / 部件"实体 | `revolve.py` |
| 组件树显示的是**母线段链**（每段一行），不是规格举例的整流罩 / 贮箱 / 喷管 | `SegmentPanel.tsx` |
| `Compound(children=[具名子体])` → GLB 得 `root（无 mesh）+ seg-0 + seg-1` 各持一个 mesh | 实测探针（build123d 1.x + OCCT 的 `RWGltf_CafWriter`） |

即：**规格要求"按级 / 按部件显示或隐藏"，而几何里没有可分级的对象**——不裁决粒度，实现只能退化为"点了没反应"或"用裁剪冒充隐藏"。

| ID | 条目 | 裁决 | 落地位置 |
|---|---|---|---|
| OI-33 | **分级显隐的粒度与 GLB 场景图契约**（OI-19 的落地口径） | ① **粒度 = 母线段链**：M1/M2 的几何是"一条母线段链回转成的单体"，真正的部件与 §5.9 的 9 段分区要到 M5 才成为独立实体；组件树当前正是段链，故显隐粒度取段链，**组件树 ↔ 视口双向联动**由此成立。⚠ 这不是降级实现——**具名节点是唯一的可分级机制**，M5 出现真部件时只把节点名从 `seg-<i>` 换成分区名，机制不动。<br>② **场景图契约**：`root`（名 `vehicle`，**无 mesh**，持 glTF 的 Z-up→Y-up 根旋转）→ `seg-0` / `seg-1` / …（各持一个 mesh；`<i>` 为 `profile.segments` 的 **0 基**下标，与参数层的 `stages[i]` **无关**）。三条硬约束：**节点名必须存在**——缺名即"点了没反应"（找不到节点=不隐藏，**静默失效**，故须逐段机检）；**根节点不得持 mesh**——否则整体网格与分段网格**叠加渲染**，同一处被画两遍；**退化段**（两端半径均为 0，回转体为空）不产出节点，前端对不存在的节点不做隐藏。<br>③ **两通道同粒度**：示意通道同样逐段渲染（后端 `ValidationReport.segment_outline` 逐段下发闭合轮廓，仍是纯搬运，ADR-011 不变），否则"隐藏"只在一侧生效。<br>④ **量测与导出不受影响**：`metrics.json`、`model.step` 与体积 / 表面积 / 质心**一律取整体体** `build_solid()`——分段体只用于 GLB 导出与显隐。分段体之间新增的界面在**未隐藏任何段时被实体完全包住、从外部不可见**，故"看到的"与"算的"仍是同一个外形（P1 / ADR-012）；隐藏某段时露出的正是该处的真实分界，不是人造结构。<br>⑤ **`SPEC_VERSION` 0.6.0 → 0.6.1**：同一输入的 GLB 字节已改变（单节点 → 逐段节点），而缓存键里只有 `canonical_json` / `kernel_version` / `spec_version` 三项可变——不递增即**静默供应单节点的陈旧产物**，表现为"显隐点了没反应"且**全程零报错** | §9.2 §11.4 §13.6 §16.4 |

> **本轮递增的性质**：第 ⑤ 条不是"几何语义变化"，但按 §9.2 精确化后的判据（**同一输入的产物字节是否变了**）必须递增。判据的精确化见 §9.2。

#### 1.7.5 第五轮裁决（2026-09-19 参数盘点 QA + OI-34）

M3 数据层开工前，用户发起一次**参数系统 QA**：盘点 §6.1 十层全部用户可自定义参数、确认哪些不必要，并提出两条设计要求——**数据库导入的已有型号参数必须是"提示形式"**（按用户自定的火箭名判断是否命中已知型号，提示后用户可二次更改），以及**参数模块必须按火箭功能与系统工程划分、不得混成一团**。盘点确认 §6.1 十层分层（总纲 / 级 / 发动机 / 贮箱 / 构型 / 气动 / 发射场 / 时序 / 回收 / 损失）**已经是**系统工程划分，前端参数面板亦按层分区；QA 查出的结构问题逐条裁决如下：

| ID | 条目 | 裁决 | 落地位置 |
|---|---|---|---|
| QA-1 | **级层比冲三字段冗余**：`Stage.isp_vacuum_s/isp_sea_level_s/isp_source` 与 `Engine.isp_*` 是两份真相——`default` 模式下用户还得再填一遍发动机里已有的数 | **修**（用户确认）：发动机标称值为唯一权威；`default` ⇒ 级层**省略** `isp_*`，后端复制下发；`default`+不一致 → hard；`default`+一致副本 → 警告（多余存储迟早漂移）；`custom` 缺值 → hard。DAG 输入解析负责取值，绕过诊断直调且 custom 缺值 → 拒绝 | §6.1（级层比冲唯一权威段）constraints.py dag.py schema.py |
| QA-2 | 加注比例双层重复（`Stage.fill_fraction` 与 `Tank.fill_fraction` 各一份） | **不修**（用户裁决）：保留双层——级层加注比例在 M4 定尺求解中是整体输入，箱层是 §5.9 派生的输入；二者语义随 M4 建模后如仍重叠再裁 | — |
| QA-3 | 材料 / 壁厚三处自由文本（`Vehicle.material` / `Stage` / `Tank`），无工艺校验 | 随 **M3 材料库**建库一并处理：`material` 变为材料库引用，壁厚受工艺下限校验 | M3 |
| QA-4 | 占位字段（`wind_profile_ref` / `fins_enabled` / `trigger` / `efficiency_factor` / `LossFactors`） | **全部保留**（用户裁决）：它们是 M4/M5/M6 的既定挂点，删除再加回徒增迁移 | — |
| OI-34 | **型号提示交互**：模板库 / 快照型号与用户自建的关系 | 用户自建火箭**优先**；名称栏输入/修改时前端调名称匹配端点，**命中已知型号 → 提示条**（非弹窗强制）「检测到已知型号 X（来源已核对）——载入其参数？」；载入后**全部字段保持可编辑**，且该型号的 `sourced_fields`（字段 → 出处）一并下发、逐控件标注；用户改动任何值后该字段出处标注转为"用户修改"，溯源链不断 | §16 M3 行（模板库交付时一并实现） |
| OI-35 | **推力 / 比冲的环境条件口径**（用户要求：海平面与真空参数必须区分、标注、禁止混用） | ① **海平面** = ISA 标准海平面（海拔 0 m、15 °C、101.325 kPa）；发射场当日实际大气归 §8.6 损失模型（M4）。② **真空** = 理想真空（背压 0）理论值。**标注强制**：字段名携带 `_sea_level_`/`_vacuum_`；UI 标签、`metrics`/`provenance`、模板 `sourced_fields` 每个推力/比冲值必须随值标注环境，无环境限定的"推力/比冲"一律视为缺陷。**配对规则**：ṁ 只用真空推力配真空比冲；T/W 只用一级海平面总推力配 GLOW；海平面比冲不得代入真空流量公式。**数据层**：GCAT stages 的 Thrust（真空）/ThrustSL（海平面）分列；engines 的 Thrust 官方未声明环境 → 口径中立命名 `typical_thrust_n` + 标注"环境未声明"，不参与配对计算；engines `mass` 据官方原文更正为 **loaded（满装，仅固体）** | §6.1（环境条件口径段）§7.3 §7.5 |

> **`SPEC_VERSION` 保持 `0.6.1` 不变**：QA-1 是参数层语义变更，**不改变同一输入的 GLB 字节**——按 §9.2 判据不得使 `artifacts/` 缓存失效。

---

## 2. 设计原则

| 原则 | 含义 | 派生硬规则 |
|---|---|---|
| **P1 单一真相源** | 一套几何内核、一套单位系统、一套参数 Schema | 禁止两套几何实现并存；禁止界面层做单位换算 |
| **P2 计算与渲染解耦** | 精确 NURBS 用于计算与导出，离散网格仅用于预览 | 前端永不接触 NURBS；导出永不使用网格 |
| **P3 计算廉价化** | 让重建足够快，而不是让渲染更复杂 | 优先优化「几何生成 + 计算」，而非「渲染管线」 |
| **P4 结论可辩护** | 输出点值必须附区间与敏感度 | 单点值的 API 响应视为不完整 |

**P3 的战略含义**：原方案的交互瓶颈被误判为前端渲染，因此设计了三层 LOD、渐进式重建、200ms 防抖等渲染侧复杂度。本平台通过**内容寻址缓存 + 预计算表**把几何重建与性能评估压到毫秒级，从而**取消这些复杂度**，并额外解锁设计寻优（见 §14）。

---

## 3. 技术选型与许可审计

### 3.1 选型总表

| 层 | 组件 | 选型 | 许可证 | 说明 |
|---|---|---|---|---|
| 运行时 | 语言 | **Python 3.12** | PSF | 版本锚点见 §3.2 |
| 运行时 | 环境/锁 | **uv** | MIT/Apache-2.0 | 单管理器；替代 conda + Poetry 双轨 |
| 几何 | BREP 内核 | **build123d 0.11.x**（OCP/OCCT） | Apache-2.0 | pip 可装；类型完备 |
| 几何 | 网格化 | 内核自带 mesher + 自研 LOD | — | 输出 glTF/GLB |
| 推进 | 热力学 | **cea 3.3.x**（NASA 官方重写版） | **Apache-2.0** | 替代 GPL-3 的 rocketcea |
| 数值 | 计算 | numpy / scipy | BSD | 插值、求根、优化 |
| 数据 | 表格 | pandas / pyarrow | BSD / Apache-2.0 | Parquet 快照 |
| 数据 | 存储 | **SQLite**（+ 可选 DuckDB 分析） | 公有领域 / MIT | 单文件、免部署 |
| 后端 | API | FastAPI + uvicorn | MIT / BSD | |
| 后端 | 校验 | Pydantic v2 + pydantic-settings | MIT | Schema 与配置统一 |
| 前端 | 构建 | **Vite** | MIT | 弃 Next.js（无 SSR 需求） |
| 前端 | 构建插件 | `@vitejs/plugin-react` | MIT | React Fast Refresh + JSX 转换 |
| 前端 | 包管理 | **npm** | Artistic-2.0 | 随 Node 24 分发；锁文件 `package-lock.json` |
| 前端 | 框架 | React 19 + TypeScript | MIT | |
| 前端 | 3D | three + @react-three/fiber + drei | MIT | |
| 前端 | 状态 | Zustand | MIT | |
| 前端 | 样式 | Tailwind CSS 4 + shadcn/ui | MIT | 骨架层 |
| 前端 | 航天组件 | `@zendir/ui`（锁版本，适配层隔离） | 见 §3.4 | 仅专用组件 |
| 前端 | 图表 | ECharts | Apache-2.0 | |
| 测试 | 后端 | pytest + hypothesis | MIT | 属性测试 |
| 测试 | 前端 | vitest | MIT | |
| 测试 | 前端 DOM 环境 | jsdom | MIT | vitest `environment: 'jsdom'` |
| 测试 | 前端渲染 | `@testing-library/react` + `jest-dom` + `user-event` | MIT | 组件行为断言（非实现细节） |
| 契约 | 前端类型生成 | `openapi-typescript` | MIT | OpenAPI → `schema.d.ts`；⚠ 依赖 `typescript` 5.x 的 Compiler API（R-31） |
| 质量 | 静态检查 | ruff + mypy（strict） | MIT | **对 AI 生成代码是刚需** |
| MCP | Agent 接入 | FastMCP（自建） | Apache-2.0 | 领域工具，非通用 CAD |
| 交付 | 打包 | **PyInstaller 6.22.3**（onedir） | **GPL-2.0-with-bootloader-exception** | 冻结后端与 Python 运行时为 exe；例外条款见 §3.4 ⚠ |
| 交付 | 桌面壳 | **pywebview 6.2.1**（Windows 侧 winforms 后端 → EdgeChromium） | BSD-3-Clause | 原生窗口承载 `dist/`，双击 exe 即用（ADR-015） |
| 交付 | 壳的传递依赖 | **pythonnet 3.1.0** + `clr-loader` | MIT | ⚠ pywebview 在 Windows 上**经 .NET/WinForms 建窗**，故引入此依赖；要求系统有 .NET Framework（Win10/11 自带，本机 4.8） |
| 交付 | 运行时依赖 | **Microsoft Edge WebView2 Runtime** | 微软专有，**免费可再分发** | ⚠ 非开源，属授权策略例外之一（另一处为 PyInstaller），见 §3.4 |
| 交付 | 安装包（M7） | **NSIS**（首选，zlib/libpng，OSI 认可）/ Inno Setup（备选） | zlib-libpng / Inno Setup 自由使用许可（**非 OSI**） | 单文件安装器与卸载；**选 Inno Setup 则须再立一处许可例外，故默认 NSIS**；M0.5 不产出安装包 |

### 3.2 已验证事实与版本锚点

以下为立项时**实测核实**的结论，构成选型的事实基础：

| 事实 | 结论 | 影响 |
|---|---|---|
| `pythonocc-core` 仅存在于 conda-forge，**不存在于 PyPI** | Poetry/pip 无法安装 | ❌ 弃用（原方案 PoC 会直接失败） |
| conda-forge 上最新为 7.9.3（win-64） | 无 "^7.8" 版本号 | ❌ 弃用 |
| `build123d` 提供 `pip install build123d`（0.11.1） | 基于 OCP/OCCT，pip 全流程可用 | ✅ 采用 |
| `cadquery` wheel 仅支持 **Python 3.9–3.12** | 上限 3.12 | 版本锚点 |
| `cea` 需 **Python ≥3.11**，Apache-2.0，NASA 官方 | 许可宽松、官方维护 | 版本锚点 + ✅ 采用 |
| `rocketcea` 为 **GPL-3** | 传染性许可 | ❌ 弃用（法务地雷） |
| `@react-three/fiber` v9 支持 React 19 | 组合可行 | ✅ |
| npm 上**不存在** `occt-mcp-server` / `cad-cae-copilot-mcp` | `npx -y` 必然失败 | ❌ 改为自建 MCP |

**M0 依赖运行时探验（实测，非推测）**——以下结论均为在本机装好依赖后**实际跑通**得到的，构成 M0 验收证据：

| 事实 | 实测结论 | 影响 |
|---|---|---|
| OCCT 发行名为 **`cadquery-ocp-novtk`**（7.9.3.1.1） | `importlib.metadata.version("cadquery-ocp")` **必抛** `PackageNotFoundError`，而 `import OCP` 与几何内核**完全正常** | 依赖探测与锁文件校验必须使用发行名 `cadquery-ocp-novtk`，禁止按 `cadquery-ocp` 判定"缺失" |
| `build123d` 0.11.1 在**模块导入期**无条件扫描系统字体，且 `register_font()` **无异常保护**（`text.py:271` → `register_system_fonts` → `TTFont(path)`） | 系统内**任一**以 `.ttf` 命名的非字体文件都会使 `import build123d` 整体崩溃。本机实测 `C:\Windows\Fonts\mstmc.ttf`：4096 B 随机数据、sfnt 版本标识非法、且**未注册于三个 Windows 字体注册表键**（HKLM / HKCU / WOW6432Node 均 0 命中），属残留文件而非系统字体 | 环境预检须含字体完整性检查；详见 ADR-014 |
| OCCT 几何内核**可用性已验证** | `Cylinder(r=1.85, h=10.0)` 解析体积 `πr²h = 107.521009` vs 内核体积 `107.521009`，**误差 0.0**；包围盒 `[3.7, 3.7, 10.0]` 精确 | ✅ R-01/R-03 的消除结论得到运行时确认 |
| `Shape.mesh()` **不是**网格生成器 | 其行为是**原地**触发 OCCT 三角化并返回 `None`；取网格数据须用 `Shape.tessellate(tolerance, angular_tolerance) -> (vertices, indices)`（实测 10 m 圆柱 @0.5 → 506 顶点 / 500 三角面） | M1 LOD 网格实现口径；误用 `.mesh()` 会得到 `None` 而非报错 |
| GLB 导出可用：`export_gltf(shape, path, unit=..., binary=True, linear_deflection=0.001, angular_deflection=0.1) -> bool` | 实测导出成功（16 888 B）。**默认 `unit=Unit.MM`** | ⚠ 本工程以 **米** 为单位，调用必须显式传 `unit=Unit.M`，否则几何整体缩放 1000× —— 直接触发 R-25 双通道漂移 |
| **GLB 的 Z-up → Y-up 由「根节点旋转」承担，访问器里仍是 Z-up** | 实测导出的柱 `r=1, L=3`：JSON 块内 `nodes = [{"rotation": [-0.7071067811865475, 0, 0, 0.7071067811865475], "mesh": 0}]`（即 `Rx(-90°)`），而 `accessors[0].min/max = [-1, -0.999689, 0] / [1, 0.999689, 0]`（Z-up，轴向在 z） | ⚠ **读包围盒/朝向必须走场景图节点变换**（`M = T·R·S`，矩阵**列主序**），否则朝向判断整体差 90°。M2 首项的实际缺陷即由此而来（详见 §16.3 结案记录） |
| `cea` 3.3.4 为 NASA 官方仓（github.com/nasa/cea），Apache-2.0，自带 `libcea` 原生库与 `data/thermo.lib` | 无需配置外部数据文件路径，`import cea` 即可用 | ✅ 离线自包含 |
| `cea` 内置 ThermoDB **含 `RP-1`**（真实物种，非替代物） | 液氧/煤油可直接精确建模 | ✅ 覆盖 §1.2 目标组合 |
| `cea` 内置 ThermoDB **不含** `Al2O3(L/S)`、`CO2(S)`、`H2O(S)`、`C(S)` 等 | 含铝/含固相推进剂**无法**用内置库建模 | 范围显式限定为**非含铝液体推进剂**组合 |
| `RocketSolver.solve()` 必须提供 `hc` 或 `tc` | 仅给 `pc` + `supar` 会抛 `ValueError: either hc or tc must be provided`；`hc` 须由 `reac.calc_property(cea.ENTHALPY, weights, T0)/cea.R` 求得 | 工作点求解器接口契约 |
| **`Isp` / `Isp_vacuum` 返回的是有效排气速度（m/s），不是秒** | 实测 `Isp_vacuum / c* ≡ 真空推力系数`（LOX/CH4 @ε=40：3651.84 / 1859.88 = 1.9635），且 `Isp / c* ≡ 海平面推力系数`（3461.76 / 1859.88 = 1.8613），两个独立恒等式同时成立 → 该字段量纲为速度 | ⚠ **换算为秒必须除以 `g₀`**，否则 Δv 全线高估 9.80665×。详见 ADR-014 |
| **产物列表顺序可致静默错误解** | 凝相物种（如 `H2O(L)`）置于列表中部（实测索引 10）且其后仍有物种时，返回 `Tc ≈ 882 K`、`Isp` 腰斩的错解，而 **`last_error` 仍为 0**。将同一物种移至列表**末位**即恢复正常（`Tc = 3629 K`） | ⚠ **静默失效**，必须由 ADR-014 契约与健全性门禁拦截 |
| `@types/react` / `@types/react-dom` 必须与 React 主版本**同大版本** | 实测 React 19.3.0 配 `@types/react` 19.3.0，`tsc -b` 通过 | 类型包随 React 大版本同步锁定 |
| **`typescript` 必须停留在 5.x，禁止升 7.x** | 实测安装 `typescript@latest` 得到 **7.0.2**（原生重写版），其包不再暴露完整 Compiler API：`import('typescript')` 的 `default.factory` 为 `undefined`，导致 `openapi-typescript@7.13.0` 在**模块加载期**即抛 `TypeError: Cannot read properties of undefined (reading 'createKeywordTypeNode')` | ⚠ 前端类型生成链路（OpenAPI → `schema.d.ts`）整体失效。`package.json` 必须锁定 `typescript@^5.7`（附录 A），升级需先核实 Compiler API 可用性 |
| npm 默认缓存目录写入被沙箱拒绝 | 本机 npm 默认 `cache = D:\nodejs\npm-cache`，安装时抛 `EPERM: syscall open` 并被沙箱拦截（与被拦截的 pip/uv 系统路径同类） | 与 uv 一致改用**工程内自包含**缓存：`.npmrc` 指定 `cache=../.tools/npm-cache`（§15），安装即可用 |
| 前端工具链实测版本组合可用（Node 24.18.0） | Vite 8.3.0 + `@vitejs/plugin-react` 6.1.1 + Vitest 5.0.1 + jsdom 30.1.0 + `@testing-library/react` 16.3.3，React 19.3.0 下 `tsc -b` / `vitest run` / `vite build` **三者全绿** | ✅ 版本组合锁定（附录 A）；规格早期写的 `vite ^6` / `vitest ^2` 已被实测版本取代 |
| Vite 开发服务器代理链路可用 | 实测 `http://localhost:5173/api/health` 经代理转发至 `127.0.0.1:8000`，返回 `{"status":"ok","name":"aeroforge","version":"0.1.0"}` | ✅ M0「打通一次请求」验收证据 |

**M0.5 桌面壳依赖探验（实测，非推测）**：

| 事实 | 实测证据 | 结论 / 影响 |
|---|---|---|
| `pywebview` 6.2.1 可导入 | `import webview` 成功；`webview.platforms.winforms` 可导入 | ✅ 壳层可用 |
| **Windows 侧 pywebview 经 .NET/WinForms 建窗** | 安装 `pywebview` 时自动引入 `pythonnet 3.1.0` + `clr-loader 0.3.1` + `cffi`；`import clr` 成功 | ⚠ **新增一层 .NET 依赖**：冻结时须一并收集 pythonnet/clr-loader；系统需有 .NET Framework（Win10/11 自带） |
| 本机 .NET Framework 就位 | 注册表 `HKLM\SOFTWARE\Microsoft\NET Framework Setup\NDP\v4\Full` → `Release=533509`、`Version=4.8.09221` | ✅ 4.8，满足 pythonnet 要求 |
| 本机 WebView2 运行时已安装 | 注册表 `HKLM\SOFTWARE\WOW6432Node\Microsoft\EdgeUpdate\Clients\{F3017226-FE2A-4295-8BDF-00C3A9A7E4C5}` → `pv=153.0.4234.32` | ✅ R-33 的前提在本机成立；**该键位即探测接口**，启动自检与安装包检测都用它 |
| `PyInstaller` 6.22.3 就位 | 随 dev 组安装，附 `pyinstaller-hooks-contrib 2026.7` | ✅ 打包器可用 |


**CEA 探验基准值（黄金测试锚点，供 M4 反标定使用）**——平衡流（`iac=True`），ε = 40，产物为**纯气相列表**：

| 组合 | O/F | Pc | 反应物 T₀ | Tc (K) | c\* (m/s) | Isp_vac 字段 | **Isp_vac (s)** | Isp_sl (s) |
|---|---|---|---|---|---|---|---|---|
| LOX / RP-1 | 2.56 | 68.9 bar | 300 K | 3662.5 | 1796.2 | 3496.9 | **356.6** | 339.1 |
| LOX / CH4 | 3.40 | 100 bar | 300 K | 3629.2 | 1859.9 | 3651.8 | **372.4** | 353.0 |
| LOX / LH2 | 5.50 | 68 bar | 20 K | — | 2319.2 | 4400.1 | **448.7** | — |

> 三组真空 Isp 与公开文献值（LOX/RP-1 ≈ 355–358 s、LOX/CH4 ≈ 370–380 s、LOX/LH2 ≈ 450–455 s）吻合，构成 `Isp(秒) = 字段值 / g₀` 这一换算关系的**外部锚定**，同时说明产物列表正确时求解器输出可信。

**版本锚点：Python = 3.12**
- 下限约束：`cea` 需 ≥3.11
- 上限约束：`cadquery-ocp-novtk` wheel 支持 ≤3.12
- 交集 → 3.12

### 3.3 关键决策记录（ADR）

| 编号 | 决策 | 理由 | 被否方案 |
|---|---|---|---|
| ADR-001 | 几何内核用 **build123d 单内核** | pip 可装、类型完备、可 mypy 检查 | pythonocc-core（需 conda）；CadQuery（与 build123d 同内核但 API 冗余）；三套混用（ABI 冲突） |
| ADR-002 | 环境用 **uv + Python 3.12**，删除 conda | 全依赖回到 PyPI，单管理器 | conda + Poetry 双轨（必然冲突） |
| ADR-003 | 热力学用 **NASA `cea`** | Apache-2.0，官方维护 | rocketcea（GPL-3 传染） |
| ADR-004 | CEA **离线预计算 + 运行时插值** | 快 10³ 量级、去除 Fortran 运行时依赖、表作为数据可自由分发 | 运行时调用 Fortran |
| ADR-005 | 几何表示**双轨制**（STEP 精确 / glTF 预览） | glTF 无法承载 NURBS，原方案承诺不可兑现 | 前端直传 NURBS（不可行） |
| ADR-006 | 几何与计算走**独立 worker 进程** | OCCT 同步、CPU 密集、部分非线程安全 | async handler 直调（阻塞事件循环 + 随机崩溃） |
| ADR-007 | 存储用 **SQLite** | 数据量 ~5×10³ 条，单文件免部署 | PostgreSQL（过度设计，与本地工具定位冲突） |
| ADR-008 | 前端用 **Vite SPA**，弃 Next.js | 本地工程工具无 SSR/SEO 需求 | Next.js（徒增复杂度） |
| ADR-009 | **自建领域 MCP** | 原指定 MCP 不存在；领域工具价值更高且离线可控 | 第三方 CAD MCP（不存在 / 只读 / 语言栈不符） |
| ADR-010 | 运力输出**必须带区间** | 单点值不可辩护，工程决策需要误差边界 | 点值输出 |
| ADR-011 | **物理计算的权威在后端 Python；前端零物理公式** | 满足补充手册"单一数据源"原则与 §1.4-4 溯源红线。双份实现必然漂移，且前端结果无法溯源（无快照与内核版本）。"立即跳动"的体感由**本地服务低延迟 + 缓存命中 < 50 ms + 前端示意网格**三者共同满足，**不靠在前端重算物理** | 补充手册"初期用 TypeScript 实现前端物理公式计算"（若采纳，同一 ΔV 会有两个实现，且前端结论不可溯源） |
| ADR-012 | **3D 采用双通道预览**：前端**示意网格**（即时）+ 后端**权威 GLB**（替换） | 兼顾补充手册要求的"无缝重建"体感与 §5.1 双轨表示契约。示意网格仅由尺寸参数程序化生成，**禁止参与任何计算**，且必须在 300 ms 内被权威 GLB 替换；二者包围盒偏差 > 1% 即视为实现缺陷（门禁见 §13.6） | 全部走后端（本地往返仍会有感延迟）；全部走前端程序化网格（违背 §5.1，失去 NURBS 精度与 STEP 导出能力） |
| ADR-013 | **设计 token 分两层：语义层 + 主题层**；提供 `default-dark` 与 `vscode-dark` 两套主题 | 补充手册给了另一套深色配色（`#1e1e1e / #252526 / #007acc / #4ec9b0`），与本规格 §11.2 既有 token 冲突。二者都是**视觉偏好**而非架构约束，用语义层解耦可同时满足，且避免未来每次改色都动组件代码 | 二选一硬合并（必然有一方需求落空）；组件内直接写色值（NFR-05 禁止） |
| ADR-014 | **CEA 调用契约**（三条硬约束）：① `Isp`/`Isp_vacuum` 字段量纲为**有效排气速度 m/s**，转秒必须 `/ g₀`；② 产物列表**固定"纯气相 + 凝相仅置于末位"**，凝相物种不得出现在列表中部；③ 每次求解后必须过**健全性门禁**（`last_error == 0` **且** `Tc` 落在物理区间 **且** O/F 单调性通过），任一不满足即判失败并报错，禁止返回结果 | 实测 `cea` 3.3.4 两处**静默失效**：单位上，`Isp_vacuum / c* ≡ 真空推力系数`、`Isp / c* ≡ 海平面推力系数` 两个独立恒等式同时成立，证明该字段是速度而非秒，若当秒用会使 Δv 与运力全线高估 **9.80665×**；产物列表上，凝相物种位于索引 10 且其后仍有物种时返回 `Tc` 3629 K → 882 K、Isp 腰斩的错解，而 **`last_error` 仍为 0**（同一物种移至末位即恢复正常）。二者都不会抛异常，只能靠契约约束与门禁拦截 | 直接用 `Isp` 数值当秒（隐蔽 10× 量级错误）；信任 `last_error` 作为唯一收敛判据；由使用者各自注意单位与列表顺序（§1.4 溯源红线要求口径唯一） |
| ADR-015 | **桌面交付走 PyInstaller + pywebview（WebView2）原生窗口，不用 Tauri**；交付物为**双击即用的 exe**，不是"访问某个网址" | 平台的质量与复杂度全在 Python 侧（OCCT 几何内核、CEA 原生库、numpy/scipy）。PyInstaller 只需维护**一套**打包链，即可把后端与 Python 运行时一并冻结进 exe；pywebview 以**原生窗口**承载构建好的 `dist/`，无浏览器外壳、无地址栏。Tauri 虽外壳更精致且自带 MSI/更新器，但其 Windows 侧**仍须**用 PyInstaller 把 Python 冻结成 sidecar，等于两套构建系统叠加 sidecar 生命周期与端口握手，集成面显著更大。前端代码在两种壳下**完全相同**，壳可替换，故此选择不锁死未来 | Tauri v2 + Python sidecar（两套工具链 + 进程管理复杂度）；纯浏览器访问 localhost（**用户已明确否定**："目标是 exe 启动的软件，不是网页"）；Electron（额外 ~150 MB Chromium，且与 Python 侧运行时重复） |

### 3.4 许可审计规则

- 引入新依赖流程：登记进 §3.1 表格（含版本、许可原文链接）→ 审查 → 落锁文件 → 复核。
- **禁入名单**：GPL-2.0/3.0、AGPL、SSPL、CC-NC、以及任何"非商用"条款。
- **⚠ 例外一：PyInstaller = GPL-2.0 *with bootloader exception***。许可正文本身是 GPL，但其 **Bootloader Exception** 明确允许"将本程序打包后的可执行文件按任意许可分发"，即**不传染**被冻结的私有代码。结论：可作为**构建期工具**使用，产物（exe）不受 GPL 约束。裁决依据为官方 `COPYING.txt` 的例外段落，须在落锁时存档原文链接。**限制**：不得修改 PyInstaller 自身源码后分发（否则触发 GPL）；不得把 PyInstaller 链接进我们的 Python 包。
- **⚠ 例外二：Microsoft Edge WebView2 Runtime 为专有软件**（免费、可再分发，但非开源），是本平台**运行时**唯一的非开源依赖。裁决：接受，理由是它是 Win10/11 的系统级渲染组件（多数机器已预装），且桌面壳无开源等价物能在 Windows 上同时满足"原生窗口 + WebGL2 + 体积可接受"。**对策**：① 交付文档中显式声明该依赖与许可；② 安装包检测运行时是否就绪，缺失时引导安装而非静默失败；③ 若未来授权要求严格的纯开源栈，替换路径为 pywebview 的 QtWebEngine 后端（LGPL，但体积与许可复杂度显著上升）——因 §11.7 适配层隔离，替换成本被限制在壳层。
  **注意**：全部例外共 **两处**（PyInstaller 之 GPL 例外、WebView2 之专有许可），此外不得再引入第三处；M7 安装包因此**默认选 NSIS**（zlib，OSI 认可）而非 Inno Setup（自由使用但非 OSI）。
- **`@zendir/ui` 特殊条款**：许可需在 M0 先行核实。采用**适配层隔离**策略——仅允许 `frontend/src/adapters/space-ui/` 引用，业务代码禁止直接 import。若许可不可接受或包停止维护，仅需替换一个目录。
- **数据许可**：GCAT 及其镜像的许可须在 M3 前核实并记录在 `data/snapshots/<snapshot>/LICENSE-NOTICE.md`；不确定则降级为"用户自备数据"模式（提供导入工具而不打包数据）。

### 3.5 国内外生态适配策略

| 需求 | 策略 |
|---|---|
| PyPI 拉取慢 | 走清华 TUNA / 阿里云镜像（`UV_INDEX_URL` 或 pip 配置），**构建与运行本身永不联网** |
| npm 拉取慢 | 走 npmmirror |
| GitHub 依赖慢 | 镜像加速或 Gitee 镜像拉取后固化 |
| 数据源不可达 | 一次性下载 → 快照固化 → 之后完全离线 |
| 文档语言 | 全中文优先，代码标识符英文 |

---

## 4. 总体架构

### 4.1 分层图

```text
┌──────────────────────────────────────────────────────────────┐
│  桌面壳  pywebview 原生窗口（WebView2）· 无浏览器地址栏         │
│  ├──────────────────────────────────────────────────────────┤ │
│  │  前端  Vite + React 19 + R3F（构建产物 dist/ 由后端托管） │ │
│  │        参数面板 · 3D 视口 · 性能图表 · 方案对比           │ │
│  └──────────────────────────────────────────────────────────┘ │
├──────────────────────────────────────────────────────────────┤
│  API   FastAPI  ·  REST 即时读 + 异步作业 + WebSocket 进度     │
│        静态托管 dist/ + SPA fallback + 空闲端口自选            │
├──────────────────────────────────────────────────────────────┤
│  领域层                                                       │
│    params/       参数 Schema · 依赖 DAG · 约束 · 单位系统      │
│    geometry/     旋转体 · 共底 · 喷管 · 尾翼 → BREP / 网格     │
│    propulsion/   CEA 表查询 · 效率因子 → 实际 Isp              │
│    sizing/       质量回归 · 多级迭代 · 损失 → 运载能力         │
│    uncertainty/  Monte Carlo → 置信区间                        │
│    optimize/     多目标优化 · 权衡研究 · 批量扫描              │
├──────────────────────────────────────────────────────────────┤
│  支撑层                                                       │
│    cache/   内容寻址缓存（参数+内核+数据 → 哈希）              │
│    worker/  进程池 · 作业生命周期 · 进度事件                   │
│    data/    快照 · ETL · 仓储 · 溯源                           │
└──────────────────────────────────────────────────────────────┘
```

依赖规则：**只能向下依赖，禁止反向与跨层跳跃**。`geometry` 不得直接读写数据库；`sizing` 不得直接调用 OCCT；前端不得绕过 API 访问领域层。

**生产形态的同源契约（ADR-015）**：打包后**不存在** Vite 开发服务器。启动流程为 —— 后端进程启动 → 绑定一个**空闲端口**（不写死 8000，避免与用户机器上其他服务冲突）→ 以 `StaticFiles` 挂载 `dist/` 并对未知路径做 SPA fallback → pywebview 打开 `http://127.0.0.1:<实际端口>/`。因此前端**必须始终使用同源相对路径**（`/api/...`、`/ws/...`），**禁止**硬编码主机名或端口；开发期的 `server.proxy` 只是为了在浏览器里复现同一同源语义，不是生产链路的一部分。

### 4.2 主数据流

```text
输入源：手改参数 / 母线轮廓编辑器 / 数据库型号载入 / AI 助手对话
   ↓
[params] Schema 校验 + DAG 传播 + 单位归一（→ SI）+ 约束诊断
   ↓
[geometry] 母线剖面 → 旋转/放样 → BREP
   ├─→ STEP（精确，落盘）
   └─→ LOD 网格 → GLB（预览，落盘）
   ↓
[propulsion] CEA 表插值 → 理想 Isp → ×效率因子 → 实际 Isp
   ↓
[sizing] 质量回归 → 多级迭代 ↔ 任务时序（质量事件耦合） → 损失 → 运力
   ↓
[orbital] 目标轨道 ΔV 需求（含 Hohmann 转移）→ 各轨道运力
   ↓
[uncertainty] Monte Carlo → P5 / P50 / P95 + 敏感度排序
   ↓
[API] 返回 {点值, 区间, 敏感度, 诊断, provenance}
```

关键点：**任一环节的产物都以内容哈希为键落盘**，后续相同输入直接命中缓存（见 §9.2）。

### 4.3 几何与计算的契约

| 项 | 规定 |
|---|---|
| 长度单位 | m（内部） |
| 角度 | rad（内部），界面可用 deg |
| 质量 | kg |
| 压力 | Pa |
| 坐标系 | Z 轴为飞行器纵轴，原点在头部顶点；XZ 平面为基准对称面 |
| 对称性 | 旋转体按其轴线生成；尾翼按 n 重周向阵列 |
| 精度 | 几何容差 1×10⁻⁶ m；数值迭代收敛 1×10⁻⁶ 相对误差 |

---

## 5. 几何子系统

### 5.1 双轨表示契约（ADR-005）

| 用途 | 格式 | 精度 | 消费方 |
|---|---|---|---|
| 权威几何 | **BREP / STEP** | 精确 NURBS | 计算、导出、下游 CAD |
| 静特性 | **解析积分** | 精确 | 体积、表面积、质心（不依赖离散化） |
| 预览渲染 | **glTF / GLB** | LOD 网格 | 前端视口 |
| 网格导出 | STL（可选） | 网格 | 3D 打印 / 快速验证 |

**禁止**：把网格用于体积/质量计算；把 NURBS 语义承诺给前端。

### 5.2 母线轮廓编辑器（2D→3D 入口）

平台的**第一入口**是"在 2D 平面绘制/导入回转体母线 → 旋转/放样成 3D"。这是「2D→3D」能力的落点，不是附属功能。

| 能力 | 说明 |
|---|---|
| 绘制 | 折线、圆弧、切线卵形、抛物线、冯·卡门、椭圆、幂律、样条（曲线族见 §5.3） |
| 参数化约束 | 每段绑定参数（长度／半径／锥角／切线角／长径比）；段间声明 G0/G1 约束，由求解器保证，不靠手调 |
| 导入 | 由 STEP/DXF 或 CSV 点列反算母线；由 GCAT 型号尺寸生成初值 |
| 实时校验 | 半径非负、自交检查、单调性/凸性提示、G1 违反即时高亮 |
| 输出 | 母线定义 → §5.3 旋转/放样 → 实体 |

**存储格式**：母线以**声明式 JSON** 存储（段类型 + 参数 + 约束），**不存采样点**。这样才能参数化可复现、可 diff、可被 Agent 直接改写。

```json
{
  "meridian_id": "stage1-body",
  "segments": [
    { "type": "ogive",       "length": 3.2, "base_radius": 1.675, "tangent_len": 1.1 },
    { "type": "line",        "length": 12.0 },
    { "type": "ellipse_arc", "a": 1.675, "b": 0.9 },
    { "type": "line",        "length": 8.0 }
  ],
  "constraints": [
    { "kind": "g1", "between": [0, 1] },
    { "kind": "g1", "between": [1, 2] }
  ]
}
```

母线参数是 `Geometry` Schema 的一个子树，变更走 §6.2 同一套 DAG 传播。

### 5.3 参数化旋转体建模

所有回转构件统一以**母线剖面** `r(z)` 分段定义，再旋转成体：

```text
        头部顶点
           │
    ┌──────┴──────┐  ← 头锥段：母线类型可选
    │             │     锥形 / 切线卵形 / 抛物线 / 冯·卡门
    ╞═════════════╡  ← 柱段：r = const
    │             │
    ├─────────────┤  ← 级间段：锥台（r 线性变化）
    │  上贮箱      │
    ├─────────────┤  ← 共底：椭球面 / 球面（凹向一侧）
    │  下贮箱      │
    ├─────────────┤
    │             │
    └──╥───────╥──┘  ← 尾段/推力结构
       ║       ║     ← 喷管：钟形（抛物线近似）或锥形
```

**母线曲线族**（`geometry/meridian/`）：

| 类型 | 参数 | 用途 |
|---|---|---|
| 直线 | 起止半径 | 柱段、锥段、锥台 |
| 圆弧 | 半径、圆心 | 球头、球底 |
| 切线卵形 | 长径比、切线长度 | 头锥（工程常用） |
| 抛物线 | 系数 | 头锥（低阻） |
| 冯·卡门 | 长度 | 头锥（跨声速最优） |
| 椭圆 | 半轴比 | 椭球底、共底 |
| 幂律 | 指数 n | 通用过渡 |
| 钟形喷管 | 喉部半径、出口半径、长度比 | 喷管型面 |
| 样条 | 控制点 + 权重 | 自定义/逆向 |

**连续性规则**：任意两相邻段的连接处必须满足

- **G0**：位置连续（强制）
- **G1**：切线连续（曲面光滑的充要条件，无折痕）

违反 G1 的连接点在生成阶段即报错并指出段索引，不允许"能生成但看起来有折痕"的结果。

### 5.4 曲面光顺与曲率分析

回转体曲面在段连接处、锥台过渡处最易出现肉眼可见的折痕或"荷叶边"。仅靠 G1 约束**不足以**保证工程意义上的光顺，必须有显式的曲率分析与光顺后处理。

| 步骤 | 内容 | 通过判据 |
|---|---|---|
| 曲率分析 | 沿母线计算曲率 κ(s) 与曲率梳（curvature comb） | 无符号突变、无尖峰 |
| 连续性判定 | 段间 G0/G1/G2 判定 | 目标 ≥ G1；头锥→柱段过渡建议 G2 |
| 光顺 | 对超差段做张力样条重拟合，最小化 ∫(dκ/ds)² ds | 曲率梳单调或单峰 |
| 回写 | 光顺后的**参数回写母线定义**（非烘焙） | 母线 JSON 与实体内核一致 |
| 报告 | 曲率极值位置、连续性等级、光顺前后偏差 | 偏差 ≤ 弦高容差 |

**铁律**：光顺必须是**参数回写**，禁止对网格做拉普拉斯/平滑处理——网格平滑会破坏 NURBS 精确性，并导致"导出的 STEP 与看到的预览不一致"（违反原则 P1）。

### 5.5 特殊构型规则

**共底贮箱（common bulkhead）**

- 隔板为凹向一侧的椭球/球面，共享给上下两个贮箱
- 校验项：
  1. 隔板与柱段的连接处 G1 连续
  2. 上贮箱容积 + 隔板占体 + 下贮箱容积 = 柱段包络容积
  3. 隔板矢高 ≤ 允许值（避免与上下箱干涉）
  4. 两侧压差不同时的最小壁厚差异需在质量模型中体现（§8.4）

**尾翼**

- 非回转体，按剖面翼型（平板 / 楔形 / 双楔）+ 展长 + 根梢比 + 后掠角 + 周向数量定义
- 与柱段连接处做倒圆/根部整流，避免应力集中（几何上体现为局部圆角）

**装配树**

- 层级：飞行器 → 级 → 部件 → 特征
- 每个部件携带：几何 + 材料 + 质量贡献 + 参数来源（哪个 Schema 字段生成）

### 5.6 网格化与 LOD

| 级别 | 用途 | 目标面片数 | 生成时机 |
|---|---|---|---|
| LOD0 | 导出/精查 | 不限（按弦高容差） | 按需 |
| LOD1 | 默认视口 | ≤ 60k 三角面 | 建模时 |
| LOD2 | 旋转/缩放中 | ≤ 6k 三角面 | 建模时（预生成） |

- 弦高容差（linear deflection）默认 0.005 × 特征半径，角度容差 0.3 rad
- 网格以二进制 GLB 输出，前端不做几何运算
- 面片数与生成耗时记入 `metrics.json`，作为性能回归基线

### 5.7 几何验证

| 检查 | 方法 | 拦截目标 |
|---|---|---|
| 拓扑有效性 | OCCT `is_valid()` + 缝合检查 | 破面、非流形 |
| 实体性 | 体积 > 0 且封闭 | 自交、开口 |
| **解析对照** | 体积用旋转体积分/Pappus 定理；表面积用 ∫2πr·ds | 内核使用错误、母线定义错误 |
| 质量一致性 | 几何体积 × 材料密度 vs 质量回归结果（§8.4） | 两套质量来源背离 |
| 设计意图断言 | 如「总长 = 各段长度之和 ±1e-9」 | "有效但错误"的模型 |

**核心认知**：`is_valid() == True` **不等于**模型正确。必须补解析对照与意图断言，否则会稳定产出"能跑但错"的几何。

### 5.8 多格式导出与互操作

| 格式 | 角色 | 精度 | 用途 |
|---|---|---|---|
| **STEP (AP214/AP242)** | **权威交换格式** | 精确 NURBS | 下游 CAD（FreeCAD / SolidWorks / CATIA） |
| IGES | 旧工具兼容 | 精确 | 仅按需 |
| glTF / GLB | 预览与 Web | 网格（LOD） | 前端视口、报告插图 |
| STL | 快速成型 | 网格 | 3D 打印、快速验证 |
| OBJ | 通用网格 | 网格 | 第三方渲染/可视化 |
| VTK | 科学可视化 | 网格 | 后处理（可选） |
| JSON / CSV 报告 | 数据导出 | — | 参数表、质量预算、性能结果 |

规则：

1. **STEP 是唯一权威格式**，其余全部为衍生（回应原方案风险表中的"多格式导出兼容性"）
2. 导出走独立模块 `geometry/export/` 与独立 API（§10.1），不与建模职责耦合
3. 每次导出记录格式、容差、内核版本到 provenance
4. **导出精确格式前强制跑一次 §5.7 验证；验证未通过则拒绝导出**，只允许导出网格格式并附警告

### 5.9 工程剖面图的分区建模口径（OI-18）

**定位**：2D 工程剖面图（§11.10）不是"示意图"，而是**同一套几何参数的第二视图**——它必须能反过来核对 3D 模型是否漏件、错序。因此分区本身是**几何语义**的一部分：分区定义变了，同一输入的产出就变了（故本轮 `SPEC_VERSION` 递增，§1.7.2）。

**七条共性（建模依据）**：公开火箭平面剖面图（工程剖视图）经比对后收敛出七条稳定特征，本平台**按此建模**：

1. **轴向分区位置固定**（自上而下）：整流罩 / 有效载荷舱 → 载荷适配器 → 仪器（航电）舱 → 前裙 → 氧化剂箱 →（**级间舱 intertank** 或 **共底 common bulkhead**）→ 燃料箱 → **推力结构** → 后裙 → 发动机 / 喷管 → 尾段整流罩
2. **级间段（interstage）与级间舱（intertank）是两个不同部件**：**级间段**是**两级之间**的分离舱段（如 Falcon 9 一级与二级之间那段），**级间舱**是**同一级内两箱之间**的承载舱段（如 S-IC 的 intertank、航天飞机外贮箱的 intertank）。二者**不可混用同一字段**——§6.1 `Stage.级间段类型` 指前者（该级与其上级之间的分离段），本节的 9 段分区第 6 段指后者
3. **共底**用隔热夹层（蜂窝）把两箱封头合为**一张**，缩短级长、减重（历史实例：S-II 省约 10 ft / 近 4.5 t）
4. **氧化剂箱体积通常大于燃料箱**（煤油机约 2.3 kg 氧 / 1 kg 燃料），但**不是铁律**（见下）
5. **箱内附件固定清单**：防晃隔板（环状板）、增压气瓶 / 氦瓶、液位与温度传感器、加注 / 增压 / 排气管道、集液阱（sump）、绝热层
6. **输送方式两类**：**泵压式**（涡轮泵 + 大直径输送管）与**挤压式**（增压气瓶，常见于上面级）；上箱输送管**要么穿下箱内部**（S-IC 的 LOX 吸入管穿过 RP-1 箱）**要么沿箭体外侧**（Falcon 9 的 LOX 输送管是燃料箱的组成部分）
7. **尺寸标注是固定成分**（不是可选项）：总长、各箱长、前 / 后裙高、级间（舱）高、推力结构高、直径

> ⚠ **一条必须点明的「非铁律」**：氧化剂箱在上还是在下**没有统一规则**——S-IC（LOX 上）、S-II（LOX 上）、Falcon 9 一级与二级（LOX 上），而 S-IVB 是 **LH₂ 在上（占约 3/4）、LOX 在下（占约 1/4）**。故该次序**必须是用户可配置项**（§6.1 `Geometry.储箱排列`），**不得**在实现里写死"氧在上"。

**9 段轴向固定分区（`SectionBand`）**：剖面图与 3D 装配树共用同一分区枚举，按上表第 1 条落地：

| # | 分区 | 是否随参数出现 | 说明 |
|---|---|---|---|
| 1 | `fairing` | 有整流罩时 | 整流罩 / 有效载荷舱 |
| 2 | `adapter` | 有整流罩时 | 载荷适配器 |
| 3 | `avionics` | 恒有（可为 0 高） | 仪器（航电）舱 |
| 4 | `forward_skirt` | 恒有（可为 0 高） | 前裙 |
| 5 | `ox_tank` | 恒有 | 氧化剂箱 |
| 6 | `intertank_or_bulkhead` | 二选一 | **非共底 → 级间舱**；**共底 → 共享隔板** |
| 7 | `fuel_tank` | 恒有 | 燃料箱 |
| 8 | `thrust_structure` | 恒有 | 推力结构（含后裙） |
| 9 | `engine_bay` | 恒有 | 发动机 / 喷管 + 尾段整流罩 |

⚠ 第 5 / 7 段的**上下次序由 `储箱排列` 决定**，本表按"氧在上"的常见构型书写——实现必须读字段而非按序号硬编码。

**四项建模口径（裁决）**：

| # | 口径 | 具体要求 |
|---|---|---|
| 1 | **分区** | 一律按上表 9 段建模；每段的高度 `length_m` 由后端算出并**随结果下发**，前端不得自算（ADR-011） |
| 2 | **共底** | **默认关闭**（`共底设计 = false`）。开启后：① 级长缩减量由两箱封头矢高**公式反算**（不得手填）；② 缩减量与该级 `common_bulkhead_saving_m` 一并标注在剖面图上；③ **LH₂ 侧强制加隔热层**（其沸点低，共享隔板必须隔温），隔热层厚度计入质量模型 |
| 3 | **输送管道** | **默认外置沿箭体**（`delivery_pipe_routing = "external"`）；另提供「内置穿越」（`"internal"`）开关对应上箱管穿下箱内部。**两种走法都只做几何与质量表达，不引入任何 CFD / 流阻 / 流动计算**（非目标 §1.3） |
| 4 | **尺寸标注** | 出**全部**尺寸线（总长 / 各箱长 / 前后裙高 / 级间（舱）高 / 推力结构高 / 直径）；**数值一律由后端下发**，前端只做排版——禁止在前端由几何反推（ADR-011） |

**箱体比例由混合比派生（本节唯一的几何语义变更）**：氧箱与燃料箱的**容积比**不再由用户直接给定，而由发动机混合比与两剂密度派生：

```text
m_ox / m_fuel = O/F                              ← 混合比（质量比）
V_ox / V_fuel = (O/F) · (ρ_fuel / ρ_ox)          ← 容积比：后端派生，前端不得自算
```

该式经**两次独立反算核对**（由公开的推进剂质量与容积数据反算，非推演）：

| 型号 | 公开数据 | 公式结果 | 实测容积比 |
|---|---|---|---|
| S-IC（LOX / RP-1） | LOX 3 178 000 lb / 334 500 gal；RP-1 1 400 000 lb / 209 000 gal → `O/F = 2.27` | `2.27 × (6.70/9.50) = **1.60**` | `334 500 / 209 000 = **1.60**` ✅ |
| S-II（LOX / LH₂） | LOX 83 000 gal；LH₂ 260 000 gal；J-2 `O/F = 5` | `5 × (71/1141) = **0.311**` | `83 000 / 260 000 = **0.319**` ✅ |

派生规则：

1. 箱长 = 容积 / 截面积；截面积取该级直径（**分段柱体近似**，锥段按平均半径折算），**近似方式与误差必须在 `provenance` 中声明**
2. 用户若显式给定箱长，则该级**转为 `user` 来源并标注冲突**（§6.2：改 `derived` 节点须提示将覆盖哪些上游约束），不做静默覆盖
3. 混合比 `O/F` 由 §8.2 的**最优 O/F 工作点**给出（M4 前可由用户手填，标注「未经优化」）

---

## 6. 参数系统

### 6.1 Schema 分层

| 层 | 内容 | 示例字段 |
|---|---|---|
| `Vehicle` | 飞行器总纲 | 级数、目标轨道、发射场、载荷质量、总长、最大直径、**箭体材料（全局默认）**、**推进剂组合**、**整流罩直径** |
| `Stage` | 单级 | 推进剂、**直径**、**高度**、壁厚、材料、**结构系数 σ（存储权威）**、**干质比**（= `m_dry/m_prop`，§1.7）、**加注比例**（= `实际加注量/满装量`，**上限 1.0**）、**最大允许加注量**（可选，仅超装场景启用）、发动机台数、**发动机高度**（含喷管：安装基准面 → 喷管出口端面）、**发动机高度（不含喷管）**（可选）、工作时间、**级间段类型**、**真空比冲 / 海平面比冲**、**比冲来源**（自定义／默认）、可回收性 |
| `Geometry` | 构型 | 母线段列表（类型 + 尺寸 + 约束，§5.2）、尾翼、装配关系、**共底设计（开关）**、**共底类型**、**储箱排列**（燃料在上／氧化剂在上） |
| `Engine` | 发动机 | 型号、**推进剂相态**（`liquid` / `solid` / `hybrid`，默认 `liquid`，§1.7.3 OI-30——是 §6.5 固体 T/W 档的唯一判据）、循环方式、室压、面积比、效率因子、推力、Isp、**混合比 O/F**（箱体比例派生的输入，§5.9） |
| `Tank` | 贮箱 | 类型（共底／非共底）、直径、长度、壁厚、材料、容积、**标称满装量**（容积 × 推进剂密度）、**加注比例**、**最大允许加注量**（可选）、**液面高度（派生）**、**输送管走法**（外置／内置穿越，§5.9）、**共底隔热层厚度**（共底且 LH₂ 侧时必填） |
| `Aero` | **气动（简化）** | 阻力系数 `Cd`、参考面积 `A_ref`（默认取最大截面积）。⚠ 二者均为**工程惯例值**，非权威来源；MVP 只做简化模型，**不做 CFD**（非目标 §1.3）。缺失时按默认值并附 `warning` |
| `LaunchSite` | **发射场** | 名称、**纬度**、**海拔**、**发射方位角**（默认值，可被 `Mission` 覆盖）。纬度是自转加成与转向损失的唯一输入（§8.6），**不得**在计算中另设常量 |
| `Sequence` | **任务时序** | 事件列表（点火／级分离／整流罩抛离／入轨／着陆）、相对时刻、触发条件 |
| `Recovery` | **回收与复用** | 是否回收、级次、方式、着陆推进剂余量、回收系统质量代价 |
| `Mission` | 任务 | 目标轨道类型（LEO / SSO / GTO / GEO / **TLI** / **TMI** / 逃逸 / 自定义）、**轨道高度**、**倾角**、**偏心率**、**近地点高度**、**远地点高度**、发射场引用（或内联纬度 / 海拔 / 方位）、损失系数、风廓线引用 |

**关键派生量**（由 DAG 自动计算，不允许手工填写）：

```text
推进剂流量 ṁ = F_vac / (Isp_vac · g₀)   ← 真空推力只配真空比冲（OI-35 配对规则，禁止混用）
工作时间   t = 推进剂质量 / ṁ
贮箱容积   V = 推进剂质量 / 推进剂密度（区分氧化剂箱/燃料箱）
长径比     = 总长 / 最大直径
液面高度   h_liq = 加注比例 × 该贮箱可装高度

# 箱体比例（§5.9，OI-18）——唯一的几何语义变更，故 SPEC_VERSION 增至 0.6.0
容积比     V_ox / V_fuel = (O/F) · (ρ_fuel / ρ_ox)
箱长       箱长 = 容积 / 截面积（分段柱体近似，近似方式须写入 provenance）
共底级长缩减 由两箱封头矢高反算（不得手填），随结果下发 common_bulkhead_saving_m

# 剖面条带（§5.9 的 9 段分区）——高度由后端算出并随结果下发，前端不得自算
SectionBand.length_m            ← 9 段各自的轴向长度
SectionBand.dimension_labels    ← 尺寸标注的数值与锚点（前端只排版）

# 多级火箭自转加成（§8.6 / OI-25）
自转线速度  v_rot = ω_地球 · (R_地球 + 海拔) · cos(纬度)
自转加成    Δv_assist = v_rot · cos(方位角) · 与目标倾角的相容因子 ← 两阶段：先点值，后 MC 区间

# 质量比参数换算（§1.7 OI-02）
结构系数        σ = m_dry / (m_dry + m_prop)      ← 存储权威
干质比          m_dry / m_prop = σ / (1 − σ)      ← 派生
推进剂质量分数  m_prop / (m_dry + m_prop) = 1 − σ ← 派生
```

> **唯一权威原则**：σ 是存储与计算（回归、迭代求解、不确定度）的**唯一权威变量**；干质比与推进剂质量分数均为**同一自由度的等价表示**，仅在 API 边界与 UI 显示层做换算。**禁止三者同时作为独立输入字段**——否则同一物理量会有三份可互相矛盾的存储（NFR-07）。
>
> **级层比冲的唯一权威（§1.7.5 QA-1，v0.6.1 续）**：发动机标称比冲（`Engine.isp_vacuum_s` / `isp_sea_level_s`）是权威，**级层（`Stage.isp_*`）只在用户显式覆写（`isp_source = "custom"`）时才存值**；`default` 语义下级层**省略**这两个字段，后端从发动机复制下发（DAG 输入解析）。约束引擎判三类：default 却填了**不一致**的值 → **hard**（`HARD_ISP_DEFAULT_MISMATCH`——DAG 直接采用级层值，警告挡不住矛盾数字进计算）；default 填了**一致**的副本 → 警告（`ENGINEER_ISP_DEFAULT_REDUNDANT`，多余存储迟早漂移）；`custom` 缺任一值 → **hard**（`HARD_ISP_CUSTOM_REQUIRES_VALUES`）。前端 default 下隐藏级比冲控件，切回 default 时清空覆写值。
>
> **推力 / 比冲的环境条件口径（§1.7.5 OI-35，v0.6.1 续）**：发动机性能参数**必须区分并标注两种环境**——
> ① **海平面** = ISA 标准海平面条件（海拔 **0 m**、气温 **15 °C**、气压 **101.325 kPa**）下的测量/换算值；发射场**当日实际大气**的影响不属于本口径，它由 §8.6 的损失模型承载（M4）；
> ② **真空** = 理想真空（环境背压 0）下的理论性能值。
> **标注是强制项**：Schema 字段名一律携带 `_sea_level_` / `_vacuum_`；UI 控件标签、`metrics` / `provenance` 报告、模板 `sourced_fields` 中每个推力 / 比冲值**必须随值标注其适用环境**，只写"推力""比冲"而无环境限定一律视为缺陷。**配对规则（禁止混用）**：质量流量 ṁ 只用**真空推力配真空比冲**；起飞推重比 T/W 只用**第一级海平面总推力**配 GLOW；海平面比冲只作海平面工况的伴生量下发，**不得**代入真空流量公式（§7.5 规则 3）。**数据层同此纪律**：GCAT `stages` 表官方明载 `Thrust`（真空）/ `ThrustSL`（海平面），直接分列；GCAT `engines` 表的 `Thrust` 官方**未声明环境**，故按口径中立命名 `typical_thrust_n` 存储并标注"环境未声明"，**不参与**任何海平面/真空配对计算（§7.5 规则 2 同源）。
>
> **加注比例约束（§1.7 OI-03）**：取值域 `(0, 1.0]`，超过 1.0 必须报错并要求用户显式启用「最大允许加注量」字段，不得静默放行。
>
> **液面高度由后端随计算结果下发**（`liquid_level_m`），前端不得自行推导——这是 ADR-011「前端零物理公式」在 2D 剖面视图上的具体落实。
>
> **偏度系数不在此 Schema 内**：按 §1.7 OI-01 的裁决它是统计派生量，落 §8.7 / §8.8。

- 全部用 **Pydantic v2** 定义，天然获得校验、序列化、JSON Schema 与文档
- Schema 版本号随飞行器一起存储，破坏性变更需迁移函数

### 6.2 依赖 DAG 与传播

参数间存在派生关系，必须显式建模为有向无环图：

```text
推进剂 + 目标运力 ──┐
                    ├─→ 所需推进剂质量 ─→ 贮箱容积 ─→ 贮箱尺寸 ─→ 柱段长度
室压 + 面积比 ──────┘                                  ↑
                                             直径（用户给定/优化）
```

规则：

- DAG 在启动时构建并**检测环路**，有环即拒绝加载
- 变更传播按拓扑序执行，单次传播内每个节点最多计算一次
- 每个节点标记 `source`：`user`（用户给定）或 `derived`（推导得出）
- 用户改 `derived` 节点时，提示将覆盖哪些上游约束

### 6.3 约束与校验

| 约束类型 | 示例 | 处理 |
|---|---|---|
| 硬约束 | 长径比 > 0；级数 ≥ 1 | 拒绝并返回字段级错误 |
| 工程约束 | 长径比 ∈ [5, 30]（过大纵振风险） | 警告 |
| 相容约束 | 喷管出口直径 < 级直径；**相态为固体时共底 / 储箱排列 / 混合比 / 加注比例不生效**（§1.7.3 OI-30） | 警告 + 高亮冲突部件 |
| 安全边界 | 贮箱壁厚 ≥ 最小工艺厚度 | 警告 + 标注违反项 |

返回结构统一为 `{level, code, field_path, message, suggestion}`，前端逐项渲染。

> **`field_path` 的唯一口径（§1.7.3 OI-32）**：本节的约束裁定与 §6.5 的方案诊断**共用同一个 `field_path` 形状**——一律用**数组下标**（`stages[0].geometry.oxidizer_tank.wall_thickness_m`）而非级号，且请求校验通路（§10.3 的 `details.diagnostics`）下发的路径也已剥掉请求位置前缀，与本节逐字一致。前端因此只维护**一张**「路径 → 控件」映射，控件以 `data-field-path` 承载同一字符串。**不得**为某一类裁定另设路径格式——两条通路一旦分叉，界面就会把同一处缺陷渲染成两个位置。

### 6.4 单位系统

- 内部一律 SI（红线 §1.4-3）
- 输入/输出层显式携带单位（Pydantic 字段带 `unit` 元数据）
- **界面显示单位固定一套，不做用户可切换**（OI-07）：

| 量 | 显示单位 | 内部 SI |
|---|---|---|
| 力 / 推力 | `kN` | N |
| 质量 | `t` | kg |
| 长度 | `m` | m |
| 比冲 | `s` | s |
| ΔV / 速度 | `km·s⁻¹` | m·s⁻¹ |
| 压力 | `bar` | Pa |
| 温度 | `K` | K |
| 密度 | `kg·m⁻³` | kg·m⁻³ |

  **换算只在 API 边界发生**（原则 P1：禁止界面层做单位换算）。该组单位是**显示口径**，不改变任何存储、计算与缓存键——`key = sha256(canonical_json + …)` 中的 canonical JSON **一律是 SI**，故显示单位调整**不得**使 `artifacts/` 缓存失效。
  ⚠ 与 §1.4-3 的关系：红线管的是**计算与存储**，本表管的是**呈现**；两者不冲突，但任何新增显示单位必须回填本表，不得在组件里就地格式化（NFR-05 同源口径：口径唯一才可机检）。
- **本表由后端下发**（§1.7.3 OI-32）：`GET /api/params/units` 把上表逐行作为 `{quantity, label, symbol, si_symbol, factor}` 交出，`factor` 的方向固定为 **SI = 显示值 × factor**。前端**零换算系数**——不得在前端再抄一份表或反推系数；界面显示什么，完全由该响应决定。
- 禁止在领域层出现任何魔法数字形式的换算系数

### 6.5 方案诊断与改进建议

参数合法**不等于**方案合理。每次求解后运行诊断规则集，输出结构化诊断而非"通过/不通过"。

**阈值可配置（OI-08）**：下表数值是**默认值**，实现为**可配置项**（`pydantic-settings`，可经 `config.toml` 或环境变量覆盖，§18.4），**默认值即下表数值**。

- **配置入口必须显式存在**（§1.7.3 OI-31）：后端提供配置模板 `config.example.toml`，并以 **`GET /api/params/thresholds`**（各项的**生效值**与**来源**）与 **`PUT /api/params/thresholds`**（写入 `config.toml`）对外；界面设置项须**同时显示生效值与来源**。
- ⚠ **「未配置」是一种必须可见的状态**：§6.5 表中有判据而未给数值的项（现为「最小工艺厚度」）**不设数值默认值**，未配置即**不判定**并如实下发 `deferred_reason`——不得填一个"看着合理"的数字，也不得在界面上留空（须显式标注"未配置"）。

> ⚠ **来源声明（强制）**：下表全部阈值为**工程惯例值，非权威来源**——它们来自公开工程的量级经验，**没有任何一条可以引用为标准或论文结论**。因此：
> ① 每条阈值在代码与 UI 中都必须携带该来源标注，**不得**以"规格规定"的语气呈现为硬性真理；
> ② M4 用基准火箭（§13.2）反标定时**只改配置、不改代码**，并在 PR 中记录标定前后取值与依据；
> ③ 阈值调整**不构成几何语义变更**，不递增 `SPEC_VERSION`、不使 `artifacts/` 缓存失效（§9.2）。

| 规则 | 判据（默认值） | 级别 | 建议方向 | 来源 |
|---|---|---|---|---|
| 起飞推重比 | T/W ≥ 1.2（液体）／ ≥ 1.5（固体助推）<br>⚠ 档位取**第一级**发动机的 `propellant_phase`（§1.7.3 OI-30）：`solid` → 1.5，`liquid` / `hybrid` → 1.2 | 硬 | 增加台数或提高单机推力 | 工程惯例 |
| 结构质量比 | 箭体结构质量 / 推进剂质量 ∈ [3%, 8%]（一级） | 警 | 复核壁厚与材料 | 工程惯例 |
| 贮箱壁厚 | ≥ 最小工艺厚度，且 < 直径/2 | 硬 | — | 工艺下限 |
| 长径比 | ∈ [5, 30] | 警 | 调整分级或直径 | 工程惯例 |
| 级间 ΔV 分配 | 与该级 Isp、结构系数匹配（偏离最优分配 > 20%） | 警 | 重算最优分配（§8.5） | 工程惯例 |
| 上面级余量 | 分离后剩余 ΔV 余量 ∈ [1%, 5%] | 警 | 调整加注量 | 工程惯例 |
| 运力缺口 | 估算运力 < 载荷需求 | 硬 | 给出敏感度最高的 3 个参数作为改进入口 | — |

诊断输出统一为 `{level, code, field_path, message, suggestion, impact}`。

**`impact` 字段的归属（OI-11 裁决）**：`impact`（"若按建议调整，运力预计提升 X%"）**必须**由 §8.7 的敏感度结果**实算得出，禁止凭空给出百分比**。
由此产生一条**里程碑约束**：敏感度属 **M4**，故 **M2 不交付带 `impact` 的诊断**——

- **M2** 只落**不依赖 `impact` 的规则**（推重比 / 壁厚 / 长径比 / 结构质量比 / 上面级余量 / 运力缺口），输出 `{level, code, field_path, message, suggestion}`（六字段，与 §6.3 校验结构同形）；
- **M4** 起补 `impact` 字段，输出升级为七字段；
- ⚠ **不得**为凑齐 M2 验收而在 M2 用经验系数**编造** `impact`——这正是 §1.4-4 溯源红线要拦的东西（"没报错 ≠ 正确"的同族错误）。

**逐规则账目必须随结果一并下发（三条账目）**：诊断报告不只给"命中了哪些"，还要给**每条规则的去向**——`diagnostics`（已判，可能为空 = 通过）/ `deferred_reason`（整条未判定，写明缺什么）/ `uncovered`（已判但有分支未启用）。理由：只下发命中项时，"这条规则没判"与"这条规则判了且通过"在界面上**长得一模一样**，而本项目最贵的一课恰是"没报错 ≠ 正确"。前端**必须**把这三类分别渲染（见 §11.5）。

**界面消费契约（§1.7.3 OI-32）**：诊断清单按 `level` / `code` 分组渲染，点击某条即按其 `field_path` **定位到对应控件**（契约见 §6.3 末注，控件以 `data-field-path` 承载同一字符串）；定位失败时**降级显示路径文本**而**不得丢弃该条**。诊断阈值的**生效值与来源**须在设置项中可见（§6.5 上方的配置入口），且来源字样**只许照抄后端 `source` 字段**，前端不得自拟措辞。

---

## 7. 数据子系统

### 7.1 数据源与快照

| 数据源 | 内容 | 获取方式 | 风险 |
|---|---|---|---|
| **GCAT** | 运载火箭目录：vehicles 1,817 / engines 1,462 / stages 1,596（合计 4,875 条） | **一次性下载 → 快照固化** | 镜像许可不明；字段缺失、口径不一 |
| **SPACEMATDB** 等材料库 | 铝合金 / 复合材料 / 聚合物 / 陶瓷 / 高温合金的物性与许用值 | 一次性获取 + 文献补充 | 需核许可与试验条件 |
| **OpenRocket / RASP `.eng`** | 固体发动机推力-时间曲线与总冲 | 文件导入（§7.5 解析器） | 格式方言多，需容错 |
| 推进剂物性 | 密度、沸点、贮存条件 | 文献整理 | 需标注来源 |
| NASA CEA | 热力学数据 | `cea` 包内置数据库 | Apache-2.0，低风险 |
| **人工补录** | GCAT 缺口字段（室压、循环方式、面积比） | 表单录入 + 审核（§7.9） | 需强制来源与审核流 |

**快照规范**：每次导入产生一个不可变快照目录

```text
data/snapshots/gcat-2026Q3/
├─ records.parquet        # 规范化后的记录
├─ snapshot.json          # {source_url, fetched_at, sha256, record_count}
├─ LICENSE-NOTICE.md      # 许可声明与核实结论
└─ field_report.md        # 字段缺失率、异常值、单位疑点
```

`sha256` 参与计算缓存键（§9.2），保证「同一份数据」可复现。

**M3 前置门禁：许可先于抓取（§17 R-10 的对策落点）**

GCAT 及其镜像的**许可状态不明**（R-10，等级**高**），而 M3 的全部产出（质量回归、三层标签索引、发动机族谱）都建立在它的快照之上。因此把顺序固定为：

1. **抓取之前**完成许可核实，结论写入 `data/snapshots/<snapshot>/LICENSE-NOTICE.md`（含许可原文链接与结论依据）；
2. **核得通过** → 按上述快照规范固化，`sha256` 参与缓存键；
3. **核不过或查不清** → **立即转自建数据集**（人工补录 + 公开文献整理 + 用户自备导入，§7.9 / §3.4「用户自备数据」模式），**不得**"先用着、以后再补许可"；
4. ⚠ **不得**先抓取再补许可结论——一旦数据进了快照，后续所有数字的溯源链都会挂在一个许可不明的来源上（§1.4-4 溯源红线）。

> 该门禁**前置到 M3 开工**，而不是 M3 验收：验收时才发现许可不过，等于 M3 全部工作要重做。

### 7.2 三层标签体系

数据必须支持**任意维度组合筛选**——例：`现役 + LOX/CH4 + 分级燃烧循环 + 美国` → 定位到 Raptor 系列。这是数据库的核心价值，不是附属功能。

| 层级 | 维度 | 示例值 |
|---|---|---|
| 第一层：实体类型 | 发动机 / 上面级 / 完整火箭 / 材料 / 推进剂 | — |
| 第二层：技术特征 | 循环方式、推进剂组合、推力等级、级数、可回收性、发射状态 | LOX/CH4、分级燃烧循环、现役 |
| 第三层：地理/机构 | 国家/地区、制造商 | SpaceX、ULA、CASC、ESA |

实现要点：

- 标签以**规范化枚举**存储（不收自由文本），枚举值随快照版本管理
- 标签与记录的关联独立建表（`tags` / `record_tags`），支持高基数组合查询
- 查询必须走标签索引，**禁止全表扫描**（写入 AGENTS.md 领域规则，见附录 D）
- 标签缺失的记录标 `tag_incomplete`，不参与依赖该标签的统计

### 7.3 GCAT 字段映射

**vehicles**

| 原始字段 | 内部字段 | 单位变换 | 备注 |
|---|---|---|---|
| `lv_name` / `lv_family` / `lv_variant` | `name` / `family` / `variant` | — | 三者共同构成唯一标识 |
| `lv_manufacturer` | `manufacturer` | — | 关联第三层标签 |
| `lv_min_stage` | `min_stages` | — | 最小级数 |
| `length_m` / `diameter_m` | `length` / `diameter` | m | 需核验是否混入英制 |
| `launch_mass_t` | `glow` | t → kg | 起飞质量 |
| `leo_capacity_kg` / `gto_capacity_kg` | `payload_leo` / `payload_gto` | kg | 黄金基准来源（§13.2） |
| `to_thrust_kn` | `liftoff_thrust` | kN → N | 推重比校验用 |

**engines**

| 原始字段 | 内部字段 | 备注 |
|---|---|---|
| `name` / `manufacturer` / `family` | 同名字段 | 族谱关联键 |
| `oxidizer` / `fuel` | 推进剂组合 | 映射到规范化推进剂枚举 |
| `mass_kg` | `loaded_mass_kg` | ⚠ 官方文档明载为 **loaded（满装）质量、仅固体**——v0.6.1 续据官方原文更正，原表述"干重"有误 |
| `impulse_kns` | `total_impulse_ns` | **总冲，kN·s → N·s**（多为固体）；与推力/比冲是三种不同量纲，禁止互称（OI-35） |
| `thrust_kn` | `typical_thrust_n` | ⚠ 官方文档**未声明环境口径**（只写 "Typical thrust (kN)"）——按口径中立命名存储并标注"环境未声明"，**不参与**海平面/真空配对计算（OI-35） |
| `isp_s` | `isp_vacuum_s` | 官方定义 "vacuum Isp where available"——**真空比冲**（OI-35 配对规则的真空侧）；无海平面对应列 |
| `duration_s` | `burn_duration_s` | 典型工作时间 |

**stages**：级序、推进剂质量、干质量、直径、工作时间 → 供结构系数回归（§8.4）使用。

**已知缺口**：室压、循环方式、面积比在 GCAT 中普遍缺失，须经 §7.9 手动录入或公开资料补充。

### 7.4 发动机族谱与材料库

**发动机族谱**（按族聚合，支持跨型号对比）字段：

型号名称、制造商、循环方式、推进剂、海平面推力、真空推力、海平面 Isp、真空 Isp、室压、膨胀比、质量、推重比、首次飞行年份、当前状态。

**材料库**：

| 字段 | 用途 |
|---|---|
| 密度 | 质量估算（§8.4） |
| 比强度 / 比刚度 | 材料选型对比 |
| 许用应力 / 弹性模量 | 壁厚下界估算 |
| 工作温度范围 | 与推进剂、热环境的相容性 |
| 来源 / 许可 / 试验条件 | 溯源（红线 §1.4-4） |

许可不明或试验条件缺失的材料数据降级为"参考值"，**禁止用于结论性导出**。

### 7.5 ETL 与单位规范化

导入流程（`tools/gcat_etl.py`，离线运行）：

```text
原始数据 → 列映射 → 单位识别与转换(SI) → 缺失值策略 → 异常值标记
        → 质量标签标注 → 写 Parquet → 产出 field_report
```

**单位规范化是本项目最容易出错的地方**：原始数据混用英制/公制，推力量纲口径不一（推力/冲量/比冲混称），比冲不区分海平面/真空。ETL 必须：

1. 逐字段显式声明源单位（不允许"猜"）
2. 无法确定单位的记录 → 标记 `unit_uncertain` 并排除出统计拟合
3. 比冲统一为**真空比冲**，海平面值另存字段，禁止混用

**外部格式解析器**（`data/parsers/`）：

| 格式 | 内容 | 要点 |
|---|---|---|
| RASP `.eng` | 固体发动机推力-时间曲线、总冲、推进剂质量 | 兼容 RASP/OpenRocket 方言差异、单位行（N/lb、s）自动识别、异常曲线检测 |
| OpenRocket `.ork` | 完整火箭定义（可选） | 用于基线导入，不保证全字段保真，导入结果须标注 `derived` |
| RSE / `.rse` | 电机数据（可选） | 同上 |

解析器必须**容错并报告**：遇到无法识别的行不静默跳过，而是输出行号与原文片段。

### 7.6 数据质量标签

每条记录带 `quality` 字段，防止把推算值当权威值：

| 标签 | 含义 | 是否参与回归拟合 |
|---|---|---|
| `official` | 官方公布 | ✅ 权重 1.0 |
| `literature` | 文献来源 | ✅ 权重 0.7 |
| `derived` | 由其他字段推算 | ⚠️ 权重 0.3 |
| `uncertain` | 来源不明或单位存疑 | ❌ 排除 |

### 7.7 存储与查询

- **SQLite 单文件**（ADR-007）：`data/aeroforge.db`
- 分析型批量查询可选 DuckDB 直读 Parquet（只读，不引入服务）
- 表设计：`vehicles` / `stages` / `engines` / `engine_families` / `materials` / `propellants` / `tags` / `record_tags` / `snapshots` / `manual_overlay` / `provenance`
- 所有查询走仓储层接口，禁止业务代码拼 SQL

### 7.8 溯源

每次计算产出 `provenance.json`：

```json
{
  "spec_version": "0.1",
  "kernel_version": "build123d-0.11.1/OCCT-7.8",
  "cea_table_id": "cea-3.3.4-grid-v1",
  "data_snapshot": "gcat-2026Q3@sha256:...",
  "params_hash": "sha256:...",
  "code_version": "git:...",
  "computed_at": "2026-09-10T12:00:00Z"
}
```

红线 §1.4-4：**没有 provenance 的数值不得出现在任何报告或界面上**。

### 7.9 手动录入与数据维护

GCAT 的缺口（室压、循环方式、面积比、部分 Isp）无法自动补全，**人工补录通道是数据库可用性的前提**，不是可选项。

| 能力 | 说明 |
|---|---|
| 录入 / 编辑 | 表单式录入，字段级单位选择，必填来源与置信标签 |
| 来源强制 | 每条人工记录必须填写来源（URL／文献／报告号），否则拒绝保存 |
| 审核状态 | `draft → reviewed → published`；**只有 `published` 参与回归拟合** |
| 快照隔离 | 人工录入存于独立 `manual_overlay` 层，快照更新时不覆盖 |
| 冲突检测 | 人工值与快照值不一致时并排展示两值与来源，由用户裁定 |
| 审计 | 记录录入人、时间、理由，写入 provenance |

---

## 8. 推进与性能计算子系统

### 8.1 七层模型总览

```text
① 发动机性能层
   CEA 表 → 理想 Isp → × 效率因子 → 实际 Isp
   ── 修正：理想值必须打折，否则运力系统性高估

② 质量估算层
   GCAT 统计回归 + 几何解析 → 干质量模型
   ── 修正：这是运力精度的决定因素，必须独立成层

③ 级质量迭代层
   目标 ΔV + 结构系数 → 迭代求解各级质量分配
   ── 修正：级间质量强耦合，反向求解必须迭代

④ 弹道损失层
   重力损失 + 气动损失 + 转向损失（依发射场纬度与目标轨道）
   ── 修正：ΔV 不是常数，不能跨发射场照搬

⑤ 不确定度层
   Monte Carlo 传播 → 区间 + 敏感度排序
   ── 修正：输出点值不可辩护

⑥ 任务时序层
   点火/分离/抛整流罩/着陆 → 质量突变事件 → 回写 ③
   ── 修正：分离与抛罩是质量突变，必须与迭代耦合而非事后套用

⑦ 轨道层
   目标轨道 ΔV 需求 + Hohmann 转移 + 平面变更 + 逃逸
   ── 修正：ΔV 需求随发射场与轨道而变，不是常数（见 §8.10）
```

### 8.2 CEA 预计算表（ADR-004）

**离线生成**（`tools/cea_tablegen.py`，仅在开发期运行，需要 `cea`）：

```text
网格维度：
  推进剂组合（离散）：LOX/RP-1, LOX/LH2, LOX/CH4, N2O4/MMH, 固体(若干)
  混合比 O/F       ：50 点（线性 + 端点加密）
  室压 Pc          ：20 点（对数，5–200 bar）
  面积比 ε         ：20 点（对数，5–300）
  环境压 Pamb      ：5 点（真空、0.1、0.5、1.013、5 bar）

输出字段：
  Isp_vac, Isp_opt, c_star, T_chamber, gamma, MW, chamber_density
```

**运行时**：`scipy.interpolate.RegularGridInterpolator`，在对数坐标下插值。

- 表规模：每组合约 2×10⁴ 点 × 7 字段 → 总计 < 10 MB
- 查询耗时目标：**< 1 ms**
- 表本身是**纯数据**，可自由分发；生成器脚本不参与运行时分发

**注记**：CEA 输出为**理想值**（无喷管损失、无边界层、无摩擦），未经 §8.3 修正不得进入质量估算。

**工作点求解（最优混合比）**：CEA 表不仅用于正查，还用于**反求工作点**。

| 目标 | 求解方式 | 用途 |
|---|---|---|
| 最大 Isp | 在给定 Pc、ε 下对 O/F 一维搜索求极值 | 真空级最佳混合比 |
| **最大密度比冲** | 最大化 `Isp × 平均密度` | **一级/助推应采用的真实目标** |
| 给定推力求流量 | `ṁ = F / (Isp · g₀)` | 台数与工作时间匹配 |
| 可用混合比区间 | 满足 Tc、c* 稳定且余氧系数 λ 合理的 O/F 范围 | 工程可行区间 |
| 稳定燃烧边界 | 避开 Tc 峰值附近的高敏感区 | 标注风险区 |

**关键工程点**：一级与上面级的优化目标**不同**——上面级追求比冲最大，一级追求**密度比冲最大**（一级瓶颈常是体积与阻力，而非质量）。平台按级次采用不同目标函数，并允许用户显式覆盖。

### 8.3 效率因子

```text
Isp_actual = η_c* × η_Cf × Isp_ideal
```

| 循环方式 | η_c* 典型 | η_Cf 典型 | 组合效率典型 |
|---|---|---|---|
| 燃气发生器 | 0.94–0.97 | 0.96–0.98 | ~0.93 |
| 分级燃烧 | 0.96–0.99 | 0.97–0.99 | ~0.96 |
| 膨胀循环 | 0.96–0.98 | 0.97–0.99 | ~0.96 |
| 固体 | 0.92–0.95 | 0.94–0.97 | ~0.90 |

- 效率因子是**显式参数**，可从界面调参并纳入不确定度采样
- 默认值按循环方式取上表区间中值，且必须记录在 provenance 中

### 8.4 质量估算层

**双来源 + 交叉校验**：

| 来源 | 方法 | 适用 |
|---|---|---|
| 几何解析 | 表面积 × 面密度；容积 × 材料密度 | 贮箱、整流罩、级间段 |
| 统计回归 | 从 GCAT 拟合结构系数与干质量 | 发动机、推力结构、全箭校验 |

**结构系数** σ = m_dry / (m_dry + m_prop)，回归目标（按级序、推进剂、直径、年代分箱）：

- 一级（液体）：σ ∈ [0.04, 0.08]
- 上面级（液体）：σ ∈ [0.08, 0.15]
- 固体级：σ ∈ [0.08, 0.12]

**等价表示（§1.7 OI-02）**：

```text
干质比        m_dry / m_prop            = σ / (1 − σ)
推进剂质量分数 m_prop / (m_dry + m_prop) = 1 − σ
```

三者为**同一自由度的不同表示**。σ 为唯一权威（回归变量、迭代求解变量、不确定度抽样变量），干质比与推进剂质量分数为派生量，**仅用于显示与 API 边界换算，不得作为独立输入存储**。

**回归要求**：

- 只用 `official` / `literature` 标签的样本
- 报告 R²、残差分布、外推范围警告
- 超出拟合域的参数组合必须**显式警告**，不得静默外推

**一致性校验**：几何解析质量与回归质量偏差 > 20% 时报警——这意味着构型异常或回归外推。

### 8.5 多级质量迭代层

**问题**：给定各级 Isp 与结构系数、载荷质量、目标总 ΔV，求各级质量分配。

**关键认知**：级间质量**强耦合**——上面级的质量是下面级的有效载荷，因此**反向求解必须迭代**，不存在"逐级累加"的解析直推。

**求解算法**：

```text
外层：对起飞质量 GLOW 迭代
  内层：自上而下逐级正向计算
         m_payload,i+1 = m_stage,i （上面级总质量）
         m_prop,i 由 ΔV_i 与 Isp_i 反解
         m_dry,i   = m_prop,i × σ_i / (1 - σ_i)
  校验：Σ ΔV_i == ΔV_target ?
  未收敛 → 修正 GLOW → 重复
收敛判据：|ΔV_残余| / ΔV_target < 1e-6
```

- ΔV 的级间分配以**最优分配（Lagrange 解析解）作为初值**加速收敛
- 求解器需报告：迭代次数、残差、是否命中迭代上限、是否越出结构系数有效域
- 齐奥尔科夫斯基方程仅用于**级内**：`ΔV_i = Isp_i · g₀ · ln(m₀,i / m_f,i)`

### 8.6 弹道损失层

ΔV 需求 = 理想轨道速度增量 + 损失预算。损失项按精度分两级实现：

**L1（M4 起，参数化经验模型）**

| 损失项 | 典型范围（LEO） | 影响因素 |
|---|---|---|
| 重力损失 | 1.0–1.5 km/s | 推重比、俯仰程序 |
| 气动损失 | 0.1–0.3 km/s | 长径比、发射剖面 |
| 转向损失 | 0.1–0.5 km/s | 发射场纬度、方位角 |
| 压力/控制余量 | 0.1–0.2 km/s | — |

**L2（M6 可选，简化上升弹道积分）**

点质量 2D 积分：指数大气 + 重力转弯程序 + 变重力 → **直接算出**损失，替代经验常数。这是把"经验系数"升级为"物理模型"的路径。

**目标轨道 ΔV 需求**（含发射场纬度依赖）：

| 轨道 | 典型 ΔV（km/s） | 备注 |
|---|---|---|
| LEO（低纬） | 9.3–9.5 | 含损失 |
| SSO | 9.5–10.0 | 需较大转向损失 |
| GTO（低纬发射场） | 11.3–11.8 | 库鲁最优 |
| GTO（高纬发射场） | 12.0–12.6 | 纬度惩罚显著 |
| **TLI（地月转移射入）** | 12.6–13.0 | LEO 停泊段 + 转移射入段（后者 ≈ 3.1 km/s）；**C3 < 0**（仍为地心束缚轨道） |
| **TMI（地火转移射入）** | 13.5–14.0 | 强窗口依赖；**主变量是 C3**（典型 8–15 km²/s²），ΔV 随窗口波动显著，故**必须与 C3 一并输出** |
| 逃逸 | ≥ 13.0 | — |

⚠ **本表是量级锚定，不是权威结论**：数值来自公开工程的量级经验（与 §6.5 诊断阈值同口径）。M3 起须按 §13.2 的做法**逐条二次核对权威来源并记录引用**，M6 用 L2 弹道积分收窄区间（§8.6 L2）。**禁止**把本表当作标准或论文结论引用。

**发射场纬度对运力的影响（自转加成）**：

```text
v_rot      = ω_地球 · (R_地球 + 海拔) · cos(纬度)      ← 发射场随地球自转的线速度
Δv_assist  = v_rot · cos(方位角) · 与目标倾角的相容因子  ← 可用加成（向东发射取最大）
可用运力随 Δv_assist 单调增加（§13.3 属性测试：固定其余参数，纬度 ↓ ⟹ 运力 ↑）
```

约束：

1. **低纬向东发射运力更高**是本层的**验收趋势**（FR-19）：纬度 ↓ → `v_rot` ↑ → 所需 ΔV ↓ → 运力 ↑。该趋势必须由测试显式断言，不能只在文档里声称
2. **方位角与目标倾角不相容时**（如需大倾角而向东发射），加成被 `相容因子` 削减，且必须产生转向损失增量——二者是同一件事的两面，不得只算加成不算损失
3. **禁止**把 ΔV 需求作为与发射场无关的常数使用（这是原方案的系统性误差来源）
4. 纬度变更的重算**分两阶段**（OI-25）：先给**解析点值**（毫秒级，立即刷新运力表与 Δv 预算），MC 区间**异步到达后覆盖**；**10 000 样本不得进入交互路径**（R-39）

### 8.7 不确定度传播（ADR-010）

**方法**：拉丁超立方采样 + 蒙特卡洛。

| 参数 | 分布假设 | 典型 1σ |
|---|---|---|
| 实际 Isp | 正态 | ±2% |
| 结构系数 σ | 正态 | ±15% |
| 损失预算 | 正态 | ±20% |
| 回归质量残差 | 由回归残差分布确定 | — |
| 效率因子 | 均匀（在区间内） | — |

- 样本数默认 10,000（可配 1,000–100,000），用拉丁超立方降低方差
- 输出：**P5 / P50 / P95**、分布直方图数据、收敛诊断
- **敏感度**：一阶差分或 Sobol 指数，输出参数影响排序（Top-N）

**两阶段返回契约（OI-25，仅作用于交互路径）**

MC 的 10 000 样本天然是**秒级**量级，而"调整发射场纬度 → 运力即时更新"要求**毫秒级**反馈——二者不可兼得，故**显式分两阶段**，而不是把 MC 塞进交互路径后假装它很快：

| 阶段 | 内容 | 时机 | 载荷 |
|---|---|---|---|
| **① 点值**（同步） | 解析求解（齐奥尔科夫斯基 + 损失预算 + 自转加成） | 防抖窗口结束即返回 | `point` 与 `delta_v_budget` |
| **② 区间**（异步） | MC 采样 → `interval` / `moments` / `sensitivity` | 后台作业完成后覆盖 | 其余字段 |

规则：

1. 阶段 ① 的响应**必须标记 `interval_pending = true`**；前端据此把区间列显示为**占位态**（不是显示上一次的数字——那会让人误以为已更新）
2. 阶段 ② 到达后**整体覆盖**同名键；**未到达前不得把旧区间与新区间点值混排**
3. 阶段 ① 的求解路径**不得调用 MC**（§9.1 的 worker 与 §8.7 的采样是两个作业）；混淆二者是本条要拦的形态
4. 该契约**不影响** §8.8 的"缺 `interval` 视为实现缺陷"——最终态必须齐备，缺的只是**中间态的标记方式**

**分布形态指标：偏度系数（§1.7 OI-01 裁决）**

```text
总体偏度      γ₁ = μ₃ / σ³                     （μ₃ = E[(X − μ)³]）
修正样本偏度  G₁ = [√(n(n−1)) / (n−2)] · m₃ / m₂^(3/2)
              其中 m_k = (1/n) · Σ (xᵢ − x̄)^k
```

- **默认口径**：`adjusted_sample`（与 Excel `SKEW`、pandas `Series.skew()` 默认行为一致）
- 输出**必须同时声明 `n` 与 `estimator`**（取值 `population` | `adjusted_sample`）——两种口径数值不同，不声明即不可复现
- `n < 3` 或 `m₂ = 0`（样本全部相同）时**返回 `null` 并附原因**，不得输出 0
- 用途：判断运力/质量的分布对称性。**偏度显著偏离 0 时，报告必须提示"均值 ≠ P50"**，避免使用者拿 P50 当期望值使用

> 归类说明：偏度系数按补充手册被列在"几何参数"中，但按统计偏度定义它是**样本的派生统计量**，故归本节（§1.7 OI-01 已记录该归类修正）。

### 8.8 输出规范

任何性能评估响应必须包含：

```json
{
  "point": { "payload_leo_kg": 22800, "glow_kg": 549000, "c3_km2_s2": -1.8 },
  "interval": { "payload_leo_kg": { "p5": 20100, "p50": 22800, "p95": 25300 } },
  "interval_pending": false,
  "moments": {
    "payload_leo_kg": {
      "n": 10000,
      "estimator": "adjusted_sample",
      "skewness": -0.18,
      "mean_vs_p50_note": null
    }
  },
  "delta_v_budget": {
    "target_orbit": "LEO",
    "ideal_dv_km_s": 7.81,
    "gravity_loss_km_s": 1.24,
    "drag_loss_km_s": 0.18,
    "steering_loss_km_s": 0.22,
    "back_pressure_loss_km_s": 0.15,
    "rotation_assist_km_s": 0.39,
    "total_dv_km_s": 9.21,
    "assumptions": ["理想脉冲近似，不含有限推力损失", "损失为 L1 参数化经验模型（§8.6）"]
  },
  "payload_latitude_curve": {
    "payload_key": "payload_leo_kg",
    "points": [ { "lat_deg": 5.2, "payload_kg": 23400 }, { "lat_deg": 28.5, "payload_kg": 22100 } ]
  },
  "sensitivity": [
    { "param": "stage1.isp_efficiency", "impact": 0.42 },
    { "param": "stage2.structure_coeff", "impact": 0.31 }
  ],
  "warnings": [
    { "code": "REGRESSION_EXTRAPOLATION", "message": "直径超出拟合域 [1.8, 5.2] m" }
  ],
  "provenance": { "...": "见 §7.8" }
}
```

**缺 `interval` 或 `provenance` 的响应视为实现缺陷。** `moments` 中的每项必须带 `n` 与 `estimator`——缺失口径的统计量不可复现，等同于无来源数值（CON-04）。

**本版新增的三个输出项（OI-23）**：

| 字段 | 单位 | 用途 | 约束 |
|---|---|---|---|
| `point.c3_km2_s2` | `km²·s⁻²` | **特征能量 C3**：逃逸/转移轨道的能量表征（`C3 = v∞²`，对束缚轨道为负） | **TLI / TMI 必须输出**（其 ΔV 是窗口强依赖的，单看 ΔV 会误导；§8.6）；逃逸轨道必输出 |
| `delta_v_budget` | `km·s⁻¹` | **ΔV 瀑布图**数据：理想 ΔV → 各损失项 → 自转加成 → 总 ΔV | **逐项列全，不得只给总和**——瀑布图的意义就是把"损失吃掉多少"显式化；`assumptions` **必填**（用户核心目标明确要求"允许简化但必须标明假设"）。`ideal_dv` 与各项之和必须与 `total_dv` 闭合（容差 1e-6 km/s） |
| `payload_latitude_curve` | `kg` / `deg` | **运力—纬度曲线**：固定其余参数、扫纬度得到的运力序列 | 必须**单调不增**于纬度（在方位角固定为向东时）——这是 FR-19 趋势验收的可机检形式；采样点与步长写入 `provenance` |

⚠ `back_pressure_loss_km_s` 即 §8.6 L1 表中的「压力/控制余量」，**同一件事的两个名字**（用户核心目标称"背压损失"）——实现只保留一个字段，**禁止**两处各算一份。

⚠ `interval_pending`（§8.7 OI-25）：**仅**在两阶段契约的阶段 ① 取值 `true`；阶段 ② 覆盖后必须为 `false`。该字段是"区间是占位态还是终态"的唯一判据，前端据此决定显示占位还是数字。

### 8.9 任务时序与质量事件耦合

几何与性能之间靠**时序**连接：分离、抛整流罩、着陆点火都是**质量突变事件**，直接改变后续级的初始质量。

| 事件 | 时刻 | 质量效应 | 对 ΔV 的影响 |
|---|---|---|---|
| 一级点火 | t = 0 | — | 起飞推重比校验（§6.5） |
| 一级关机 / 级分离 | 一级推进剂耗尽或达目标速度 | 抛掉一级干质量（含回收代价） | 提高上面级初始质量比 |
| 整流罩抛离 | 通常 100–150 s，动压低于阈值后 | 抛掉整流罩质量 | 提高上面级运力 |
| 上级点火 / 关机 | 按 ΔV 分配 | — | — |
| 入轨 | 目标速度达成 | — | 有效载荷分离 |
| **回收点火**（若有） | 再入—着陆段 | **消耗一级预留推进剂** | **直接减少运力** |

规则：

1. 时序中的质量事件必须**回写到 §8.5 多级迭代**——迭代须在时序约束下进行，而不是先算质量再套时序
2. 整流罩抛离时刻受**动压与热流约束**（Q 与 q̇ 上限），不得为优化运力而无限制推迟
3. 回收代价分三项独立建模：**回收系统自身质量** + **着陆推进剂余量** + **增强结构与热防护质量**
4. 时序以列表存储（§6.1 `Sequence`），在界面上以时间线视图编辑（§11）

### 8.10 轨道层：GEO 与 Hohmann 转移

§8.6 覆盖 LEO/GTO/SSO/TLI/TMI 的 ΔV 需求；本层补充**更高能量轨道**与**轨道机动**。

| 目标 | 计算方式 | 说明 |
|---|---|---|
| **GEO（直送）** | 发射场直接送入地球同步轨道 | 总 ΔV 最高，受发射场纬度强影响 |
| **GEO（GTO + 远地点点火）** | 近地点点火入 GTO → 远地点点火圆化 | 工程上最常见路线 |
| **Hohmann 转移** | 两次脉冲（近地点加速 + 远地点加速） | 解析闭式解 |
| **TLI（地月转移射入，OI-22）** | 由 LEO 停泊轨道施加单次脉冲入地月转移轨道 | **输出 C3 < 0**（仍为地心束缚）；ΔV 由 `C3` 反算（见下） |
| **TMI（地火转移射入，OI-22）** | 由 LEO 停泊轨道施加单次脉冲入日心转移轨道 | ⚠ **强窗口依赖**：同一构型在不同窗口的 C3 可差数倍，故 **`C3` 与 `ΔV` 必须成对输出**，单独给 ΔV 会误导 |
| 轨道圆化 | 单次脉冲消除偏心率 | 解析 |
| 平面变更 | `Δv = 2v·sin(Δi/2)`，可与远地点点火复合 | 高纬发射场必需 |
| 逃逸 | 由双曲线超速 v∞ / C3 反推 | — |

**Hohmann 转移实现**：

```text
μ  = 地球引力常数
a_t = (r1 + r2) / 2
Δv1 = sqrt(μ/r1) · ( sqrt(2·r2/(r1+r2)) - 1 )
Δv2 = sqrt(μ/r2) · ( 1 - sqrt(2·r1/(r1+r2)) )
总Δv = Δv1 + Δv2
```

约束：

1. 一律用**理想脉冲（impulsive）近似**，并显式声明不含有限推力损失；有限推力损失由 §8.6 损失预算覆盖
2. 复合机动（远地点点火 + 倾角变更）必须**矢量合成**，禁止标量相加
3. 所有结果标注所用的 **μ 与参考半径**，禁止裸数值
4. 轨道计算的输入输出必须是 ΔV 与质量，**不涉及入轨后长期轨道演化**（非目标 §1.3）

**C3 与转移射入 ΔV（OI-23）**

C3（特征能量）是转移/逃逸轨道的能量表征，与停泊轨道上的一次脉冲 ΔV 有闭式关系：

```text
C3    = v∞²                                   ← 单位 km²·s⁻²；束缚轨道为负（TLI），逃逸为正
v_p   = sqrt(2μ/r_p + C3)                     ← 转移轨道在停泊轨道半径 r_p 处的速度
Δv    = v_p − sqrt(μ/r_p)                     ← 从半径 r_p 的圆轨道射入转移轨道所需的脉冲
```

约束：

1. **`C3` 是必输出项**，凡 TLI / TMI / 逃逸轨道一律输出（§8.8 `point.c3_km2_s2`）——**禁止**只给 ΔV 而不给 C3
2. 反向也要成立：给定 `ΔV` 与 `r_p` 必须能反算 `C3`，**双向一致性纳入回归**（否则两者必有一处是硬编码的）
3. `μ`、`r_p`、停泊轨道高度**全部标注**（沿用本节约束 3 的口径），且 ΔV 与 C3 必须用同一组 `μ` / `r_p` 算出
4. ⚠ **TMI 的窗口依赖**：C3 随发射窗口（地火相位）波动显著，故 `provenance` 必须记录**所用窗口 / 相位假设**；未记录窗口假设的 TMI 结果**视为不可复现**（同 CON-04 口径）

### 8.11 风廓线与散布分析（M6 可选）

| 能力 | 说明 | 依赖 |
|---|---|---|
| 风廓线导入 | 从离线文件（CSV/JSON）导入高度—风速风向剖面 | **仅离线文件**，不接任何在线天气服务（红线 §1.4-1） |
| 散布分析 | 对风、推力偏差、质量偏差、分离时刻做蒙特卡洛 → 落点与入轨散布 | 复用 §8.7 采样框架 |
| 结果 | 落点椭圆、入轨误差带、分离时刻敏感度 | — |

原方案提到 RocketPy 支持"从复杂数据集导入天气条件（如风廓线）"，本平台保留该能力但**严格离线化**。

---

## 9. 执行模型与缓存

### 9.1 进程模型（ADR-006）

```text
HTTP 请求 ──→ FastAPI（只做 I/O、校验、派发）
                  │  投递作业
                  ▼
          Worker 进程池（几何 + 计算，CPU 密集）
                  │  产物落盘
                  ▼
          artifacts/<content-hash>/
                  │
                  ▼
          WebSocket 推送进度与结果
```

硬规则：

1. **async handler 内禁止调用 OCCT 或任何 > 10 ms 的 CPU 计算**（会阻塞事件循环）
2. **几何对象（TopoDS_Shape）不跨进程传递**，只传参数 JSON 与 artifact 路径
3. 单个 worker 内对 OCCT 的调用串行化（避免内核内部状态竞争）
4. 作业可取消；取消后清理半成品

### 9.2 内容寻址缓存

缓存键：

```text
key = sha256( canonical_json(params)
            + kernel_version
            + spec_version
            + data_snapshot_hash
            + cea_table_id )
```

产物目录：

```text
artifacts/<key>/
├─ model.step
├─ model.glb
├─ metrics.json      # 体积、表面积、质心、面片数、耗时
├─ perf.json         # 运力、区间、敏感度
└─ provenance.json
```

**收益**：

- 参数未变 → 零计算返回（命中即毫秒级）
- 相同参数在不同会话/机器上产生**完全相同**的结果（可复现）
- 内核或数据升级 → 哈希变化 → 自动失效，不会误用陈旧结果

**`SPEC_VERSION` 的递增判据（精确化，OI-33 ⑤）**：

> **判据 = 同一输入的产物字节是否变了。**

不写成"是否发生几何语义变化"——"语义"要靠人判断，而**缓存键里只有 `canonical_json` /
`kernel_version` / `spec_version` 三项可变**（`data_snapshot_hash` / `cea_table_id` 待 M4 数据层接入）。
任何改动只要使同一输入的**产物字节**发生变化，而 `spec_version` 未同步递增，就会：

```text
改前的产物仍在 artifacts/<旧 key>/ → 命中旧键 → 返回旧 GLB → 全程零报错
```

即**静默供应陈旧产物**（"没报错 ≠ 正确"的同族失效：判据本身不响，无从发现）。

| 情形 | 产物字节 | 是否递增 | 实例 |
|---|---|---|---|
| 几何语义变化（形状 / 尺寸 / 分区改变） | 变 | **是** | OI-18 箱体比例派生：0.5.2 → 0.6.0 |
| **形状不变、产物字节改变**（导出结构 / 场景图 / 网格参数改变） | **变** | **是** | **OI-33 GLB 由单节点改为逐段具名节点：0.6.0 → 0.6.1** |
| 只加带默认值的枚举字段 / 只读端点 / DOM 标记 / 配置入口 | 不变 | 否 | OI-21～OI-26（§1.7.2）、OI-30～OI-32（§1.7.3） |
| 只改显示单位（前端按 `/api/params/units` 格式化） | 不变 | 否 | OI-32 ① |

⚠ 递增会使**既有 `artifacts/` 全部失配**——这是预期行为，正是"不会误用陈旧结果"的机制本身。
故递增必须同时按规定 §16.2 **重冻 `dist-desktop/` 并复跑探针**，因为冻结产物内也带着旧 `SPEC_VERSION`。

### 9.3 作业生命周期

```text
queued → running → (succeeded | failed | cancelled)
```

- 每个作业带 `job_id`，状态与进度可通过 WS 订阅
- 进度分阶段上报：`params → meridian → solid → mesh → step → perf → mc`
- 失败返回结构化错误（阶段 + 原因 + 可操作建议）
- 缓存命中时**不创建作业**，直接同步返回

---

## 10. API 契约

### 10.1 REST 端点

| 方法 | 路径 | 类型 | 说明 |
|---|---|---|---|
| GET | `/api/health` | 同步 | 健康检查 + 版本 |
| GET | `/api/catalog/propellants` | 同步 | 推进剂清单与物性 |
| GET | `/api/catalog/materials` | 同步 | 材料库查询与选型建议（§7.4） |
| GET | `/api/catalog/engines` | 同步 | 发动机族谱查询（§7.4） |
| GET | `/api/data/gcat` | 同步 | 按条件检索（国家/级数/推进剂/年代 + 三层标签） |
| POST | `/api/import/motor` | 同步 | 导入 `.eng` / `.ork` / `.rse` → 推进参数（§7.5） |
| POST | `/api/params/diagnose` | 同步 | 方案诊断与改进建议（§6.5） |
| GET | `/api/params/units` | 同步 | §6.4 显示单位全表（`factor` 方向固定 SI = 显示值 × factor；前端零换算系数，OI-32） |
| GET | `/api/params/thresholds` | 同步 | §6.5 阈值的**生效值**与**来源**（含"未配置"状态，OI-31） |
| PUT | `/api/params/thresholds` | 同步 | 写入 `config.toml`（§18.4 优先级不变：环境变量 > config.toml > 默认值） |
| GET | `/api/params/template` | 同步 | 参数面板的**起始箭**：一份显式标注「未经来源核对」的示例骨架 + 各字段出处表（完整内置模板库按 OI-29 归 M3） |
| POST | `/api/geometry/contour` | 同步 | 母线轮廓保存 / 载入（声明式 JSON，§5.2） |
| POST | `/api/geometry/build` | 异步 | 参数 → 几何，返回 `job_id` |
| POST | `/api/geometry/validate` | 同步 | 设计意图与约束校验 |
| POST | `/api/perf/evaluate` | 混合 | 缓存命中同步返回，否则异步 |
| POST | `/api/sizing/solve` | 同步 | 多级质量迭代（纯数值，无 OCCT） |
| POST | `/api/sizing/sequence` | 同步 | 任务时序与质量事件（§8.9） |
| POST | `/api/orbital/capacity` | 同步 | 各轨道运力（含 Hohmann 转移，§8.10） |
| POST | `/api/uncertainty/mc` | 异步 | Monte Carlo（含风廓线散布，§8.11） |
| POST | `/api/optimize` | 异步 | 多目标优化 |
| POST | `/api/trade-study` | 异步 | 多方案并行评估 |
| POST | `/api/export` | 异步 | 多格式导出（STEP/IGES/STL/GLB，§5.8） |
| GET | `/api/artifacts/{key}/{file}` | 同步 | 取产物（GLB/STEP/JSON） |
| GET | `/api/provenance/{key}` | 同步 | 溯源信息 |

### 10.2 WebSocket

```text
/ws/jobs/{job_id}      作业进度（几何 / 计算）
→ { "stage": "mesh", "progress": 0.62, "eta_ms": 180 }
→ { "stage": "done", "result_key": "sha256:..." }

/ws/assistant          AI 助手对话流（可选通道）
→ { "type": "tool_call", "tool": "rocket.build", "args": {...} }
→ { "type": "param_diff", "changes": [...] }
```

**离线降级契约**：`/ws/assistant` 依赖外部 LLM，是本平台**唯一**需要联网的可选能力。断网时该通道不可用，界面必须自动隐藏入口并保持全部手工功能可用——不得因 AI 不可用而阻断任何建模或计算流程（对应 §16 的 M7 验收标准）。

**AI 通路约定（OI-10）**：

| 项 | 约定 |
|---|---|
| 形态 | **外部 API，OpenAI 兼容协议**（`base_url` + `api_key` + `model` 三项由用户在设置中填写） |
| 密钥归属 | 平台**不自带**密钥、**不代管**模型服务、**不**预置任何第三方 endpoint；无配置即视为未启用 |
| 降级触发 | ① 未填 `api_key`；② 断网 / 请求超时；③ 上游返回错误。三者**任一**成立即降级，**不得**只处理其中一条（R-35 同源：任何"既不成功也不失败"的等待都必须设超时） |
| 降级表现 | AI 面板入口自动隐藏，**全部建模与计算功能照常可用**；手工表单是唯一权威输入路径，AI 只是**加速器** |
| 禁止 | 不得把 AI 通路作为任何参数的**唯一**录入方式；不得因 AI 不可用而阻断启动、构建或求解 |

> 与 CON-01 的关系：OI-10 不削弱离线红线——**红线管的是"其余全部功能在断网时照常可用"**，AI 助手在断网时**不可用**是它的既定语义，不是红线例外。故 §1.4-1 无需修改，但每个里程碑的断网复跑（OI-15 / §13.7）必须**在 AI 未配置的状态下**执行，否则测不到真正的离线路径。

**作业进度通道的降级契约**：`/ws/jobs/{job_id}` **不参与作业执行**——通道断掉只是失去进度反馈，后端作业照常跑完并把产物落盘。因此前端必须满足三条：

1. **地址同源推导**，禁止硬编码主机名与端口（开发态是 Vite 端口，生产态是桌面壳的动态端口）；
2. 三条失败路径**都必须**降级为轮询 `GET /api/jobs/{job_id}`（500 ms）：构造异常、`onerror`、**握手悬置**；
3. 第 3 条最易漏——**连接建立阶段的失败既不派发 `error` 也不派发 `close`**，socket 会无限停在 `CONNECTING`。故必须显式设**握手超时**（3 s）兜底，**不得只依赖 `onerror` / `onclose`**。

> ⚠ **已实际发生一次（M2 期间查出）**：Vite 的 `server.proxy` 只转发了 `/api`、漏了 `/ws`，升级请求不被转发 ⇒ 握手永久悬置 ⇒ 降级轮询永不启动 ⇒ 界面冻结在「当前阶段：排队中（0%）」**超过 2.5 分钟**，而后端**早已在 0.3 s 内构建完成并落盘**。全程**控制台零报错、无 HTTP 失败**，唯一可观测症状是"界面不前进"（A 类「没报错 ≠ 正确」）。详见 §16.3 修复记录。

**开发态代理契约**：开发期前端由 Vite dev server 托管、后端在 `127.0.0.1:8000`，故 `server.proxy` 必须**同时**转发 `/api`（HTTP）与 `/ws`（须带 `ws: true` 才转发 WebSocket 升级）。生产态由后端同源托管 `dist/`（§4.1），**不存在该代理层**——这正是此类缺陷只在开发态复现、因而容易长期潜伏的原因。

### 10.3 错误模型

```json
{
  "error": {
    "code": "GEOMETRY_G1_DISCONTINUITY",
    "stage": "meridian",
    "message": "第 3 段与第 4 段连接处切线不连续（G1 违反）",
    "details": { "segment_index": 3, "tangent_diff_deg": 12.4 },
    "suggestion": "调整级间段锥角，使与柱段相切；或插入过渡段"
  }
}
```

错误必须包含**可操作的修复建议**，"未知错误"式响应视为实现缺陷。

---

## 11. 前端架构

### 11.1 形态与栈（ADR-008）

| 项 | 决策 |
|---|---|
| 形态 | **Windows 桌面应用（exe 双击启动）**：pywebview **原生窗口**（WebView2）承载前端，后端同进程族托管 `dist/`（ADR-015）。⚠ 开发期为迭代速度可另用浏览器访问 Vite dev server，但**那不是交付形态**，也不得让任何实现依赖它存在 |
| 构建 | Vite |
| 框架 | React 19 + TypeScript（strict） |
| 3D | three + @react-three/fiber + drei |
| 状态 | Zustand（分片：params / model / jobs / data / view） |
| 样式 | Tailwind CSS 4 + shadcn/ui |
| 图表 | ECharts（经 `@zendir/ui` 暴露），承载 ΔV 预算、质量分布、敏感度图 |
| 通信 | REST（查询/提交）+ WebSocket（建模与作业进度） |
| 航天专用组件 | `@zendir/ui`，仅经适配层使用 |
| 字体 | Inter（正文）+ JetBrains Mono（参数与代码），本地自托管，禁 CDN |

> **不做 Next.js**：本项目是纯本地单页工具，无 SEO / SSR / 多路由诉求，App Router 与 Server Components 只会引入构建复杂度与 hydration 边界问题。Vite + React SPA 是更契合的形态。

### 11.2 设计系统

**风格定位**：暗色为基底的现代简约，融合玻璃拟态（Glassmorphism）。关键词：沉浸、层次分明、低干扰、精准。

**Token 分两层（ADR-013）**：组件只能引用语义层，主题层只负责给语义 token 赋值。

**第 1 层 · 语义层**（组件唯一允许引用的名字）：

```css
/* 表面 */
--color-bg-app            应用背景
--color-bg-surface        卡片 / 面板表面
--color-border            边框
/* 文本 */
--color-text-primary
--color-text-secondary
/* 强调 */
--color-accent            主强调（选中、聚焦、活动态）
--color-accent-alt        辅助强调
--color-data-highlight    数据高亮（数值、关键指标）
/* 状态（语义固定，跨主题不变） */
--color-status-ok  --color-status-warn  --color-status-error
/* 推进剂数据编码色（2D 剖面图用，见 §11.10；跨主题不变） */
--color-prop-lox          液氧
--color-prop-ch4          液态甲烷
--color-prop-kero         煤油
--color-prop-lh2          液氢（备用）
```

**第 2 层 · 主题层**（仅赋值，组件代码不得出现原始色值）：

| 语义 token | `default-dark`（默认） | `vscode-dark`（备选，源自补充手册） |
|---|---|---|
| `--color-bg-app` | `#0f0f1a` | `#1e1e1e` |
| `--color-bg-surface` | `rgba(255,255,255,0.04)` | `#252526` |
| `--color-border` | `rgba(255,255,255,0.08)` | `#3c3c3c` |
| `--color-text-primary` | `rgba(255,255,255,0.90)` | `#d4d4d4` |
| `--color-text-secondary` | `rgba(255,255,255,0.45)` | `#9d9d9d` |
| `--color-accent` | `#00d4ff` | `#007acc` |
| `--color-accent-alt` | `#7c5dfa` | `#4ec9b0` |
| `--color-data-highlight` | `#00d4ff` | `#4ec9b0` |

主题规则：

- 状态色与推进剂编码色**跨主题保持一致**——它们承载语义（危险等级、推进剂种类），不随视觉偏好变化
- 新增颜色必须先进语义层；组件中出现 `#` 字面量由 stylelint 阻断（NFR-05）
- 主题经根节点 class 切换；**默认主题为 `default-dark`**（§1.7 OI-05），`vscode-dark` 为可选主题，两者切换不改任何组件代码

设计理由（`default-dark`）：背景不用纯黑 `#000`（降低 OLED 屏刺眼感），文字不用纯白（90% 白降低长时间阅读疲劳），边框仅 8% 不透明度以保持极简视觉分隔。

排版规范：

| 用途 | 字体 | 字号 / 字重 | 备注 |
|---|---|---|---|
| 标题 | Inter | 24px / 600 | — |
| 正文 | Inter | 14px / 400 | — |
| 参数数值 | JetBrains Mono | 13px / 500 | 等宽，保证数值纵向对齐 |
| 标签 | Inter | 11px / 500 | 全大写 + 字距 0.05em |

**规则**：任何展示物理量数值的位置必须使用等宽字体并右对齐，便于逐位比对。

### 11.3 渲染规范

**双通道几何（ADR-012）**：

| 通道 | 几何来源 | 用途 | 时限 |
|---|---|---|---|
| 示意通道 | 前端按尺寸参数程序化生成（圆柱 / 圆锥 / 球冠），**不含任何物理量** | 参数变更后的即时视觉反馈 | 首帧 < 300 ms |
| 权威通道 | 后端 GLB（§5.1 双轨契约） | 正式显示、导出、计算引用 | 300 ms 内替换示意 |

- 权威 GLB 到达后必须**替换**示意网格，不得叠加显示
- **两通道的回转轴必须落在同一个世界轴**（three 的 Y）且自同一坐标起算：`LatheGeometry` 的轴本就是 Y（恒等变换），导出器已把 GLB 的 Z-up 转成 Y-up（根节点 `Rx(-90°)`），故**两侧都不得再加旋转**（§13.6「朝向」门禁）
- 示意网格与权威 GLB 的包围盒偏差 > 1% 视为实现缺陷（§13.6 门禁）；⚠ 尺寸一致**不代表**朝向一致，两者分列两条判据
- 示意网格**禁止参与任何计算或导出**

**资源生命周期（NFR-03）**：

- 重建时显式 `geometry.dispose()` / `material.dispose()`，并从 scene 移除旧对象
- 共享几何与材质走引用计数缓存，避免重复上传显存
- 循环重建 500 次后 `renderer.info.memory` 的对象数与显存须稳定（§13.6）

**其余规范**：

- 视口权威几何仅消费 GLB，前端不做几何运算
- LOD 切换由相机距离驱动，交互中降级到 LOD2
- 网格解析放入 **Web Worker**，避免阻塞主线程
- 大装配使用实例化（instancing）与按需加载

**材质表现（FR-05）**：材料 → 渲染参数映射，示意通道与权威 GLB 共用同一张表。

| 材料 | 金属度 | 粗糙度 | 说明 |
|---|---|---|---|
| 铝合金 | 0.9 | 0.35 | 高反射，配合环境贴图 |
| 碳纤维 | 0.2 | 0.75 | 低反射、偏漫射 |
| 复合材料（回退） | 0.4 | 0.60 | 未指定材料时的默认 |

**叠加层（FR-07）**：视口角落固定比例尺，纵向 `0 ～ 总长`、横向 `± 最大半径`，单位 m。比例尺随参数实时更新，并以正交叠加层绘制——**不随相机缩放变化**，保证读数意义。

**光照（FR-08）**：环境光 + 方向光为基准，提供亮度滑杆（0.2 ～ 2.0，默认 1.0）。滑块只影响视口视觉，**不得进入任何计算或导出**。

着色风格：工程视图（金属 / 法线 / 线框 / 半透明切换），不做追求观感的特效。

### 11.4 3D 交互规范

| 操作 | 行为 |
|---|---|
| 单指旋转 / 鼠标左键拖拽 | 绕模型轨道旋转 |
| 双指缩放 / 滚轮 | 缩放视图（透视 / 正交可切换） |
| **双指平移 / 鼠标右键拖拽** | 平移视图（OrbitControls pan），双指平移对应触屏 |
| 点击组件 | 高亮该组件，右侧参数面板定位到对应分组 |
| 双击组件 | 进入组件级编辑模式，其他组件半透明 |
| 长按 / 右键单击组件 | 上下文菜单（编辑参数 / 替换型号 / 查看数据源） |

**右键语义消歧（FR-06）**：右键承载"平移"与"上下文菜单"两种语义，按**拖拽判决**区分——按下后位移 ≤ 4 px 且时长 ≤ 200 ms 判为单击（弹菜单）；超过任一阈值即进入平移，抬起时不再弹菜单。该判决必须在同一个 pointer 事件序列内完成，禁止用两套监听器分别处理（会抖动）。

**参数 ↔ 模型联动契约**（前端只做展示与触发，重建一律由后端完成）：

1. 参数面板采用"**拖拽滑块 + 数值输入 + 数字步进器**"三模式（FR-02），三者读写同一 store 字段，显示精度与步长一致。
2. 改值后经 **200ms 防抖**发出重建请求；重建期间显示半透明过渡，旧模型保持可见。
3. 单次重建预计超过 1s 的复杂构型（如共底贮箱）走**渐进式重建**：先返回低精度网格或线框，再替换为完整 GLB。
4. 重建失败（约束冲突）时**保留上一可用模型**，仅将参数框标红并展示诊断，绝不留下空白视口。

**显示控制：分级显隐 / 剖切 / 爆炸视图（OI-19）**

| 控制 | 落点 | 行为 |
|---|---|---|
| **分级显隐** | **M1**（增补项） | 按级 / 按部件（整流罩、尾翼、喷管、贮箱…）显示或隐藏；隐藏是**纯视图状态**（`store/view`），**不得**触发后端请求、不得改变任何几何或计算结果（§11.6）。组件树与视口双向联动 |
| **剖切** | **M1**（增补项） | 单个可移动剖切平面（法向沿箭体轴向或径向），实时显示内部构型；**纯渲染裁剪**（`clippingPlanes`），**不重新生成几何**——不得因剖切触发重建。剖切结果**不可导出**（导出恒为完整几何） |
| **爆炸视图** | **M5** | 按级 / 部件沿轴向分离展示（位移为**视图偏移**，非几何变更）；位移量与顺序由 §5.9 的 9 段分区决定，取分区枚举而非硬编码件名 |

**分级显隐的粒度与场景图契约（OI-33，落 M1/M2 的实际口径）**

规格原文举例的"整流罩 / 尾翼 / 喷管 / 贮箱"要到 **M5** 才成为独立实体；M1/M2 的几何是**一条母线段链回转成的单体**，几何层不存在"级 / 部件"对象。故显隐粒度取**母线段链**——组件树当前正是段链，**组件树 ↔ 视口双向联动**由此成立：

| 项 | 契约 |
|---|---|
| **粒度** | `profile.segments` 的每一段 = 一个可显隐对象。⚠ 这不是降级实现——**具名节点是唯一的可分级机制**；M5 出现真部件时只把节点名从 `seg-<i>` 换成分区名，机制不动 |
| **节点命名** | `seg-<i>`，`<i>` 为 `profile.segments` 的 **0 基**下标，与参数层 `stages[i]`（"第几级"）**无关**，两者**不得**互相推演 |
| **场景图** | `root`（名 `vehicle`）→ `seg-0` / `seg-1` / …；**两通道同粒度**：权威通道按 GLB **节点名**显隐，示意通道按后端逐段下发的 `ValidationReport.segment_outline` 逐段渲染（仍是纯搬运，ADR-011 不变），否则"隐藏"只在一侧生效 |
| **量测与导出** | 体积 / 表面积 / 质心 / `model.step` **一律取整体体**；分段体只用于 GLB 导出与显隐。分段体之间新增的界面在**未隐藏任何段时被实体完全包住、从外部不可见**，故"看到的"与"算的"仍是同一个外形（P1 / ADR-012）；隐藏某段时露出的是该处的真实分界，不是人造结构 |

⚠ **三条硬约束**（每条都对应一种静默失效，须逐条机检）：

1. **节点名必须存在**——前端按名找节点，找不到即**不隐藏**（点了没反应），全程零报错；
2. **根节点不得持 mesh**——否则整体网格与分段网格**叠加渲染**，同一处被画两遍（几何被画厚了一层，肉眼不易察觉）；
3. **退化段不产出节点**——两端半径均为 0 的段，其回转体为空；前端对不存在的节点**不做隐藏**，也不得报错。

⚠ **三者的共同红线**：全部是**视图状态**。任何一项若改变了送往计算或导出的几何，即违反 P1（单一真相源）与 ADR-012——"看到的"与"算的"必须是同一个东西。剖切与爆炸的位移**不得**写回 `store/params`。

### 11.5 布局与核心操作流

主界面 = **顶部导航 + 三栏式布局 + 底部状态栏**。左栏 = 组件树（可折叠） + 参数面板；中栏 = 3D 视口；右栏 = **2D 工程剖面图 + 整箭数据面板 + 轨道运力面板**。

**顶部工具栏承载高频全局操作**（不占用三栏面积）：**新建 / 选择模板**、**发射场纬度**（含海拔 / 方位）、**目标轨道**、**计算**按钮、保存 / 导出。

> **布局裁决（OI-17，2026-09-19 确认）**：用户核心目标要求"**同一界面内同时具备 3D 视图、2D 平面模型和参数调整模块**"——本节的**三栏同时可见**即满足该要求（3D 在中栏、2D 在右栏、参数在左栏，三者同屏无切换）。**不采纳**"把 2D 塞进中栏与 3D 并排"的读法：那会挤掉右栏的整箭数据与轨道运力，并使 §11.9 的窄屏降级策略失去落点。⚠ 唯一例外见 §11.9 的 `<1024px` 断点——该断点下 3D 全屏、其余经底部 Sheet 切换，属**响应式降级**而非布局变更。

```
┌────────────────────────────────────────────────────────────────┐
│  [Logo] 建模 │ 数据库 │ 计算 │ 导出 │       [搜索] [设置] [主题] │
├───────────┬──────────────────────────────┬─────────────────────┤
│ 组件树     │                              │  2D 工程剖面图       │
│ ▸ 箭体     │         3D 视口               │  ┌───────────────┐  │
│  ▸ 一级    │                              │  │ ▤ 液氧 (LOX)  │  │
│  ▸ 二级    │      [火箭模型实时预览]        │  │ ▤ 液态甲烷     │  │
│ ▸ 助推器   │                              │  │ ▭ 发动机舱     │  │
│ ▸ 整流罩   │   ↕ 0~70 m   ↔ ±1.85 m       │  └───────────────┘  │
├───────────┤                              │  [＋] [－] [复位]    │
│ 参数面板   │   亮度 ───●────               ├─────────────────────┤
│ 总长 [70m] │                              │ 整箭数据             │
│ 直径[3.7m] │                              │ 总质量    549.0 t   │
│ 加注 [95%] │                              │ 推进剂    487.0 t   │
│ 干质比[0.127]│                            │ 干重       62.0 t   │
│           │                              │ 总高/最大直径/罩径   │
│           │                              ├─────────────────────┤
│           │                              │ 轨道运力 ☑自动更新   │
│           │                              │ LEO  21,951.1 kg    │
│           │                              │ [导出报告]          │
├───────────┴──────────────────────────────┴─────────────────────┤
│ 计算完成（自动实时更新） │ 3D 已重建（12 段网格） │ ΔV │ T/W │ 运力 │
└────────────────────────────────────────────────────────────────┘
```

**整箭数据面板字段（FR-10）**：总质量（同时给 kg 与 t）、推进剂总质量、干重、总高度、箭体最大直径、整流罩直径。数值一律来自后端 `calculatedData`，前端**只格式化不换算**（换算按 §6.4 在 API 边界完成）。

**轨道运力面板（FR-11）**：显示当前目标轨道运力（如 `21,951.1 kg`），配"自动更新"开关。

**"自动更新"关闭语义（§1.7 OI-04 裁决）**：

1. 关闭后**停止自动重算与自动刷新**（参数变更不再触发后端请求，也不触发 2D 与面板重绘）。
2. 界面**保留最后一次结果**，同时显示常驻标记 **「已冻结 / 可能过期」**，标记不得被后续渲染覆盖或隐藏。
3. 参数一旦在冻结期间被改动，标记升级为**警示态**（`--color-status-warn`）并注明"输入已变更"。
4. 重新开启时**立即按当前输入重算一次**，然后恢复自动更新。
5. 冻结不阻断任何其它功能：3D 几何重建、2D 剖面图、诊断、导出仍按各自规则工作（冻结只作用于"计算结果的自动刷新"）。

五条核心操作流：

**① 参数编辑流**
选中参数 → 3D 中对应组件高亮 → 改值 → 模型更新 → 2D 剖面图与右侧数据面板同步刷新 → 状态栏指标重算。违反约束时参数框变红并显示诊断文案（文案来自 §6.5 诊断规则，**前端不得硬编码物理规则**）。
⚠ **纬度 / 海拔 / 目标轨道的编辑走同一条流**，但结果刷新按 §8.7 的**两阶段**契约：点值与 ΔV 预算**立即**更新，区间列先显示占位态，MC 完成后覆盖（`interval_pending` 由 `true` 转 `false`）。

**诊断清单与「路径 → 控件」定位（§1.7.3 OI-32 / §6.5）**：左栏参数面板下方为 `DiagnosticsPanel`，消费 `POST /api/params/diagnose` 的完整报告（不只命中项）：

1. **按 `level` / `code` 分组**渲染；`hard` 与 `warning` 分别用 `--color-status-err` / `--color-status-warn`，并显示判定码（`HARD_*` / `ENGINEER_*` / `COMPAT_*` / `SAFETY_*` 前缀即 §6.3 的类别）。
2. **三类账目必须分别渲染**（§6.5）：命中项列在顶部；**未判定的规则**（`deferred_reason`）单独一栏，写明"缺什么"；**分支未启用**（`uncovered`）再一栏。**不得**只渲染命中项——那会让"未判"与"通过"看起来一样。
3. **点条目定位控件**：按该条 `field_path` 找到带同值 `data-field-path` 的控件，滚动至可见并高亮。控件标记的取值口径与 §6.3 末注**逐字一致**（两条通路共用一张映射）。
4. **定位失败要降级、不许丢弃**：`field_path` 为空、或当前视图里没有该控件（如所在级被折叠、面板未挂载）时，该条**仍须显示**并把路径作为纯文本渲染（不可点击），**不得**从清单里消失。
5. **阈值与来源可见**（OI-31）：设置项中列出各阈值的**生效值**与**来源**；未配置的项显式显示「未配置」，来源文字**照抄后端 `source`**，前端不得自拟措辞或改写为"规格规定"。
6. **起始箭来源**（§1.7.3 OI-32）：参数面板的初值取自 `GET /api/params/template`（§10.1），并**原样呈现**其 `note`（含「未经来源核对」字样）与 `sourced_fields`；**不在** `sourced_fields` 里的数值一律按**占位值**渲染（§1.4-4）。**禁止**在前端硬编码一套"看起来像真的"型号数字——那会形成与 §13.2 基准表冲突的第二套数字，违反 P1。⚠ 该端点交付的是**示例骨架**，不是 ⑤ 的内置模板库：后者须逐条核对公开来源，仍按 OI-29 在 M3 交付。

**② 数据库浏览流**
切到"数据库" → 左侧标签筛选器（燃料 / 国家 / 级数 / 状态，取自 §7.2 三层标签）→ 中间型号卡片网格（`SpacecraftCard`）→ 点选后右侧显示详情 → "载入到建模器"按钮将参数写入 `store/params` 并切回建模视图。

**③ 计算评估流**
点"计算"（或在自动更新开启时由参数变更触发）→ 底部弹出计算面板（**不遮挡 3D 视口**）→ 展示 **ΔV 瀑布图**、**运力—纬度曲线**、各轨道运力表、质量分布、不确定度区间 → 运力不足时面板顶部红色警告并附改进建议（来自 §6.5）。

- **ΔV 瀑布图**：数据取自 `delta_v_budget`（§8.8）——理想 ΔV 起，逐项减去重力 / 气动 / 转向 / 背压损失，再加自转加成，落在总 ΔV。**逐项显示而非只显示总和**，并显式渲染 `assumptions`（"允许简化但必须标明假设"是用户的明确要求）。
- **运力—纬度曲线**：数据取自 `payload_latitude_curve`（§8.8）——横轴为纬度、纵轴为该轨道运力，并标出当前发射场位置。它是"低纬向东运力更高"这一趋势的**可视化证据**（FR-19）。
- **图表的数值一律来自后端**：ECharts 只负责排版；**前端不得自行计算曲线上的任何点**，也不得对曲线做平滑/插值后当成数据（ADR-011）。

**④ AI 对话集成**
右下角悬浮按钮展开 `ChatPanel`，用户以自然语言描述需求（例："造一个类似猎鹰 9 号的火箭，二级换甲烷发动机"）→ Agent 经 MCP 解析并写入参数、触发建模。ChatPanel 支持结构化块（参数 diff、诊断清单、图表）。

**⑤ 模板载入流（OI-29 / FR-22）**
顶部"新建"→ 模板选择（**内置示例火箭**：Falcon 9 / 长征五号 / 土星五号等公开型号）→ 载入后 `store/params` 被整套写入 → 3D 与 2D 立即按新参数重绘 → 面板显示该模板的公开运力作为对照。规则：

1. 模板是**产品能力**，不是测试夹具——目的是让用户**快速对现役与历史型号建模，省去外部查资料**（用户原话）
2. 模板参数**必须来自公开资料并在 `provenance` 中标出来源**；**禁止**把模板里的数字当作"已验证结论"呈现（§1.4-4）
3. 载入模板**不覆盖**用户的飞行器名称与发射场选择（除非用户显式选择"完全替换"）
4. 模板与 §13.2 的基准表**同源**——基准表的参数即模板的初值，避免"两套数字各说各话"

### 11.6 状态管理

```text
store/params     ← 用户参数（唯一可写来源，SSOT）
store/computed   ← 后端计算结果（只读，含 liquid_level_m 等派生量）
store/model      ← 当前 artifact key + 网格句柄
store/jobs       ← 作业状态与进度（WS 驱动）
store/data       ← GCAT 查询结果（缓存）
store/view       ← 相机、LOD、着色模式、主题、光照亮度（不参与计算）
```

规则：

- `view` 的任何变化**不得触发后端请求**（改主题、拉亮度都不重算）
- `computed` 只由后端响应写入，**前端不得写入或修改**（NFR-07 单一数据源）
- 除 `computed` 外，前端不得保存任何第二份派生物理量

### 11.7 组件分层与适配层隔离

```text
views/          页面级组合
components/     业务组件（自有）
adapters/space-ui/   ← 唯一允许 import @zendir/ui 的目录
r3f/            3D 场景组件
api/            API 客户端（含类型定义，由后端 OpenAPI 生成）
workers/        Web Worker
```

`@zendir/ui` 替换成本被限制在 `adapters/space-ui/` 一个目录内。

### 11.8 航天专用组件白名单

`@zendir/ui` 基于美国太空军 Astro UX Design System，据称含 40+ 图表类型、3 个 3D 查看器、完整 TypeScript 类型、WCAG 2.2 AA 无障碍。**仅以下组件进入适配层白名单**：

| 组件 | 用途 |
|---|---|
| `SpacecraftCard` | 火箭型号卡片（缩略图 + 关键参数 + 状态） |
| `StatusIndicator` | 六级状态指示（颜色 + 形状双重编码，满足色盲可达性） |
| `DataTable` | 可排序 / 可筛选的发动机与火箭数据表 |
| `SidePanel` | 参数编辑面板（滑出式交互） |
| `ChatPanel` | AI 对话界面（结构化块 + 多 LLM 集成策略） |
| `MonitoringIcon` | 遥测监控图标 |

**风险控制**：该库是外部依赖中的不确定项（可用性、维护状态、许可条款均未在本机验证）。因此 §11.7 的适配层隔离是硬约定——一旦其停更或许可变更，替换成本被限制在 `adapters/space-ui/` 单个目录内，业务组件只依赖适配层导出的自有接口。**M1 必须先做该库的可用性探针**（能否安装、能否在 React 19 下渲染），探针失败则退化为自研等价组件。

### 11.9 响应式与前端性能策略

| 断点 | 布局 |
|---|---|
| ≥1440px | 三栏完整布局 |
| 1024–1439px | 组件树可折叠，参数面板浮层化 |
| <1024px | 3D 视口全屏，组件树与参数面板经底部 Sheet 切换 |

性能策略：

- 3D 场景 `frameloop="demand"`，仅在状态变化时渲染
- 参数编辑防抖 + 渐进式重建（§11.4）
- 数据库列表虚拟滚动 + 分页加载，禁止一次渲染全部记录
- 计算面板图表按需挂载，切走即卸载

### 11.10 2D 工程剖面视图（FR-09）

**定位升级（OI-18 / §5.9）**：本视图从"推进剂剖面示意图"升级为**工程剖面图**——它是 3D 模型与几何参数的第二视图，须能反过来核对 3D 是否漏件、错序。分区定义、共底口径、输送管走法、箱体比例**全部以 §5.9 为唯一权威**，本节只规定**呈现**。

**职责**：可视化"内部构型"——按 §5.9 的 **9 段轴向分区**展示各级的条带布置、贮箱内容（推进剂种类与液位）、输送管道、发动机舱位置，并标注主要尺寸。**纯渲染，不做任何物理计算**（ADR-011）。

**输入契约**（只读）：

| 来源 | 字段 | 用途 |
|---|---|---|
| `store/params` | 各级直径、贮箱长度、共底设计、共底类型、储箱排列、加注比例、推进剂种类、**输送管走法** | 绘制几何与分区 |
| `store/computed` | 各级推进剂质量、推进剂密度、**液面高度 `liquid_level_m`**、**9 段分区条带 `SectionBand.length_m`**、**共底级长缩减 `common_bulkhead_saving_m`**、**尺寸标注 `SectionBand.dimension_labels`** | 条带高度、液面位置与尺寸标注 |
| `store/view` | 缩放档位、选中部件 | 视图状态 |

⚠ **条带高度与尺寸标注数值一律由后端下发**：前端**不得**由直径/长度反推条带高度，也**不得**自行生成尺寸数值（ADR-011）。前端只做映射与排版。

**坐标映射**：纵向按**整箭总长**归一（0 在头部顶点，向下为正，与 §4.3 坐标系一致）；横向按**最大直径**归一，双向**等比**。总长或最大直径变化时整体重算映射，不得产生拉伸失真。

**绘制规范**：

| 元素 | 表现 |
|---|---|
| **分区条带** | 按 §5.9 的 **9 段分区**自上而下逐段绘制，段高取后端下发的 `SectionBand.length_m`；段边界用细实线，段名以引线标注在右侧 |
| 贮箱分区 | 按级依次排列，边框用 `--color-border` |
| 推进剂液体 | 按 `liquid_level_m` 填充，颜色取推进剂编码色（液氧 / 甲烷 / 煤油，见 §11.2） |
| 气枕区（未加注部分） | 同色 15% 透明度，与液体明确区分 |
| 共底 | 共底开启时共享隔板绘制为**单线**（两箱共用），非共底绘制为双线；共底开启时**右侧标注级长缩减量** `common_bulkhead_saving_m`；**LH₂ 侧绘制隔热层**（斜线填充，§5.9 口径 2） |
| 储箱排列 | 按 `储箱排列` 决定燃料箱与氧化剂箱的上下次序（**读字段，不按序号硬编码**，§5.9 的非铁律） |
| **输送管道** | 按 `输送管走法` 绘制：`external` → 沿箭体外侧平行于轴线；`internal` → 穿过下箱内部。**只画几何走法，不画流阻/流量等任何计算量**（§5.9 口径 3） |
| 发动机舱 | 各级底部绘制发动机舱轮廓，宽度按发动机包络 |
| **尺寸标注** | 出**全部**尺寸线（总长 / 各箱长 / 前后裙高 / 级间（舱）高 / 推力结构高 / 直径），数值取 `SectionBand.dimension_labels`；**尺寸线是固定成分，不得因缩放或空间不足而省略**（§5.9 共性第 7 条） |
| 标注 | 每箱标注推进剂名与液面高度（m）；hover 显示该箱推进剂质量 |

**可访问性**：颜色不是唯一编码——每块区域必须带文字标签（推进剂名 + 高度），与 §11.8 的 `StatusIndicator` 双重编码原则一致。

**渲染实现**：HTML5 Canvas 2D，按 `devicePixelRatio` 缩放避免模糊；参数或结果变化时**全量重绘**（单帧成本低于增量同步的复杂度），重绘经 `requestAnimationFrame` 合并。

**交互**：放大 / 缩小 / 复位（缩放档位 0.5×～4×，复位回 1× 并居中）；缩放只改 Canvas 视口变换，**不改映射基准**。

### 11.11 状态栏与反馈契约（FR-15）

状态栏是**唯一**的全局反馈位，文案由状态机驱动，禁止各模块自行拼接字符串。

| 状态 | 触发 | 文案模板 |
|---|---|---|
| `computing` | 提交计算 | `计算中…` |
| `computed` | 计算结果返回 | `计算完成（自动实时更新）`；手动模式为 `计算完成（手动）` |
| `geometry_ready` | 权威 GLB 就绪 | `3D 已重建（{n} 段网格）` |
| `geometry_preview` | 示意网格显示中 | `3D 预览中（等待精确模型）` |
| `geometry_failed` | 重建失败 | `重建失败：{诊断摘要}`（同时标红相关参数） |
| `section_ready` | 2D 图重绘完成 | `2D 已刷新` |
| `frozen` | 自动更新被关闭 | `已冻结（可能过期）`；输入已变更时为 `已冻结（输入已变更）` |
| `idle` | 无作业 | 上次完成状态 + 时间戳 |

规则：

1. 状态由 `store/jobs` 事件流驱动，文案集中在单一 i18n 表（为多语言留口）。
2. **同一时刻只显示一条主状态**；多项就绪时按 `计算 → 几何 → 2D` 优先级取最新一条，避免状态栏退化成日志墙。
3. 失败状态**不得自动消失**，须由用户操作或下一次成功覆盖。
4. 状态栏右侧常驻指标：ΔV 预算、推重比、运力（与右侧面板同源，均取自 `store/computed`）。
5. `frozen` 与其它状态**可同时存在**（冻结的是计算刷新，不冻结几何重建）：冻结期间状态栏主状态照常显示 `geometry_ready` / `section_ready`，`frozen` 作为右侧常驻标记与结果面板标记呈现。

### 11.12 导出：参数集与报告（FR-03 / FR-11）

| 导出物 | 触发位置 | 格式 | 内容 |
|---|---|---|---|
| 参数集 | 参数面板"导出" | JSON（权威）+ CSV（表格友好） | 完整 `store/params`，含 Schema 版本号 |
| 运力报告 | 轨道运力面板"导出报告" | Markdown + JSON | 参数摘要、质量分布、ΔV 预算（含瀑布图数据）、各轨道运力、不确定度区间、诊断清单、**provenance** |
| 几何 | 顶栏"导出" | STEP / IGES / STL / GLB（§5.8） | 权威 BREP 或网格 |
| **图片** | 2D 剖面图工具条 / 3D 视口工具条 | **PNG** | **当前视图快照**：2D 工程剖面图（含尺寸标注）或 3D 视口（含当前相机、剖切与显隐状态） |

规则：

1. 参数集导出必须**可回读**（导入后逐字段一致）——这是 FR-03 的验收方式。
2. 报告**必须附 provenance**（CON-04）；缺少 provenance 时导出按钮置灰并说明原因。
3. 报告中**输入参数与计算结果必须分区标注**，禁止把 `store/computed` 的数值当作输入参数导出——否则使用者会把结果误当输入复用。
4. 几何与报告导出走异步作业（§9.3），带进度提示；参数集导出为同步（体积小）。
5. **PNG 导出（OI-26）的分辨率口径**：按 `devicePixelRatio` 的**整数倍**导出（至少 2×），尺寸取**视图当前像素尺寸**——**禁止**导出被 CSS 拉伸后的模糊位图，也不做"先放大再截图"的伪高清。导出前须等待 `requestAnimationFrame` 完成一帧，避免截到中间态。
6. **PNG 内容与被导出对象的红线**：3D 快照**可以**包含剖切与显隐等视图状态（它就是"所见即所得"），但**必须**在文件元数据或相邻的 `provenance` 中记录该状态——**画面不是几何权威**，权威仍是 STEP/GLB（§5.1）。2D 快照必须包含尺寸标注（§5.9 共性第 7 条）。

---

## 12. MCP 与 Agent 工作流（ADR-009）

### 12.1 开发范式：四段法

采用「规格驱动 + 计划-执行分段 + 子代理委派 + 检查点回退」的人机协作回路：

```text
Explore（Plan Mode，只读） → Plan → Implement → Commit
```

**适用判据**：跨多文件修改、不熟悉的代码区域、方向不确定时走完整四段法；一行拼写修正或加一行日志直接做。

**本项目专属硬约束**：

1. 任何架构级变更必须先修订本规格文档，再写代码（Doc-First）。
2. 涉及几何 / 推进 / 总体三层中任意两层的改动，必须拆成独立 PR。
3. Agent 不得自行引入新依赖，须先走 §3.4 审计流程。
4. Agent 生成的数值结论必须附来源（CEA 表 ID / GCAT 记录 ID / 解析解），**无来源的数字视为无效输出**。

### 12.2 项目级配置：AGENTS.md

AGENTS.md 是 Agent 的"宪法"，置于仓库根目录，每次 Agent 会话启动时自动加载，包含五部分：项目结构、代码规范、关键命令、禁止操作、领域规则。领域规则摘要：

```markdown
## 领域规则
- 所有几何参数必须有物理约束校验（壁厚 < 直径/2 等）
- 齐奥尔科夫斯基计算必须使用多级累加模式
- 数据库查询必须走标签索引，禁止全表扫描
- CEA 表只读，禁止在计算路径内实时调用 CEA
- 网格（GLB）不得作为任何计算的输入
- 几何构造必须走 build123d 声明式 Builder API，禁止裸 OCCT 命令流
```

完整模板见附录 D。

### 12.3 自建领域 MCP 工具清单

```text
rocket.build(params)                    → model_id
rocket.measure(model_id)                → {mass, volume, area, centroid, inertia}
rocket.validate(model_id)               → 违规清单 + 建议
propulsion.evaluate(prop, of, pc, eps)  → {isp_vac, isp_actual, c_star, warnings}
sizing.solve(target_dv, stages, ...)    → 质量预算 + 收敛信息
sizing.uncertainty(...)                 → 区间 + 敏感度
gcat.query(filters)                     → 记录集
validate.compare(model_id, ref_rocket)  → 偏差报告
```

设计要点：**工具暴露领域语义，不暴露通用 CAD 原语**。这比通用 CAD MCP 更有价值，且完全本地、可版本化。

> **澄清**：原始方案中配置的 `occt-mcp-server`、`cad-cae-copilot-mcp` 在 npm 上并不存在（§3.2 已验证）。本方案**不依赖任何第三方 CAD MCP**，上述工具全部自建，随仓库版本化。

### 12.4 Agent 分工

| Agent 角色 | 职责 | 工具 | 产出物 | 边界 |
|---|---|---|---|---|
| **Architect** | 读规格与 AGENTS.md，产出实现计划 | Plan Mode（只读） | 可编辑的实现计划 | 不写代码 |
| **Geometry** | 几何模块实现 | 自建几何 MCP | 几何代码 + 单测 | 只经 MCP 调几何工具，不直连内核 |
| **Data** | GCAT / 材料 ETL、Schema、查询 API | 自建数据 MCP | 数据层代码 + 迁移脚本 | 只写快照，不改业务代码 |
| **Propulsion** | CEA 表生成与推进计算 | 自建推进 MCP | 计算代码 + 验证用例 | 只读 CEA 表，禁止改表 |
| **Sizing** | 质量预算与运力求解 | 自建总体 MCP | 求解器代码 + 基准对比 | 只消费几何静特性与推进结果 |
| **UI** | 前端实现 | API 类型定义 | 组件 + Storybook | 只消费 API 类型，不碰后端模型 |
| **Validator** | 跨模块集成验证 | 独立会话，只读 + 执行测试 | 测试报告 + 缺陷清单 | **禁止修改被测代码** |

Agent 之间通过文件系统约定异步协作：共享接口定义放 `/specs/`，各 Agent 产出代码放各自模块目录。

### 12.5 Agent Skills 清单

使用 Markdown 格式的 `SKILL.md` 注入航天领域知识。本项目需自建的 Skills：

| Skill 名称 | 用途 | 关键内容 |
|---|---|---|
| `rocket-parametric-modeling` | 火箭参数化建模 | 参数 Schema 定义规范、几何生成流程、约束校验规则 |
| `gcat-data-import` | GCAT 数据导入 | 字段映射、三层标签体系、查询模式 |
| `tsiolkovsky-solver` | 总体求解 | 多级 ΔV 公式、损失系数、反向求解算法 |
| `orbital-capacity` | 轨道运力评估 | LEO/GTO/GEO/SSO 的 ΔV 预算、Hohmann 转移 |
| `common-bulkhead-tank` | 共底贮箱几何 | 共享曲面拓扑、容积计算、质量估算 |
| `material-selection` | 材料选择 | 材料库查询、性能对比、选型建议 |
| `cea-table-gen` | CEA 表生成 | 推进剂组合清单、O/F-Pc-ε 网格、表格式与校验 |
| `sequence-planning` | 时序规划 | 事件类型定义、时间与质量事件耦合规则 |

Skill 书写规则：**先澄清再分析**——Skill 内不得直接给出数值结论，必须先声明所需的输入参数与适用假设。

### 12.6 `/specs/` 协作约定

```text
/specs/
├── interfaces/     跨模块接口定义（唯一真源，双方只读）
├── geometry/       几何构型规格（如 common-bulkhead-tank.md）
├── propulsion/     推进计算规格
├── sizing/         总体求解规格
└── ui/             前端交互规格
```

规则：跨模块协作**先改 `/specs/` 再改实现**；接口定义变更必须同步更新调用方的测试用例，否则 PR 不予合并。

### 12.7 验证闭环（Agent 输出的门禁）

```text
第 1 层   静态：ruff + mypy(strict) + tsc + eslint + stylelint
第 1.5 层 非功能：§13.6 专项（双通道一致性 / 资源释放 / 零物理公式 / token 合规 / 防抖）
第 2 层   编程式：几何有效性 + 解析对照 + 意图断言
第 3 层   定量：与黄金基准火箭对比（§13.2）
```

**关键警示**：第 1、2 层能拦住"跑不起来"和"明显不自洽"，但拦不住"自洽却错"。因此**第 3 层的定量对比是硬门禁**——任何几何或计算模块的 PR 必须附带与基准的偏差数字。

三层验证的具体工具与阈值见 §13。

### 12.8 工作流示例：实现共底贮箱几何生成

1. **Spec 编写（人工）**：在 `/specs/geometry/common-bulkhead-tank.md` 写清几何特征、参数 Schema、容积计算公式、输出格式。
2. **Explore + Plan（Architect Agent）**：只读现有几何模块、参数 Schema、数据库中的贮箱参数格式，产出实现计划。
3. **人工审批计划**：开发者审查并补充遗漏（如底深与直径的约束关系），确认后退出 Plan Mode。
4. **并行实现（Geometry + Data Agent）**：Geometry Agent 写 NURBS 曲面生成代码；Data Agent 在数据库增加 `common_bulkhead` 配置项与对应校验规则。
5. **Validator Agent 验证**：跑几何有效性测试，检查容积与解析解一致性、拓扑封闭性、共享曲面正确性。
6. **Commit 与 PR**：Agent 生成提交信息并创建 PR，开发者做最终审查。

注意第 4 步的并行前提是第 1 步的接口已冻结——这正是不做 `/specs/` 先行的项目最容易踩的坑。

---

## 13. 测试与验证体系

### 13.1 测试金字塔

| 层级 | 工具 | 覆盖对象 | 门禁 |
|---|---|---|---|
| 单元 | pytest | 母线、单位换算、插值、求解器、最优混合比、时序、Hohmann、导入器 | 语句覆盖率 ≥ 80% |
| 属性 | hypothesis | 单调性、量纲、不变量 | 必须通过 |
| 集成 | pytest + HTTP | API 端到端 | 必须通过 |
| 黄金基准 | pytest（回归） | 全链路运力 | 偏差阈值见 §13.2 |
| 前端 | vitest | store、API 客户端、适配层 | 必须通过 |
| 性能 | 自研 benchmark | 建模延迟、缓存命中率 | 见 §13.5 |

### 13.2 黄金用例（基准火箭）

用公开数据构造回归基准。**M3 实施时须逐条二次核对权威来源并记录引用**，本表仅确立方法与量级：

| 基准 | 级数 | 目标轨道 | 公开运力 | 起飞质量 |
|---|---|---|---|---|
| Falcon 9 | 2 | LEO | ~22.8 t | ~549 t |
| Falcon Heavy | 2.5 | LEO | ~63.8 t | ~1420 t |
| 长征五号 | 2.5 | LEO | ~25 t | ~867 t |
| Electron | 2 | LEO | ~0.3 t | ~13 t |
| **土星五号（Saturn V）** | **3** | **LEO / TLI** | **~140 t / ~43.5 t** | **~2 970 t** |

**土星五号的特殊价值（OI-29）**：它是**唯一的三级基准**，且**同时覆盖两个轨道**（LEO 与 TLI）——补它解决两个此前无基准可测的盲区：① **多级（3 级）质量迭代**（§8.5 的外层 GLOW 迭代在 2 级基准上测不出"中间级分配错误"）；② **高能轨道**（TLI 是 §8.6 / §8.10 新增轨道类型中唯一有充裕公开运力数据的）。
⚠ **取值口径须在 M3 核对时一并记录**：土星五号的公开数值随**构型与任务**而异（LEO 设计值约 140 t、阿波罗任务实际约 118 t；TLI 载荷约 43.5–48.6 t，取决于是否计入登月舱）。核对时**必须写明取的是哪一档**，否则"误差 < 15%"这一阈值可以直接靠换口径来达成。

**基准与内置模板同源（OI-29 / FR-22）**：上表同时是 §11.5「模板载入流」中**内置示例火箭模板**的参数初值——**同一份数据，两个用途**（回归基准 + 产品模板）。⚠ **禁止**为模板另建一套数字：那会让"测试用一套、用户看到另一套"，属 P1（单一真相源）违反。模板的公开运力对照值即上表"公开运力"列。

**验收阈值**：

- M4 完成：LEO 运力估算误差 **< 15%**
- M6 完成：**< 10%**
- 任一基准失败 → 阻塞发布

### 13.3 属性测试（不依赖具体数值）

- `ε ↑ ⟹ Isp_vac ↑`（面积比增大，真空比冲单调不减）
- `Pc ↑ ⟹ c* 单调变化方向正确`
- `σ ↓ ⟹ 运力 ↑`（结构更轻，运力更高的单调性）
- `ΔV_target ↑ ⟹ 所需推进剂质量 ↑`
- 单位换算往返恒等：`to_SI(from_SI(x)) == x ± 1e-12`
- 级数增加不应降低运力（在合理构型下）
- 缓存键对参数顺序不敏感（canonical JSON）
- 母线定义 → 实体 → 导出母线 往返一致（同一轮廓不因旋转/光顺而漂移）
- 时序事件后各级质量单调不增，且末态质量 = GLOW − 累计消耗
- Hohmann 转移 ΔV 单调性：固定 r₁ 时 `r₂ ↑ ⟹ ΔV_total ↑`

### 13.4 静态检查（对 AI 生成代码是刚需）

- Python：`ruff`（lint + format）+ `mypy --strict`，CI 阻断
- TypeScript：`tsc --noEmit` + `eslint`
- 禁止 `Any` 无理由出现；禁止裸 `except`
- 后端 OpenAPI → 前端类型**自动生成**，禁止手写重复类型

### 13.5 性能基线

| 指标 | 目标 |
|---|---|
| 参数变更 → 预览更新（缓存未命中） | < 500 ms |
| 参数变更 → 预览更新（缓存命中） | < 50 ms |
| CEA 单点查询 | < 1 ms |
| 多级迭代求解 | < 50 ms |
| 时序求解 | < 10 ms |
| Hohmann / 轨道运力表 | < 5 ms |
| 10k 样本 Monte Carlo | < 10 s |
| 缓存命中率（连续调参会话） | > 70% |
| 参数变更 → 右侧面板数值更新 | < 100 ms（NFR-04） |
| 参数变更 → 3D 首帧示意网格 | < 300 ms（NFR-04） |
| 参数变更 → 2D 剖面图重绘 | < 50 ms |

#### 13.5.1 性能预算门禁（可机器判定）

上表是**产品目标**，覆盖面广但多数无法自动判定。本节把其中**能长期自动守住**的几条抽出来，
固化为带「测量方式」的预算；由 `backend/tests/unit/test_perf_budget.py` 执行（§13.8）。

| 预算项 | 上限 | 测量方式 | 当前实测 |
|---|---|---|---|
| 前端产物 JS 总体积（min，未 gzip） | **1400 kB** | **CI 实测**：`frontend/dist/assets/*.js` 求和 | 1173.72 kB（M2 进度通道修复后重建；较修复前 +0.50 kB） |
| 冻结产物总体积（PyInstaller `onedir`） | **550 MB** | **产物实测**：`dist-desktop/AeroForge/` 目录求和；无产物则跳过并留痕 | 451.72 MB |
| `POST /api/geometry/validate` p95（`TestClient`，n = 40） | **100 ms** | **CI 实测** | 中位 2.27 ms / p95 2.67 ms（`TestClient` 口径；同一接口走 uvicorn 真进程为 3.76 / 5.49 ms） |
| 改参数 → 预览更新（含 200 ms 防抖） | **500 ms** | **人工实测**（CI 不覆盖）：真实浏览器脚本，数字记入 §16.2 复验记录 | **253.6 ms**（M2 首项复验重测；5 次最大 270.2 ms） |
| 冷启动 → 首帧渲染完成（冻结产物 `--probe`） | **7.0 s** | **产物实测**（CI 不覆盖）：`AeroForge.exe --probe` 输出 | **4.55 s**（M2 首项复验） |

> **体积口径**（刻意不同，勿"顺手统一"）：前端 JS 用**十进制** kB（与 `vite build` 日志的
> `1,173.72 kB` 一致），冻结产物用**二进制** MB（与资源管理器显示的 `451.72 MB` 一致）。
> 两者各自沿用所在生态的惯用口径，改口径会使本表数字与既有记录、构建日志对不上号
> （实测：1173.72 kB 按 1024 算得 1146.21，差 2.3%，很容易被误读成"包变小了"）。

> **数字时点**：冻结产物体积（451.72 MB）与 `validate` p95 取自 M1 收尾的 CI 实测、M2 首项复验
> **未变**；**改参数 → 预览** 与 **冷启动 → 首帧** 两条在 M2 首项交付链复验时重测并刷新；
> **前端包体积**在 M2 进度通道修复后重建时由 1173.22 → **1173.72 kB**（§16.3 修复记录）。
> 换行改数字时请连"取自哪一次"一起写清，否则读者无法判断该数字是否已随代码失效
> （教训：M1 的包体积曾按 1024 口径与构建日志对不上，正是"数字在、时点与口径不在"造成的）。

**规则**：

1. **不允许存在"无人测量"的预算**——每条预算要么有对应的机器门禁，要么在测试里显式登记为
   「人工实测 / 产物实测」并写明理由。新增预算若不表态即失败（同 §13.8 的"夹具须显式归类"口径）。
2. **超限即失败，不得靠调高上限来消除失败**。上调上限必须在修订记录中给出理由与新的实测依据。
3. 各项上限按"实测值 ×（1.2–20×）"留出余量，目的是**拦住量级回归**（依赖膨胀、原生库漏收/多收、
   热路径退化为 O(n²)），而不是把当前机器性能写死。CI 上的实际数值会与本机不同，故余量偏宽。

> **「CI 实测」的落点（OI-12）**：本表所称 CI 指 `.github/workflows/ci.yml`（§13.9）。
> ⚠ 该工作流在本版之前**并不存在**——"CI 实测"一度是**无实体的措辞**（门禁只在人工敲 `pytest` 时执行）。
> 故两处须一并成立：① 工作流真实存在并被推送；② 本表内"CI 实测"与"人工/产物实测"的归类与实际执行方式一致。
> **不进 CI 的三项**：端到端预览延迟、`--probe` 首帧、断网复跑（前两项无图形/产物环境，第三项需真实断网）——
> 它们在本表中登记为人工或产物实测，故**不得**被塞进工作流假装自动化（§13.8 口径：`warn` / `skip` 都要留痕，不许"悄悄通过"）。

### 13.6 非功能与前端一致性测试

补充手册提出的多条红线属于**非功能需求**，无法靠单元测试覆盖，必须用专项测试兜住：

| 测试 | 方法 | 通过判据 | 对应需求 |
|---|---|---|---|
| **双通道一致性（尺寸）** | 同一参数集下，对比前出示意网格与后端 GLB 的**世界**包围盒（长 / 最大直径 / 分段数） | 尺寸偏差 ≤ 1% | ADR-012 / FR-04 |
| **双通道一致性（朝向）** | 两侧各把包围盒搬到**渲染器实际看到的世界坐标**（权威侧含场景图节点变换，示意侧为恒等）后比对**轴向归属**与轴向起止 | 回转轴落在同一世界轴、自同一坐标起算（后端 `test_dual_channel_orientation` / 前端 `orientation.test.ts`） | ADR-012 / R-25 |
| **GLB 场景图契约（分段显隐的前提）** | 解析导出的 `model_lod2.glb`（OI-33 ②）：① 断言根节点存在、名为 `vehicle` 且**不持 mesh**；② 断言 `seg-<i>` 节点数与 `profile.segments` 逐段对应、**逐段缺一即失败**（缺名 = 找不到节点 = 不隐藏 = **静默失效**）；③ 断言各段节点各持一个 mesh；④ 退化段不产出节点；⑤ **未隐藏任何段时**，分段 GLB 的**世界**包围盒与单段整体体一致（偏差 ≤ 1%）——分段不得改变外形 | 上述五项全过（后端 `test_glb_segments.py`）——③④⑤ 属"看到的仍是同一个外形"（P1 / ADR-012）的机检形态 | §11.4 / OI-33 / ADR-012 |
| **显隐与剖切是纯视图状态** | 依次隐藏各段、拖动剖切平面到两个轴向、开合剖切开关；同时记录 `fetch` / XHR 调用与 `store/params` | **零后端请求**、`store/params` 逐字段不变；剖切不得触发重建（§11.4 共同红线） | ADR-011 / §11.6 |
| **组件树 ↔ 视口双向联动** | 在组件树勾选隐藏 `seg-i` → 断言权威通道与示意通道**各自**都隐藏了同一段；反向点击视口中的段 → 断言组件树高亮同一行 | 两通道同粒度、同一段；不存在"只在一侧生效" | §11.4 / OI-33 ③ |
| **作业进度通道降级** | 把 WS 替身换成**永不回调**（既不 `open` 也不 `error`/`close`），构造握手悬置；再走一次必然缓存 miss 的真实构建 | 3 s 内降级为轮询 `GET /api/jobs/{job_id}` 并最终收敛为权威通道；**不得**无限停在初始态（前端 `geometry.test.ts` / `model.test.ts`） | §10.2 / NFR-01 |
| **资源释放** | 循环重建 500 次，采样 `renderer.info.memory`（geometries / textures） | 对象数收敛不增长（±5 内波动） | NFR-03 |
| **防抖有效性** | 脚本连改参数 100 次（间隔 20 ms） | 后端仅收到 1 次重建请求 | NFR-01 |
| **前端零物理公式** | 静态扫描 `frontend/src`：禁止出现 `9.81`、`g0`、`ln(` 等物理常量与公式；计算结果只能来自 API 类型 | 命中数 = 0 | NFR-06 / ADR-011 |
| **token 合规** | stylelint 规则：组件样式内禁止 `#rgb`/`#rrggbb`/`rgba(` 字面量 | 命中数 = 0 | NFR-05 |
| **状态栏文案映射** | 注入各作业事件，断言状态栏文本 | 与 §11.11 模板逐条一致 | FR-15 |
| **液面高度** | 给定加注比例与贮箱几何，比对 2D 图绘制的液面像素位置与后端 `liquid_level_m` | 换算回物理尺寸后误差 ≤ 1 mm | FR-09 |
| **参数集往返** | 导出 JSON → 重新导入 → 逐字段 diff | 完全一致 | FR-03 |
| **偏度口径** | 固定样本序列，与 pandas `skew()`（`bias=False`）及 Excel `SKEW` 对照；另测 `n=2` 与全同样本 | 数值一致（相对误差 ≤ 1e-9）；`n<3` 或 `m₂=0` 返回 `null` 并附原因 | §8.7 / OI-01 |
| **质量比换算** | 随机 σ，断言 `干质比 = σ/(1−σ)`、`质量分数 = 1−σ`，并做 σ → 干质比 → σ 往返 | 相对误差 ≤ 1e-12；Schema 内不存在第二份独立存储字段 | §8.4 / OI-02 |
| **加注比例约束** | 输入 `1.01` 且未启用"最大允许加注量" | 拒绝并提示启用该字段，不得静默放行 | §6.1 / OI-03 |
| **冻结语义** | 关闭自动更新 → 改参 → 观察请求与标记 → 重新开启 | 冻结期零计算请求、标记常驻且不丢；开启后立即重算一次 | §11.5 / OI-04 |
| **发动机高度口径** | 断言该字段含喷管（安装基准面 → 喷管出口端面）；不含喷管时读取独立字段 | 两字段互不覆盖、互不推断 | §6.1 / OI-06 |

**说明**：其中"双通道一致性"与"前端零物理公式"是 ADR-011/012 的**唯一防线**——没有这两项，双通道与前端渲染会在几个迭代后悄悄漂移成分叉实现。

> **朝向必须单列一条（M2 首项实测所得）**：尺寸类断言对"多转 90°"**完全失明**——三边长度不变，
> 只是逐轴的归属变了。M1 期间两侧的尺寸断言**各自在自己的坐标系里自洽**（后端比的是访问器局部
> 坐标、前端只比 Lathe 自身），于是"渲染器实际看到的空间里回转轴落在哪根轴上"无人断言，
> 直至目视核对才发现权威通道被多转了一次（详见 §16.3 的核对记录）。故判据的坐标系必须是
> **渲染器实际看到的世界**，而非任一侧的中间数据。

### 13.7 端到端验收（补充手册交付标准）

把补充手册的交付标准改写为可执行断言。

**场景**：冷启动 → 载入默认构型（直径 5.0 m、级高 10.0 m、真空比冲 360 s）→ 逐项改参。

| 步骤 | 断言 |
|---|---|
| 1 | 启动后 3 s 内三栏渲染完成，状态栏处于 `idle` 或上次完成状态 |
| 2 | 改"直径" → 右侧数据面板数值在 **100 ms** 内变化（FR-10 / NFR-04） |
| 3 | 同一操作下 2D 剖面图在 **50 ms** 内按新比例重绘；改"加注比例"时液面高度随之变化（FR-09） |
| 4 | 3D 视口先出示意网格（< 300 ms），随后被权威 GLB 无缝替换，**无闪烁、无空洞**（ADR-012） |
| 5 | 状态栏最终稳定在 `计算完成（自动实时更新）` + `3D 已重建（N 段网格）`（FR-15） |
| 6 | 全程连续改参不出现内存增长（NFR-03）与请求风暴（NFR-01） |
| 7 | **断开网络**后重复步骤 2–5，结果与联网时一致（CON-01） |

步骤 1–6 作为 CI 冒烟用例；**步骤 7 的离线复测按 OI-15 提级**：

- **不再是"发版前手工执行一次"**，而是**每次交付链复验固定加一步**（§16.2）：断网 → 跑 `--preflight` / `--probe` → **并完整走一遍「改参 → 预览 → 构建」**；
- 复测须在 **AI 助手未配置**的状态下执行（§10.2 的降级语义），否则会测不到真实离线路径；
- 理由：CON-01 是全篇**第一条红线**，却只挂在 M7 验收上——按原安排，它要到项目末期才第一次被验证，违反"没报错 ≠ 正确 / 全绿 ≠ 跑起来了"的同族教训。

### 13.8 工程自检门禁（已发生错误的回归固化）

§13.1–§13.7 检查**产品**是否正确；本节检查**研发过程**自身是否在退步。
每一条都由一次真实事故换来，因此必须长期在 CI 内运行——没有机器门禁的"记住教训"，
在下一个会话就会失效。

| 门禁 | 拦截的事故 | 实现 |
|---|---|---|
| **文档命令可执行性** | `AGENTS.md` 写着 `npm run test:nfr`（脚本不存在）、`uv run python -m aeroforge.geometry.validate`（模块无 `__main__`，执行后**静默无事**）、`uv run python tools/cea_tablegen.py`（文件不存在）——三条死命令，静态阅读完全看不出来 | `backend/tests/unit/test_agents_contract.py` |
| **附录 D 逐行同步** | 规格附录 D 与 `AGENTS.md` 各改各的（同一行 `见 §3.2` vs `见规格 §3.2`），物化产物与源脱钩 | 同上（`difflib` 输出可读 diff） |
| **解析规则自检** | 命令解析正则若失效，"无引用"与"解析不到"无法区分，守卫会**永远跳过**而无人察觉 | 同上 |
| **可写根落在临时区** | `artifacts_root() = data_root().parent / "artifacts"` 曾使产物落到 `%TEMP%\artifacts` 并跨会话留存，症状是"第二次跑测试莫名 `cache_hit` 为 True" | `backend/tests/unit/test_test_isolation.py` |
| **夹具必须过产品校验器** | 夹具自己在轴处退化（`base_radius=1.0` 配 `arc(1→0)`）导致测试失败，当时的诱惑是**放宽断言**——正确处置是修夹具 | `backend/tests/unit/test_fixture_profiles.py` |
| **夹具须显式归类** | 新增夹具若不表态"合法 / 合法但有警告 / 非法"，就会绕过上一条 | 同上（未归类的工厂直接失败） |
| **验收记录必须显式留白** | 验收记录只写"全绿"，把未做的核对（如双通道朝向目视）留在字里行间——"看起来全绿"会让人误以为覆盖完整 | `backend/tests/unit/test_acceptance_records.py` |
| **性能预算不允许无人测量** | 预算写在文档里却没有任何门禁执行，等于没有预算；M1 之前"首帧 4.74 s / 包 1173 kB"这类数字只存在于叙述中，无人拦回归 | `backend/tests/unit/test_perf_budget.py`（§13.5.1） |

**验收记录的留白格式（强制）**：§16 内每一条以「（已达成）」结尾的验收记录/复验，**必须**包含一段以

```
**本记录明确未覆盖的项**：
```

开头的声明；确实无留白时写「无」——**不允许省略该段**。同一节内若出现 `**本记录明确未覆盖的一项**`
等变体，视为未按格式书写（门禁会直接报出该节的标题，要求统一）。

**口径**：`warn` 与 `skip` 都计入通过，但必须在报告里显式留痕——本工程不允许存在
"悄悄通过"的检查（与 §5.7「`is_valid() == True` 不等于模型正确」同源）。

### 13.9 持续集成（GitHub Actions）

**由来**：本规格有 **22 处**提到"CI"（§13.4 阻断、§13.5.1 实测、§13.8 长期运行、R-31 增加一步……），
而仓库里**没有任何工作流文件**——全部门禁只在人工敲 `pytest` 时执行。**"文档承诺了不存在的东西"**
与 §15 曾声明 `docs/architecture.md` 是同一类缺陷（OI-12 / OI-14）。本节把 CI 从措辞变成实体。

**工作流**：`.github/workflows/ci.yml`——**单一 job、步骤严格按序**（不用多 job 并行，理由见下）。

| 段 | 步骤 | 对应门禁 |
|---|---|---|
| 后端静态 | `uv sync --frozen` → `ruff format --check .` → `ruff check .` → `mypy` | §13.1 §13.4 |
| 前端 | `npm ci` → `npm run typecheck` → `npm run test` → `npm run build` | §13.1 §13.4 §13.6 |
| 契约新鲜度 | 后端重建 `specs/openapi.json` → `git diff --exit-code` → `npm run gen:api` → `git diff --exit-code` | §15 R-31 |
| **后端测试** | `uv run pytest`（含 §13.8 五类工程自检门禁与 §13.5.1 的 CI 实测预算） | §13.5.1 §13.8 |

**硬约定**：

1. **触发条件**为 `push` 与 `pull_request`，两者同等对待（主干开发，§18.3）；
2. **运行器为 `windows-latest`**：本平台是 Windows 优先交付（ADR-015），`uv.lock` 也按 Windows 解析；换 Linux 运行器会改变 OCCT / CEA 这类含原生扩展的依赖实际解析到的 wheel，使 CI 的绿与本地/交付环境脱钩；
3. **契约新鲜度检查走两步，缺一不可**（R-31）：先由后端**重新导出** `specs/openapi.json` 并比对，再由前端 `npm run gen:api` 比对 `schema.d.ts`。只做第二步发现不了"后端接口改了、`openapi.json` 没重导"这一类漂移；
4. **「后端测试」必须排在前端构建之后**：§13.5.1 的前端包体积预算量的是 `frontend/dist/assets/*.js`；顺序颠倒会让该预算每轮**显式 skip**，"CI 实测"随即重新变成一句空话（这正是 R-36 的形态）；
5. **不得为了让 CI 变绿而放宽门禁**（§13.8 口径）：`--no-verify`、`|| true`、注释掉断言、把失败步骤改成 `continue-on-error` 均属违反；
6. **CI 不构建冻结产物、也不跑断网复跑**：含 OCCT + CEA 的 `onedir` 冻结是数百 MB 级且需真实 Windows 桌面环境；断网复跑需真实断网。三者（含端到端预览延迟）在 §13.5.1 已登记为**人工 / 产物实测**，由 §16.2 复验记录承载——**不得**把它们塞进工作流假装自动化。

---

## 14. 优化与权衡研究

架构红利（原则 P3）：因为几何与性能评估被压到毫秒级，以下能力**成为可能**：

| 能力 | 说明 | 目标尺度 |
|---|---|---|
| 多目标优化 | 最小起飞质量 / 最大运力 / 最少级数 | NSGA-II 或贝叶斯优化 |
| 权衡研究 | 多方案并行评估与对比表 | ≤ 20 方案 |
| 敏感度报告 | 参数影响排序，指导设计重点 | Top-10 |
| 批量扫描 | 推进剂 × 级数 × 轨道 全因子 | ≤ 10⁴ 组合 |
| 逆向设计 | 给定运力目标求最小构型 | 约束优化 |

**约束**：优化过程必须复用缓存，且每个候选方案都要带 provenance，否则"最优解"无法审计。

---

## 15. 目录结构

```text
aeroforge/
├─ AeroForge-Spec.md            # 本文档（唯一真理源）
├─ AGENTS.md                    # Agent 宪法（模板见附录 D）
├─ pyproject.toml               # uv 管理
├─ uv.lock
├─ .python-version              # 3.12
├─ .tools/                      # 工程内自包含工具链（gitignore）
│  ├─ uv/                       # uv.exe / uvw.exe / uvx.exe
│  ├─ python/                   # cpython-3.12.14（由 uv 安装）
│  ├─ uv-cache/                 # 本地解析缓存
│  └─ npm-cache/                # npm 缓存（前端，见 .npmrc）
├─ .venv/                       # uv 创建的虚拟环境（gitignore）
├─ backend/
│  ├─ aeroforge/
│  │  ├─ api/                   # FastAPI 路由 · 依赖注入 · 错误映射
│  │  ├─ selfcheck.py           # 环境预检唯一实现（字体 / OCCT / CEA，R-29 / R-30）
│  │  ├─ params/                # Schema · DAG · 约束 · 单位 · 诊断规则
│  │  ├─ geometry/              # meridian(2D轮廓)/ solid/ loft/ mesh/ smooth/ validate/ export/
│  │  ├─ propulsion/            # cea_table/ efficiency/ workpoint(最优 O/F)
│  │  ├─ sizing/                # mass/ staging/ losses/ sequence(任务时序)/
│  │  ├─ orbital/               # hohmann/ capacity/ geo/
│  │  ├─ uncertainty/           # monte_carlo/ sensitivity/ wind(风廓线)/
│  │  ├─ optimize/              # objectives/ algorithms/ trade_study/
│  │  ├─ data/                  # repository/ etl/ models/ materials/ lineage/
│  │  ├─ cache/                 # 内容寻址 · 键计算
│  │  └─ worker/                # 进程池 · 作业 · 进度
│  └─ tests/                    # unit/ property/ integration/ golden/
├─ frontend/
│  ├─ src/{views,components,adapters/space-ui,r3f,contour,section2d,statusbar,export,store,workers,api,styles,test}/
│  ├─ package.json
│  ├─ package-lock.json         # npm 锁文件（与 uv.lock 同为依赖锁定依据）
│  ├─ .npmrc                    # registry（npmmirror）+ cache（工程内，见下）
│  └─ vite.config.ts
├─ desktop/                     # 桌面壳与打包（ADR-015，M0.5 起）
│  ├─ launcher.py               # 入口：路径编码门禁 → 取空闲端口 → 启动后端 → 开 pywebview 原生窗口；--probe / --preflight
│  ├─ resource_path.py          # 资源定位（兼容源码运行与 sys._MEIPASS）；ascii_root() 判定安装路径是否纯 ASCII（R-34）
│  └─ packaging/
│     ├─ aeroforge.spec         # PyInstaller onedir 配置
│     └─ hooks/                 # 自定义 hook：收集 OCP / cea / lib3mf 的动态库与数据文件
├─ mcp/                         # 领域 MCP Server
├─ skills/                      # Agent Skills（SKILL.md，见 §12.5）
├─ specs/                       # 跨模块接口与构型规格（见 §12.6）
│  └─ openapi.json              # 后端 OpenAPI 导出（前端类型生成的契约来源，由后端生成）
├─ data/
│  ├─ snapshots/                # 数据快照（不可变）
│  └─ tables/                   # CEA 预计算表
├─ tools/                       # cea_tablegen.py · gcat_etl.py · model_import.py · benchmark.py
│                               #   · preflight.py（预检 CLI：转调 aeroforge.selfcheck，不另存实现）
├─ testdata/
│  └─ ref_models/               # 参考模型文件（.ork/.eng/.rse），供导入器回归
├─ artifacts/                   # 内容寻址产物（gitignore）
├─ .github/
│  └─ workflows/
│     └─ ci.yml                 # 持续集成（§13.9）：后端 ruff/mypy/pytest + 前端 typecheck/test/build + 契约新鲜度
└─ docs/                        # adr/ · backlog.md（新想法收纳，R-16 / R-20 的落点）
```

> **`.tools/` 的存在理由**：本机沙箱对 `C:\Python*` 与 `D:\nodejs\npm-cache` 等系统路径的写入被拦截，导致 `pip install uv` 失败（`WinError 5 拒绝访问`）、`npm install` 失败（`EPERM`）。因此 uv、Python 3.12、uv 解析缓存与 **npm 缓存**全部落在工程内自包含目录。使用时须设
> `$env:UV_PYTHON_INSTALL_DIR` 与 `$env:UV_CACHE_DIR` 指向 `.tools/` 下对应子目录；前端则由 `frontend/.npmrc` 的 `cache=../.tools/npm-cache` 自动生效，无需环境变量。
> `tools/preflight.py` 为环境预检的**命令行入口**（字体完整性、OCCT 内核自检、CEA 点火自检），见 R-29 / R-30。⚠ **实现不在该脚本内**：唯一实现在 `backend/aeroforge/selfcheck.py`，脚本仅转调。原因见 §16.2「预检保留」——预检必须随交付产物一同冻结，故实现必须在被冻结的导入图内。

> **OpenAPI 契约的新鲜度**：`specs/openapi.json` 与由它生成的 `frontend/src/api/schema.d.ts` 都是**派生产物**。后端接口变更后必须重跑导出并 `npm run gen:api`，二者不一致时 CI 应判失败（§18.2 禁止手写重复类型）。

> **目录清单不得声明不存在的文件（OI-14 的由来）**：上文曾写着 `docs/architecture.md`，而该文件**从未建立**——
> 文档承诺了不存在的东西，读者（与 Agent）据此去找文件时会一无所获，且没有任何门禁会响。
> 故本节只列**已存在**或**已由里程碑明确排期**的路径；新增条目须与实际落地同一提交完成。
> 当前 `docs/` 下只有 `adr/`（占位）与 `backlog.md`；`architecture.md` **明确不建**——架构描述的唯一真理源就是 `AeroForge-Spec.md` 本身，再立一份必然漂移。

---

## 16. 里程碑路线图

| 阶段 | 内容 | 验收标准（垂直切片） |
|---|---|---|
| **M0 地基** | uv + Python 3.12 + 依赖锁定；FastAPI 骨架 + Vite 骨架；打通一次请求 | `uv sync` 后 `uv run pytest`、`npm run build` 双绿；`/api/health` 返回版本 |
| **M0.5 桌面壳探针** ✅ | **打通 exe 交付链**：PyInstaller 冻结后端（含 OCCT / CEA 原生库）→ 后端以 `StaticFiles` 托管 `dist/` + SPA fallback → 空闲端口自选 → pywebview 原生窗口加载该端口 | **在无 Python / 无 Node 的机器上双击 exe 即可启动**：出现原生窗口（无浏览器地址栏）→ 状态栏显示 `就绪` → **3D 视口渲染出验证立方体**（证明 WebView2 的 WebGL2 可用）→ 断网可启动 → 关闭窗口后进程归零（无孤儿）。**实测记录见 §16.2「M0.5 验收记录」** |
| **M1 几何闭环** | build123d 封装旋转体（柱/锥/球冠/椭球/共底）；**母线轮廓编辑器（2D→3D 入口）**；LOD 网格；3D 预览；**缓存机制**；**三栏布局骨架 + 示意网格通道 + 比例尺叠加层 + 光照滑杆**；**增补（OI-19）：3D 分级显隐 + 剖切** ✅ **增补已于 v0.6.1 验收（§16.4 末记录）** | 改一个参数 → 预览更新 < 500 ms；解析体积与内核体积误差 < 0.1%；母线编辑器保存/重载往返一致；**双通道包围盒偏差 ≤ 1%**；**分级显隐与剖切是纯视图状态**（不触发后端请求、不改几何、不可导出；v0.6.1 端到端实测：8 次显隐/选中/剖切交互 **0 次新增 GLB 请求**）。⚠ **增补项并入 M2 验收**：M1 本体已于 v0.5.1 验收通过，增补不回滚既有结论 |
| **M2 参数系统** | Pydantic Schema（含 Tank/Sequence/Recovery 层）；依赖 DAG；约束与错误模型；**单位系统**（显示单位固定一套，§6.4）；**方案诊断规则**（⚠ 按 OI-11 裁剪：**不含 `impact` 字段**）；**增补（OI-21 / OI-24）：`LaunchSite` 层（纬度/海拔/方位）、`Mission` 轨道要素（高度/倾角/偏心率/近地点/远地点）、`Aero` 层（Cd / 参考面积）**；**箱体比例派生（§5.9，OI-18）** | 非法参数被拒并给字段级建议；循环依赖可检出；单位往返恒等测试通过；**诊断输出为六字段**（`{level, code, field_path, message, suggestion}`，`impact` 归 M4，§6.5）；**纬度/海拔/方位与轨道要素可存取且过校验**；**`V_ox/V_fuel` 与 §5.9 的两次公开数据反算一致** |
| **M3 数据层** | ⚠ **前置门禁：先核 GCAT 许可，再抓取**（§7.1 / R-10）；GCAT 快照 + ETL + 单位规范化 + 质量标签 + **三层标签索引** + **发动机族谱 / 材料库** + **`.eng`/`.ork`/`.rse` 导入器** + SQLite + 溯源；**内置示例火箭模板（OI-29 / FR-22：与 §13.2 基准表同源）**；**型号名称匹配提示（OI-34）：命中已知型号 → 提示条 → 载入后全可编辑 + `sourced_fields` 随行** | 可按国家/级数/推进剂/年代检索；产出 `field_report.md`；**许可结论已在抓取前写入 `LICENSE-NOTICE.md`**（核不过则改交付自建数据集，不得带病开工）；导入器对样例文件解析成功率 100%；**模板载入后的关键参数与 §13.2 基准表逐项一致**（含土星五号的三级构型） |
| **M4 计算内核** | CEA 表生成器；**最优 O/F 工作点求解**；效率因子；多级迭代求解器；质量回归；损失模型 L1；**任务时序层与质量事件耦合**；MC 不确定度；**ΔV 瀑布图数据 `delta_v_budget`（OI-23）**；**发射场纬度灵敏度与两阶段重算（OI-25：点值先、区间后）** | **Falcon 9 / 长征五号 / 土星五号 运力误差 < 15%**；属性测试全通过；时序事件后质量与 GLOW 闭合；**`ideal_dv` 与各损失项之和与 `total_dv` 闭合（容差 1e-6 km/s）**；**固定其余参数时纬度 ↓ ⟹ 运力 ↑（FR-19 趋势断言）**；**纬度编辑后点值即时更新、区间随后覆盖，且 `interval_pending` 正确翻转** |
| **M5 完整飞行器** | 整流罩/尾翼/喷管型面/级间段；装配树；**共底贮箱几何**；**曲面光顺与曲率分析**；**回收方案与质量代价**；**2D 工程剖面图**（§5.9 九段分区 / 共底 / 输送管 / 全尺寸标注）；**3D 爆炸视图（OI-19）**；**整箭数据面板 + 轨道运力面板**；STEP/IGES/STL 导出；**PNG 导出（OI-26）** | 导出 STEP 可被 FreeCAD 打开；几何质量与回归质量偏差 < 20%；回收质量代价可解释；**2D 液面高度误差 ≤ 1 mm**；**剖面图与 3D 装配树的分区枚举一致（无漏件、无错序）**；**尺寸标注数值全部来自后端**；爆炸视图为纯视图状态、不改几何 |
| **M6 优化与对比** | 多目标优化；权衡研究；敏感度；损失模型 L2；**轨道层（GEO / Hohmann 转移、运力表）**；**TLI / TMI 与 C3 输出（OI-22 / OI-23）**；**运力—纬度曲线（OI-23）**；**风廓线散布分析（可选）**；报告导出 | 一次优化 < 1 min；**基准误差 < 10%**；各轨道运力表与公开数据对照合理；**C3 与 ΔV 双向一致（给定 ΔV 与 r_p 可反算 C3）**；**`payload_latitude_curve` 对纬度单调不增**；**TMI 结果附带窗口 / 相位假设** |
| **M7 交付** | **安装包与分发**（NSIS 首选 / Inno Setup 备选，含 WebView2 运行时检测与引导安装）；**AI 助手面板（ChatPanel）**——外部 API、**OpenAI 兼容协议**，`base_url` / `api_key` / `model` 由用户在设置中填写（§10.2 / OI-10）；**零样本手录与方案诊断**；文档（含第三方依赖与许可声明）；数据包与代码分离 | **断网环境全功能可用**；安装 → 启动 → 卸载全流程无残留；缓存与 provenance 完整；**AI 未配置或断网时入口自动隐藏并降级为纯本地表单操作**（三条降级触发都要覆盖，§10.2） |

**M0 验收记录（已达成）**——全部为实测输出，非推测：

| 门禁 | 命令 | 实测结果 |
|---|---|---|
| 后端单元测试 | `uv run pytest -q` | `2 passed`（含 `/api/health` 返回版本 + OpenAPI schema 一致性） |
| 后端静态检查 | `uv run ruff check .` | `All checks passed!` |
| 后端类型检查 | `uv run mypy backend` | `Success: no issues found in 16 source files` |
| 环境预检 | `uv run python tools/preflight.py` | 字体 / OCCT / CEA 三项全通过，`exit = 0` |
| 前端类型检查 | `npm run typecheck` | `tsc -b` 无输出（通过） |
| 前端单元测试 | `npm run test` | `Test Files 1 passed (1)` / `Tests 4 passed (4)` |
| 前端构建 | `npm run build` | `✓ built in 277ms`（`dist/` 产出 221.17 kB JS + 2.35 kB CSS） |
| 打通一次请求 | `GET http://localhost:5173/api/health`（经 Vite 代理） | `{"status":"ok","name":"aeroforge","version":"0.1.0"}` |

> **M0 阶段的复核在浏览器中完成**——此时桌面壳尚不存在（M0.5 才打通），故以浏览器访问 Vite dev server 作为验证手段是**该阶段的合法做法**；复核确认：状态栏显示 `就绪` 且携带后端 meta `aeroforge v0.1.0`，`<html class="theme-default-dark">` 生效、背景为深色，无 JS 运行时异常。⚠ 这不代表交付形态是网页：交付形态见 ADR-015，其验证门禁在 **M0.5**。
> 唯一残留的控制台 `net::ERR_ABORTED /api/health` 是 React StrictMode **开发期故意双挂载 effect** 所致：第一个 effect 的请求在 cleanup 中由 `AbortController` 取消，故留下浏览器的失败请求记录（非 JS 异常——若是未处理拒绝，Chrome 会另打一条 `Uncaught (in promise) DOMException`，实测不存在）。[StatusBar.tsx](file:///d:/Vibe%20Coding/aeroforge/frontend/src/statusbar/StatusBar.tsx) 的 `.catch` + `active` 标志已正确处理该分支，第二次挂载正常取得 200。
> 该记录属**开发期产物**：StrictMode 的 effect 双调用仅为开发特性，生产构建不会发生，故不会出现。

**本记录明确未覆盖的项**：

1. **构建产物对真实后端的端到端复核**：按 ADR-015，生产等价形态是 exe（M0.5 验证），`npm run preview` 只是本地静态服务，既不套用 dev 的 `server.proxy`、也不是交付形态，故**不再为其补 `preview.proxy`**；构建产物与真实后端的联调统一由 M0.5 探针覆盖。
2. **冻结产物证据**：M0 阶段桌面壳尚不存在（M0.5 才打通），故上表八项门禁**全部取自源码/浏览器态**，不含任何 exe 实测数据。

> **裁决（v0.3.3）：暂不处理，保持 `AbortController` 现状。** 理由：它是真实卸载时取消挂起请求的唯一手段，M1 作业轮询 / M4 计算任务订阅均依赖该能力；为消一条开发期日志而拆除它属负收益。被否方案：① 拆掉 `AbortController` 仅留 `active` 守卫（失去 HTTP 层取消，且给长连接立错先例）；② 模块级 in-flight 去重（能同时消噪并保留语义，但需改造 `StatusBar.test.tsx` 首个用例的永不 settle mock）——方案②在 M1「多组件消费同一接口」时收益转正，届时一并评估。

**顺序铁律**：

1. **M3 必须早于 M4**（质量回归依赖 GCAT 数据）
2. **缓存机制必须在 M1 落地**，不得推迟（否则 M4–M6 的迭代成本失控）
3. **M2 的约束系统必须早于 M4**（否则非法参数会污染计算结果）
4. **母线编辑器（M1）必须先于任何 2D→3D 导入器**（形态一致性的前提）
5. **时序层（M4）与轨道层（M6）晚于质量迭代**，二者都消费质量结果，不得反向定义质量
6. **M0.5 桌面壳探针必须早于 M1**（交付形态未验证前不得扩张 UI 面积，见 R-32）
7. **GCAT 许可核实必须早于 M3 的任何抓取动作**（§7.1 / R-10；核不过即改自建数据集，不得"先用着"）
8. **持续集成必须早于 M3**（§13.9 / OI-12）——M3 起引入外部数据与黄金基准，
   此后"改一处、动全链"的回归成本陡升；在此之前把机器拦网铺好，代价最低

**第二轮裁决（OI-17～OI-29）在里程碑上的落点**（汇总，便于核对无遗漏）：

| 裁决 | 落点 | 说明 |
|---|---|---|
| OI-17 布局维持 | 无需里程碑动作 | §11.5 现状即满足 |
| OI-18 工程剖面图 + 箱体比例派生 | **M2**（Schema / 派生）+ **M5**（视图） | 箱体比例派生是 `SPEC_VERSION` 递增的唯一依据 |
| OI-19 分级显隐 + 剖切 | **M1 增补**（并入 M2 验收）✅ **已于 v0.6.1 验收**（§16.4 末记录） | 不回滚 M1 既有验收结论 |
| OI-19 爆炸视图 | **M5** | — |
| OI-20 单位维持 | 无需里程碑动作 | 与 §6.4 现状一致 |
| OI-21 发射场 / 轨道字段 | **M2** | Schema 层 |
| OI-22 TLI / TMI | **M6** | 与轨道层同批 |
| OI-23 C3 | **M6** | 与 TLI/TMI 同批必输出 |
| OI-23 ΔV 瀑布图 | **M4** | 依赖损失模型 L1 |
| OI-23 运力—纬度曲线 | **M6** | 依赖轨道层 |
| OI-24 气动参数 | **M2** | Schema 层 |
| OI-25 两阶段重算 | **M4** | 依赖 MC 采样框架 |
| OI-26 PNG 导出 | **M5** | 与其余导出同批 |
| OI-27/OI-28 维持自研 / ECharts | 无需里程碑动作 | 确认性裁决 |
| OI-29 土星五号 + 内置模板 | **M3**（模板数据）+ **M4/M6**（基准回归） | 基准与模板同源 |

> **「产品可用」门槛（OI-13）**：上表 M0–M7 全是**工程验收**（能不能算、算得对不对、交付链通不通），
> 没有一条回答"到哪一步它才是**能用来设计火箭**的东西"。故补一条产品门槛：
>
> **M4 完成之后**，平台才首次具备"可用"的资格——判据是三项同时成立：
> ① 给出**第一个可信的运力数字**（基准火箭误差 < 15%，§13.2）；
> ② 给出**最优 O/F 工作点**（§8.2）；
> ③ 走通**多级质量迭代**（§8.5）。
>
> **M0–M3 一律定位为地基与数据准备**，此间不得对外宣称"可用"，也不得据此召集试用——按 §1.1 的同一条裁决。

### 16.1 补充手册开发阶段与本节里程碑的映射

补充手册给出了 6 个实现阶段（含 Prompt 建议）。其**阶段划分是实现顺序建议**，本节的 M0–M7 是**验收门禁**；两者冲突时以验收标准为准。

| 手册阶段 | 手册内容 | 映射到 | 差异说明 |
|---|---|---|---|
| 阶段 1 | 项目初始化与状态管理（`RocketConfig`） | M0 + M2 | 手册的单一 `RocketConfig` 在本规格展开为 §6.1 的 `Vehicle/Stage/Geometry/Tank` 分层 Schema；**参数体系必须先过 M2 的约束系统** |
| 阶段 2 | UI 布局与参数面板 | M1 | 三栏布局、三模式输入控件、状态栏骨架（§11.5 / §11.11） |
| 阶段 3 | 计算引擎与数据面板 | M4 | ⚠ **"在状态层实现计算逻辑"被 ADR-011 否决**：计算落在后端 worker，前端只订阅 `store/computed` |
| 阶段 4 | 2D 剖面图绘制 | M5 | §11.10；依赖 M4 产出的 `liquid_level_m`，故不得早于 M4 |
| 阶段 5 | 3D 渲染与交互 | M1 | 示意通道可在 M1 先行（ADR-012）；权威 GLB 通道随 §5 交付 |
| 阶段 6 | 细节打磨与导出 | M5 → M7 | 比例尺、防抖、导出（§11.12）；资源释放与双通道一致性纳入 CI（§13.6） |

**顺序提醒**：手册的阶段顺序基本可用，唯一需要调整的是**阶段 4 不得与阶段 3 并行**——2D 液面依赖后端派生量，前端提前实现只能靠猜物理，直接违反 ADR-011。

### 16.2 M0.5 桌面壳探针（交付链路验证）

**目的**：在 UI 面积扩张之前，把「冻结 → 启动 → 原生窗口 → 后端可用 → 3D 可用」这条链一次性打通。它**不是**产品功能里程碑，产物是**内部验证用的可执行目录**，不是发布包（安装包属 M7）。

**技术契约**：

| 项 | 约定 | 理由 |
|---|---|---|
| 冻结方式 | PyInstaller **`onedir`**（**不用 `onefile`**） | `onefile` 每次启动把 OCCT 的数百 MB DLL 解压到临时目录，启动慢且极易被杀毒软件拦截；`onedir` 启动快、可增量排查 |
| 必收资源 | `OCP` 包整体（`OCP.cp312-win_amd64.pyd` + **316 个纯 Python 子包**）、**`site-packages/cadquery_ocp_novtk.libs/` 的 70 个 DLL（61.9 MB）**、build123d 数据文件、**`lib3mf/lib3mf.dll`**、`cea/lib/libcea.cp312-win_amd64.pyd` 与**同目录的 `cea_bindc.dll`**、`cea/data/{thermo,trans}.lib` 与 `cea/share/cea/{thermo,trans}.lib` | 这些是**运行时按路径加载**的二进制与数据，PyInstaller 的静态分析看不见，漏收即在冻结后报 `ModuleNotFoundError` 或 DLL 加载失败。⚠ **实测口径（v0.4.1 更正）**：OCCT **主体**静态链接进 `OCP.pyd`（89.4 MB，包目录内无 DLL），但该 `.pyd` 的导入表**直接引用 5 个改名 `TK*.dll`**（`TKDESTEP`/`TKFeat`/`TKMeshVS`/`TKOffset`/`TKOpenGl`），并由 delvewheel 改名的 **70 个 DLL** 提供其余依赖——`OCP/__init__.py` 用 `os.add_dll_directory()` 挂载它们。故**「动态库收集不全」这一头号坑位确实成立**，不因"静态链接"而豁免。**`lib3mf` 是同类第二例**：它由 `build123d/mesher.py` 拉入导入图，`__init__.py` 自行拼路径且缺文件即抛 `ImportError`，而该 DLL 不在任何已收集二进制的 PE 导入表内、hooks-contrib 亦无 hook——**实测冻结后 `import build123d` 正崩在此处，由 `--preflight` 拦下**。故判据只能是冻结产物真跑 `--preflight`，**构建成功不等于收集完整** |
| 预检保留 | 冻结产物必须保留预检入口 `AeroForge.exe --preflight`，与 `tools/preflight.py` **共用同一实现** `aeroforge.selfcheck` | 三件事同一处落点：① R-30 要求**部署环境**也能自检（该崩溃并非开发机独有）；② 该模块对 `build123d` / `cea` 的真实 `import` 使必收资源**进入导入图**，`hook-OCP` / `hook-cea` 才会被 PyInstaller 触发；③ 它同时是「原生库是否真的收全」的唯一可信判据。⚠ 仅把收集逻辑写成 hook、却无任何导入路径回到该包，hook 不会执行（实测踩过：产物缺 OCP / cea，仅 88.4 MB） |
| 资源路径 | 统一走一个 `resource_path()` 解析函数（兼容源码运行与 `sys._MEIPASS`） | 冻结后 `__file__` 语义变化，散落的相对路径必然失效 |
| 安装路径编码 | 启动时判定资源根是否**纯 ASCII**，不通过即返回码 **5** 并给出换目录指引（**先于 `--preflight`**） | NASA `cea` 的 C 扩展用窄字符 API 打开数据表，非 ASCII 路径**必定失败**且报错误导为"文件找不到"（R-34 / 坑位 4）；前置判定是为了把它变成一句可执行的修复指引，而不是 C 层栈回溯 |
| 端口 | 运行时 `bind(('127.0.0.1', 0))` 取空闲端口，再传给 pywebview | 写死 8000 会与用户机器上其他服务冲突（ADR-015） |
| 静态托管 | FastAPI `StaticFiles` 挂 `dist/` + 未匹配路径回落 `index.html` | 前端 SPA 刷新/深链依赖 fallback |
| 窗口 | pywebview，Windows 侧 EdgeChromium 后端 | 原生窗口，无地址栏（用户核心诉求） |
| 3D 验证 | 一个**最小 R3F 场景**（旋转立方体），不接入 M1 正式功能 | 唯一目的是证明 WebView2 的 **WebGL2** 可用；否则 M1 的几何预览方案要整体重做 |
| 退出 | 窗口关闭 → 后端 shutdown，进程归零 | 避免孤儿进程占住端口导致下次启动失败 |

**必须实测记录的量**：冷启动到窗口可见的秒数、到状态栏 `就绪` 的秒数、到立方体渲染完成的秒数。这三个数决定用户体验基线，也决定是否需要做启动画面。

**已知需在探针中排掉的坑**：

1. **OCCT 动态库收集不全** —— 最高概率的失败点，故列为探针第一目标
2. **杀毒软件误报未签名 exe** —— 记录现象即可，签名不在本项目范围
3. **WebView2 运行时缺失** —— 探针须能明确报错并给出安装指引，不得静默白屏
4. **中文路径** —— ⚠ **实测成立且为致命**：NASA `cea` 的 C 扩展用窄字符 API 打开数据表，**安装路径含非 ASCII 字符时必定失败**，报错却是*"Could not locate thermo database file"*（文件其实存在，误导性极强）。**与空格无关**：`d:\Vibe Coding\aeroforge`（含空格、纯 ASCII）全程正常。规避见下方「坑位 4」结论。

**退出码约定**（冻结产物，探针与安装器共用）：

| 码 | 含义 |
|---|---|
| 0 | 正常 |
| 2 | 缺 Microsoft Edge WebView2 运行时 |
| 3 | 缺前端构建产物 `dist/`（或 `index.html` 不全） |
| 4 | 后端 30 s 内未就绪 |
| 5 | **安装路径含非 ASCII 字符**（CEA 原生层无法加载数据表，坑位 4） |

**退出准则**：§16 M0.5 行所列全部验收项通过，且上述四项坑逐一有结论（通过或已记录规避方案）。

**交付链复验的固定步骤（持续验证约定，OI-15 起生效）**：M0.5 之后每一次冻结产物复验都按同一组步骤执行，缺一步即视为未完成：

| # | 步骤 | 判据 |
|---|---|---|
| 1 | 重冻：`… aeroforge.spec --distpath dist-desktop --workpath .tools/pyinstaller-build --noconfirm` | 产物落在 `dist-desktop/`（**不得**落在默认 `dist/`，§16.2 已发生过的漂移） |
| 2 | 核对体积、文件数与**包内前端哈希** | 体积在 §13.5.1 预算内；包内 JS 与本次 `npm run build` 同名同尺寸**且逐字节一致**（"交付物真的换新了"的唯一判据） |
| 3 | 冻结态 `--preflight` | 三项全通过、`exit = 0` |
| 4 | 冻结态 `--probe` | 通过：`WebGL2` 可用、后端就绪 / DOM 就绪 / 首帧三基线记入 §13.5.1 |
| 5 | 退出零残留 | 比对探针前后的 `msedgewebview2` 子进程**逐条启动时间**（只比数量会把别家应用的 WebView2 误判为孤儿进程），且 `AeroForge` 无进程 |
| 6 | **断网复跑（OI-15）** | **断开网络**后跑 `--preflight` / `--probe`，并完整走一遍「改参 → 预览 → 构建」（§13.7 步骤 7）。**须在 AI 助手未配置的状态下执行**（§10.2） |
| 7 | 全量门禁复跑 | 后端 `pytest` / `ruff check` / `ruff format --check` / `mypy`；前端 `typecheck` / `test` / `build` |

> **第 6 步的由来（OI-15）**：CON-01 离线红线是全篇**第一条**红线，此前却只挂在 M7 验收上——按原安排它要到项目末期才第一次被验证。
> 而"断网可用"恰恰是**最容易被无声破坏**的一类属性（新增一个 CDN 字体、一条遥测、一次在线字体/图标拉取即可破坏它），
> 且**联网状态下全部门禁都会绿**。故把它从"末期一次性验证"改为"每次复验固定一步"。

**明确不在本探针范围**：安装包（M7）、代码签名、自动更新、原生菜单与文件对话框（随 M5 导出功能一并落地）。

#### M0.5 验收记录（已达成）

全部为**冻结产物实测输出**，非推测。冻结命令：`uv run pyinstaller desktop/packaging/aeroforge.spec --distpath dist-desktop --workpath .tools/pyinstaller-build --noconfirm`。

| 门禁 | 命令 | 实测结果 |
|---|---|---|
| 产物体积 | `dist-desktop\AeroForge\`（onedir） | **451.55 MB**（`AeroForge.exe` 38.2 MB + `_internal` 413.4 MB）；此前漏收原生库时仅 88.4 MB |
| 后端静态检查 | `uv run ruff check .` | `All checks passed!` |
| 后端类型检查 | `uv run mypy` | `Success: no issues found in 15 source files` |
| 冻结产物预检 | `AeroForge.exe --preflight` | 字体 382 个 / OCCT `107.521009 vs 107.521009`（相对误差 `1.322e-16`）/ CEA `Tc = 3629.2 K`、`Isp_vac = 372.4 s`；`exit = 0` |
| 冻结产物探针 | `AeroForge.exe --probe` | 见下表 |
| 退出后残留 | `Get-Process AeroForge` | 零残留（无孤儿进程） |

**冻结产物 `--probe` 启动耗时基线**：

| 量（即本节要求实测的三个数） | 第 1 次运行 | 第 2 次运行 |
|---|---|---|
| 后端就绪（`/api/health` 200） | 0.60 s | 0.58 s |
| 冷启动 → 窗口可见（DOM 就绪） | 3.86 s | 3.97 s |
| 窗口可见 → 首帧渲染完成（增量） | 0.31 s | 0.30 s |
| **合计到 3D 立方体可见** | **4.17 s** | **4.27 s** |

> 两次运行差异 ≤ 0.1 s，可视为稳定；冷启动的 3.9 s 几乎全部消耗在 WebView2 运行时初始化，与后端无关（后端仅 0.6 s）。按本节"决定是否需要做启动画面"的要求：**4.2 s 需在 M7 评估启动画面**，但它不是 M0.5 的阻断项。

**冻结产物探针环境明细**：WebView2 运行时 `153.0.4234.32`；`WebGL2` **可用**；渲染器 `ANGLE (NVIDIA, NVIDIA GeForce RTX 5070 Laptop GPU (0x00002D58) Direct3D11 vs_5_0 ps_5_0, D3D11)`；监听 `http://127.0.0.1:60010/`（空闲端口自选生效）；探针结论 **通过**。

**必收资源核对清单（逐一确认存在于冻结产物）**：

| 资源 | 位置 | 结果 |
|---|---|---|
| `OCP.cp312-win_amd64.pyd` | `_internal/OCP/` | ✅ 89.4 MB |
| OCCT 改名 DLL | `_internal/cadquery_ocp_novtk.libs/` | ✅ 46 个 `TK*.dll`（⚠ **属下一行 70 个 DLL 之内的子集，非并列**；M2 首项复验更正，原先记为 `_internal/` 顶层） |
| delvewheel 库目录 | `_internal/cadquery_ocp_novtk.libs/` | ✅ 70 个 DLL（61.87 MB，含上表 46 个 `TK*.dll`；无子目录） |
| `libcea.cp312-win_amd64.pyd` | `_internal/cea/lib/` | ✅ 0.6 MB |
| `cea_bindc.dll` | `_internal/cea/lib/` | ✅ 2.4 MB |
| `thermo.lib` / `trans.lib` | `_internal/cea/data/` 与 `_internal/cea/share/cea/` | ✅ 612,751 B / 35,108 B（两处齐全） |
| `lib3mf.dll` | `_internal/lib3mf/` | ✅（**首次冻结时缺失，由 `hook-lib3mf.py` 修复**） |
| 前端构建产物 | `_internal/frontend/dist/index.html` | ✅ |

**四个坑位的结论**：

| 坑位 | 结论 | 依据 |
|---|---|---|
| 1. OCCT 动态库收集不全 | ✅ **已解决** | 46 个 `TK*.dll` + 70 个 `cadquery_ocp_novtk.libs` DLL 均进包，冻结态 `--preflight` 真实跑通几何自检。根本机制是 **PyInstaller 只为「导入图内出现过」的包执行 hook**——原先 `launcher.py` 只导入 FastAPI，故 `hook-OCP` / `hook-cea` 从未触发，且**构建不报错**。修复方式是把 `aeroforge.selfcheck`（内含 `build123d` / `cea` 的真实 `import`）接入 `launcher.py` 模块级导入。**此类失效只能靠冻结产物跑预检发现，静态检查与构建日志都看不见** |
| 2. 杀毒软件误报未签名 exe | ⚠ **未复现，但风险已记录** | 本机**同时装了 4 款安全产品**（金山毒霸铠甲防御 / Windows Defender / 火绒 / McAfee），产物 `NotSigned`，而 `--preflight`、`--probe` 全程**未被拦截**；`Get-MpThreatDetection` 无 `AeroForge` 记录。判断：`onedir` 不自解压到临时目录（`onefile` 的典型恶意软件行为模式），误报率显著更低——这与选择 `onedir` 的理由互为印证。⚠ **须与本机另一现象区分**：运行期会看到 `TRAE Sandbox Error: hit restricted` 拦截 `msedgewebview2_*.log`、NVIDIA `DXCache` 等路径写入——那是**开发工具自身的沙箱**，**不是安全软件**，与本产物分发无关。**M7 待办**：代码签名 + 向各厂商提交误报申诉 |
| 3. WebView2 运行时缺失 | ✅ **已解决** | 删除系统组件风险过高，改用 **monkeypatch 注入**验证报错路径：`webview2_version → None` 时返回码 **2** 并打印 Evergreen 运行时安装指引（含检测的注册表 GUID），不静默白屏。同一手法验证 `frontend_dist()` 指向不存在目录时返回码 **3** 并提示先 `npm run build` |
| 4. 中文路径 | ⚠ **坑位成立，已给出规避（fail-fast）** | 见下 |

**坑位 4 详述（本轮新发现，属交付链硬约束）**：

将冻结产物整目录拷到 `D:\测试 目录\AeroForge 中文路径\` 后：

- `AeroForge.exe --preflight` **崩溃**（`exit = 1`），字体与 OCCT 两项通过，**CEA 项失败**：
  `ValueError: Invalid path; thermo.lib not found.` + `ERROR: Could not locate thermo database file: …\_internal\cea\data\thermo.lib`
- **排除漏收**：该文件确实存在（612,751 B，与源一致）→ 是**路径编码**问题，不是收集问题。
- **定位到根因**：`cea/__init__.py` 无参调用 `libcea_init()`，C 扩展自行拼路径后用**窄字符 API** 打开；路径含非 ASCII 时必然失败。
- **受控对照实验**（同一份数据表，仅改变目录名）：

  | 数据目录 | `libcea.init(path=…)` 结果 |
  |---|---|
  | `C:\ProgramData\AeroForge\cea-data-probe`（纯 ASCII） | ✅ 成功 |
  | `D:\测试 目录\cea-data-probe`（含中文） | ❌ `Invalid path; thermo.lib not found.` |

- **`GetShortPathName`（8.3 短名）规避不可行**：本机 NTFS 已禁用 8.3 名称生成，`ShortPath` 原样返回中文长名。
- **空格不是问题**：`d:\Vibe Coding\aeroforge` 含空格、纯 ASCII，源码态与冻结态均全程正常。

**已采纳的规避（本轮落地，已在冻结产物上验证）**：启动器在**一切之前**（含 `--preflight`）判定资源根是否纯 ASCII，不通过则 `return 5` 并给出可执行的修复指引（换成 `C:\Program Files\AeroForge` 等），把一句误导性的"C 层说文件找不到"替换为明确原因。实现见 [resource_path.py](file:///d:/Vibe%20Coding/aeroforge/desktop/resource_path.py) 的 `ascii_root()`。

> **冻结产物实测（fix 后重冻）**：在 `D:\测试 目录\AeroForge 中文路径\` 下 `AeroForge.exe --preflight` → `exit = 5`，输出"安装路径含非 ASCII 字符，CEA 热力学数据库无法加载 / 当前路径：…\_internal / 请将 AeroForge 安装或解压到纯英文路径"；同一产物在 `d:\Vibe Coding\aeroforge\dist-desktop\`（纯 ASCII）下 `--preflight` `exit = 0`、`--probe` 通过。**门禁只在前置判定处生效，未干扰正常路径。**

**已实测可行但未采纳的升级路径**（留给 M7 决策）：`CEA_SKIP_INIT=1` + 显式 `libcea.init(path=<纯 ASCII 数据目录>)` 可**绕过该限制**——上表 ASCII 一组即为此路径的验证。之所以暂不采纳：它要求把 CEA 数据表复制到第二个位置（如 `C:\ProgramData\AeroForge\`）并管理其生命周期，属为"用户可能装到中文目录"引入的状态复杂度；在 M7 安装器能**默认给出 ASCII 安装路径**的前提下，fail-fast 更简单且不产生副本。**若 M7 确认必须支持中文用户名下的 `%LOCALAPPDATA%` 安装，则改用此路径**（⚠ 注意 `%LOCALAPPDATA%` = `C:\Users\<用户名>\…`，**用户名含中文时同样踩坑**，故安装器默认路径不应选它）。

**本记录明确未覆盖的项**：

1. **安装包与卸载**（M7）：本产出是内部验证用的可执行目录，**不是发布包**；安装路径选择、卸载残留、WebView2 引导安装均未验证。
2. **代码签名与误报申诉**：产物为 `NotSigned`，"本机未复现拦截"**不等于**他人机器不会误报——该边界见坑位 2，处置排在 M7。
3. **原生菜单与文件对话框**：随 M5 导出功能落地，探针不含。
4. **异构硬件**：`WebGL2` 可用性只在本机（NVIDIA RTX 5070 Laptop + ANGLE D3D11）确认；**无独显 / 老驱动 / 远程桌面**场景未演练，R-33 的后备路径（服务端渲染降级）仍是纸面方案。
5. **首帧无预算约束**：M0.5 只记录 4.17–4.27 s，当时**尚无** §13.5.1 的性能预算门禁；该门禁在 M1 收尾时补立（上限 7.0 s）。

#### M1 交付链复验（已达成）

按本节「交付链路必须持续可验证」的约定，M1 完成后**重冻产物并复跑探针**——冻结命令同 M0.5，产物仍在 `dist-desktop/`（不入库）。

| 项 | M0.5 基线 | M1 复验 |
|---|---|---|
| 产物体积（onedir） | 451.55 MB | 451.72 MB（7758 个文件） |
| 冻结态 `--preflight` | 三项全通过，`exit = 0` | 三项全通过（字体 382 个 / OCCT `107.521009 vs 107.521009`、相对误差 `1.322e-16` / CEA `Tc = 3629.2 K`、`Isp_vac = 372.4 s`），`exit = 0` |
| 冻结态 `--probe` | 通过 | **通过**：WebView2 `153.0.4234.32`；后端就绪 **0.62 s**；窗口可见（DOM 就绪）**4.14 s**；首帧渲染完成 **4.74 s**；`WebGL2` **可用**；渲染器 ANGLE / NVIDIA RTX 5070 Laptop；空闲端口自选生效（`http://127.0.0.1:60210/`） |
| 包内前端确为 M1 产物 | — | ✅ 已核对：`_internal/frontend/dist/assets/index-CSatOwYl.js` 为 1,173,223 B，与 `npm run build` 输出同名同尺寸。旁证：探针由**真实 `Viewport` 在首帧**发布（`ProbeScene.tsx` 已删除），非 M0.5 的临时立方体场景 |
| 退出后残留 | 零残留 | 零残留（`Get-Process AeroForge` 无输出） |

> 两点观察：① 到首帧 **4.74 s**，比 M0.5 的 4.17–4.27 s 慢约 0.5 s，方向与前端包由 221 kB 增至 1173 kB 一致——「M7 前评估启动画面」的结论不变，但 M7 前也应先评估 **code-splitting**（当前单 chunk 已触发 Vite 的 500 kB 提示）。② 本次复现了 M0.5 已记录的同一条**开发工具沙箱**日志（`TRAE Sandbox Error: hit restricted`，被拦的是 `msedgewebview2_*.log`、NVIDIA `DXCache` 等运行期缓存路径）：**它是开发工具自身的沙箱，不是安全软件，也与本产物分发无关**，不构成缺陷。

**本记录明确未覆盖的项**：

1. **仍未脱离本机环境**：与 M0.5 同一台开发机、同一 WebView2 版本，故坑位 2（杀软误报）与坑位 4（中文路径）的边界不变；本复验只回答"冻结链是否仍可用"，**不构成二者的复现或反证**。
2. **未覆盖中文路径复跑**：本次未在非 ASCII 路径下重跑，坑位 4 的结论仍取自 M0.5 的受控对照实验。
3. **无外设/多显示器场景**：窗口在非默认 DPI、多显示器热插拔下的行为未验。
4. **首帧 4.74 s 只对"可用"负责**：§13.5.1 的门禁为 ≤ 7.0 s（不阻断），"好用"目标（启动画面 / code-splitting）排在 M7 前评估。

#### M2 交付链复验（已达成）

M2 首项修复了权威通道被多转 -90° 的缺陷（§16.3 结案记录），但**修复只落在源码与前端构建产物上**：
`dist-desktop/` 里仍是修复**前**冻结的那份（包内 JS 为 M1 的 `index-CSatOwYl.js`），
即**交付给用户的 exe 仍带着"模型躺倒"这个已经修好的缺陷**。故按本节「交付链路必须持续可验证」
的约定重冻并复跑探针，冻结命令同 M0.5。

| 项 | M1 复验 | M2 复验 |
|---|---|---|
| 产物体积（onedir） | 451.72 MB（7758 个文件） | 451.72 MB（7758 个文件；`AeroForge.exe` 38.23 MB + `_internal` 413.50 MB） |
| 冻结态 `--preflight` | 三项全通过，`exit = 0` | 三项全通过（字体 **382** 个 / OCCT `107.521009 vs 107.521009`、相对误差 `1.322e-16` / CEA LOX/CH4 O/F=3.4 Pc=100.0 bar ε=40 → `Tc = 3629.2 K`、`Isp_vac = 372.4 s`），`exit = 0` |
| 冻结态 `--probe` | 通过；首帧 4.74 s | **通过**：WebView2 `153.0.4234.32`；后端就绪 **0.58 s**；窗口可见（DOM 就绪）**3.91 s**；首帧渲染完成 **4.55 s**；`WebGL2` **可用**；渲染器 ANGLE / NVIDIA RTX 5070 Laptop；空闲端口自选生效（`http://127.0.0.1:53346/`） |
| 包内前端确为**本次**产物 | `index-CSatOwYl.js` 1,173,223 B | ✅ **已换新**：`_internal/frontend/dist/assets/index-DdhGJiHF.js` **1,173,228 B**，与本次 `npm run build` 输出的 `1,173.22 kB` 同名同尺寸（CSS 同为 `index-BkdnKLOw.css` 9,367 B）。**这是"修复确实进了交付物"的唯一判据**：文件名与尺寸都比对得上，且与上一版**不同** |
| 退出后残留 | 零残留 | 零残留，且**判据已加强**（见下） |

**必收资源逐项复核**（位置与口径均按本次实测）：`OCP.cp312-win_amd64.pyd` 89.4 MB；
`cadquery_ocp_novtk.libs/` **70 个 DLL、61.87 MB**（该目录无子目录）；`libcea.cp312-win_amd64.pyd`
0.6 MB；`cea_bindc.dll` 2.4 MB；`lib3mf.dll` 2.4 MB；`thermo.lib` 612,751 B 与 `trans.lib` 35,108 B
在 `cea/data/` 与 `cea/share/cea/` **两处齐全**；包内前端见上表。

> **顺带更正 M0.5 审计表的一处位置口径错误**（本次逐项实测发现）：原表把「46 个 `TK*.dll`」记在
> `_internal/` 顶层、与「70 个 DLL」**并列成两行**，读起来像 116 个互不相交的 DLL。实测：
> `_internal/` **顶层 0 个** `TK*.dll`；46 个 `TK*.dll` **全部位于 `_internal/cadquery_ocp_novtk.libs/`**，
> **是那 70 个 DLL 之内的子集**；全产物 `*.dll` 共 **252** 个。**结论不变**（原生库确实收全，由冻结态
> 预检独立证明），但"并列相加"的读法会让人误判收集量——审计表既是证据，就不能有歧义位置。

**退出残留判据加强（本次新增）**：M0.5 / M1 只查 `Get-Process AeroForge`。本次同时查 `msedgewebview2`
子进程：现存 6 个的启动时间为 `9:31:06–9:31:09`，早于本次探针约 **2.8 小时**，属其他常驻应用，
**不是本产物遗留**；探针自身未新增任何 WebView2 子进程。⚠ 该判据必须带「启动时间」比对才有意义——
只看数量会把别家应用的 WebView2 误判成自己的孤儿进程（反之，若未来出现与本产物同期的子进程，即为真残留）。

**两条人工/产物实测门禁的刷新**（§13.5.1）：

| 预算项 | 上限 | M1 收尾 | M2 复验 |
|---|---|---|---|
| 冷启动 → 首帧渲染完成（冻结产物 `--probe`） | 7.0 s | 4.74 s | **4.55 s**（−0.19 s） |
| 改参数 → 预览更新（含 200 ms 防抖） | 500 ms | 237 ms | **253.6 ms**（5 次：256.0 / 270.2 / 258.5 / 237.7 / 245.5；最大 270.2 ms） |

> **预览重测的方法与三个坑位**（供下次复用；本次数字是在**开发态**测的，非冻结产物）：
> ① **被测量的信号必须经后端往返**：取母线编辑器校验明细中「总长」那一条
> （`.meridian-editor__check-detail`，形如 `总长 5.010000000 m = 各段长度之和 5.010000000 m`，
> 精度 1e-9 m，来自 `POST /api/geometry/validate` 的意图断言）。同一次编辑中另有 4 个信号
> （`.meridian-editor__hint` 的包络与解析体积、`.scale-overlay__size`、母线 SVG `path` 的 `d`、
> 闭合性明细）**在同一时刻**变化，说明是一次统一重渲染。**若改用表单本地即时渲染作信号，会绕过
> 防抖与后端往返，数字显著偏小**——这类信号不可用于本门禁。
> ② **延迟构成已被拆开**：每次编辑都出现一条 `/api/geometry/validate`，其 `startTime` 落在输入事件后
> **214–239 ms**（≈200 ms 防抖 + 调度），`responseEnd` 223–249 ms，DOM 更新 237.7–270.2 ms ⇒
> ≈ **防抖 200 ms + 后端 7.3–10.1 ms + 渲染 15–25 ms**。与 M1 结论一致：压延迟应调防抖窗口。
> ③ ⚠ **窗口被遮挡/后台会让数字整体失真**：首次测量时 Chrome 把定时器对齐到约 1 s 网格
> （`setTimeout(10)` 实测 **507–996 ms**，触发相位恒为 `performance.now() % 1000 ≈ 920`），应用的
> 200 ms 防抖被放大到 ~900 ms，端到端测成 **888.9 ms**——**该数字无效、不可用于门禁**。
> 此时 `document.visibilityState` 仍为 `visible`，**故不能靠它判断是否被节流**；须先实测
> `setTimeout(10)` 的耗时（前台窗口健康值约 11.6 ms）确认环境未节流，再开始采数。

**本记录明确未覆盖的项**：

1. **仍未脱离本机环境**：与 M0.5 / M1 同一台开发机、同一 WebView2 版本，故坑位 2（杀软误报）与
   坑位 4（中文路径）的边界不变；本次**未在非 ASCII 路径下重跑**（R-34 的结论仍取自 M0.5 的受控对照
   实验），本复验只回答"冻结链是否仍可用"。
2. **`--probe` 只对"可用"负责**：`WebGL2` 可用性仍只在本机（NVIDIA RTX 5070 Laptop + ANGLE D3D11）
   确认；**无独显 / 老驱动 / 远程桌面**场景未演练，R-33 的服务端渲染后备路径仍是纸面方案。
3. **端到端预览只在开发态测得**：Vite dev + uvicorn 源码进程，**未在冻结产物内测**。冻结态走
   FastAPI 静态托管、无 Vite 代理，链路不同（理论上更短），但**未验证**。
4. **「构建几何」首次卡在"排队中（0%）"的过程异常仍未定位**（§16.3 结案记录留白第 2 项）；本次复验
   全程未点「构建几何」，故既未复现也未反证。
   ~~**→ 已定位并修复（本项留白据此结案）**：根因为 Vite 代理缺 `/ws` + 前端无握手超时，见
   §16.3「作业进度通道缺陷修复」与本节「M2 交付链复验 · 作业进度通道修复后」。~~
5. **构建日志中的两项 PE 写入重试未定位**：本次冻结日志出现
   `set_exe_build_timestamp` 的 `OSError(22)` / `PermissionError(13)` 与 `update_exe_pe_checksum` 的
   `OSError(22)` 重试（均在第 3 次内成功，随后 `Building EXE from EXE-00.toc completed successfully`）。
   现象与既有记录的**开发工具沙箱**拦截同类，但本次**未定位根因**，也**未验证** PE 校验和/时间戳
   最终是否确实写入正确——`--preflight` / `--probe` 都不读这两项。
6. **预览数字含人工前置条件**：测量前需由人工把浏览器窗口切到前台（后台节流无法程序化解除），故该
   数字 **CI 不可复现**；这既是它被登记为"人工实测"的原因，也是它的局限。

#### M2 交付链复验 · 作业进度通道修复后（已达成）

本轮修复（§16.3「作业进度通道缺陷修复」）改的是**生产态也会执行的代码**——`openJobStream` 的握手
超时兜底不是开发态补丁；而 `dist-desktop/` 里仍是修复**前**冻结的那份（包内 JS 为 `index-DdhGJiHF.js`），
即**交付给用户的 exe 仍会在握手悬置时永久冻结在"排队中（0%）"**。故按本节「交付链路必须持续可验证」
的约定重冻并复跑探针，冻结命令见附录 D。

| 项 | M2 复验 | 本次复验 |
|---|---|---|
| 产物体积（onedir） | 451.72 MB（7758 个文件） | 451.73 MB（7758 个文件；`AeroForge.exe` 38.23 MB + `_internal` 413.50 MB）。字节数 **473,668,072 B**，与上版实为同一量级 |
| 冻结态 `--preflight` | 三项全通过，`exit = 0` | 三项全通过（字体 **382** 个 / OCCT `107.521009 vs 107.521009`、相对误差 `1.322e-16` / CEA LOX/CH4 O/F=3.4 Pc=100.0 bar ε=40 → `Tc = 3629.2 K`、`Isp_vac = 372.4 s`），`exit = 0` |
| 冻结态 `--probe` | 通过；首帧 4.55 s | **通过**：WebView2 `153.0.4234.32`；后端就绪 **0.58 s**；窗口可见（DOM 就绪）**3.91 s**；首帧渲染完成 **4.51 s**；`WebGL2` **可用**；渲染器 ANGLE / NVIDIA RTX 5070 Laptop；空闲端口自选生效（`http://127.0.0.1:51332/`） |
| 包内前端确为**本次**产物 | `index-DdhGJiHF.js` 1,173,228 B | ✅ **已换新**：`_internal/frontend/dist/assets/index-Dyj8e-pe.js` **1,173,727 B**（CSS 同为 `index-BkdnKLOw.css` 9,367 B），与本次 `npm run build` 日志的 `1,173.72 kB`（十进制口径，1173727/1000 = 1173.727）一致；且冻结后重新执行 `npm run build` 复现了同名同尺寸产物，两者 **SHA256 逐字节相同**（`8BFF6696…C60E1BB`）——判据由"同名同尺寸"升级为"同名同尺寸**且逐字节可复现**" |
| 退出后残留 | 零残留，判据已加强 | 零残留（判据见下） |

**必收资源逐项复核**（本次把**实际位置**一并记下，此前记录只给了体积）：`OCP.cp312-win_amd64.pyd`
89.41 MB（`_internal/OCP/`）；`cadquery_ocp_novtk.libs/` **70 个 DLL、61.87 MB**；`libcea.cp312-win_amd64.pyd`
0.64 MB 与 `cea_bindc.dll` 2.42 MB（均在 `_internal/cea/lib/`）；`lib3mf.dll` 2.41 MB（`_internal/lib3mf/`）；
`thermo.lib` 612,751 B 与 `trans.lib` 35,108 B 在 `cea/data/` 与 `cea/share/cea/` **两处齐全**；
`_internal/` 顶层 `TK*.dll` **0 个**、全产物 `*.dll` **252** 个（与 M2 复验的更正结论一致）。

**退出残留判据（沿用加强版）**：探针前基线为 17 个 `msedgewebview2`（`9:31:06–09` 六个 + `12:54:37–38`
十一个），探针后**进程集合逐条相同、启动时间未变**，且 `AeroForge` 无进程 ⇒ 判"零残留"。
⚠ 该判据必须带启动时间比对：只看数量会把别家常驻应用的 WebView2 误判为本产物孤儿进程。

> **本次发现并修掉的一处文档缺陷（"没报错 ≠ 正确"的又一例）**
>
> 按附录 D / `AGENTS.md` 的原文执行冻结命令，产物落在了 `dist/` 而不是文档与全部既有记录
> （含 `test_perf_budget.py` 的 `FROZEN_PRODUCT` 常量）所指的 `dist-desktop/`：**PyInstaller 的默认
> `--distpath` 就是 `dist`，而文档那行漏了 `--distpath`**。它不报错，只是：
> ① 451 MB 写进 `dist/`（未被 `.gitignore` 覆盖，以 `?? dist/` 现身——**这是它唯一的暴露途径**）；
> ② 真正的 `dist-desktop/` 停在旧版本，"已经修好的东西"没进交付物。命令"跑得通"、日志
> `Build complete`、既有门禁全绿，**三者都不足以证明交付物被换新**。
>
> - **修复**：文档命令补全为 `… aeroforge.spec --distpath dist-desktop --workpath .tools/pyinstaller-build
>   --noconfirm`（`AGENTS.md` 与附录 D 两处同步；逐行一致由既有门禁保证）。**注意漂移只发生在这份副本上**：
>   本节开头（M0.5 段）给出的冻结命令**本来就是带 `--distpath` 的**——同一份规格里两种写法并存，
>   而 `AGENTS.md` 恰好抄了错的那份。`--workpath` 指向
>   `.tools/` 是为了让 451 MB 中间产物落在已忽略目录内。
> - **门禁**：`test_agents_contract.py` 新增 `test_pyinstaller_command_writes_where_the_doc_claims`
>   ——声明「产物在 `X/`」就必须带 `--distpath X`，缺省按 PyInstaller 默认值 `dist` 判；并在解析自检里
>   **用本次真实的漂移形态断言守卫会响**（`_distpath_declarations(… ) == [("dist-desktop", "dist")]`）。
>   只钉住正则、不钉住"它会响"，守卫仍可能是个空壳。
> - **判据补强（供下次复用）**：**冻结后必须核对包内前端文件名与尺寸**——那是"修复是否真的进了交付物"
>   的唯一判据，上表第 4 行即此判据的实测记录。

**冻结日志**：本次 `set_exe_build_timestamp` / `update_exe_pe_checksum` 的 PE 写入重试**未出现**
（M2 复验记录里曾出现最多 3 次重试），仅有 7 条既有的
`Failed to collect submodules for 'OCP.TopoDS.TopoDS'` 与 `Hidden import … not found` 类警告。
故 M2 复验留白第 5 项（"两项 PE 写入重试未定位"）可判为**间歇性**，但**根因仍未定位**。

**本记录明确未覆盖的项**：

1. **仍未脱离本机环境**：同机、同 WebView2 版本；也**未在非 ASCII 路径下重跑**，R-34 结论仍取自
   M0.5 的受控对照实验。本复验只回答"交付链是否仍可用、交付物是否已换新"。
2. **未在冻结产物内演练进度通道降级**：本次只跑 `--preflight` / `--probe`（探针不点「构建几何」）。
   包内 JS 确已换成含握手超时的那份（上表第 4 行），但"强制悬置 → 3 s 降级 → 轮询收敛"这条端到端
   链路**只在开发态验过**；冻结态走 FastAPI 静态托管、无 Vite 代理，链路不同，**未验证**。
3. **端到端预览仍未在冻结产物内测**（沿用 M2 复验留白第 3 项）。
4. **`--probe` 只对"可用"负责**：无独显 / 老驱动 / 远程桌面场景未演练，R-33 的后备路径仍是纸面方案。
5. **PE 写入重试未定位根因**：本次未复现，故只登记为"间歇性"观察，**未做根因分析**，也未验证
   PE 校验和 / 时间戳最终是否写入正确（`--preflight` / `--probe` 都不读这两项）。
6. **误建目录的清理只有"已删除"这一条证据**：本次误建的 `dist/` 与 `build/`（约 451 MB + 中间产物）
   已删除、`git status` 复归干净；但**未把 `dist/` 加入 `.gitignore`**——这是刻意的（保留 `?? dist/`
   作为该类错误的暴露途径），该取舍的长远影响未评估。

### 16.3 M1 几何闭环（实现契约）

> 本节是 §5 / §9 / §10 / §11 在 M1 的**落地裁剪**：里程碑行（§16 表）列出的范围在此展开为可验收的契约。与 §5 全量能力冲突时，以本节裁剪为准（后续里程碑逐块放开）。

**M1 曲线族裁剪**：母线段类型 = `line`（柱/锥/锥台）、`arc`（球冠/球底）、`ellipse`（椭球底/共底）。§5.3 其余曲线（切线卵形/抛物线/冯·卡门/幂律/钟形/样条）随 M5 头锥与喷管构型接入——M1 验收只覆盖上表五构型，提前实现无验收判据。

**作业模型（M1 化）**：§9.1 的进程池在 M1 降为**单 worker 后台线程**（OCCT 调用持锁串行）。§9.1 四条硬规则全部保留（async handler 不碰 OCCT / TopoDS 不出线程 / 串行 / 可取消）。**升级触发点 = M4**（MC 与多目标优化这类真·CPU 密集作业到达时再扩池，届时只需替换 runner，API 与缓存键不变）。

**缓存键（M1 子集）**：`key = sha256(canonical_json(profile) + kernel_version + spec_version)`；`data_snapshot_hash` 与 `cea_table_id` 字段**预留**，M4 数据层接入后纳入。产物目录（M1）：`artifacts/<key>/{model.step, model_lod1.glb, model_lod2.glb, metrics.json, provenance.json}`。

**API 形状（M1 子集，均含于 §10.1 表）**：

| 端点 | M1 语义 |
|---|---|
| `POST /api/geometry/contour` | 保存母线：归一化为 canonical JSON 落盘 `data/contours/<id>.json`，返回 canonical（往返一致判据） |
| `GET /api/geometry/contour/{id}` | 载入母线 |
| `POST /api/geometry/validate` | 同步纯 Python 校验（G0/G1、半径非负、自交、意图断言、共底闭合）+ **返回分段采样点**（供前端 2D 剖面与示意通道消费，前端零几何运算） |
| `POST /api/geometry/build` | 缓存命中 → 同步返回 `{cache_hit: true, key, metrics}`；未命中 → 建作业返回 `{job_id}`（§9.3） |
| `GET /api/jobs/{job_id}` / `WS /ws/jobs/{job_id}` | 作业状态/进度（阶段：`meridian → solid → mesh → step → done`） |
| `GET /api/artifacts/{key}/{file}` | 取产物（GLB/STEP/JSON） |

**前端范围（M1）**：三栏骨架（左=组件树+段参数面板 / 中=3D 视口 / 右=**母线编辑器**——即 §5.2 的 2D→3D 入口，M1 以"表单 + SVG 剖面"形态落地，自由手绘随 M5）；右栏的 2D 推进剂剖面图/数据面板/运力面板是 M4/M5 消费者，M1 仅留占位。示意通道：**消费 validate 返回的分段端点/采样点程序化生成网格**（直线段=圆柱/锥台一体，弧/椭弧=端点精确的薄锥台串）——端点来自后端数值，前端不做任何几何计算，包围盒因此天然与权威通道一致（§13.6 双通道门禁的两半：后端测 GLB 包络 vs 解析包络，前端测示意包络 vs 同一解析包络，均 ≤ 1%）。

**M1 验收门禁映射**（§16 里程碑行 → 测试落点）：

| 验收项 | 落点 |
|---|---|
| 解析体积 vs 内核体积 < 0.1% | pytest 参数化五构型（柱/锥/球冠/椭球/共底），解析值用闭式解二次校验 |
| 母线保存/重载往返一致 | pytest contour roundtrip（canonical JSON 逐字节相等）+ 前端编辑器单测 |
| 双通道包围盒 ≤ 1% | 后端：GLB **世界**包围盒（含场景图节点变换）vs 解析包络；前端：示意网格 bbox vs 后端采样包络；**朝向**另由 `test_dual_channel_orientation` / `orientation.test.ts` 在**世界坐标**下比对轴向归属 |
| 改参数 → 预览更新 < 500 ms | 桌面壳/dev 实测记录（§16.3 验收记录表），CI 不覆盖 |

#### M1 验收记录（已达成）

全部为**实测输出**，非推测。后端在工程根目录执行，前端在 `frontend/` 执行。

| 门禁 | 命令 | 实测结果 |
|---|---|---|
| 后端单元测试 | `uv run pytest -q` | `45 passed`（五构型三方对拍 / G1 与 pinch / 缓存键三分量敏感 / staging 原子性与 abort / API 集成 / WS 流到终态） |
| 后端静态检查 | `uv run ruff check .` | `All checks passed!` |
| 后端格式检查 | `uv run ruff format --check .` | `45 files already formatted` |
| 后端类型检查 | `uv run mypy` | `Success: no issues found in 27 source files` |
| 前端类型检查 | `npm run typecheck` | `tsc -b` 无输出（通过） |
| 前端单元测试 | `npm run test` | `Test Files 5 passed (5)` / `Tests 29 passed (29)` |
| 前端构建 | `npm run build` | `✓ built in 732ms`；`dist/assets/index-*.js` 1173.22 kB（gzip 322.91 kB）+ `index-*.css` 9.36 kB；仅有 chunk > 500 kB 提示 |
| 契约新鲜度 | `npm run gen:api` 后 `git diff --exit-code src/api/schema.d.ts` | 重生成耗时 87.9 ms，差异为空（生成链路可用且契约未漂移；即 R-31 所要求的 CI 步骤在本地等价执行） |

**四项验收门禁的落点与结果**：

| 验收项 | 判据 | 结果 |
|---|---|---|
| 解析体积 vs 内核体积 < 0.1% | pytest 参数化五构型（柱/锥/球冠/椭球/共底），解析值再用教科书闭式解二次校验（容差 1e-9） | ✅ 全通过（门禁 `1e-3`） |
| 母线保存/重载往返一致 | 后端 `POST /geometry/contour` 返回的 canonical JSON 与原剖面逐字节相等；前端 `editorFromProfile` / `profileFromEditor` 互逆单测 | ✅ 两侧均通过 |
| 双通道包围盒 ≤ 1% | 后端：GLB **世界**包围盒 vs 解析包络；前端：示意网格 bbox vs 同一解析包络 | ✅ 两侧均通过（门禁 `1e-2`）——⚠ 该判据只覆盖**尺寸**，**朝向**当时未验证，见本节留白第 1 项 |
| 改参数 → 预览更新 < 500 ms | 真实浏览器端到端实测（见下），CI 不覆盖 | ✅ 均值 ≈ 237 ms，全部 < 500 ms |

**「改参数 → 预览更新」端到端实测**（M1 唯一不在 CI 覆盖内的门禁，故留原始数字）：

方法：uvicorn 真进程 + Vite dev server + 真实浏览器；对柱段 `length` 连续改 5 次（3.2 / 3.4 / 3.6 / 3.8 / 4.0），用 `performance.now()` 量「派发输入事件 → 右栏 `.meridian-editor__outline` 的 `d` 属性变为新值」的墙钟耗时。该量**已经包含**前端 200 ms 防抖 + 一次 `POST /api/geometry/validate` 往返 + React 重绘 SVG，即用户真正感知的延迟（不是只测后端）。

| 第几次 | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|
| 耗时（ms） | 239.3 | 232.5 | 233.5 | 232.6 | **248.6** |

均值 **≈ 237 ms**、极差 16 ms，**五次全部 < 500 ms**（余量 > 2×）。

同一会话逐段拆解的耗时（对 uvicorn **真进程**发 HTTP，非 `TestClient`）：

| 段 | 方法 | 实测 |
|---|---|---|
| `POST /api/geometry/validate`（预览链路的后端段） | n = 40，每次改一个参数 | 中位数 **3.76 ms** / p95 5.49 ms / 最大 5.89 ms |
| `GET /api/health`（空转基线，用于排除测量本身的开销） | n = 10 | 中位数 2.40 ms |
| `POST /api/geometry/build` **缓存命中** | 同步返回 | 16.55 ms（`cache_hit = true`） |
| `POST /api/geometry/build` **缓存未命中** | 建作业 → 轮询至 `succeeded` | 364.3 ms |
| LOD1 GLB 取回 | `GET /api/artifacts/<key>/model_lod1.glb` | HTTP 200，28,736 B |

> 由表可得两条结论：① 预览之所以稳定在 237 ms，是因为**后端段只占 ~4 ms**，其余几乎全是 **200 ms 防抖**这一固定项——若要进一步压延迟，唯一有效手段是调防抖窗口，而不是优化几何计算；② 权威通道（GLB）走异步作业（本次缓存未命中 364 ms），**按设计不参与** 500 ms 预览门禁（ADR-012 双通道：示意通道先给即时形状，权威 GLB 到达后强制替换）。

**同次实测顺带确认的两项**（非 M1 门禁，但落在 R-33 的关注点内）：

- 页面内 `window.__aeroforgeProbe` = `{webgl2: true, renderer: "ANGLE (NVIDIA GeForce RTX 5070 Laptop GPU …, D3D11)", rendered_ms: 388.5}`；页面**无 JS 运行时异常**，唯一 error 级日志是既有的开发期 `net::ERR_ABORTED /api/health`（React StrictMode 双挂载所致，见 §16 M0 记录，非缺陷）。
- 三栏均正常渲染：左栏组件树 + 段参数、中栏 canvas、右栏「母线剖面（后端采样）」+ 6 条校验项（含 G1 接头 ≤ 0.5°）+ 构建区。

**本记录明确未覆盖的项**（列出而非略过，避免"看起来全绿"）：

1. **双通道朝向对齐未做目视核对**（**已由 M2 首项结案，见下方「双通道朝向目视核对」**）：示意网格用 `LatheGeometry`（three 的轴为 Y），当时误以为权威 GLB 的回转轴在文件 z，靠 `<group rotation={[-π/2, 0, 0]}>` 对齐——该处理**仅有静态推演**，未在视口内目视确认两通道重叠。R-25 所述的"形态分叉"风险恰在此处，故列为 M2 首项待补。⚠ **核对结果：该推演是错的**（GLB 场景图已自带 `Rx(-90°)`，再转一次共 -180°，模型躺倒），缺陷与根因见下方结案记录。
2. **端到端预览延迟无法在 CI 复现**：该数字必须由真实浏览器取得（脚本见上），故 §13.5.1 将其登记为「人工实测」门禁；CI 只覆盖它的后端段（`validate` p95）。**这意味着"预览延迟"的回归不会自动报警**，须在每次动到防抖窗口或预览链路时重跑本表。
3. **单机单次取样**：五次测量同机同时段完成，未覆盖低端机 / 高负载 / 节流场景；余量 > 2× 是对机器差异的粗略对冲，不是统计保证。

> 交付链复验（冻结产物内是否仍能"启动 → 原生窗口 → 3D 渲染"）**已完成**，见 §16.2「M1 交付链复验」。该项按 §16.2 的持续验证约定执行，不属 M1 门禁，故单列。

#### 双通道朝向目视核对（M1 留白结案）（已达成）

**背景**：结案 M1 验收记录留白第 1 项——双通道朝向对齐此前**只有静态推演**（`<group rotation={[-π/2, 0, 0]}>`）。本项是该留白的 M2 首个动作，也是 §13.6 新增「朝向」判据的由来。

**方法**：uvicorn 真进程 + Vite dev server + 真实浏览器，**同一窗口、同一相机**下分别量测两个通道的屏幕剪影——把 WebGL canvas 在 **`requestAnimationFrame` 帧内** `drawImage` 到 2D canvas，再逐像素分类（青色 `#00d4ff` = 示意通道 / 中性灰 = 权威通道），取各类的包围盒、质心与逐行宽度剖面。

> ⚠ 方法坑位（本轮踩到，供后续复用时避让）：① WebGL canvas 的 `preserveDrawingBuffer` 为 `false`，在帧外直接 `drawImage` 只能读到空白帧（实测全 0），必须在渲染帧内读回；② 参考网格同为中性灰会污染分类，故权威通道的数字在**关闭参考网格**后取；③ 两通道在视口内**互斥**（权威到达即替换示意），故比对的是"同相机下的两次剪影"而非同帧叠加。

**核对结果（修复前——缺陷）**：

| 通道 | 剪影（px） | 高宽比 | 质心 |
|---|---|---|---|
| 示意（青） | 168 × 373 | 2.22 | 居中 |
| 权威（灰） | 222 × 184 | 0.83 | 偏移 +98 / +124 px（约 +20.5% / +23%） |

两侧剪影的像素交并比 ≈ 0.15，而**控制台零报错**、尺寸类门禁全绿（§16.3 四项之二）。

**根因（读 GLB 的 JSON 块实测，非推测）**：OCCT 导出器按 glTF 规范把几何从 Z-up 转成 Y-up，方式是给**根节点**写 `rotation: [-0.7071067811865475, 0, 0, 0.7071067811865475]`（即 `Rx(-90°)`）；而 **accessor 的 min/max 仍是 Z-up 的局部坐标**（实测柱 `r=1, L=3`：`x/y ∈ ±1、z ∈ [0, 3]`）。`glb_bounding_box()` 当时只读 accessor、忽略节点变换，得出"轴向在文件 z"的错误结论，前端据此**又**转 -90°，与节点自带旋转叠加成 **-180°**，模型躺倒朝向相机。

**修复（两处，缺一不可）**：

1. 后端 `glb_bounding_box()` 改为**走场景图**求世界坐标（新增 `_node_matrix` / `_matrix_multiply` / `_transform_point`，全部按 glTF 的**列主序**语义）；POSITION 的局部包围盒取 **8 个角点**逐个变换后再合并（旋转后不能只取 min/max 两端）。修复后实测：柱 `r=1, L=3` → 世界 `size = (2.000000, 3.000000, 1.999378)`、`min = (−1, −0, −1)`、`max = (1, 3, 1)`。
2. 前端 `AUTHORITATIVE_ROTATION` 由 `[-π/2, 0, 0]` 改为 **`[0, 0, 0]`**——GLB 经 `GLTFLoader` 施加节点变换后已是 Y-up，**不得再旋转**；该常量保留为"朝向结论的锚点"而非占位。

**门禁固化**（把"朝向"变成会响的断言，坐标系统一为**渲染器实际看到的世界**）：

- 后端 `test_dual_channel_orientation.py`：断言 GLB **世界**包围盒的回转轴落在世界 Y 且自 `y = 0` 起（柱与胶囊两构型）；另钉住"访问器局部是 Z-up、世界是 Y-up"（即本轮查出的根因）、以及矩阵辅助函数的列主序语义（父·子复合路径在单节点 GLB 上走不到，只能直测）。
- 前端 `orientation.test.ts`：断言该常量为恒等、Lathe 剪影同样轴向在世界 Y，并含两条**反向自检**——改用修复前的 -90° 常量、或把访问器局部盒当作世界盒时，两通道必然分叉（证明门禁不是恒真）。
- `test_geometry_closed_loop.py` 的"逐轴 zip"同步改为**显式轴向映射**：内核包络是 Z-up `(2R, 2R, L)`，glTF 世界是 Y-up `(2R, L, 2R)`，**不可按轴序硬对齐**（这正是原先"看似两侧都对拍过"的漏洞）。
- §13.6 新增「双通道一致性（朝向）」一行，并把原行的坐标系由"访问器 min/max"更正为"**世界**包围盒"。

**复验（修复后，同法量测）**：canvas `478 × 536`；示意通道剪影 `182 × 376`（高宽比 2.07，box `[148, 53, 329, 428]`）；权威通道（关网格）`182 × 395`（高宽比 2.17，box `[148, 52, 329, 446]`）。关键佐证：两者**最宽行同为 `y = 142`、最宽值同为 182 px、x 范围同为 `148–329`**——位置、尺度、朝向一致；控制台无 error 级日志。宽度差 0 px、高度差 +19 px 来自灰色类掩膜更严（GLB 暗部与抗锯齿像素被剔除），非几何差异。

**本记录明确未覆盖的项**：

1. **未重冻冻结产物**：`dist-desktop/` 仍是修复**前**的前端产物（内含错误的朝向常量）。本项只验证源码态（dev server）与 CI 门禁，冻结产物的重跑按 §16.2 的持续验证约定留待下一次交付链复验。
2. ~~**首次构建卡死既未复现也未定位**~~ **→ 已定位并修复（本项留白据此结案）**：M2 首轮核对出现的"点『构建几何』后停在『排队中（0%）』超过 2.5 分钟、刷新后约 6 秒完成"，已在后续专项排查中定位为 **Vite 代理未转发 `/ws` 导致的 WS 握手悬置**（可观测症状只有"界面不前进"，控制台零报错）。详见本小节「作业进度通道缺陷修复」记录。
3. **取景缩放的非确定性未定位**：同一 canvas 尺寸下两次页面加载的示意剪影高度为 376 px 与 233 px（约 1.6×）。疑似量测期间 OrbitControls 被缩放所致，未做受控实验，故本项不据此下任何结论（若将来要做像素级回归，须先固定取景）。
4. **只核对了默认胶囊一例**：柱 / 锥 / 球冠 / 椭球 / 共底其余四构型未逐一目视——其中柱与胶囊已由上述 CI 门禁覆盖，其余依赖同一套"世界坐标 + 轴向归属"判据而非逐例目视。
5. **屏幕量测的精度下限**：剪影比对只能证明"两通道在同一世界朝向下重合到像素量级"，不能替代顶点级比对；顶点级判据由上述 CI 门禁承担。

#### 作业进度通道缺陷修复（已达成）

**背景**：结清上一条记录留白第 2 项——"首次点『构建几何』卡在『排队中（0%）』超过 2.5 分钟、刷新后约 6 秒完成"。该项当时**未复现、未定位**，只作登记备查。

**定位（每一环都有运行时证据，非推测）**：

| 环节 | 取证手段 | 实测结果 |
|---|---|---|
| 界面为何停在 0% | 找「排队中（0%）」的唯一来源 | `MeridianEditor` 为 `job === null ? 排队中 : <真实阶段>`——即**前端还没拿到任何作业快照**，并非后端在排队 |
| 后端是否卡住 | 读缓存产物的 `provenance.json` | `timings_ms.done` = **299.0 / 329.55 ms**——后端早已建完 |
| 降级为何不启动 | `performance.getEntriesByType('resource')` | `/api/jobs` 条目**为空** ⇒ 轮询从未启动 |
| 通道为何不报错 | PowerShell `ClientWebSocket` 双路对照 | 经 Vite `:5173` 的 `/ws/jobs/…` **6 s 后无任何响应**（`readyState = 0`）；直连 `:8000` **8 ms 即 `open`** 并收到 `JOB_NOT_FOUND` 帧 |

**根因**：`vite.config.ts` 的 `server.proxy` 只配了 `/api`，**没有 `/ws`** ⇒ Vite 不转发 WebSocket 升级请求 ⇒ 握手永久悬置。而 `openJobStream` 只挂了 `onmessage` / `onerror` / `onclose`，**无 `onopen`、无握手超时** ⇒ 悬置状态下三个回调全不触发 ⇒ 降级永不执行 ⇒ `job` 恒为 `null` ⇒ 界面无限冻结。

**触发条件（解释"为何首次出现、刷新后就好了"）**：`SPEC_VERSION` 变更后的**首次**构建缓存必然 miss ⇒ 走异步作业路径（`cache_hit = false` + `job_id`）⇒ 首次必然踩中；而用户刷新后第二次构建时，首次那个作业**其实已把产物落盘**（`created_at` 比后端启动晚 95 s）⇒ 转为 `cache_hit = true` 的**同步**路径 ⇒ 不碰 WS ⇒ "6 秒完成"（实为整页重载耗时）。**"刷新后变快"被误读为"后端恢复了"，实际是走了另一条代码路径。**

**修复（三处）**：

1. `vite.config.ts`：`server.proxy` 增补 `/ws` → `{ target: 'ws://127.0.0.1:8000', ws: true }`。
2. `geometry.ts`：新增 `JOB_HANDSHAKE_TIMEOUT_MS = 3000`；`openJobStream` 增 `onopen` 与握手定时器，超时即 `abandon()`（清定时器、摘回调、关闭 socket）并回调 `onError` ⇒ store 降级轮询。`abandon()` 幂等，避免迟到事件回流造成 WS 与轮询双通道写入。
3. `model.ts` 的通道契约**未变**——降级入口本就是 `onError`，本次只是**让它在"既不成功也不失败"的挂起下也会被调用**。

**验证（真实浏览器 + 真实后端 + 真实 Vite，两轮对照）**：

| 轮次 | 构造 | 实测 |
|---|---|---|
| 一（正常通道） | 改 `#1 line` 的 `length` 3 → 4 强制缓存 miss，点「构建几何」 | WS 逐帧推进：`排队中(0%)` → `生成实体(40%)` → `生成网格(70%)` → `导出 STEP(90%)`（相对点击时刻 8.7 / 74.6 / 80.6 / 194.8 ms），约 232 ms 后转为 `已就绪（权威）`；`/api/jobs` 资源条目 **0 条**（= 未降级，WS 正常）；console 零 error 零 warning |
| 二（握手悬置） | 页面内把 `window.WebSocket` 换成永不回调的替身，再改 `length` 3 → 4.5 并构建 | `排队中（0%）` **冻结 3599 ms**（正是挂起窗口）→ **3575 ms** 时出现首个 `GET /api/jobs/<job_id>`（3 s 超时 + ≈500 ms 轮询间隔）→ **3666 ms** 收敛为 `已就绪（权威）` 并给出完整指标（体积 18.3260、解析-内核相对误差 0.000e+0） |

**门禁固化**：`geometry.test.ts` 新增 2 条（握手挂住 ⇒ 超时降级且迟到事件不回流；`onopen` 后超时不再触发）；`model.test.ts` 新增 1 条（握手挂住 ⇒ 3 s 超时 → 轮询 → 仍收敛为权威通道，即"不再卡在排队中"的 store 级断言）。全量门禁复跑：后端 **82 passed / 1 skipped**、`ruff check` / `ruff format --check`（51 files）/ `mypy`（27 files）全通过；前端 `tsc -b` 通过、vitest **34 → 37 passed**、`vite build` **1173.72 kB**（+0.50 kB，§13.5.1 已同步刷新，预算 1400 kB 内）。

**顺带清掉一个假绿风险（过程性教训）**：定位过程中发现用于人工复验的 uvicorn 是**陈旧进程**（启动早于 `SPEC_VERSION` 递增 35 分钟），其 `POST /api/geometry/build` 恒返回 `cache_hit = true` + `spec_version = "0.5.1"`——**任何"看界面正常"的人工复验都会静默命中旧缓存**。已按其原始命令行重启。**判据**：人工复验前必须确认后端进程启动时间**晚于**最后一次契约变更，否则"跑通了"可能只是旧进程的旧答案。

**本记录明确未覆盖的项**：

1. **未重冻冻结产物**：本次修复只落在源码与开发态。该缺陷本身是**开发态专属**（生产态由后端同源托管 `dist/`，无 Vite 代理层，§10.2），故冻结产物不受其影响；但握手超时兜底属生产态也会执行的代码，其重冻与 `--probe` 复验按 §16.2 的持续验证约定留待下一次交付链复验。
2. **未在真实反向代理下做受控实验**：握手悬置是用"页面内替换 `window.WebSocket`"构造的，未搭 nginx 等反代复现同类挂起，故 3 s 超时值对广域网时延的适用性未验证（本机直连实测 < 10 ms，余量 > 300×）。
3. **降级期间的多帧推进未覆盖**：实测中降级后的**首个**轮询即返回终态（后端早已完成），故"降级后 UI 仍能展示 0% < progress < 100% 的中间阶段"未被观测；该路径由后端 `stage` 字段与轮询逻辑静态保证，未用慢作业实测。
4. **"握手成功后再中途断开"的时序未单独构造用例**：现有用例覆盖握手期悬置与显式 `onerror`；`abandon()` 与 store 的 `degraded` 标志虽均幂等，但该时序未被单独断言。
5. **仅按默认胶囊构型实构建**：两轮实测用的都是默认剖面改段长，未覆盖其它四构型与失败/取消作业在降级路径下的表现。

### 16.4 M2 参数系统（实现契约）

> 本节是 §6 在 M2 的**落地裁剪**：与 §6 全量描述冲突时以本节为准（后续里程碑逐块放开）。M2 的交付物是**纯后端参数系统 + 一个诊断端点**，不新增任何 3D 界面。
>
> ⚠ **本节不覆盖 M1 增补项（OI-19：3D 分级显隐 + 剖切）**：§16 里程碑表把该增补项标注为「并入 M2 验收」，**本基础记录对应那一轮**的交付范围是参数系统，当时该增补项尚未实现，故不构成本记录的门禁，并在下方验收记录的留白段**首条显式登记**——不得因"参数系统验收通过"而读成"M2 整行已交付"。
>
> ✅ **更新（v0.6.1）**：该增补项**已交付并验收**，见本节末「M2 增补验收记录 · OI-19 3D 分级显隐 + 剖切（已达成）」及其交付链复验记录；上方两段历史记录（M2 参数系统 / M2 收口）是各自时点的证据，原文不回改。

**模块落点**（后端 `aeroforge/params/`）：

| 模块 | 对应节 | 内容 |
|---|---|---|
| [schema.py](file:///d:/Vibe%20Coding/aeroforge/backend/aeroforge/params/schema.py) | §6.1 | 分层 Pydantic Schema：`Vehicle / Stage / Geometry / Tank / Engine / Aero / LaunchSite / Mission / Sequence / Recovery / LossFactors`；`si_field()` 描述符把「内部一律 SI」与「契约里的显示单位符号」绑在同一字段上（§6.4 / P1） |
| [dag.py](file:///d:/Vibe%20Coding/aeroforge/backend/aeroforge/params/dag.py) | §6.2 / §5.9 | 依赖图：重名 / 未知依赖 / **环检测（报出环路路径）**；拓扑传播；来源标记（`user` / `derived`）；覆盖提示；未用输入报告；派生量含 `V_ox/V_fuel` 与箱长 |
| [constraints.py](file:///d:/Vibe%20Coding/aeroforge/backend/aeroforge/params/constraints.py) | §6.3 | 四类约束（**判定码前缀即类别**）：`HARD_` / `ENGINEER_` / `COMPAT_` / `SAFETY_`，输出与 §6.5 **同形**的六字段 |
| [diagnostics.py](file:///d:/Vibe%20Coding/aeroforge/backend/aeroforge/params/diagnostics.py) | §6.5 | 八条规则（§6.5 表 7 条 + §6.3 举例的喷管出口直径 1 条）；阈值可配置；逐规则账目 `diagnostics` / `deferred_reason` / `uncovered` |
| [units.py](file:///d:/Vibe%20Coding/aeroforge/backend/aeroforge/params/units.py) | §6.4 | 显示单位表（8 个量纲）+ 双向换算（换算只在 API 边界发生） |
| [report.py](file:///d:/Vibe%20Coding/aeroforge/backend/aeroforge/params/report.py) | §6.5 | `Diagnostic` 六字段模型 + `has_hard()` |
| [propellants.py](file:///d:/Vibe%20Coding/aeroforge/backend/aeroforge/params/propellants.py) | §5.9 | 推进剂组合 → 两剂标称密度（箱体比例派生的输入） |
| [api/params.py](file:///d:/Vibe%20Coding/aeroforge/backend/aeroforge/api/params.py) | §10.1 | `POST /api/params/diagnose`（结构错与硬违反均走 §10.3 错误模型） |

**M2 的范围边界（OI-11 裁决）**：诊断**只输出六字段** `{level, code, field_path, message, suggestion}`，**不含 `impact`**——「按建议调整可提升运力 X%」必须由 §8.7 敏感度**实算**，而敏感度属 M4。**不得**用经验系数编造百分比（§1.4-4 溯源红线）。

**逐规则账目（M2 可判的 4 条 + 显式 deferred 的 4 条）**：

| 规则（§6.5 表） | 级别 | M2 状态 |
|---|---|---|
| 起飞推重比 T/W | hard | 判据可判，但**需要 M4 的 GLOW**；M2 不提供推进剂质量 ⇒ 整条规则进 `deferred_reason`（**不填默认值**）。另：§6.5 表的固体助推档（1.5）**判不了**——Schema 尚无固体助推标识，登记为 `uncovered` 而非"悄悄套 1.2 说全覆盖" |
| 结构质量比（一级） | warning | ✅ 可判。判据用 σ 的派生量 `σ/(1−σ)`，**不依赖 M4 任何结果**（§6.1 唯一权威原则：σ 是存储权威，干质比为派生） |
| 贮箱壁厚 ≥ 最小工艺厚度 | hard | §6.5 表**只给判据、未给数值** ⇒ 默认 `None` = 未配置即**不判定并留痕**，阈值字符串如实写「未配置」，**不印一个凭空的毫米数**。上界（壁厚 < 直径/2）由 §6.3 的 `SAFETY_TANK_WALL_TOO_THICK` 判，本规则**不重复**（否则前端会看到两条"看似两处缺陷"的告警） |
| 长径比 | warning | ✅ 可判（逐级） |
| 级间 ΔV 分配 / 上面级余量 / 运力缺口 / 喷管出口直径 | warning / warning / hard / warning | **显式 deferred 至 M4**，各自写明缺什么；`PAYLOAD_CAPACITY_GAP` 另按 OI-11 须由敏感度给出可改进参数，M2 **不得编造** |

> **为什么把"判不了"写进结果而不是跳过**：空集通过会让"未验证"与"已验证"长得一模一样，而「没报错 ≠ 正确」正是本项目最贵的一课（§13.8 的由来）。故三条账目**随结果一并下发**：`diagnostics`（已判，空 = 通过）、`deferred_reason`（整条未判定）、`uncovered`（已判但有分支未启用）。

**阈值可配置（OI-08 / §18.4）**：`DiagnosticThresholds` 的**默认值即 §6.5 表内数值**，优先级 `环境变量 > config.toml > 默认值`（环境变量名 = 字段名大写加 `AEROFORGE_` 前缀）。每条阈值的 `description` **强制携带来源标注**「工程惯例值，非权威来源」并随 OpenAPI 下发——M4 用基准火箭（§13.2）反标定时**只改配置、不改代码**。配置文件路径必须在**实例化时**求值（写在类体里会在 import 期把路径冻住，测试重定向数据根后就取不到，同 `artifacts_root` 那一课）。

**M2 验收门禁映射**（§16 里程碑行 → 测试落点）：

| 验收项（§16 表原文） | 落点 | 结果 |
|---|---|---|
| 非法参数被拒并给字段级建议 | `test_params_schema.py`（23）/ `test_params_constraints.py`（20） | ✅ |
| 循环依赖可检出 | `test_params_dag.py::test_cycle_is_detected_with_the_cycle_path` | ✅ |
| 单位往返恒等测试通过 | `test_params_units.py::test_round_trip_is_identity`（8 量纲参数化） | ✅ |
| 诊断输出为六字段 | `test_params_schema.py::test_diagnostic_shape_is_six_fields` + `test_params_diagnostics.py::test_diagnostics_are_six_fields_in_json` | ✅ |
| 纬度 / 海拔 / 方位与轨道要素可存取且过校验 | `test_params_schema.py` 的 `LaunchSite` / `Mission` 往返 + `test_params_constraints.py` 的轨道要素三态与发射场两态 | ✅ |
| `V_ox/V_fuel` 与 §5.9 的两次公开数据反算一致 | `test_params_dag.py::test_section_5_9_reverse_calculations[S-IC]` / `[S-II]`（**两侧都核**：与规格「公式结果」列比，再与公开数据实测容积比比） | ✅ |

**规格 ↔ 代码逐行机检**：`test_params_spec_contract.py` 直接解析 §6.4 单位表与 §6.5 规则表的**原文行**，比对单位符号、阈值默认值、规则名与级别；并内置**解析器自检**（`test_parsers_find_the_expected_rows`）——排版一变即解析为空集，若无自检，全部断言会**假通过**。另有一条文档侧守 OI-11：断言 §6.5 正文仍写着「六字段」与不得**编造** `impact`。

#### M2 验收记录（已达成）

全部为**实测输出**，非推测。后端在工程根目录执行，前端在 `frontend/` 执行。

| 门禁 | 命令 | 实测结果 |
|---|---|---|
| 后端单元测试 | `uv run pytest -q` | **219 passed / 1 skipped**（共 220 项）。skip 为 `test_agents_contract.py:147` 的 `python -m` 引用守卫：`AGENTS.md` 当前未使用该形式故为空集，按设计 `pytest.skip` 留痕（守卫不允许静默 `return`） |
| 后端格式检查 | `uv run ruff format --check .` | `67 files already formatted` |
| 后端静态检查 | `uv run ruff check .` | `All checks passed!` |
| 后端类型检查（CI 口径） | `uv run mypy` | `Success: no issues found in 35 source files` |
| 后端类型检查（AGENTS.md 本地口径，多覆盖 tests） | `uv run mypy backend` | `Success: no issues found in 57 source files` |
| 前端类型检查 | `npm run typecheck` | `tsc -b` 无输出（通过） |
| 前端单元测试 | `npm run test` | `Test Files 6 passed (6)` / `Tests 37 passed (37)` |
| 前端构建 | `npm run build` | `✓ built in 632ms`；`index-Dyj8e-pe.js` **1,173.72 kB**（gzip 323.11 kB）+ `index-BkdnKLOw.css` 9.36 kB；仅 chunk > 500 kB 提示（§13.5.1 预算 1400 kB 内） |
| 契约新鲜度（第 1 步：后端重导） | 重导 `specs/openapi.json` 后比对 SHA256 | 与首次刷新结果**逐字节相同**（`openapi same=True`） |
| 契约新鲜度（第 2 步：前端重生成） | `npm run gen:api` 后比对 SHA256 | 与首次生成结果**逐字节相同**（`schema same=True`）；链路耗时 120.5 ms |

**本轮新增的测试**（7 个文件、**136 条**，含参数化展开）：

| 文件 | 条数 | 覆盖 |
|---|---|---|
| `test_params_schema.py` | 23 | 六字段形状 / 未知字段拼写建议 / 数组下标路径 / 边界与枚举建议 / SI 字段双符号 / 12 个模型的可空字段在契约中确为 optional / **`body` 前缀不泄漏进字段路径** |
| `test_params_dag.py` | 21 | 重名 / 未知依赖 / **环检测报出环路** / 拓扑序 / 菱形依赖只算一次 / 缺输入 deferred / 用户值覆盖派生 / 未用输入 / 覆盖提示点出上游链 / 派生量闭式对拍 / 箱体比例按质量与密度双向交叉核对 / **§5.9 两次公开数据反算** |
| `test_params_constraints.py` | 20 | 首条最严：**正例夹具一条裁定都不出**；四类逐个覆盖（级号连续 / 超装须显式 / 发射场 / 轨道要素三态 / 共底三态 / LH₂ 隔热 / 直径 / 壁厚 / 发动机包容 / Isp 来源 / 整流罩 / 回收三态）/ 路径一律带数组下标 / **不抛异常** / 级别只有 hard 与 warning |
| `test_params_diagnostics.py` | 30 | 规则码账目齐全且不重复 / **deferred 按两类成因分别钉死**（能力缺口属 M4 vs 本机未配置）/ 六字段 JSON / T/W 触发与未覆盖分支 / 结构质量比正负例 / 壁厚未配置与已配置两态 / 长径比逐级 / 阈值来源标注逐字段 / 环境变量与配置文件覆盖（**并验真的改变了裁定**）/ 环境变量优先于配置文件 / 非正值被拒 / 未知键忽略 / 诊断对飞行器无副作用 |
| `test_params_units.py` | 20 | 显示单位覆盖全部量纲 / 换算方向（`SI = 显示值 × factor`）/ 往返恒等（8 量纲双向）/ 同名单位 factor 必为 1.0 / SI 字段同发两种符号 / 默认值决定是否 required。期望值**手写**而非从 `DISPLAY_UNITS` 读，避免同义反复 |
| `test_params_spec_contract.py` | 15 | 解析器自检 + §6.4 单位表逐行 / 每阈值都挂到规格行 / 阈值默认值出现在规格行原文 / 规则名与级别逐行 / §6.5 仍拒 `impact` |
| `test_api_params.py` | 7 | `POST /api/params/diagnose` 200 与 422 两路 / deferred 规则随响应下发 / **结构错通路的字段路径恰为 `stages[0].engine.mixture_ratio`** / 空级清单 / 随级数缩放 / 端点在 OpenAPI 契约中且 `Diagnostic` 恰五字段、无 `impact` |

**本轮查出并修复的一处真实契约缺陷（"没报错 ≠ 正确"的又一例）**：

- **现象**：经 HTTP 送达的结构错误，其 `field_path` 是 `body.stages[0].engine.mixture_ratio`、`body.stages`。
- **根因**：FastAPI 的 `RequestValidationError.loc` 比 pydantic 的 `ValidationError.errors()` **多一层"请求位置标记"**（本例即 `"body"`）。同一处错误，pydantic 给 `('stages', 0, 'engine', 'mixture_ratio')`，FastAPI 给 `('body', 'stages', 0, …)`。
- **为什么它是缺陷而不是瑕疵**：§6.3 约束层下发的路径是 `stages[0]…`（无前缀），而结构错通路多一层前缀 ⇒ 前端「路径 → 控件」映射**不可能对两类清单同时成立**；且错误文本本身"看起来很正常"（消息、建议、级别都对），只有把两条通路**放在一起比对**才暴露。
- **修复**：`schema.errors_to_diagnostics` 入口统一剥除请求位置标记（`_REQUEST_LOCATIONS` + `_normalise_loc`），使 API 层与约束层共用同一套路径。
- **门禁**：`test_params_schema.py` 新增两条防回归机检——对同一错误列表**加**与**不加** `"body"` 前缀，两条通路的 `field_path` 必须一致；另钉住「loc 仅 `("body",)` 时回落到 `vehicle` 根」。

**顺带清掉的两处既有类型债**：`uv run mypy backend`（AGENTS.md 记录的本地命令，比 CI 的裸 `mypy` 多覆盖 tests）此前有 2 处 `no-any-return` 未清（`test_dual_channel_orientation.py` / `test_api_geometry.py`），本轮一并修掉，使**两条口径同时真绿**——CI 看不见的类型错不等于不存在。

**本记录明确未覆盖的项**：

1. **M1 增补项（OI-19：3D 分级显隐 + 剖切）未实现**：§16 里程碑表把它标注为「并入 M2 验收」，本轮交付范围为参数系统（用户裁定），**该增补项尚未落地**。故 §16 表的 M2 行**不得据本记录判定为整行交付完成**。其验收判据（§11.4：纯视图状态、不触发后端请求、不改几何、不可导出）与 2D 工程剖面图（§11.10）同属尚未开工的界面工作。
2. **`impact` 字段不在 M2**（OI-11 裁剪）：诊断恒为六字段。这意味着**"按建议调整能提升多少运力"这个问题当前无人回答**，且 §6.5 的四条规则（级间 ΔV 分配 / 上面级余量 / 运力缺口 / 喷管出口直径）M2 **一条也未判定**——它们随 M4 的定尺求解与敏感度一并交付。
3. **固体助推的推重比阈值判不了**：§6.5 表给了 1.5 这一档，但 Schema 尚无"该级是否固体助推"的标识，故只登记 `uncovered`（阈值字段已在契约中且带来源标注）。**不是产品缺陷，但也不是覆盖**。
4. **贮箱最小工艺厚度默认未配置**：`min_tank_wall_thickness_m` 默认 `None`，开箱即用时该条判据**不判定**。规格 §6.5 表本身未给数值（刻意不填一个凭空的下界），故需由使用者经 `AEROFORGE_MIN_TANK_WALL_THICKNESS_M` 或 `config.toml` 显式配置；**本机未做"配置后再跑一遍真实飞行器"的复核**。
5. **诊断未接入任何真实界面**：`POST /api/params/diagnose` 本轮只有集成测试覆盖，**前端尚无消费它的视图**（参数面板的分区/控件、诊断清单渲染均属后续里程碑），故"字段路径 → 控件"这条映射链**只在契约与测试层验证，未在真实界面中目视核对**。
6. **未在冻结产物内跑诊断**：本轮只跑源码态门禁与开发态前端构建；`dist-desktop/` **未重冻**，故产物内是否仍可用、包内前端是否换新均**未验证**——按 §16.2 的持续验证约定留待下一次交付链复验。
7. **阈值本身未经标定**：§6.5 全部阈值为**工程惯例值、非权威来源**，本轮的机检只保证"代码默认值与规格表数值一致"与"来源标注随契约下发"，**不保证这些数值本身合理**——合理性由 M4 用 §13.2 基准火箭反标定（只改配置不改代码）。
8. **两条既有留白不受本轮影响**：§16.2 的"未脱离本机环境 / `--probe` 只对可用负责 / PE 写入重试根因未定位"与 §16.3 的"取景缩放非确定性"均沿用其原记录，本轮**未触及**。

#### M2 收口验收记录 · 诊断接入界面 + 两处配置项（已达成）

全部为**实测输出**，非推测。后端在工程根目录执行（本机 `uv` 不在 PATH，取工程内 `.tools/uv/uv.exe`），前端在 `frontend/` 执行。

| 门禁 | 命令 | 实测结果 |
|---|---|---|
| 后端单元测试 | `uv run pytest` | **243 passed / 1 skipped**（共 244 项），12.34 s。skip 仍为 `test_agents_contract.py` 的 `python -m` 引用守卫（空集按设计 `pytest.skip` 留痕） |
| 后端静态检查 | `uv run ruff check .` | `All checks passed!` |
| 后端格式检查 | `uv run ruff format --check .` | `70 files already formatted` |
| 后端类型检查 | `uv run mypy` | `Success: no issues found in 37 source files` |
| 前端类型检查 | `npm run typecheck` | `tsc -b` 无输出（通过） |
| 前端单元测试 | `npm run test` | `Test Files 11 passed (11)` / `Tests 78 passed (78)`，6.08 s |
| 前端构建 | `npm run build` | `✓ built in 638ms`；`index-D_8g-TNp.js` **1,194.32 kB**（gzip 328.19 kB）+ `index-DaOYCLM2.css` 14.24 kB（§13.5.1 前端预算 1400 kB 内） |
| 契约新鲜度（第 1 步：后端重导） | 重导 `specs/openapi.json` 后比对 SHA256 | 与首次刷新结果**逐字节相同**（`openapi same=True`） |
| 契约新鲜度（第 2 步：前端重生成） | `npm run gen:api` 后比对 SHA256 | 与首次生成结果**逐字节相同**（`schema same=True`）；链路 123.8 ms |

**本轮交付的三件东西**（对应 §1.7.3 OI-30 / OI-31 / OI-32，规格条文已同批落地）：

| 项 | 后端 | 前端 |
|---|---|---|
| 推进剂相态标识（OI-30） | `Engine.propellant_phase` 字段 + §6.5 固体 T/W 档（1.5）**真判定** + 相态↔推进剂配对相容警告 | 参数面板下拉选择，读数来自契约 |
| 阈值配置入口（OI-31） | `GET`/`PUT /api/params/thresholds`（生效值 + `origin` 四态：`环境变量`/`config.toml`/`默认值`/`未配置`）+ 仓库根 `config.example.toml` | 阈值设置项：显示**生效值**与来源、改值即写回、清空即删键 |
| 单位表与定位契约（OI-32） | `GET /api/params/units` 下发 `factor`（方向固定 `SI = 显示值 × factor`） | `data-field-path` 标记 + 「路径 → 控件」映射；诊断清单可点 `field_path` 定位 |

**起始箭来源**：`GET /api/params/template` 返回单级液体示例骨架，`label`/`note`/`sourced_fields` 随响应下发——**不在 `sourced_fields` 里的数值一律是占位值**（§1.4-4）。完整模板库（§13.2 基准火箭）仍归 M3。

**本轮新增 / 扩充的测试**（后端 **+24 条**，前端 **+41 条**，均为实测计数）：

| 文件 | 条数 | 覆盖 |
|---|---|---|
| `test_params_thresholds.py`（新） | 17 | 键集与模型字段一一对应（两份清单即两份真相）/ `config.example.toml` 逐项列出全部阈值且**不得**出现模型没有的键 / **「最小工艺厚度」在模板中不得以生效行给出数值**（否则等于抄一个自称有来源的工艺下限）/ `origin` 四态 / 环境变量压过 `config.toml` / 写入落在 `config_file()` 且**断言路径在会话临时区内** / `null` 即删键 / **注释与无关键不被抹掉** / 新键插在首个 `[table]` 之前 / 未知键与越界值 422 且**不留半个文件** / 坏文件被报告而非覆盖 / **配置后该规则由「未判定」转为真判定**、并可报出硬违反 |
| `test_params_diagnostics.py`（30 → 34） | 34 | 追加：固体相态使 1.5 档真判定 / 相态与推进剂不配对的警告 / 两类"判不了"的成因仍分列 |
| `test_params_constraints.py`（20 → 22） | 22 | 追加：相态相容约束的正反两例 |
| `test_params_schema.py`（23 → 24） | 24 | 追加：`propellant_phase` 的缺省与非法值建议 |
| `api/units.test.ts`（新） | 8 | 换算**方向**（`toDisplayValue` 用除、`toSiValue` 用乘）/ 二者互逆 / 缺项原值返回 |
| `store/fieldPath.test.ts`（新） | 9 | `stages[0].engine.isp_vacuum_s` → 4 段（下标为**数字**）/ 不可达返回 `undefined` / 写入返回新对象且深拷贝到最深一级 / 不可达**抛出** |
| `store/vehicle.test.ts`（新） | 8 | 起始箭**只来自后端**且 `note` 含「未经来源核对」/ 422 通路的字段级裁定**不丢** / 改值经 200 ms 防抖（199 ms 不发、200 ms 只发一次）/ **迟到响应不得覆盖新结果** |
| `components/DiagnosticsPanel.test.tsx`（新） | 10 | 三条账目**分别可见** / 账目未产出时**不得用「0 / 无」冒充**「没有未判定的规则」/ 定位失败**降级**（该条仍在、路径转纯文本、按钮撤掉） |
| `components/ThresholdPanel.test.tsx`（新） | 6 | 「未配置」是**可见状态**（输入框为空、显式写明该条判据不判定）/ 改值写回、清空写 `null` / **没改值就离开不发请求** |

**端到端实测**（真起 `uvicorn` + `vite`，非 jsdom）：本轮跑了两条路径，并因此查出**三处真实界面缺陷**。

1. **降级路径**：`ENGINEER_AERO_DEFAULTED` 的 `field_path = 'aero'` 在参数面板里**没有对应控件**，故定位必然失败——但**降级提示正确出现、该条仍留在清单里**（这是设计要求的形态：定位失败不等于裁定消失）。
2. **成功路径**：把 `stages[0].fill_fraction` 改成 `1.5` → 后端返回 `HARD_FILL_OVERFILL`（路径 `stages[0].fill_fraction`）→ 点「定位到控件」后 `document.querySelectorAll('.field-located').length === 1`、`data-field-path` 命中该 input；**2.5 s 后再查为 0**（高亮 1.6 s 后自动摘掉）。恢复 `0.95` 后面板回到 `命中项（硬 0 · 警告 1）`。

**端到端实测催生的三处修复**（静态检查与单元测试**全绿**，只有真跑一遍才暴露）：

- **422 时三条账目不存在，界面却渲染成「（0）/ 无」**：硬约束 422 时后端拒绝进入诊断，`rules` 根本不存在，界面照常渲染「未判定的规则（0）」——读起来就是"判过了、没有未判定的"，**把"未判"与"通过"混为一谈**（正是 §13.8 要拦的形态）。修复：引入 `ledgerReady`，账目缺失时三个分栏只显示标题、正文改为「本轮未产出逐规则账目（诊断被拒绝或尚未完成），这不等于「没有这一栏的情况」」。
- **定位失败后按钮仍在**：规格要求降级时路径以纯文本呈现、**不可点击**。修复：定位失败即撤掉按钮——留一个按不出结果的按钮只会反复误导用户。
- **`deferred_reason` 未显示**：界面上「未判」与「未通过」无从区分。修复：在规则行补「缺什么」一行，用 `--color-status-warn` 呈现（"未判定"是必须可见的状态，不是排版细节）。

**方法与环境的两个记要**：① 端口占用的症状是 **404 而不是连接失败**——既有 uvicorn 进程（无 `AEROFORGE_DATA_DIR`）缺新端点，探 `GET /api/params/template` 得 404，故"先探端点再信端口"；② `vite` 默认 5173 被占用，实际服务在 5174。

**本记录明确未覆盖的项**：

1. **OI-19（3D 分级显隐 + 剖切）仍未实现**：§16 表把它标注为「并入 M2 验收」，本轮交付范围为参数系统（用户裁定），**该增补项尚未落地**。故 §16 表的 M2 行**仍不得据任何记录判定为整行交付完成**。跟随其后的是 §11.10 的 2D 工程剖面图，同样未开工。
2. **`impact` 字段与 §6.5 的四条规则仍不在本轮**（OI-11 裁剪）：诊断恒为六字段，级间 ΔV 分配 / 上面级余量 / 运力缺口 / 喷管出口直径**一条也未判定**——随 M4 的定尺求解与敏感度一并交付。故"按建议调整能提升多少运力"当前**无人回答**。
3. **「最小工艺厚度」交付的是入口，不是数值**：本轮实测该阈值 `origin` 仍为**未配置**，故开箱即用时那条硬规则**依然不判定**。规格 §6.5 表本身未给数值（不填凭空的下界），须由使用者经 `config.toml` 或环境变量显式配置。端点层已实测「配置后由未判定转为真判定、并可报出硬违反」，但**未在真实界面上点一次「改阈值 → 诊断清单随之变化」的完整回路**——该回路只在 TestClient 与 jsdom（mock fetch）两层分别验证。
4. **「阈值本身是否合理」仍无人回答**：§6.5 全部阈值为**工程惯例值、非权威来源**。本轮机检只保证"代码默认值与规格表一致""来源标注随契约下发""写入后真的改变裁定"，**不保证数值本身合理**——合理性由 M4 用 §13.2 基准火箭反标定（只改配置不改代码）。
5. **定位的降级路径只目视核对了一例**：实测覆盖的是 `aero`（整箭层判定、面板无对应控件）。**未穷举**其余无控件路径（如仅存在于契约而未进入面板的字段），"降级提示必然出现"只在契约与单元测试层成立。
6. **未在冻结产物内跑诊断与阈值写入**：`dist-desktop/` **未重冻**，本轮只跑源码态门禁与开发态前端构建；产物内是否仍可用、包内前端是否换新、冻结态下 `config.toml` 落在何处（`data_root()` 上溯不到 `pyproject.toml` 时会走 `%LOCALAPPDATA%`）均**未验证**——按 §16.2 的持续验证约定留待下一次交付链复验。
7. **起始箭是"未经来源核对的示例骨架"**：`GET /api/params/template` 的字段值只取规格内已出现的数字（§3.2 黄金锚点与 §6.1 既有默认值），`aero`/`fairing`/`profile`/`sequence`/`recovery` 一律留空。**它不是任何真实飞行器**，不得被当作设计起点引用；完整模板库归 M3（OI-29）。
8. **两条既有留白不受本轮影响**：§16.2 的"未脱离本机环境 / `--probe` 只对可用负责 / PE 写入重试根因未定位"与 §16.3 的"取景缩放非确定性"均沿用其原记录，本轮**未触及**。

#### M2 增补验收记录 · OI-19 3D 分级显隐 + 剖切（已达成）

全部为**实测输出**，非推测。后端在工程根目录用工程内 `.tools/uv/uv.exe` 执行，前端在 `frontend/` 执行。
本轮落地 §11.4 / §1.7.4 的 **OI-33 裁决**：分级显隐粒度 = **母线段链**（`vehicle` 根不持 mesh →
`seg-<i>` 各持一 mesh，`<i>` 是 `profile.segments` 的 0 基下标，与 `stages[i]` **无关**）；
两通道同粒度（`ValidationReport.segment_outline` 逐段下发）；量测与导出仍取整体 `build_solid()`。
`SPEC_VERSION` **0.6.0 → 0.6.1**（§9.2 判据：同一输入的 GLB 字节因分段节点而改变），既有
`artifacts/` 缓存按设计失效。

| 门禁 | 命令 | 实测结果 |
|---|---|---|
| 后端单元测试 | `uv run pytest` | **260 passed / 1 skipped**（skip 仍为 `test_agents_contract.py` 的 `python -m` 空集守卫） |
| 后端静态检查 | `uv run ruff check .` | `All checks passed!` |
| 后端格式检查 | `uv run ruff format --check .` | `71 files already formatted` |
| 后端类型检查 | `uv run mypy` | `Success: no issues found in 37 source files` |
| 前端类型检查 | `npm run typecheck` | `tsc -b` 无输出（通过） |
| 前端单元测试 | `npm run test` | **`Test Files 15 passed (15)` / `Tests 111 passed (111)`**（上轮 78，本轮净增 33） |
| 前端构建 | `npm run build` | `✓ built in 695ms`；`index-bbrh633d.js` **1,197,436 B**（gzip **329.05 kB**）+ `index-DVJGwRGQ.css` 15,081 B；§13.5.1 前端预算 1400 kB 内 |
| 契约刷新两步 | 后端重导 `specs/openapi.json` → `npm run gen:api` | `segment_outline: [number, number][][]` 进入 `schema.d.ts`；两步均逐字节落盘 |

**本轮新增测试（后端 +17，前端 +33）**：

| 文件 | 条数 | 覆盖 |
|---|---|---|
| `backend/tests/unit/test_glb_segments.py`（新） | 17 | 分段节点名 `seg-<i>` 存在且段序一致 / 根 `vehicle` 不持 mesh / 退化段不产出节点 / **各段体积之和 = 整体 `build_solid()` 体积** / 分段 GLB **世界**包络与整体一致（节点带变换）/ 量测与导出仍取整体 |
| `store/view.test.ts`（新） | 6 | 显隐/选中/剖切取值与幂等 / 位置夹 0–1 / reset 全清 / **全部视图操作零后端请求（fetch 与 WebSocket 双盯）且 `store/params` 连对象引用都不变** |
| `r3f/sceneState.test.ts`（新） | 16 | 名字往返与非法名一律 null / 多 primitive 时子网格**向上找分段祖先** / 隐藏不存在下标不报错不隐藏 / 根节点不参与显隐 / **两通道（直 Mesh 与 Group 包子网格两形态）同粒度隐藏同一段** / 高亮是材质替换且共享原材质零改写 / 裁剪面数量变化必重编译、仅挪位置不重编译 / 轴向径向平面构造与夹取 |
| `components/SegmentPanel.test.tsx`（新） | 6 | 勾选 ↔ `hiddenSegments` 双向 / 「全部显示」清空与 disabled 态 / 树选中 ↔ `selectedSegment` ↔ 表单标题反向联动 / 越界下标不崩 |
| `r3f/useGlbScene.test.tsx`（新） | 5 | **同 url 任意次重渲染零新加载（视图交互门禁）** / 挂载加载一次、卸载释放 / url 变化释放旧场景并加载新场景 / 卸载后才兑现的请求被释放且不写入 / 加载失败交 `onFailure` 且保持 fallback |

**端到端实测（真起 uvicorn + vite + 真实浏览器，非 jsdom）**：

1. **两通道逐段显隐**（上一轮实测数据）：示意通道取消中段 → 非背景像素 **66734 → 38123（−43%）**，两端球冠保留；恢复后**精确回到 66734**。构建几何（约 3 s 状态栏变 `已就绪（权威）`）后权威通道同样隐藏中段 → 35624，轮廓结构与示意通道同构。
2. **剖切**：轴向 0.5 → 保留下半体（纵向跨度 344 → 144 px）；0.15 → 只剩底端一小块，单调正确。尺度全部取后端 `total_length=5` / `max_radius=1`，前端无几何推导。
3. **零请求受控复测（本轮，修复后）**：真实导航到新文档（基线 glb=0）→ 构建几何后 glb=1 → 连续 8 次纯视图交互（取消/恢复第 1 段、取消第 2 段、选中第 0 段、启用剖切、位置 0.3、0.6、关闭剖切），每次实测的 GLB 资源条数为 **[1,1,1,1,1,1,1,1]**；静置 3 s 仍为 **1**。叠加层 `权威通道（GLB）`，`[role=alert]` 为 0，控制台无 error/warning。

**实测查出并修复的真实缺陷（"没报错 ≠ 正确"的又一例）**：

- **现象**：修复前受控测量为 glb 1 → 9——**每次显隐/剖切交互都重新拉取一次 `model_lod2.glb`**（另观测到 7 次 `net::ERR_ABORTED` 的重叠请求），违反 §11.4 / §13.6「纯视图状态、零后端请求」；界面功能表面完全正常（缓存命中、5 ms、无红条），只有数网络请求才暴露。
- **根因**：旧实现用 `useLoader`（suspend-react 全局缓存）并在卸载清理里调 `useLoader.clear` 防"复用已释放资源"；StrictMode 首挂载的 **setup→cleanup→setup** 在 GLB 刚加载完时清空缓存，此后每次 `viewState` 变化引发的重渲染都缓存未命中 → 重新 `loader.load()` → Suspense 回退 → 卸载再 clear，自激成"每交互一请求"。
- **修复**：新 [useGlbScene.ts](file:///d:/Vibe%20Coding/aeroforge/frontend/src/r3f/useGlbScene.ts) 按 url **手动加载并持有场景**，释放时机只有三种：url 变化（新产物）、切回示意通道、Canvas 卸载；视图状态变化不进该 hook。作废请求（StrictMode 首次挂载）兑现时只释放不写入。
- **教训固化**：该缺陷配了独立会响的门禁（`useGlbScene.test.tsx` 第 2 条：同 url 五次重渲染加载次数必须为 0）——单靠"界面看起来对"与既有单测都拦不住它。

**两条控制台噪声的定性**（均非应用缺陷，留痕以防重复排查）：① `net::ERR_ABORTED /api/health` 是 StrictMode 开发期 effect 双挂载、首个 AbortController 被 cleanup 取消（M0 记录已裁，v0.3.3 保持现状）；② `TypeError: drawElements … 4 arguments required` 是**测试代理自己注入的 WebGL 像素钩子**破坏了渲染循环——干净页面复测为 0 条。

**交付链复验（§16.2 持续验证约定，OI-15 固定步骤；冻结命令同附录 D）**：

| # | 步骤 | 实测结果 |
|---|---|---|
| 1 | 重冻到 `dist-desktop/` | exit 0；**7758 个文件 / 474,035,620 B = 452.08 MB**（`AeroForge.exe` 38.42 MB + `_internal` 413.65 MB；§13.5.1 预算 550 MB 内） |
| 2 | 包内前端逐字节比对 | `_internal/frontend/dist/assets/index-bbrh633d.js` **1,197,436 B**，SHA256 `F32B56F378A0373E1CF21467442F3030AAE8BD51D724DF8D78CBE38D7171AA96`，与本次 `npm run build` 产物**逐字节相同**；CSS 同为 `index-DVJGwRGQ.css` 15,081 B。上版冻结包内 JS 为 `index-Dyj8e-pe.js` 1,173,727 B，**确已换新** |
| 3 | 冻结态 `--preflight` | 三项全通过（字体 **382** / OCCT `107.521009 vs 107.521009`、相对误差 `1.322e-16` / CEA `Tc = 3629.2 K`、`Isp_vac = 372.4 s`），**exit = 0** |
| 4 | 冻结态 `--probe`（两次） | **两次均通过**：WebView2 `153.0.4234.32`、WebGL2 可用、ANGLE / NVIDIA RTX 5070 Laptop。第 1 次：后端就绪 **0.68 s** / DOM **5.03 s** / 首帧 **5.70 s**（冻结刚结束且本机同时跑着 uvicorn、vite 与两个浏览器会话）；第 2 次：**0.59 / 3.94 / 4.62 s**，回到 M2 复验基线（4.51–4.55 s）量级，7.0 s 预算内 |
| 5 | 退出零残留 | `Get-Process AeroForge` = **0**；探针时间窗内无新增 `msedgewebview2` 子进程（启动时间比对口径） |
| 7 | 全量门禁 | 即本表上方八行（后端 / 前端 / 契约），记录写入后复跑 |

冻结日志备注：`update_exe_pe_checksum` 在 **attempt #2/20** 出现一次 `OSError(22)` 重试后成功（`Building EXE … completed successfully`）——与 M2 复验登记的**间歇性 PE 写入重试**同类，根因仍未定位；另有既有的 7 条 OCP submodule / Hidden import 收集警告。探针期出现的 `TRAE Sandbox Error: hit restricted`（搜狗输入法日志、NVIDIA DXCache、TokenBroker 缓存）与一行 `can't open file for write !` 均为**开发工具沙箱拦截 WebView2 子进程的旁路写入**，M0.5 坑位 2 已定性，与产物无关；exe 自身退出码为 0。

**本记录明确未覆盖的项**：

1. **断网复跑与冻结态内的完整交互回路未执行**（OI-15 固定步骤第 6 步）：本轮只在冻结产物上跑了 `--preflight` / `--probe`；逐段显隐、双向联动与剖切的端到端实测全部在**开发态**（Vite + uvicorn）完成，冻结态走 FastAPI 静态托管、无 Vite 代理，链路更短但**未在 exe 窗口内复演**；本会话环境也无法物理断网。该步按 OI-15 属"缺一步即未完成"，故在此显式登记，下次交付链补齐。
2. **径向剖切极值的观感未裁定**：位置 0.15 + 默认相机方位（相机在 +x 侧）时视口会空掉——平面保留下侧外壳，被背面剔除且剖切**不生成剖面封盖**（clipping 是纯渲染裁剪，刻意不产出几何，故不可导出）。行为与 three `Plane` 语义自洽（0.85 时像素回升、模型可见），但是否需要"自动翻转保留侧 / 极值夹取"属 UX 决策，**未裁定**。
3. **逐段对齐只做了间接目视**：朝向与分段几何由后端 `test_dual_channel_orientation` + GLB 节点 z 区间（`seg-0` z∈[0,1]、`seg-1` z∈[1,4]、`seg-2` z∈[4,5]）取证，视口内"两通道三段是否**逐段**重合"未逐段目视核对；"中段消失、两端球冠保留且两通道像素变化同构"是间接证据，不等同逐段核对。
4. **母线段 ↔ 分级（`stages[i]`）的映射展示未做**：本轮 `<i>` 严格是母线段下标（§11.4 硬约束），组件树文案为"第 N 段"；"这一段属于哪一级"的用户可见关联随装配树（M5）落地。
5. **首次探针 5.70 s 偏高**：只做了同机第二次复测（4.62 s）确认回归基线，**未在空载 / 第三方机器上复测**；首帧预算的人工登记口径不变。
6. **PE 写入重试根因仍未定位**：本轮再次出现（attempt #2 后成功），`--preflight` / `--probe` 都不读该校验和字段，沿用 M2 交付链复验留白第 5 项。
7. **既有留白不受本轮影响**：未在非 ASCII 路径下重冻复跑（R-34 结论仍取自 M0.5 对照实验）；爆炸视图（M5）、§11.10 2D 工程剖面图（M5）未开工；前端 chunk 1197 kB 的 code-splitting 评估仍在 M7 前（§16.2 登记项，预算内）。

---

## 17. 风险登记册

| 编号 | 风险 | 等级 | 对策 |
|---|---|---|---|
| R-01 | `pythonocc-core` 无法 pip 安装导致环境不可建 | — | ✅ **已消除**：改用 build123d（pip 可装） |
| R-02 | 指定 MCP Server 不存在 | — | ✅ **已消除**：自建领域 MCP |
| R-03 | 多套 OCCT 绑定 ABI 冲突 | — | ✅ **已消除**：单内核（OCP） |
| R-04 | 几何计算阻塞事件循环 / 内核崩溃 | 中 | worker 进程隔离 + 形状不跨进程 + 单 worker 内串行 |
| R-05 | RocketPy 用于轨道运力评估是范畴错误 | — | ✅ **已消除**：运力自研；RocketPy 仅作亚轨道可选，强制 ISA 1976 |
| R-06 | 理想 Isp 未修正导致运力高估 | 中 | 效率因子显式建模并纳入不确定度 |
| R-07 | 多级反向求解漏迭代导致数学错误 | **高** | 外层 GLOW 迭代 + 内层正向计算 + 残差收敛判据 |
| R-08 | 质量估算不准导致运力结论失真 | **高** | 双来源交叉校验；外推必须警告 |
| R-09 | NURBS 与 glTF 表示矛盾 | — | ✅ **已消除**：双轨契约（§5.1） |
| R-10 | GCAT 数据许可不明 / 单位口径混乱 | **高** | **许可核实前置为 M3 的开工门禁**（§7.1 / 顺序铁律第 7 条）：抓取**之前**完成核实，结论写入 `data/snapshots/<snapshot>/LICENSE-NOTICE.md`（含许可原文链接与依据）；**核不过或查不清即转自建数据集**（人工补录 + 文献整理 + 用户自备导入，§7.9 / §3.4），**不得**"先用着、以后再补"。单位显式声明；无法确定单位的记录排除出拟合 |
| R-11 | 存储选型过度（PostgreSQL） | — | ✅ **已消除**：SQLite |
| R-12 | 性能优化重心错位 | — | ✅ **已消除**：缓存 + 预计算表 |
| R-13 | 无误差传播导致结论不可辩护 | 中 | MC + 敏感度，区间为必填输出 |
| R-14 | 结果不可溯源 | 中 | 内容寻址哈希 + 快照 + provenance 红线 |
| R-15 | `@zendir/ui` 许可或维护风险 | 中 | M0 核实许可；适配层隔离，替换成本限于一个目录 |
| R-16 | 范围失控 / 工期乐观 | **高** | 以验收标准（非时间）定义里程碑；新想法一律进 [`docs/backlog.md`](file:///d:/Vibe%20Coding/aeroforge/docs/backlog.md)（⚠ 该文件此前**从未建立**，对策一度悬空——已于 2026-09-19 建立，见 OI-14） |
| R-17 | **CEA 表与真实发动机偏差**（表是理想化学平衡，不含实际循环损失与制造公差） | 中 | 用基准火箭反标定效率因子；不确定度覆盖 |
| R-18 | 简化损失模型误差主导结论 | 中 | M4 用经验模型 + 宽区间；M6 升级 L2 弹道积分收窄区间 |
| R-19 | 单人开发下的测试债 | 中 | 第 1、2、3 层门禁自动阻断；黄金基准进入 CI |
| R-20 | **2D 母线编辑器范围失控**（会滑向通用草图约束求解器） | 中 | 只支持"单母线 + 轴向对称 + 显式约束"；求解器类需求一律进 [`docs/backlog.md`](file:///d:/Vibe%20Coding/aeroforge/docs/backlog.md) 不排期（该文件已建立，OI-14） |
| R-21 | **`.eng`/`.ork`/`.rse` 无官方规范，解析器易碎** | 中 | 以样例文件驱动开发（`testdata/ref_models/`）；解析失败显式报错并降级为手动录入，禁止静默容错 |
| R-22 | 风廓线与大气统计数据的来源与版权 | 低 | 定位为 M6 可选功能；优先采用公开气象统计数据或用户自备数据 |
| R-23 | **CEA 表覆盖不足时静默外推** | 中 | 表外推进剂组合或超出网格范围必须报错并提示扩表，禁止插值外推出界 |
| R-24 | 时序层与质量迭代相互耦合导致不收敛 | 中 | 时序只消费质量结果；质量事件（分离/抛罩）必须在迭代内更新，残差判据同时覆盖 GLOW 与时序一致性 |
| R-25 | **双通道几何漂移**：前端示意网格与后端权威 GLB 形态分叉——用户看到的形状与实际参与计算、导出的形状不是同一个 | **高** | §13.6 双通道一致性测试为硬门禁，且**尺寸与朝向分列两条判据**（包围盒偏差 ≤ 1%；回转轴须落在同一世界轴、自同一坐标起算）；示意网格禁止参与计算与导出；权威 GLB 到达即强制替换。⚠ **已实际发生一次**：M2 首项查出权威通道被多转 -90°（尺寸全绿而模型躺倒），见 §16.3 结案记录 |
| R-26 | **前端悄悄重算物理**：为"提性能"在 TS 里重写 ΔV / 质量公式，绕过 ADR-011 | **高** | §13.6 静态扫描禁止物理常量与公式；新增前端数值逻辑须在 PR 中说明数据来源 |
| R-27 | **主题与配色漂移**：组件内硬编码色值，双主题失效（ADR-013 落空） | 低 | stylelint 禁止 `#` 字面量；视觉回归快照覆盖两套主题 |
| R-28 | **未经裁决的参数被"顺手实现"**：手册提出但无定义的参数（如偏度系数、干质比）按臆测口径进入 Schema，污染整条计算链路 | **高** | §1.7 为**裁决记录**，新参数必须先入表裁决；评审要求任何新增系数附来源与口径声明 |
| R-29 | **CEA 静默错误解**：`libcea` 对产物列表中凝相物种的**位置**敏感，可在 `last_error = 0` 的情况下返回 `Tc` 腰斩、Isp 减半的结果（实测 3629 K → 882 K） | **高** | ADR-014 契约②③：固定产物列表模板（纯气相，凝相仅末位）；健全性门禁以 **`Tc` 物理区间断言 + O/F 单调性扫描 + 黄金锚点比对** 三重校验，任一不过即报错拒发结果；§13 纳入回归 |
| R-30 | **`import build123d` 因系统字体损坏而崩溃**：该库在模块导入期无条件扫描系统字体且无异常保护，系统内任一以 `.ttf` 命名的非字体文件即可致整个后端无法启动 | 中 | 环境预检纳入字体目录完整性检查（sfnt 版本标识校验）；本机已通过隔离非字体文件解除（`mstmc.ttf` → `.bak`，哈希留档可还原）。⚠ 该缺陷在**开发机之外的部署环境**同样可能出现，故预检**必须随交付产物一同冻结**（v0.4.1）：唯一实现置于 `backend/aeroforge/selfcheck.py`，`tools/preflight.py` 仅转调，冻结产物以 `AeroForge.exe --preflight` 暴露同一实现，见 §16.2「预检保留」 |
| R-31 | **前端类型生成链路被 TypeScript 大版本升级打断**：`typescript@7.x`（原生重写版）不再暴露 `ts.factory` 等 Compiler API，`openapi-typescript` 在模块加载期即崩，`npm run gen:api` 全线失效——且升级命令是常规操作，容易被无意识触发 | 中 | `package.json` 锁定 `typescript@^5.7`（附录 A）；§3.2 记录实测证据；CI 增加一步 `npm run gen:api && git diff --exit-code src/api/schema.d.ts`，既验证生成链路可用，又防止契约漂移 |
| R-32 | **交付形态验证过晚**：原计划把桌面壳打包推迟到 M7，意味着 M1–M6 的全部 UI 工作都建立在"能打成 exe"这一**未验证假设**上；一旦冻结链（OCCT/CEA 原生库收集、WebView2 依赖）走不通，返工范围是全部前端工作量 | **高** | ✅ **已消除**：新增 **M0.5 桌面壳探针**并写入顺序铁律第 6 条（必须早于 M1），把交付链路从"末期大爆炸集成"改为"地基期打通、此后持续验证"；技术契约与坑位清单见 §16.2 |
| R-33 | **WebView2 不可用或 WebGL2 缺失**：桌面壳依赖系统 WebView2 运行时；若用户机器缺失该组件，或 WebView2 的 WebGL2 不可用，则原生窗口白屏、3D 视口整体不可用——而 3D 是本平台核心界面 | 中 | M0.5 探针**显式渲染一个 R3F 立方体**作为 WebGL2 可用性判据；运行时缺失时报明确错误并给安装指引（禁静默白屏）；M7 安装包检测并按需引导安装（§3.4 例外二）。**后备路径**：若 WebGL2 实测不可用，3D 需降级为服务端渲染图像 + 交互重放，属方案级变更，须先改规格 |
| R-34 | **安装路径含非 ASCII 字符致 CEA 不可用**：NASA `cea` 的 C 扩展用窄字符 API 打开数据表，路径含中文（或其他非 ASCII）时**必定加载失败**，且报错为*"Could not locate thermo database file"*——文件其实存在，误导性强，极易被误判为打包漏收 | **高**（中文用户占比高，且 M7 安装器默认路径若选 `%LOCALAPPDATA%` 则**必然踩中**） | ✅ **M0.5 已定位并加护栏**：受控对照实验确证根因（同一数据表换目录名即复现，§16.2 坑位 4）；启动器在**一切之前**（含 `--preflight`）判定资源根是否纯 ASCII，不通过即返回码 **5** 并给出换目录指引（`ascii_root()`）；AGENTS.md 列入禁止项。**M7 待办**：安装器默认路径须为纯 ASCII（**不得**用 `%LOCALAPPDATA%`，用户名可能含中文）；若产品上必须支持中文路径，则改用 `CEA_SKIP_INIT=1` + 显式 `libcea.init(path=<ASCII 数据目录>)`（已实测可行，代价是数据表副本及其生命周期管理） |
| R-35 | **传输层"静默挂起"致 UI 冻结**：WebSocket 在**连接建立阶段**失败时浏览器**既不派发 `error` 也不派发 `close`**，socket 无限停在 `CONNECTING`；若调用方只监听这两个事件，降级路径永不触发，界面永久停在初始态（"排队中（0%）"），而后端作业其实早已完成。**控制台零报错、无 HTTP 失败**，唯一可观测症状是"界面不前进" | **高**（A 类「没报错 ≠ 正确」的典型；本轮已实际发生，冻结逾 2.5 分钟） | ✅ **已修复并加门禁**：`openJobStream` 增设**握手超时**（`JOB_HANDSHAKE_TIMEOUT_MS = 3000`，超时即 `abandon()` 并回调 `onError` ⇒ 降级轮询），`vite.config.ts` 补齐 `/ws` 代理；§10.2 写入"三条失败路径都必须降级、不得只依赖 `onerror`/`onclose`"的硬约定；§13.6 新增「作业进度通道降级」判据（用永不回调的 WS 替身构造悬置）；见 §16.3 修复记录。⚠ **同类推广**：任何"既不成功也不失败"的异步等待（含 future / 子进程 / IPC）都必须设超时，不得以"没有报错"当作正常 |
| R-36 | **门禁只存在于文档、没有执行者**：规格 **22 处**提"CI"（§13.4 阻断、§13.5.1「CI 实测」、§13.8「长期在 CI 内运行」、R-31「CI 增加一步」……），而仓库内**没有任何工作流文件**——全部门禁仅在人工敲 `pytest` 时执行。后果是**门禁的可靠性等于人的记性**：只要有一次"改完先提交、回头再跑"，回归就漏过去了；而"CI 实测"这类措辞还会让人误以为已有自动化在守 | **高**（与 §15 曾声明 `docs/architecture.md` 属同类："文档承诺了不存在的东西"） | ✅ **已建立**（OI-12）：新增 `.github/workflows/ci.yml`（§13.9）——后端 `uv sync --frozen` / `ruff format --check` / `ruff check` / `mypy` / `pytest`；前端 `npm ci` / `typecheck` / `test` / `build` / **契约新鲜度**（`npm run gen:api` 后 `git diff --exit-code`）。**三项人工/产物实测不进 CI**（端到端预览、`--probe` 首帧、断网复跑），在 §13.5.1 显式登记为不覆盖项，由 §16.2 复验记录承载；并写入顺序铁律第 8 条（**CI 必须早于 M3**） |
| R-37 | **AI 助手通路的外部依赖与密钥风险**：AI 助手是全平台**唯一**需要联网的能力，一旦设计不当（把 AI 当唯一录入路径、无 key 即报错、只处理 `onerror` 不处理超时），会把"可选加速器"变成**启动或建模的前置依赖**，直接违反 CON-01 离线红线 | 中 | **OI-10 通路约定**（§10.2）：外部 API、OpenAI 兼容、密钥由用户填写、平台不自带；**三条降级触发**（未配置 / 断网超时 / 上游报错）任一成立即隐藏入口并回退手工表单；**禁止**把 AI 作为任何参数的唯一录入方式。断网复跑（OI-15 / §13.7）须在 **AI 未配置**状态下执行，否则测不到真实离线路径 |
| R-38 | **2D 工程剖面图范围失控**：一旦"工程剖面图"被理解成"工程制图"，需求会顺着滑向多视图（俯视 / 端视 / 局部放大）、剖视剖切符号、尺寸公差与形位公差（GD&T）、公差链求解、符合制图标准的图框与标题栏——那是通用 CAD 的领域，与本平台"参数化快速评估"的定位正交，且工作量无上界（与 R-20 的母线编辑器同族） | 中 | **§5.9 已划定边界**：只做**整箭纵剖一张图** + **固定的尺寸清单**（总长 / 各箱长 / 前后裙高 / 级间（舱）高 / 推力结构高 / 直径），**不做**多视图、不做公差与形位公差标注、不做尺寸链求解、不出图框标题栏。超出即视为新需求，**先走 §1.7 裁决或进 [`docs/backlog.md`](file:///d:/Vibe%20Coding/aeroforge/docs/backlog.md)**（R-16 口径）。判据可机检：剖面图上的尺寸项**只能来自 `SectionBand.dimension_labels` 的固定枚举** |
| R-39 | **MC 被塞进交互路径致"即时重算"名存实亡**：用户明确要求"调整发射场纬度时运力**即时重算**"，而 §8.7 的 MC 默认 **10 000 样本**是秒级量级。若把二者硬拼在一起，常见结果是耗时被"优化"成偷偷降低样本数（数字失真且无人察觉），或界面卡住数秒（"即时"落空）——**两种都算"没报错但错"** | **高**（直接关系到用户验收标准的第 1 条） | **OI-25 两阶段契约**（§8.7）：交互路径只跑**解析点值**（毫秒级）并置 `interval_pending = true`，MC 作为**独立后台作业**完成后整体覆盖。**门禁**：测试须断言阶段 ① 的求解路径**不调用** MC（不是断言"很快"——只断言快，则降样本数也能过）；前端在 `interval_pending` 为 `true` 时显示占位态而非旧数字 |
| R-40 | **内置模板与回归基准各建一套数字**：模板（§11.5 操作流 ⑤，面向用户）与基准（§13.2，面向测试）若不共享同一份数据，就会出现"测试用一套、用户看到另一套"——两个数字都"有来源"，但**互相矛盾**，属 P1（单一真相源）违反；更隐蔽的变体是**模板的公开数字被当作已验证结论**展示（用户以为平台"算对了"，其实只是抄了公开值） | 中 | **§13.2 明文同源**：基准表的参数即模板初值，公开运力列即模板的对照值；**禁止**为模板另建一套数字。模板参数**必须带来源标注**，且 UI 须区分「公开资料值（对照）」与「本平台计算值（结论）」——二者视觉上不得混排。**门禁**：FR-22 的验收即"载入模板后关键参数与 §13.2 基准表逐项一致" |

---

## 18. 工程约定

### 18.1 编码规范

- **Python**：PEP 8 由 `ruff format` 强制；类型注解**全覆盖**，`mypy --strict` 通过
- 命名：模块 `snake_case`；类 `PascalCase`；函数/变量 `snake_case`；常量 `UPPER_SNAKE`
- 禁止：裸 `except`、可变默认参数、隐式单位假设、领域层出现裸数字系数
- 文档字符串：公共 API 必须含参数单位说明

### 18.2 TypeScript 规范

- `strict` 全开；禁止 `any`（必要时 `unknown` + 收窄）
- 后端类型由 OpenAPI **自动生成**，禁止手写重复类型
- 组件文件与目录同名；业务组件与适配层组件严格分目录

### 18.3 版本控制

- 主干开发（trunk-based）：`main` 常绿，短命分支 `feat/xxx`
- **Conventional Commits**：`feat(geometry): 支持冯·卡门头锥` / `fix(sizing): 修正级间质量迭代残差判据` / `docs: 修订技术规格 v0.2` / `data: 更新 GCAT 快照`
- 语义化版本：M0–M5 为 `v0.x`；M6 起 `v1.0` 冻结 API 契约
- **数据快照单独提交**并带哈希，代码与数据分离管理

### 18.4 配置与日志

- 配置：`pydantic-settings`，来源优先级 `环境变量 > config.toml > 默认值`
- **必须提供配置模板 `config.example.toml`**（§1.7.3 OI-31）：逐项列出可配置阈值及其默认值、来源标注与**生效边界**（例如「最小工艺厚度：留空 = 不判定，量级参照 Centaur 0.51 mm」）。模板中的注释**不得**把工程惯例值写成权威数值，也不得把"留空即不判定"改成"留空即用某默认值"
- 配置文件位置由 `config_file()` 给出（`data_root()` 的父目录，随 `AEROFORGE_DATA_DIR` 重定向——路径必须**在实例化时**求值，写在类体里会在 import 期冻住，导致测试重定向失效）
- 配置文件的读写走 `GET` / `PUT /api/params/thresholds`（§10.1）；`PUT` 写回后**不改变**上述优先级——环境变量仍然覆盖 `config.toml`，故界面须显示的是**生效值**而非"文件里的值"
- 日志：结构化 JSON（阶段、作业 ID、耗时、缓存命中），便于事后分析
- 禁止在日志中输出完整参数集（体积大），只输出参数哈希

---

## 19. 参考资料（开源经验来源，均可离线研读）

**几何与内核**

- **build123d**（Apache-2.0）— 参数化 BREP 建模 API 设计
- **CadQuery**（Apache-2.0）— 同内核的另一套 API，可读作对照
- **TiGL Geometry Library**（Apache-2.0）— 以 NURBS 表示飞行器几何；"参数文件 → NURBS 曲面 → 多格式导出"管线可直接迁移（点检索、平面求交、STEP/IGES/VTK/Collada/STL 导出）
- **yapCAD**（Apache-2.0）— 程序化 CAD 框架，附多级火箭装配与 STEP → FreeCAD 渲染的完整流程示例
- **OCCT 官方文档** — 曲面连续性与网格化容差

**数据源**

- **GCAT**（Hugging Face `juliensimon/gcat-launch-vehicles`）— 运载火箭 / 发动机 / 级段三表，单位口径需自行核实
- **SPACEMATDB** — 航天材料性能库（见 §7.4）
- **OpenRocket `.ork` 格式**（GPL 软件，**仅读文件格式不引代码**）— 构型与质量预算的组织方式
- **RASAero / `.eng` 发动机文件** — 推力-时间曲线的社区通用交换格式（无官方规范，见 R-21）

**推进与弹道**

- **NASA CEA / `cea`**（Apache-2.0）— 热力学计算的事实标准
- **RocketPy**（MIT）— 亚轨道弹道与大气模型（限 §8.6 L2 参考；大气模型强制 ISA 1976）

**界面与设计系统**

- **Astro UX Design System**（美国太空军）— `@zendir/ui` 的上游设计规范，可作为自研替代组件时的设计依据

**方法论**

- **Claude Code Best Practices** — 四段法（Explore/Plan/Implement/Commit）与子代理委派的原始出处

---

## 附录 A：依赖清单草案

```toml
# pyproject.toml
[project]
name = "aeroforge"
requires-python = ">=3.12,<3.13"

dependencies = [
  "build123d>=0.11,<0.12",
  "cea>=3.3,<4",
  "numpy>=2.0",
  "scipy>=1.14",
  "pandas>=2.2",
  "pyarrow>=17",
  "fastapi>=0.115",
  "uvicorn[standard]>=0.32",
  "pydantic>=2.9",
  "pydantic-settings>=2.6",
  "sqlalchemy>=2.0",
  "websockets>=13",
]

[dependency-groups]
dev = [
  "pytest>=8", "pytest-asyncio", "pytest-cov", "hypothesis>=6",
  "ruff>=0.7", "mypy>=1.13", "types-requests",
  "httpx", "pytest-benchmark",
]
```

> `.ork` / `.rse` 为 XML 格式，`.eng` 为文本表格，解析一律使用标准库，**不引入额外 XML 依赖**。

```jsonc
// frontend/package.json（M0 实测锁定版本；M1 增加 three / r3f / zustand / echarts / tailwind）
{
  "dependencies": {
    "react": "^19.3.0", "react-dom": "^19.3.0"
    // M1 追加：three "^0.170" · @react-three/fiber "^9" · @react-three/drei "^10"
    //          zustand "^5" · echarts "^5" · @zendir/ui "<锁定精确版本，M1 探针通过后填>"
  },
  "devDependencies": {
    "vite": "^8.3.0", "@vitejs/plugin-react": "^6.1.1",
    "typescript": "^5.7",              // ⚠ 禁止升 7.x（Compiler API 缺失，破坏 openapi-typescript，见 §3.2）
    "@types/react": "^19.3.0", "@types/react-dom": "^19.3.0",
    "vitest": "^5.0.1", "jsdom": "^30.1.0",
    "@testing-library/react": "^16.3.3", "@testing-library/jest-dom": "^7.0.1",
    "@testing-library/user-event": "^14.6.7",
    "openapi-typescript": "^7.13.0"
  }
}
```

> **M0 实测修正**：本文档早期版本写的 `vite ^6` / `vitest ^2` 为立项预估，实测已被上表版本取代（§3.2 已记录该组合在 Node 24.18.0 下 `tsc -b` / `vitest run` / `vite build` 三者全绿）。
> **`typescript` 的 5.x 上限是硬约束而非偏好**：7.x 为原生重写版，不再暴露 `ts.factory`，`openapi-typescript` 会在模块加载期直接抛错（§3.2 有实测记录）。

## 附录 B：术语表

| 术语 | 含义 |
|---|---|
| **GLOW** | Gross Lift-Off Weight，起飞总质量 |
| **σ（结构系数）** | m_dry / (m_dry + m_prop) |
| **Isp** | 比冲，秒；本平台内部用 m/s（= Isp·g₀）参与计算 |
| **c\*** | 特征速度（characteristic velocity），燃烧室性能指标 |
| **ε（面积比）** | 喷管出口面积 / 喉部面积 |
| **ΔV** | 速度增量预算，km/s |
| **LEO / GTO / SSO** | 低地球轨道 / 地球同步转移轨道 / 太阳同步轨道 |
| **GEO** | 地球同步轨道（Geostationary Earth Orbit） |
| **Hohmann 转移** | 两共面圆轨道间的双脉冲最小能量转移，有解析闭式解 |
| **密度比冲** | Isp × 推进剂密度，一级选型的关键指标（追求最大） |
| **最优混合比（O/F）** | 使目标量（一级密度比冲 / 上面级比冲）最大的氧化剂-燃料质量比 |
| **母线（meridian）** | 旋转体的轴向截面半轮廓，2D→3D 建模的输入 |
| **BREP** | 边界表示法（Boundary Representation），精确几何的存储方式 |
| **NURBS** | 非均匀有理 B 样条，曲面精确表达 |
| **LOD** | Level of Detail，细节层级 |
| **共底贮箱** | 相邻两级或两贮箱共享一个隔板曲面 |
| **冯·卡门（von Kármán）** | 一种低阻头锥母线 |
| **G1 连续** | 切线连续，曲面无折痕的充要条件 |
| **C1 / C2 连续** | 一阶（切线）/ 二阶（曲率）连续，曲面光顺的判据 |
| **任务时序层** | 点火 / 分离 / 抛罩 / 入轨等事件的时间序列，与质量事件耦合 |
| **风廓线（wind profile）** | 风速随高度的分布，用于发射日散布分析 |
| **加注比例** | 实际加注量 / 满装量，默认上限 100%；超装须显式启用"最大允许加注量"（§1.7 OI-03） |
| **干质比** | `m_dry / m_prop`；σ 为存储权威，干质比与推进剂质量分数是其派生表示（§1.7 OI-02） |
| **推进剂质量分数** | `m_prop / (m_dry + m_prop)` = 1 − σ，派生量，不得独立存储 |
| **储箱排列** | 同一级内燃料箱与氧化剂箱的上下次序 |
| **偏度系数** | 统计偏度：总体 γ₁ = μ₃/σ³；样本用修正样本偏度（Excel `SKEW` / pandas 默认口径）。**派生统计量，非几何输入**（§1.7 OI-01） |
| **修正样本偏度** | G₁ = √(n(n−1))/(n−2) · m₃/m₂^{3/2}，`n < 3` 时无定义 |
| **冻结态** | "自动更新"关闭后的状态：保留最后结果、停止自动重算、常驻「已冻结 / 可能过期」标记 |
| **SSOT** | Single Source of Truth，单一数据源：每个物理量只有一处权威存储与计算 |
| **示意网格 / 权威几何** | 示意网格＝前端按尺寸程序化生成的占位模型（不参与计算）；权威几何＝后端 BREP/GLB（唯一参与计算与导出） |
| **语义 token / 主题** | 语义 token 描述用途（如 `--color-accent`），主题为其赋值；组件只能引用语义 token |
| **比例尺叠加层** | 3D 视口内不随相机缩放的尺寸标尺，用于读取真实几何尺度 |
| **OI** | Open Item，需求裁决项（§1.7 为已闭环的裁决记录）；新参数须先入表裁决才可进入实现 |
| **LHS** | 拉丁超立方抽样（Latin Hypercube Sampling） |
| **P5 / P50 / P95** | 不确定度分位数，性能区间的标准表达 |
| **Sobol 指数** | 全局敏感度指标，量化单个参数对输出方差的贡献 |
| **内容寻址** | 以内容哈希为键存储产物，保证可复现 |
| **provenance** | 溯源信息：参数、数据快照、代码与内核版本的组合指纹 |

## 附录 C：本机首日命令与镜像配置

```powershell
cd "D:\Vibe Coding\aeroforge"

# 1) 环境（首次，联网；此后锁文件保证可复现）
uv python install 3.12
uv sync

# 2) 国内镜像（PyPI）
$env:UV_INDEX_URL = "https://pypi.tuna.tsinghua.edu.cn/simple"

# 3) 校验环境（不写一行业务代码即可验证工具链）
uv run python -c "import build123d, cea, numpy, scipy; print('deps ok')"
uv run pytest -q

# 4) 前端
cd frontend
npm config set registry https://registry.npmmirror.com
npm install
npm run build

# 5) 离线生成数据与性能表（开发期一次性，之后运行时不再联网）
cd ..
uv run python tools/gcat_etl.py --out data/snapshots/gcat-<YEAR>Q<N>
uv run python tools/cea_tablegen.py --out data/tables/cea-grid-v1
```

**M0 完成判据**：上述五步全绿，且 `/api/health` 与前端首页可互通一次请求。

**离线验证**：断开网络后重跑 `uv sync --offline`、`uv run pytest`、`npm run build`，全部通过。

---

## 附录 D：AGENTS.md 模板

置于仓库根目录，Agent 每次会话启动自动加载。随规格文档同步维护。

```markdown
# AGENTS.md — AeroForge 航天飞行器参数化建模与性能评估平台

> Agent 每次会话启动自动加载。本文件由 `AeroForge-Spec.md` 附录 D 物化而来，随规格同步维护。

## 项目结构
- /backend/    FastAPI + build123d 后端（api / params / geometry / propulsion / sizing / orbital / data）
- /frontend/   React 19 + Vite + three 前端（views / components / adapters / r3f / store）
- /desktop/    桌面壳与打包：pywebview 原生窗口 + PyInstaller（ADR-015，M0.5 起）
- /mcp/        自建领域 MCP Server
- /skills/     Agent Skills（SKILL.md）
- /specs/      跨模块接口与构型规格（先改这里，再改实现）
- /data/       数据快照与 CEA 预计算表（不可变，禁止手改）+ `contours/<id>.json` 母线存档（M1 起由 `POST /api/geometry/contour` 写入）
- /artifacts/  内容寻址产物：`<key>/{model.step, model_lod1.glb, model_lod2.glb, metrics.json, provenance.json}`，`key = sha256(canonical_json + kernel_version + spec_version)`（§16.3）。派生数据，不入库
- /tools/      preflight.py（环境预检 CLI，仅转调 `aeroforge.selfcheck`——R-30 要求唯一实现）；cea_tablegen.py / gcat_etl.py / model_import.py / benchmark.py 随 M3–M5 落地，**当前不存在**
- /docs/       未采纳想法与留白索引：`backlog.md`（R-16 / R-20 的落点，OI-14）· `adr/`（占位）
- /.github/    持续集成：`workflows/ci.yml`（规格 §13.9）
- AeroForge-Spec.md  唯一真理源，架构级变更必须先改本文档

> **交付形态**：Windows 桌面应用，**双击 exe 启动**，不是网页、不是在线服务（ADR-015）。
> 开发期可用浏览器访问 Vite dev server 加速迭代，但那是**开发手段而非交付形态**，任何实现不得依赖它存在。

## 代码规范
- Python：PEP 8（ruff format 强制）；类型注解全覆盖，`mypy --strict` 必须通过
- TypeScript：strict 全开，禁止 `any`；后端类型由 OpenAPI 生成，禁止手写重复类型
- 命名：模块 snake_case；类 PascalCase；常量 UPPER_SNAKE
- 文档字符串：公共 API 必须说明参数单位
- 测试：pytest（后端）+ hypothesis（属性）+ vitest（前端），每个模块必须有单元测试

## 关键命令
- 环境：`uv sync`
- 环境预检（字体完整性 / OCCT 内核 / CEA 点火）：`uv run python tools/preflight.py`
- 冻结产物预检（同一实现，验证冻结后依赖真的可用）：`dist-desktop\AeroForge\AeroForge.exe --preflight`
- 后端：`uv run uvicorn aeroforge.api.main:app --reload`
- 测试：`uv run pytest -q` ；静态检查：`uv run ruff check . && uv run mypy backend`
- 前端：`cd frontend && npm run dev` ；构建：`npm run build`
- 前端检查：`npm run test && npm run typecheck`
- 前端非功能（双通道一致性 / 资源释放 / 零物理公式 / token 合规，规格 §13.6）：**当前并入 `npm run test`**（M1 已覆盖示意通道包围盒一致性与防抖调度器）；§13.6 余项（500 次重建的资源释放扫描、stylelint token 规则）尚未落地，落地后再拆出独立的 `test:nfr` 脚本（该脚本**当前不存在**）
- OpenAPI 契约刷新（后端接口变更后必跑，在**仓库根**执行）：`uv run python -c "import json,pathlib;from aeroforge.api.main import app;pathlib.Path('specs/openapi.json').write_text(json.dumps(app.openapi(),ensure_ascii=False,indent=2)+chr(10),encoding='utf-8')"` 然后 `cd frontend && npm run gen:api`
- 几何闭环验证（解析 vs 内核体积、双通道包络、母线往返）：`uv run pytest backend/tests/unit/test_geometry_closed_loop.py`
- 工程自检门禁（文档命令可执行性 / 附录 D 同步 / 测试隔离 / 夹具合法性 / 验收记录留白 / 性能预算，规格 §13.8）：随 `uv run pytest -q` 一并运行
- 桌面端本地运行（不经浏览器）：`uv run python desktop/launcher.py`
- 桌面端打包（onedir，产物在 `dist-desktop/`）：`uv run pyinstaller desktop/packaging/aeroforge.spec --distpath dist-desktop --workpath .tools/pyinstaller-build --noconfirm`
- 桌面端冒烟（冻结后仍能起后端 + 渲染 3D）：见规格 §16.2 退出准则
- 持续集成（规格 §13.9）：`push` / `pull_request` 触发，跑后端 `ruff format --check` / `ruff check` / `mypy` / `pytest`、前端 `typecheck` / `test` / `build`，以及 OpenAPI 契约新鲜度两步比对；**端到端预览延迟、`--probe` 首帧、断网复跑三项不在 CI 内**（属人工 / 产物实测，见规格 §13.5.1 / §16.2）

## 禁止操作
- 不要修改 /data/snapshots/ 与 /data/tables/ 下的已发布数据（快照不可变）
- 不要直接操作 OCCT 的 TopoDS_Shape 内部结构（走 build123d Builder API）
- 不要在前端做任何几何运算（前端只消费 GLB）
- **不要在前端实现任何物理公式或物理常量**（ADR-011；前端只格式化接口返回的数值）
- **不要把示意网格用于计算、导出或作为几何依据**（ADR-012）
- **不要把 `artifacts/` 下的内容寻址产物入库或手工改写**（可由参数 + 内核版本 + 规格版本重建；`provenance.json` 是溯源链本身，改动即断链）
- **不要绕过 `cache/store.py` 的内容寻址缓存直连几何内核**（M1 已落地，§16 顺序铁律第 2 条要求缓存不得推迟，否则 M4–M6 迭代成本失控）
- **不要在组件样式里写字面色值**，必须引用语义 token（ADR-013）
- **不要在未经 AeroForge-Spec.md §1.7 裁决的情况下新增参数或系数**（裁决即规格，口径必须唯一）
- **不要把 CEA 的 `Isp` / `Isp_vacuum` 字段当秒用**（ADR-014：该字段量纲是有效排气速度 m/s，转秒必须除以 `g₀`）
- **不要在 CEA 产物列表中把凝相物种放在中部**（ADR-014：固定"纯气相 + 凝相仅置于末位"；任何求解结果必须先过健全性门禁，禁止只看 `last_error`）
- **不要用发行名 `cadquery-ocp` 判依赖缺失**（实际发行名为 `cadquery-ocp-novtk`）
- **不要把 `typescript` 升到 7.x**（不再暴露 Compiler API，`npm run gen:api` 会在模块加载期崩溃，见 §3.2 / R-31）
- **不要把 npm 缓存改回系统目录**（本机沙箱拒绝写入，`frontend/.npmrc` 的 `cache=../.tools/npm-cache` 是必需项，不是优化）
- **不要把交付形态做成"浏览器访问 localhost"**（交付物是**双击即用的 exe**，ADR-015）
- **不要在前端硬编码主机名或端口**（必须用同源相对路径 `/api/...`、`/ws/...`；打包后服务跑在运行时选定的动态端口上）
- **不要用 `onefile` 冻结含 OCCT 的后端**（每次启动解压数百 MB 且易被杀软拦截，用 `onedir`，见规格 §16.2）
- **不要在 `tools/preflight.py` 或任何别处复制一份预检实现**（唯一实现是 `backend/aeroforge/selfcheck.py`；R-30 要求预检随交付产物冻结，复制品不会进包）
- **不要只写 PyInstaller hook 而不建立导入路径**（hook 仅对**导入图内出现过**的包执行；没有 `import` 回指该包时 hook 静默不执行，产物会缺库且构建不报错）
- **不要把产物安装或解压到含非 ASCII 字符的路径**（NASA `cea` 的 C 扩展用窄字符 API 打开数据表，中文路径下必定加载失败，且报错会误导成"thermo.lib 找不到"；启动器已加前置判定返回码 5，见规格 §16.2 坑位 4。同理 M7 安装器默认路径**不得**选 `%LOCALAPPDATA%`——用户名可能含中文）
- **不要为了让测试变绿而放宽断言或跳过守卫**（规格 §13.8：夹具非法就修夹具，产品校验器不得因测试而放宽；`warn` / `skip` 必须显式留痕）
- 不要在计算路径内实时调用 CEA（只读预计算表）
- 不要用网格（GLB/STL）作为任何计算的输入
- 不要引入新依赖（须先走 AeroForge-Spec.md §3.4 审计流程）
- 不要在无来源的情况下输出数值结论
- **不要引入 i18n / 多语言框架**（规格 §1.3 非目标：界面文案只做简体中文，OI-09）
- **不要在组件里就地格式化单位**（规格 §6.4：显示单位固定一套，换算只在 API 边界发生，OI-07）
- **不要用经验系数编造方案诊断的 `impact`**（规格 §6.5：必须由 §8.7 敏感度实算，M2 不输出该字段、M4 起补，OI-11）
- **不要把未采纳的想法直接排期**（先进 `docs/backlog.md`；要"毕业"须回到 `AeroForge-Spec.md` 走 §1.7 裁决或立 ADR，R-16 / R-20 / OI-14）
- **不要把 3D 的剖切 / 爆炸 / 分级显隐写回参数或当作几何依据**（三者是**视图状态**：不触发后端请求、不改几何、不可导出；P1 / ADR-012 / OI-19）
- **不要把箱体比例当作用户可自由给定的输入**（`V_ox / V_fuel = (O/F) · (ρ_fuel / ρ_ox)` 由后端派生，规格 §5.9 / OI-18；用户显式给定箱长时须标注来源冲突，禁止静默覆盖）
- **不要在交互路径上跑蒙特卡洛**（纬度 / 轨道编辑只跑解析点值并置 `interval_pending`，MC 走独立后台作业；OI-25 / R-39——把 10 000 样本塞进交互路径只会得到一个既不即时也不准的数字）
- **不要把 2D 工程剖面图做成通用工程制图**（只做**整箭纵剖一张图** + 规格 §5.9 的固定尺寸清单；不做多视图 / 公差 / 形位公差 / 尺寸链 / 图框，R-38）
- **不要为内置模板另建一套数字**（模板与规格 §13.2 基准表**同源**；模板值须标注为公开资料对照，**不得**当作本平台的计算结论展示；R-40）

## 领域规则
- 所有几何参数必须有物理约束校验（壁厚 < 直径/2 等）
- 齐奥尔科夫斯基计算必须使用多级累加模式，禁止单级近似替代多级
- 数据库查询必须走标签索引，禁止全表扫描
- 质量估算必须双来源交叉校验（几何解析 + GCAT 回归），单来源结果禁止作为结论
- 多级求解必须外层 GLOW 迭代 + 内层正向计算，残差收敛判据 1e-6
- 性能结论必须附带不确定度区间（P5/P50/P95）
- 单位必须在变量名或类型中显式声明，禁止隐式单位假设
- 1 级选型看密度比冲，上面级选型看比冲
- **2D 工程剖面图与 3D 装配树必须共用 §5.9 的 9 段分区枚举**（无漏件、无错序；分区高度与尺寸标注数值**一律后端下发**）
- **轨道计算须同时输出 C3 与 ΔV**，且二者可由同一组 `μ` / `r_p` 互相反算（规格 §8.10；TMI 结果必须记录窗口 / 相位假设，否则不可复现）
- **发射场纬度是自转加成与转向损失的唯一输入**，不得另设常量；固定其余参数时**纬度 ↓ ⟹ 运力 ↑** 必须成立（FR-19）

## 协作约定
- 跨模块改动先改 /specs/，接口冻结后再并行实现
- 涉及几何 / 推进 / 总体三层中任意两层的改动，拆成独立 PR
- 几何或计算模块的 PR 必须附与黄金基准火箭的偏差数字（AeroForge-Spec.md §13.2）
- 新增任何系数或常量必须在 PR 中标注来源出处（文献 / 数据 / 标准），无来源不予合并
- 前端改动必须通过 §13.6 非功能测试（双通道一致性 / 资源释放 / 零物理公式 / token 合规）
- **交付链路必须持续可验证**：M0.5 探针打通后，任何改动若使"冻结 → 启动 → 原生窗口 → 3D 渲染"任一环失效，视为阻断级缺陷，优先于功能开发修复
```
