"""推进层：CEA 预计算表查询 · 效率因子 → 实际 Isp。

子模块规划：cea_table · efficiency · workpoint（最优 O/F 求解）。

CEA 调用契约（ADR-014，硬约束）：
1. ``Isp`` / ``Isp_vacuum`` 字段量纲为**有效排气速度 m/s**，换算为秒必须除以 ``g₀``；
2. 产物列表固定为"纯气相 + 凝相仅置于末位"，凝相物种不得出现在列表中部；
3. 每次求解后必须过健全性门禁（``last_error == 0`` 且 ``Tc`` 落在物理区间），
   任一不满足即报错拒发结果，禁止静默返回。

计算路径内禁止实时调用 CEA，只读预计算表（规格附录 D）。
"""
