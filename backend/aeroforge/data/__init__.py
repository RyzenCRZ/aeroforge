"""数据子系统（规格 §7）：GCAT 快照固化、ETL、SQLite 目录与标签查询的领域层落点。

已落地子模块：

- ``snapshot``——快照固化（§7.1：许可先于抓取、sha256 对账、快照不可变）
- ``etl``——列映射 + 单位规范化（§7.5）→ ``records.parquet``
- ``db``——SQLite 建库与三层标签索引（§7.7 / ADR-007 / §7.2）
- ``repository``——只读仓储层，业务代码取数的唯一入口（禁止业务拼 SQL）
- ``models``——读取侧数据类（字段与 ETL 内部字段名同步，有门禁）

规划中：materials（材料库，QA-3）· lineage（谱系/溯源）。
查询必须走三层标签索引，禁止全表扫描（§7.2，AGENTS.md 领域规则）。
"""
