"""总体层：质量回归 · 多级迭代 · 损失模型 · 任务时序 → 运载能力。

子模块规划：mass · staging · losses · sequence（任务时序）。

关键约束（规格附录 D）：
- 齐奥尔科夫斯基计算必须多级累加，禁止单级近似替代多级；
- 多级求解必须"外层 GLOW 迭代 + 内层正向计算"，残差收敛判据 1e-6；
- 质量估算必须双来源交叉校验（几何解析 + GCAT 回归），单来源结果不得作为结论；
- 性能结论必须附带不确定度区间（P5/P50/P95）。
"""
